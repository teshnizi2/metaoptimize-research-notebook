# MASTER TABLE — every experiment the campaign ran

Compiled 3,049 runs / 3184.1 GPU-hours (re-derived at **cycle 152, CORRECTIONS 241**, after the `cvt4` + `cvt5` ingest **[SUPERSEDED: 3,019 / 3144.2, re-derived at cycle 152, CORRECTIONS 236, after the `cvt3` + `cvt2` ingest]** **[SUPERSEDED: 2,983 / 3118.7, re-derived at cycle 152, CORRECTIONS 231, after the `cgn3` ingest]** **[SUPERSEDED: 2,971 / 3079.9, re-derived at cycle 152, CORRECTIONS 230, after the `cvt1` ingest]** **[SUPERSEDED: 2,956 / 3069.7, re-derived at cycle 151, CORRECTIONS 226, after the `cgn2` + `cpl2` ingest]** **[SUPERSEDED: 2,926 / 3049.1, re-derived at cycle 151, CORRECTIONS 220, after the `cvh1` + `cuc1` ingest]** **[SUPERSEDED: 2,884 / 3008.8, re-derived at cycle 151, CORRECTIONS 218, after the `cgn1` + `cpl1` ingest]** **[SUPERSEDED: 2,863 / 2991.6, re-derived at cycle 151, CORRECTIONS 210, after the `cau1` + `cvk1` ingest]** **[SUPERSEDED: 2,809 / 2963.0, re-derived at cycle 151, CORRECTIONS 203, after the `cvi1` ingest]**, by a hand-rolled splitter that uses no `csv` module and shares no code with the checker, and cross-checked against `analysis/c73_mastertable_check.py` run UNEDITED; **[SUPERSEDED: 2,797 / 2956.4, re-derived from `results/all_runs.csv` at cycle 150]** with an INDEPENDENT parser -- a hand-rolled RFC4180 splitter that shares no code with the checker, uses no `csv` module and no `DictReader` -- and cross-checked against `analysis/c73_mastertable_check.py` run **UNEDITED**. **[SUPERSEDED: 2,761 / 2914.4, compiled at cycle 148, which went stale INSIDE ITS OWN CYCLE -- `cdep1` (193), `cvg1` (194) and `ciso2` each ingested behind it, and the registered checker, run unedited, then EXITED 1 on exactly those two numbers. Before it, the stale 2537 / 2222.0 compiled at cycle 129, corrected at 192.]** **THE GPU-HOUR FIGURE IS A LOWER BOUND, disclosed the way `192.3` disclosed it:** it is the sum over the **3,034 rows that carry a `wallclock_min`** **[SUPERSEDED: 3,004, before the `cvt4` + `cvt5` ingest; 2,968, before the `cvt3` + `cvt2` ingest; 2,956, before the `cgn3` ingest; 2,941, before the `cvt1` ingest; 2,911, before the `cgn2` + `cpl2` ingest; 2,869, before the `cvh1` + `cuc1` ingest; 2,848, before the `cgn1` + `cpl1` ingest; 2,794, before the `cau1` + `cvk1` ingest]**; **15 rows carry none** and contribute zero. Verdicts: CONFIRMED 34 **[SUPERSEDED: 33]**, REFUTED 26, OPEN 7 **[SUPERSEDED: 8]**, WITHDRAWN 3, DEAD 2, UNINTERPRETABLE 2 across sections 1-8, plus 37 section-9 rows whose verdict column carries the REGISTERED SCORER TOKEN verbatim in backticks and which therefore join none of those six families, and 37 section-10 rows that carry the registered scorer's token the same way or, where no registered scorer exists, the record's own verdict in quotation marks with its entry number, and which therefore join none of them either, plus 12 rows appended at the very end of the file under the section-10 table heading (the `cgn1`, `cpl1`, `cvh1`, `cuc1`, `cgn2`, `cpl2`, `cvt1`, `cgn3`, `cvt3`, `cvt2`, `cvt4` and `cvt5` landings, entries 217, 218, 219, 220, 225, 226, 230, 231, 235, 236, 240 and 241) **[SUPERSEDED: 10 rows, entries 217-220, 225, 226, 230, 231, 235 and 236, before the `cvt4` + `cvt5` landing]** **[SUPERSEDED: 8 rows, entries 217-220, 225, 226, 230 and 231, before the `cvt3` + `cvt2` landing]** **[SUPERSEDED: 7 rows, entries 217-220, 225, 226 and 230, before the `cgn3` landing]** **[SUPERSEDED: 6 rows, entries 217-220, 225 and 226, before the `cvt1` landing]** **[SUPERSEDED: 4 rows, entries 217-220, before the `cgn2` + `cpl2` landing]** **[SUPERSEDED: 2 rows, entries 217 and 218 alone, before the `cvh1` + `cuc1` landing]**, which carry the registered scorer's token verbatim in backticks in section 9's convention and join none of the six families either (160 rows total) **[SUPERSEDED: 158]** **[SUPERSEDED: 156]** **[SUPERSEDED: 155]** **[SUPERSEDED: 154]** **[SUPERSEDED: 152]** **[SUPERSEDED: 150]** — **counted twice, by the registered checker and by an independent parser, not estimated.** Of the 34 CONFIRMED **[SUPERSEDED: 33]**, one is `CONFIRMED (T-B)` (a registered direction that came out against the headline), one is `CONFIRMED, RESCOPED c73`, and one is `CONFIRMED, CLOSED-OUT 115` (see below), and one is `CONFIRMED, RESCOPED 229` (line 19, the FAIR row, moved from OPEN at cycle 152 because `cuc1` closed the two counts `cau1` left open; CORRECTIONS 229). **THE THREE COUNTS THAT DISAGREED, ADJUDICATED (CORRECTIONS 192).** The cycle-129 header said 32/26/9/3/2/2 over 74 rows; `c73_mastertable_check.py` reported 31/26/9/3/2/2 over 73; the cycle-148 brief counted 33/26/8/3/2/2. **All three were right about something and none was right about everything.** The checker dropped exactly one row — old line 24 carried an UNESCAPED `|` inside a cell (in `\|Δ\| ≤ 2 SE`), which its own check 2 correctly flagged; that row is `**CONFIRMED**`, so the checker under-counted rows by 1 and CONFIRMED by 1. The escape is fixed here and nothing else in that row was touched. The brief's 33/8 came from a single row — old line 13 — whose verdict cell read `**OPEN**` while its own in-cell CLOSEOUT annotation said the verdict moves to CONFIRMED; it is the ONLY row in the file where a stray verdict token disagrees with the verdict cell in that direction, which reproduces the 33/8 split exactly. **The brief was wrong as a count of the file and right about the science**, so the closeout is now applied to the column with the superseded token kept verbatim. 30 fact-check corrections applied. **Brought current at cycle 148 after a FOURTEEN-CYCLE deferral; sections 1-8 cover c1-c129 and section 9 covers c130-c149.** **BROUGHT CURRENT AGAIN AT CYCLE 150 (CORRECTIONS 197, Track C), because `192`'s pass went stale inside its own cycle.** Two rows are added -- `cdep1` (193) and `cvg1` (194) -- each carrying its REGISTERED SCORER's own token verbatim. **`ciso2`'s row is deliberately NOT here: it is the concurrent track's to write.** Every reader used to compile the two new rows EXCLUDES `ciso2-`, and both registered scorers were re-run UNEDITED against the corpus WITH and WITHOUT `ciso2`'s 12 ingested rows and printed **bit-identical output**, so the two new rows are invariant under that ingest -- demonstrated, not asserted. **The run count and the GPU-hours above cannot be invariant and are not:** they are corpus-wide, and they INCLUDE `ciso2`'s 12 rows as of this compile. **NO SURVIVING VERDICT WAS CHANGED BY THIS PASS AND NO SUPERSESSION WAS EXECUTED, because none is pending:** all five `[CLOSEOUT, CORRECTIONS 115 ...]` annotations still in the file were audited one by one and the only one that moved a verdict column was executed at `192`; the other four read DEMOTED / LABEL CORRECTED, VERDICT HELD / CHECKED AND CLEARED / STAYS OPEN, and none of them moves a verdict. **BROUGHT CURRENT AGAIN AT CYCLE 151 (CORRECTIONS 211, Track T, ZERO GPU): SECTION 10 ADDS THE COUNT-MATCHED PARTITION AUDIT, WHICH HAD NO ROW IN THIS FILE.** 37 rows for cycles 75–110 — the thirteen batches of cycles 75–88, the twelve of cycles 89–110 that thirteen of the headline's twenty cells come from, three pre-data withdrawals and the zero-GPU analyses that decided a question — **appended after section 9 so that no existing row changes line number** (the research-notebook site keys experiments on line numbers; only this header line was edited in place). Every plateau5 number in them was re-derived from the raw `.out` series by the new `analysis/cT211_partition_audit_rederive.py` (368 runs, each matching the CSV to 0.000000; `--check` asserts 53 printed numerals), and the headline re-derives exactly: D > 0 in 20 of 20 count-matched cells, t ≥ 3.0 in 18, the eight-SGDm-cell pool +0.5556 ± 0.0448 (Q 4.21 on 7 df), the alignment null −0.009 ± 0.157 replicated at −0.018 ± 0.079. **Where the prose disagrees with the corpus, section 10 says so** — chiefly that the deficit the effect is set against is −1.807 pp in the paper but −1.796 as `c98_reproduce.py` now derives it on CIFAR-10, and −5.699 pp on CIFAR-100 (`cdn1`), outside the 1.8–4.2 pp MAIN-IDEA-REPORT quotes. **No verdict outside section 10 was changed.**

**Bottom line.** MetaOptimize does not beat a properly tuned non-meta baseline, and **the strongest measurement of that is now on CIFAR-100, not CIFAR-10.** **[AMENDED at cycle 151, CORRECTIONS 209: the denominator now ALSO holds in the parent paper's own, UNAUGMENTED setting -- on CIFAR-10 / ResNet18 ONLY.** `cau1` (27 runs, ONE batch, `AUGMENT=0` on every arm): a tuned SGD+momentum+cosine `lr` ladder peaks INTERIOR at `lr=0.2` with **89.6153 pp** on `plateau5` and beats the better of the method's two `alpha0` cells (`m6t`, `alpha0` 3e-4, **85.9980**) by **+3.6173 pp = +9.92 SE**, registered branch **`DEFICIT-HOLDS`**; the parent's own config (`m6`, `alpha0` 1e-6) sits at **74.4993**, 15.1160 pp below the best rung. TRAIN is at its ceiling on every arm, so the endpoint compares the generalisation of interpolating solutions -- the regime the parent chose. On the parent's own evidence type (AUC) the best-AUC rung still wins by +0.8448 pp (+2.68 SE_AUC), but the endpoint-best rung LOSES on AUC by 4.1099 pp. It says nothing about CIFAR-100 (the +5.699 pp `GAP_in` below stays an `AUGMENT=1` number), about other baseline families, about meta-side tuning (`alpha0` two cells, meta-stepsize 1e-3 only), or about the fixed-step counterweight. The cycle-150 wording follows unchanged.]** On the campaign's own headline 100-epoch CIFAR-100 cell (`ResNet18_c100`, batch `cdn1`, 24 runs, ONE batch so every contrast is WITHIN batch) the MetaOptimize arm plateaus at **71.8933 pp** (n=3, sd 0.0731) while a tuned plain SGD+momentum+cosine `lr` ladder peaks **INTERIOR** at `lr=0.1` with **77.5927 pp**: **`GAP_in` = +5.699 pp = +18.80 SE**, registered branch **`BELOW-BY-A-LOT`**. **EVERY rung of the ladder beats the meta cell** — 75.3733 / 76.3660 / 77.1567 / 77.5927 / 77.0900 / 76.3700 on `plateau5`, smallest margin +3.480 pp — and at 200 epochs plain SGD reaches 78.8280. Both arm means and the gap were re-derived INDEPENDENTLY from `results/all_runs.csv` at cycle 148 and match the registered scorer to four decimals. **That is more than THREE TIMES the 1.46 pp CIFAR-10 deficit this paragraph used to lead with**, which stands as measured but is now the WEAKER of the two: 1.461 pp behind SGDm+cosine at 100 epochs on the mean-of-last-20 column, **-1.807 pp on the PRIMARY `plateau5`** (CORRECTIONS 115), and a flat 1.8-2.0 pp behind AdamW+cosine across config-matched 100/300/600-epoch cells. **What MetaOptimize DOES beat is the fixed-step baseline the parent paper actually used** (+1.551 pp, t +12.2, CORRECTIONS 115) — the deficit is against a *scheduled* baseline, and that distinction survives. Every hierarchical-pooling fix the project funded (M0 shrink, M1 additive r, zpool, and at cycle 116 the two-level `beta_g = s + d_g` and the empirical-Bayes lambda) was refuted by its own controls — though `g4m` overturned the audit's claim that the r-dial is merely a meta-learning-rate knob, because that had never been tested off a single shared `ms`. **CYCLE 73 CHANGES THE HEADLINE.** The ACCURACY REVERSAL (nodewise minus layerwise goes -3.946 pp at epoch 10 with 0/7 seeds to +1.258 pp at epoch 40 with 7/7, ResNet18/CIFAR-10/ms=1e-3) was the campaign's best surviving accuracy result and was explicitly flagged as threatened by the unrun hz9 tuned-meta-step control. **hz9 HAS NOW RUN AND THE THREAT IS REALISED:** with each arm at its OWN optimal ms the trend over the same epoch-10-to-40 window runs the OTHER WAY (+3.444 -> +0.670, a change of -2.774 pp against the shared-ms +5.204 pp), and the epoch-10 sign is itself reversed. The reversal is a distance-from-optimum effect and may no longer be written as a granularity result without the 'at a shared ms=1e-3' qualifier. What survives is the MEASUREMENT line showing per-weight meta-gradients are correlated, short-range and peaked at the kernel scale, whose bridge to a partitioning prescription was itself measured and found negative by >= 2.71 decades. **SECOND AMENDMENT, CYCLE 150 -- THE SCOPE OF THE GRANULARITY RESULT MOVED TWICE SINCE `192` WROTE THIS PARAGRAPH, AND THE TWO MOVES ARE DIFFERENT CLAIMS THAT MUST NEVER TRAVEL TOGETHER.** `192` rewrote this bottom line onto the CIFAR-100 denominator when the corpus was **100 % ResNet** and the isolation's identity leg was **open**. Both of those changed, and neither change touches the deficit above, which stands exactly as written. **(1) THE GRANULARITY GAP NOW REPLICATES OFF ResNet.** On `VGG11_bn_c100` -- a plain BatchNorm conv stack with **NO residual addition anywhere** (26 parameter tensors, 9,274,532 parameters, and no parameter name contains `shortcut`) -- `plateau5(layerwise) - plateau5(scalar)` = **+31.2687 pp = +65.42 SE** at 100 epochs, 6 runs, ONE batch, all gates clean, TRAIN agreeing at **+64.1587 pp** (`cvg1`, CORRECTIONS 194, re-derived here from the runs' own `.out` files). The `network` column now holds **12 distinct values and 6 non-ResNet rows**, so **THE ResNet SCOPE CAVEAT IS LIFTED -- FOR THE GAP.** **[SUPERSEDED: 'the whole line is measured on ResNet only', which was true of the GAP as well until `cvg1` landed.]** `VGG11_bn` is **non-RESIDUAL, not non-BatchNorm**: nothing here says anything about a network without BatchNorm. **[AMENDED at cycle 151, CORRECTIONS 219/220: THAT SENTENCE IS STILL TRUE OF `cvg1` ALONE AND IS NO LONGER TRUE OF THE CAMPAIGN.** `cgn1` (CORRECTIONS 217) ran `ResNet18_gn_c100` -- ResNet18_c100 with every `BatchNorm2d(C)` replaced by `GroupNorm(32, C)`, the SAME 62-tensor list, a BITWISE-IDENTICAL initial point, no buffers and **no batch statistics anywhere** -- and the gap is there, full-size by the registered stamp: `D` = plateau5(layerwise) - plateau5(scalar) = **+37.3833 pp = +63.08 SE**, TRAIN agreeing at **+44.0300 pp**, branch `GAP-REPLICATES | FULL-SIZE`. **SO THE SCALAR->LAYERWISE GAP DOES NOT NEED BATCH STATISTICS.** Two bounds travel with that and must never be dropped: the GN scalar `beta` **never reaches the -15 clamp inside 100 epochs** (0 of 500 records on every seed; it ends at -12.30 / -11.78 / -11.55), so the GN collapse is NOT the clamp object BN's is; and the GN layerwise arm is **still rising at +0.29 to +0.34 pp/epoch over epochs 80-99**, so +37.3833 is a 100-EPOCH SNAPSHOT, not a converged gap. `GroupNorm(32, .)` also changes per-channel invariance, so what is lifted is **'the gap does not need batch statistics'** and NOT 'the gap is normalisation-free'; no other normaliser was tried. **[SUPERSEDED: 'nothing here says anything about a network without BatchNorm' as a statement about the campaign -- true when 203 wrote it, and still true of `cvg1` read alone.]**]** **(2) THE ISOLATION IS IDENTITY-OPERATIVE, NOT DEPTH.** A set of three `layer4` 512-parameter BatchNorm parameters matched to the carriers on group sizes, isolated numel (**1,536, exactly**), width, stage and normalisation-layer type, but containing NO carrier, recovers **0.36 %** of the in-batch gap (`D_DEPTH` **+0.1687 pp = +0.22 SE**) against `DELTA_ID` **+46.5233 pp = +61.56 SE** (`cdep1`, CORRECTIONS 193). **[SUPERSEDED: `ciso1` 'cannot separate tensor IDENTITY from network DEPTH' -- that leg is now closed, and only that leg.]** **(3) THE ISOLATION CAVEAT IS NOT LIFTED, AND MUST NOT BE ALLOWED TO TRAVEL WITH (1).** **[AMENDED at cycle 151, CORRECTIONS 203: AN ISOLATION ARM HAS NOW RUN ON VGG.** `cvi1` isolates `bn8.weight` alone (`[25,1]`) against its exact numel-, width-, class- and adjacent-depth twin `bn7.weight`, and returns **`IDENTITY-OPERATIVE-VGG`**: `DELTA_ID` = **+31.3033 pp = +65.40 SE**; `bn8` recovers **1.0178** of the in-batch gap and `bn7` **-0.0007**. **The caveat is therefore lifted FOR THE IDENTITY CLAIM ONLY, as registered, and for nothing else:** the tensor LIST does not transfer (cardinality **1** on VGG against **3** on ResNet), MAGNITUDE is separated on neither net (`bn8` holds 0.5857 of the in-batch mean-absolute-`L_i` mass, `bn7` 0.0023), the VGG rescue is measured at **100 epochs only** (its complement group is still descending and extrapolates to the clamp at epoch ~109-113), and `VGG11_bn` is still a BatchNorm network. **The ResNet three-tensor isolation must still NOT be described as replicated on VGG.** The text that follows is the cycle-150 wording, kept verbatim.]** The three-BN-scale isolation remains **100 % ResNet**: no isolation arm has ever run on VGG, and `cvg1`'s OWN probe records nominate a VGG carrier set of **cardinality ONE** -- `bn8.weight`, 512 parameters, 0.5784 of the pinned mean-absolute-`L_i` mass, and on its own it flips the remainder sign on **1.0000** of pinned records at all three seeds -- which **DISAGREES with ResNet's three**. Its MAGNITUDE leg is **architecturally unreachable on `ResNet18_c100`**: the carriers rank 1/2/3 of 62 by mean absolute `L_i`, every 512-parameter `layer4` non-carrier sits below the median, and no admissible matched triple closes that, so IDENTITY-OPERATIVE means **'not ANY three matched `layer4` 512-parameter BN parameters'** and does **NOT** separate tensor IDENTITY from term MAGNITUDE. **The gap replicating off ResNet licenses nothing about WHERE the gap lives on any network but ResNet.** **[AMENDED at cycle 151, CORRECTIONS 219/220: THE ISOLATION LINE IS NO LONGER A ResNet-ONLY LINE, AND ITS VGG LEG IS NO LONGER A 100-EPOCH READING.** `cpl1` (CORRECTIONS 218) isolated on `PlainNet18_c100` -- ResNet18_c100 with every `out += shortcut(x)` removed and nothing else (53 tensors) -- and returned `HEAD-CARRIES-PLAIN`: `DELTA_ID` = ISO - CTL = **+55.8460 pp = +98.49 SE**, while `D_ONE` (`layer4.0.bn2.weight` alone, which recovered ~0.91 of the gap on ResNet) = **-0.1027 pp = -0.18 SE**. So an isolation rescue exists on a network with **no residual addition anywhere**, and by subtraction it rides on `layer4.1.bn2.weight`, a summand of nothing -- `PATH`'s prediction, not `189.2`'s. `cvh1` (CORRECTIONS 219) carried `cvi1`'s VGG isolation to **328 epochs** and returned `RESCUE-SURVIVES`: `RHO` = D_ISO@328 / D_ISO@100 = **1.0001** (+32.0093 pp against +32.0053 pp, the 100-epoch control read IN-RUN from the same runs), `COMPLEMENT-PINNED`, `FROZEN-AT-PIN`, post-pin window **220.2 epochs**. **[SUPERSEDED: `cvi1`'s 'the VGG rescue is measured at 100 epochs only (its complement group is still descending and extrapolates to the clamp at epoch ~109-113)' -- the complement DID pin, at epoch 104.8 / 106.4 / 107.8 by step-size magnitude, inside `cvi1`'s own extrapolated bound.]** **WHAT THIS BUYS IS NARROWER THAN IT SOUNDS, AND THE FOLLOWING MUST NOT TRAVEL WITH IT:** the THREE-tensor set is still **100 % ResNet TOPOLOGY** **[AMENDED at cycle 151, CORRECTIONS 225: BUT IT IS NO LONGER 100 % BatchNorm.** `cgn2` isolated the same three indices {50, 53, 59} on `ResNet18_gn_c100` (every `BatchNorm2d` replaced by `GroupNorm(32, .)`, no batch statistics) and returned `IDENTITY-TRANSFERS-GN`: `DELTA_ID` = ISO - CTL = **+41.5167 pp = +67.23 SE** (TRAIN +50.2820), the numel-, width-, stage- and owner-matched {47, 48, 56} at `k01` (+0.1040 pp), and `layer4.0.bn2.weight` alone rescuing (`D_ONE` **+26.1913 pp = +42.41 SE**, RECOVERY_ONE 0.6579 against ~0.91 on BN, between-batch). So THE CARRIER SET, NOT ONLY THE GAP, DOES NOT NEED BATCH STATISTICS. Bounds that travel with it: a 100-epoch level with NO complement pin and every complement `beta` still descending (`RESCUE-PERSISTENCE-NOT-TESTED`), ISO above layerwise (+1.81 pp) while both still climb, and GroupNorm(32) is still a normaliser.]** **[AMENDED at cycle 152, CORRECTIONS 234: THE GROUPNORM RESCUE HAS BEEN CARRIED TO 430 EPOCHS, AND IT IS A REGIME, NOT A DELAYED COLLAPSE.** `cgn3` (CORRECTIONS 231, `RESCUE-SURVIVES`) ran `cgn2`'s cell to 430 epochs with the 100-epoch control read IN-RUN: `RHO` = D_ISO@430 / D_ISO@100 = +48.5193 / +39.6573 = **1.2235** (+62.34 SE against +50.95 SE; TRAIN RHO 1.6144), ISO's 59-tensor complement is `COMPLEMENT-PINNED` on 3/3 seeds (first r <= 2 at epoch 238.4 / 221.0 / 215.2) and ISO's TEST is flat for the 191.6 epochs after the latest pin (`FROZEN-AT-PIN`). `layer4.0.bn2.weight` alone survives as well and GROWS: `RHO_ONE` 1.6418, its share of the `kL` gap rising **0.68 -> 0.81** (`ONE-SHARE-RISES`), its own complement pinning at 282.6 / 282.8 / 312.6, so on GroupNorm ONE's 'partial' 100-epoch rescue was mostly slowness. Bounds that travel with it: **the level settled BEFORE the pin, not at it** -- ISO's arm-mean TEST is within 0.5 pp of its end from epoch ~162, while the complement's step size was still 49-76 x the floor and TRAIN kept climbing -- so 'the pin froze the level' is NOT licensed and why TEST plateaus is UNSURE; **the rescued arm does not track the layerwise ceiling at long horizon** -- `kL` climbs on to 66.3853 (TRAIN 99.79), passes ISO for good at epoch ~174, and ends **3.02 pp above ISO** at 430 (ISO - `kL` = -3.0233 pp = -3.88 SE, `CEIL-BELOW`), so 'ISO above layerwise' was a 100-epoch transient; D_ISO@100 in-run sits 1.96 pp below `cgn2`'s (between batch, descriptive); the matched-set control was not re-run (`NO-IN-BATCH-CTL`), so 'the carriers, not any three tensors' is still a 100-epoch statement; nothing past 430 epochs; magnitude not separated; GN(32) still a normaliser; RULE 11.]** **[SUPERSEDED: 'a 100-epoch level with NO complement pin and every complement `beta` still descending (`RESCUE-PERSISTENCE-NOT-TESTED`), ISO above layerwise (+1.81 pp) while both still climb' -- true of `cgn2` at 100 epochs; at 430 the complement pins, the rescue survives it, and `kL` sits above ISO.]** **[SUPERSEDED: 'the THREE-tensor set is still 100 % ResNet and that clause stands as written' -- still true of the topology, no longer of the normaliser.]**; the tensor LIST does not transfer (cardinality **1** on VGG, **1** on PlainNet, **3** on ResNet with BatchNorm and with GroupNorm) **[SUPERSEDED: '**2** surviving on PlainNet' -- `cpl2` (CORRECTIONS 226) isolated `layer4.1.bn2.weight` ALONE and returned `HEAD-CARRIES-ALONE-PLAIN`: `DELTA_HEAD` = HEAD - TWIN = **+52.1367 pp = +91.95 SE**, `D_PAIR` = ISO - HEAD = +3.5940 pp, inside the frozen 5.0 pp bar but resolved (+6.34 SE), so 44 adds a small increment the branch does not count]**; MAGNITUDE is separated on NO network (`MAGNITUDE-NOT-SEPARATED` is stamped on `cpl1` and `cvh1` alike, and `cvh1` is `NOT-A-ONE-VARIABLE-ABLATION`) **[AMENDED at cycle 151, CORRECTIONS 225/226: and it CANNOT be by set-matching on GroupNorm or PlainNet either -- on GN every DOWN-voting non-carrier TOGETHER holds 0.0324 of `k01`'s post-peak mean-absolute-`L_i` mass against the carriers' 0.4653 (3.1x below the smallest carrier alone); on PlainNet the largest DOWN-voting non-carrier is the TWIN itself, 691x below `layer4.1.bn2.weight`; the only magnitude-matched non-carriers on either net vote UP. Separating identity from magnitude needs an intervention on the vote, not another matched set]** **[AMENDED at cycle 152, CORRECTIONS 234: THAT INTERVENTION HAS NOW RUN, ON PlainNet, AND WHAT IT SEPARATES IS STEP SIZE FROM VOTE -- NOT IDENTITY FROM MAGNITUDE.** `cvt1` (CORRECTIONS 230, `STEP-SIZE-NEEDED-VOTE-SUFFICES`) weighted `layer4.1.bn2.weight`'s term INSIDE the shared meta-gradient sum, before the Lion sign. Silencing that term while the tensor keeps the shared step size does **NOT** rescue (MUTE **10.9860 pp** against `k01` 11.9493; DOSE x0.1 11.9487); isolating it does (HEAD **64.7940 pp**, the positive control; `P_VOTE` = HEAD - MUTE = **+53.8080 pp = +94.90 SE**); and casting a carrier-sized vote from its bn1 twin (`layer4.1.bn1.weight` x691) into HEAD's rescued complement re-pins that complement at epoch 37.6 and drops the arm to **30.1400 pp** (`P_INJECT` = HEAD - INJECT = +34.6540 pp = +61.12 SE). **So on PlainNet the rescue needs the carrier's OWN STEP SIZE, not merely the removal of its vote, and a carrier-sized DOWN vote cast by one other tensor (its bn1 twin) collapses that complement.** **[CORRECTED IN PLACE at cycle 152, CORRECTIONS 236: 'whoever casts it' claimed more than the test showed -- ONE other caster was run (`layer4.1.bn1.weight`, idx 47), into ONE complement, at one K in `cvt1` and at five K in `cvt2`; whether any other tensor's term does the same is untested. SUPERSEDED wording, kept verbatim: 'and a carrier-sized DOWN vote collapses a complement whoever casts it.']** Bounds that travel with it: INJECT is a PARTIAL collapse (0.344 of the HEAD gap kept, 18.19 pp above `k01`), and whether that residue is timing (the x691 vote turns DOWN only from ~epoch 18) or HEAD's identity is UNSURE -- the patch has no time gate, so dose and onset are confounded; 'vote removal does not suffice' is NOT 'the vote is irrelevant' -- on MUTE's records the DOWN vote is RE-CARRIED by other BN scales (47, 44, 41, 38; every record DOWN from epoch 36); DOSE cannot speak to threshold vs graded (MUTE itself sits at `k01`); magnitude is still NOT separated from identity in LEVEL; one network, one cell, 100 epochs, RULE 11. `cvt3` (232, the whole DOWN coalition silenced) and `cvt2` (233, a dose ladder on the injected vote) are registered and running and have NOT landed.]** **[AMENDED at cycle 152, CORRECTIONS 235: `cvt3` HAS LANDED, `OWN-STEP-NECESSARY` -- SILENCING THE WHOLE DOWN COALITION ON THE SHARED STEP SIZE DOES NOT RESCUE EITHER.** Silencing `layer4.1.bn2.weight` AND the 20-tensor DOWN coalition fixed by RULE C (0.828 of the vote mass on `cvt1` MUTE's post-pin records), with every tensor on the one shared step size, leaves the arm at `k01`: MUTEDOWN **11.3293 pp** against `k01` 11.5720 (`D_DOWN` -0.2427 pp = -0.43 SE, TRAIN -0.3620); MUTE50 replicates `cvt1`'s MUTE at 11.2947; HEAD 64.5960 (`D_HEAD` +53.0240 pp = +93.52 SE). G-BITE and G-BITE-SET pass on 15/15, and G-RUNAWAY stamps no runaway on any run (highest shared beta -4.11, far under the -2.3026 ceiling), so the feared UP-runaway did not happen. So 'the rescue needs 50 on its own step size' is STRENGTHENED as registered, at the coalition level. Bounds that travel with it: **the DOWN vote was re-carried AGAIN, by the next tier** -- on MUTEDOWN's post-epoch-36 records the weighted sum still votes DOWN on 0.91-0.99 of records, carried by tensors the 0.90 eligibility bar left in (`layer3.0.conv1.weight`, `layer2.0.conv1.weight`, `layer2.1.conv2.weight`, `bn1.weight`, each DOWN on 1.00 of records; 0.55-0.60 of the weighted mass votes DOWN outside the coalition), and its shared beta first reached -14 at epoch 40.4-43.0 against MUTE50's 38.2-38.6 -- so 'no silenced set rescues' is NOT licensed; `P_COAL` = MUTEDOWN - MUTECTL = +3.5080 pp (+6.19 SE) is positive because the matched control fell BELOW `k01` (MUTECTL 7.8213, `CTL-BELOW-K01`, -3.75 pp), not because MUTEDOWN rose (MUTEDOWN - MUTE50 = +0.03 pp), and why silencing 15 BN biases and 5 small convs costs 3.75 pp is UNSURE; the control is count- and class-matched, not mass-matched (1/30 of the coalition's mass); no time gate; one network, one cell, 100 epochs, RULE 11.]** **[AMENDED at cycle 152, CORRECTIONS 236: `cvt2` HAS LANDED, `GRADED` + `TOP-ATTENUATED` -- THE INJECTED COLLAPSE IS GRADED IN THE VOTE'S WEIGHT, AND `cvt1`'s 0.344 RESIDUE IS NOT A FIXED FLOOR.** HEAD's grouping with the twin's term x K, one batch: K 1 (HEAD) **64.4187 pp**, K13 **52.3493**, K33 **35.5933**, K152 **34.3420**, K691 **30.2773** (`cvt1`'s INJECT byte for byte; 30.1400 there, between batch), K2000 **25.0180**, `k01` 11.4767; the share of the HEAD gap kept falls 0.772 / 0.456 / 0.432 / 0.355 / 0.256; `TOP` = K691 - K2000 = +5.2593 pp = +7.39 SE (in-batch sigma 0.8721 used). So a vote's collapsing effect is a matter of degree in K, not a switch, and the partial level of `cvt1`'s INJECT is not a floor fixed by HEAD's identity. Bounds that travel with it: **both verdict words sit on near bars** -- K33 is `BETWEEN` only 0.32 pp outside the 5.0 pp MATCH bar of K691's plateau (+0.44 SE) and `TOP` is `ATTENUATED` only 0.26 pp over it, so `GRADED` rests on K13 alone (7.07 pp and 17.07 pp clear of both bars), and K13 is still RISING at 100 epochs (+0.015 to +0.028 pp per epoch; its complement never pins and is still descending, arm mean -12.16 at epoch 100), so K13's level may be a delay; the registered sign-saturation prior failed -- K33, below the 84-118 'sustain' K derived from `cvt1`, holds its collapse on 0.92-1.00 of its own records, because the rest's net UP vote measured in units of the twin's term scales with K along each arm's own trajectory (median 0.9 / 12.9 / 26-30 / 32-41 / 78-96 / 197-279 for K 1 ... 2000); from K33 up the complement's beta descends at Lion's maximum rate on every arm and the rungs differ mainly by ONSET (peak epoch 18.0 -> 17.2 and first beta <= -8 at epoch 24.4 -> 23.0 from K152 to K2000), so the dose effect above 152 is consistent with onset bought by dose -- dose and onset are confounded (no time gate), UNSURE; one caster, one complement, one network, 100 epochs, RULE 11. **Read together (descriptive, NOT a registered statistic, UNSURE):** in every scalar arm of `cvt3` the shared step-size multiplier over epochs 15-30 was LARGER than HEAD's complement's (arm means 10,749 / 11,364 / 13,845 x the floor for MUTE50 / MUTEDOWN / MUTECTL against HEAD's 8,672) and their TEST settled by epoch 21-25 while it was still large, whereas inside HEAD's grouping (`cvt2`) the level ranks with the complement's multiplier in that window (Spearman 0.965 over 18 runs) -- consistent with 50's OWN step size, not the complement's vote, being the binding constraint; no intervention on 50's step size alone has been run.]** **[SUPERSEDED: '`cvt3` (232, the whole DOWN coalition silenced) and `cvt2` (233, a dose ladder on the injected vote) are registered and running and have NOT landed' -- both landed, at CORRECTIONS 235 and 236.]** **[AMENDED at cycle 152, CORRECTIONS 240: `cvt4` HAS LANDED, `OWN-STEP-MAGNITUDE` -- WITH ITS VOTE OUT OF THE COMPLEMENT'S SUM, THE MAGNITUDE OF `layer4.1.bn2.weight`'S OWN STEP SIZE DECIDES THE LEVEL.** In HEAD's grouping, one batch, the patch `PATCH_BETAHOLD` held 50's beta: an exogenous replay of MUTE's measured shared trajectory (HOLDHIGH, `tri:9428`) stalls the arm at **11.1267 pp** against `k01` 11.6947, as HOLDSHARED (= MUTE in exact arithmetic) does at 11.3940; 50 frozen at the -15 floor (HOLDLOW) keeps **65.4607** against HEAD 64.7347; a replay of HEAD's own 50-trajectory (HOLDHEAD, the hold's positive control) reproduces HEAD at 64.6660. `P_HIGH` = HEAD - HOLDHIGH = **+53.6080 pp = +94.55 SE** (TRAIN +68.8720), `P_COUP` = HOLDHIGH - HOLDSHARED -0.2673 pp, `P_LOW` = HOLDLOW - HEAD +0.7260 pp; G-BITE passes on 18/18. **So on PlainNet the rescue needs 50 on a SMALL step size, confirmed by intervention.** Bounds that travel with it: **'complement free' was not 'complement like HEAD's'** -- in HOLDHIGH the unforced complement followed MUTE's collapse path and pinned at epoch 40.4 against HEAD's 97.8-99.6, so whether 50's large step size stalls the network directly or through the complement's early collapse is NOT separated (descriptive, UNSURE: the TEST gap is already +2.05 pp after 17 epochs while the two complements' beta differ by 0.001, so the stall begins before they part); no time gate and one trajectory shape per arm, so which window matters and any dose curve are untested; the floor readings are locations, and `P_LOW` (seeds +0.92 / +1.14 / +0.12, not a registered effect) speaks only to the first ~22 epochs; one network, one cell, 100 epochs, RULE 11.]** **[SUPERSEDED at cycle 152, CORRECTIONS 240: 'no intervention on 50's step size alone has been run' -- `cvt4` ran it and returned `OWN-STEP-MAGNITUDE`.]** **[AMENDED at cycle 152, CORRECTIONS 241: `cvt5` HAS LANDED, `K-DEPENDENT-EQUILIBRIUM` | `RESCUE-SURVIVES` | `K33-HOLDS-PINNED` -- ON PlainNet AT 300 EPOCHS K13's PARTIAL LEVEL HOLDS AND THE HEAD RESCUE SURVIVES ITS OWN COMPLEMENT'S PIN.** One batch, 300 epochs with the 100-epoch control read in run: K13 **53.0807 pp** against K33 **37.1347** (`G13` +15.9460 pp = +23.04 SE) and against its own 100-epoch level 52.6613 (`DROP13` +0.4193 pp), so `cvt2`'s `GRADED` is not a delay at 300 epochs; `RHO` = D_HEAD@300 / D_HEAD@100 = **1.0012** (HEAD 64.3147, `k01` 11.6700 at 300), with HEAD's complement pinned by step-size magnitude at epoch 62.2-64.0 and the level frozen for the 236 epochs after -- `cvh1`'s / `cgn3`'s sentence on a fourth network; K33's complement stays inside the magnitude pin and its level moves +0.19 pp. Bounds that travel with it: every level-holds and frozen-at-pin stamp sits only 0.96-1.34 pp inside READ_BAR 1.384 (locations, not tight nulls); K13's complement is 'pinned' by its median only -- after its first r <= 2 at ~160 it goes back above r 2 on 74 / 104 / 5 records and s94 ends the run at r 2.59, so its step size is small, not frozen; K13's TEST settled at epoch 105, ~55 epochs BEFORE that pin, so 'the pin froze the level' is NOT licensed; K13 and K33 only; nothing past 300 epochs; one network, one cell, RULE 11.]** **[SUPERSEDED at cycle 152, CORRECTIONS 241: 'K13 is still RISING at 100 epochs (+0.015 to +0.028 pp per epoch; its complement never pins and is still descending, arm mean -12.16 at epoch 100), so K13's level may be a delay' -- at 300 epochs it is not: `DROP13` +0.4193 pp, `K-DEPENDENT-EQUILIBRIUM`.]**; `layer4.1.bn2.weight` alone lifts PlainNet's scalar arm from 11.8120 to **64.1767 pp** (0.9358 of the pair's rescue) while its exact bn1 twin sits at 12.0400, and its complement still pins at the clamp at epoch ~98, so that rescue too is a level read at 100 epochs **[SUPERSEDED: '`layer4.1.bn2.weight` has never been isolated alone' -- true until `cpl2`]** **[SUPERSEDED at cycle 152, CORRECTIONS 241: 'so that rescue too is a level read at 100 epochs' -- `cvt5` read it at 300 epochs in run (`RESCUE-SURVIVES`, `RHO` 1.0012); by step-size magnitude the complement pins at epoch 62.2-64.0 and the level holds for 236 epochs after, and the 'pin at the clamp at epoch ~98' is exact-clamp dwell, which in `cvt5` starts only at 299.8 / never / 299.8.]**; the VGG rescue is a **FREEZE** (ISO's TEST moves +0.0680 pp = +0.09 SE over the 220 epochs after the pin) and not continued learning, and ISO is NOT ordered against layerwise (+0.4973 pp apart at 328, inside `READ_BAR` 1.5114); and no horizon beyond 328 epochs is measured on any network. **[SUPERSEDED at cycle 152, CORRECTIONS 234: 'no horizon beyond 328 epochs is measured on any network' -- `cgn3` (231) measured `ResNet18_gn_c100` to **430 epochs** (`RESCUE-SURVIVES`, `RHO` 1.2235); the clause stays true of every other network.]** **[SUPERSEDED: the isolation line as 'measured on ResNet only' -- it now has legs on two non-ResNet-topology networks, and neither of them replicates the three-tensor set.]** **AND THE DENOMINATOR'S OWN OPEN COUNT IS CLOSED.** `209`'s `cau1` amendment above says in terms 'It says nothing about CIFAR-100 (the +5.699 pp `GAP_in` below stays an `AUGMENT=1` number)'. `cuc1` (CORRECTIONS 220) settles it: on `ResNet18_c100` / CIFAR-100 with **`AUGMENT=0` on every one of 30 runs**, a tuned plain SGD+momentum+cosine `lr` ladder peaks **INTERIOR** at `lr = 0.4` with **65.0573 pp** and beats the best of a **five-cell** meta-stepsize x alpha0 grid (`m1e3a1e3`, **52.8793**) by `GAP_END` = **+12.1780 pp = +26.05 SE**, branch `DEFICIT-HOLDS | LADDER:INTERIOR`, with the early-stopping reading agreeing (+9.1087 pp = +25.37 SE_PEAK). **Removing augmentation does not rescue the method on CIFAR-100; on this network the deficit widens** -- +12.178 unaugmented against +5.699 augmented (`cdn1`), and against +3.617 unaugmented on CIFAR-10 (`cau1`) -- **but that three-way comparison is BETWEEN batches and is orientation, not a measurement.** Still NOT licensed by `cuc1`: the paper's own alpha0 1e-6 (not on the grid), any granularity but `chunk771`, any baseline family but SGD+momentum+cosine, any counterweight on the method side, meta-side tuning as exhausted (M* sits at a grid EDGE whose rise over the CENTRE is +0.28 SE, so ms-HIGH under-tuning is BOUNDED by a flat top, not excluded), and the AUC reading as a clean win -- at the endpoint-best rung the AUC comparison REVERSES by 2.3489 pp, exactly as on CIFAR-10. **[SUPERSEDED: 'It says nothing about CIFAR-100', true of `cau1` and now answered by `cuc1` for `AUGMENT=0` on ResNet18/CIFAR-100 and for nothing else.]**]**

**What cycles 130-148 added (section 9).** Cycles 130-139 ran ZERO GPU and were paper integration. From cycle 140 the campaign stopped asking *whether* partitioning helps and started asking **WHERE in the network the gap lives**, and that line converged: the HEAD hypothesis died (`hb1`, `REFUTED-SMALL`); group COUNT and size-BALANCE were both disqualified as the operative variable, the second by a mirror control the submitting agent added beyond the brief (`cbl1`, `in489g2`); cut POSITION localised the effect to a single step (`cpk1`, `k*=49`, replicated at a 772-epoch converged budget on fresh seeds by `cpk2`/`cpk3`); and the step was then attributed to **individual 512-parameter BatchNorm scales in layer 4** — `layer4.0.bn2.weight` (`cts1`, +24.834 pp at 100 epochs), `layer4.0.shortcut.1.weight` (`scl1`, 15.8080 pp) — surviving a clamp release (`cts2`) and shrinking but surviving a 7.72x horizon (`cts3`, `SURVIVED` **and** `SHRINKS`). An independent dynamics route arrived at **the same three tensors**: under scalar dynamics they carry the meta-gradient sign against the other 59 and vote `beta` down on every pinned record (`ctd1`), and isolating them recovers the WHOLE in-batch gap while isolating their layer-2 homologues recovers nothing (`ciso1`, +46.9307 pp vs -0.0633 pp). **Three things bound that arc and are recorded with it:** `ciso1` cannot separate tensor IDENTITY from network DEPTH; the whole line is measured on ResNet only; and H-DOMINATE *as named* is dead (184, 186) — what survives is the sign of the remainder sum, not domination. The cycle-148 batches registered to attack exactly those limits (`cdep1`, `cvg1`, `ciso2`) were in flight at compile time and are NOT in this table.


## 1. Non-meta baselines — the denominator the method must clear

| tested | varied | scale | result | verdict | so what | ref |
|---|---|---|---|---|---|---|
| Is there a tuned non-meta baseline at all, and where does its learning rate peak? | AdamW+cosine peak LR over {1e-5,3e-5,1e-4,3e-4,1e-3,3e-3,1e-2} | n=2-5/cell, 100 ep, ResNet18/CIFAR-10, AUGMENT=1 | Interior maximum at lr=3e-3: 94.417 +-0.113 pp plateau (n=5, 100 ep, SW_3e-3 x2 + fxcos-3e-3 x3), re-derived from the CSV. The 1e-3 cell is 94.028-94.087 — a rising edge, not a peak. The first 275 runs of the campaign had NO non-meta baseline at all. | **REFUTED** | Every deficit computed against 94.093 understated the gap; FINDINGS 38.5's 'interior maximum at 1e-3' is dead. | CORRECTIONS 25, closed by 30; FINDINGS 38.5/39.2/43.4 |
| Is the method-vs-baseline gap caused by the cosine SCHEDULE? | CORRECTED: the contrast originally reported (COS_TOTAL=50000 vs 422000) WAS NEVER RUN — all 14 SW-* and all fxcos-* .out headers read COS_TOTAL=50000 and differ only in warmup (10000 vs 2500 steps). The genuinely near-constant arms are fx-adamw-* (no COS_TOTAL in the ENV line) and Idea-3 arm A (COS_WARMUP=0, COS_TOTAL=1e8). | n=2-5/cell, 100 ep, R18/CIFAR-10, AUGMENT=1 | Near-constant fx_adamw peaks at 91.859 (lr 3e-4, n=5); at lr 1e-3 it is 90.264 (n=2) and at 3e-3 it collapses to 85.962 (n=2). Horizon-matched cosine peaks at 94.417 (lr 3e-3, n=5). Peak-to-peak the schedule is worth +2.56 pp; at matched lr=1e-3 it is +3.83 pp (94.087 vs 90.264). Idea-3's pre-registered three-arm design agrees: arm A (genuinely fixed LR) peaks at 91.796 (n=3) against arm C's 94.028 = 2.23 pp. | **CONFIRMED, CLOSED-OUT 115** *(superseded verdict token kept verbatim: **OPEN**. The `[CLOSEOUT, CORRECTIONS 115 — VERDICT MOVES OPEN -> CONFIRMED]` annotation has been in this cell since cycle 83 and the verdict COLUMN was never updated; applying it here is executing a registration already written into this file, not a new judgement. This is the ONLY verdict cell cycle 148 changed.)* | REOPENED. The old +0.10 +-0.10 pp null is a WARMUP-LENGTH null between two horizon-matched cosines, not evidence about the anneal. 'The gap is the peak learning rate, not the schedule' is NOT supported — on arms that actually differ in schedule the anneal is worth 2.2-3.8 pp. (Also: 0.102 pp is above the 0.05 pp floor; what made it unresolvable was the +-0.10 spread.) | CORRECTIONS 30 mis-attributes 422000; docs/IDEA3-robustness.md:79 contradicts FINDINGS 6734 | **[CLOSEOUT, CORRECTIONS 115 — VERDICT MOVES OPEN -> CONFIRMED, re-scored on PRIMARY plateau5.]** The gap is the SCHEDULE. The genuinely unscheduled arm already existed and was not used: `i3a-*` (COS_WARMUP=0, COS_TOTAL=1e8, LR constant to 6.2e-7 relative). All arms at their own INTERIOR bracketed optima: constant LR 91.767+-0.167 @3e-4; MetaOptimize 93.317+-0.143 @alpha0=3e-4; AdamW+cosine 94.769+-0.157 @3e-3; SGDm+cosine 95.124+-0.105 @lr=0.1. meta - constant = +1.551 (t +12.2); meta - best cosine = -1.452 (t -14.3). **Against the fixed-step AdamW baseline the parent paper actually used, MetaOptimize WINS.** THE SHAPE/PEAK-LR SPLIT IS NOT INVARIANT AND IS REPORTED AS A RANGE, NOT A NUMBER: the shape term at matched peak LR is +1.59 pp at 1e-4, +2.29 at 3e-4 and +4.86 at 1e-3, so the shape share runs 39%-76% depending on the matching point and no invariant decomposition exists. ALSO: this row's own SGDm baseline 94.766 is the mean-of-last-20 column; on plateau5 the tuned peak moves to lr=0.1 at 95.124 and the head-to-head deficit is -1.807, not -1.462. GUARD DISCLOSURE: every MetaOptimize number here carries BETA_CLIP=-15:-2.3026 with NO probe directory, and at alpha0=3e-4 the ceiling is algebraically reachable from ~epoch 12 — occupancy is UNMEASURED, so this row is CLOSED-WITH-GUARD-CAVEAT.
| Does the experimental harness reproduce a known-good reference, i.e. is the deficit a property of the method rather than of our code? | SGD+momentum+cosine, lr {0.01, 0.03, 0.1} | n=5/cell, 100 ep, R18/CIFAR-10 | 94.766 pp at lr=0.03 (n=5, 100 ep, sd 0.096) — inside the pre-registered 94.5-95.5 band. Best MetaOptimize arm at the same budget: 93.304 +-0.144 (i3b-3e4, n=3, 100 ep). | **CONFIRMED** | Harness validated, so the 1.461 pp deficit is the method's. Every '0.79 pp deficit' sentence written before this is stale. | FINDINGS 39.3 |
| Does the deficit close if MetaOptimize is given a longer horizon (the parent paper's implicit defence)? | budget 100 -> 300 -> 600 epochs | n=3-10/cell, R18/CIFAR-10 | Every individual cell re-derives (bg300_cos 94.999 / bg300_meta 93.033 / bg600_cos 95.138 / bg600_meta 93.201, all n=3, all complete). CORRECTED: the arms are NOT matched across the budget axis — the 100-ep pair used AdamW+cosine lr=3e-3 vs SGDm+Lion ms=1e-4, while 300/600 use AdamW+cosine lr=1e-3 vs AdamW+Adam ms=1e-3. Config-matched to the bg family at 100 ep: cosine lr=1e-3 = 94.087 (n=7) vs AdamW+Adam layerwise a0=1e-4 ms=1e-3 = 92.270 (n=10), deficit 1.817 pp. Matched series = 1.82 -> 1.97 -> 1.94. | **REFUTED** | The long-horizon defence is CLOSED as a clean negative — but the gap is FLAT at 1.8-2.0 pp, it does not widen. Do not quote 1.622 alongside 1.967/1.937. | CORRECTIONS 36(2); FINDINGS 41.4/42.1/43.5/44.1 |
| Does the baseline's win survive a change of architecture (is it an R18 accident)? | AdamW+cosine at lr {3e-4, 1e-3} x network {ResNet34, ResNet50} | n=3/cell, 100 ep, CIFAR-10 | R34 94.674 (n=3, sd 0.026), R50 94.855 (n=3, sd 0.155) at lr=1e-3, 100 ep. Best R34 MetaOptimize arm 93.930 (r34f r=0.025, n=3) -> deficit 0.744 pp, but that is the ARGMAX of a 13-point eta_ratio sweep and is therefore selection-inflated; against the unselected r=0 cell (93.097) the R34 deficit is 1.577 pp. | **CONFIRMED** | A single tuned LR transfers across 11.17M -> 23.52M params = ~2.1x (NOT 4.8x — there is no non-meta cosine baseline at ResNet10 anywhere in the CSV), which weakens the 'tuning-free is worth the deficit' defence. | FINDINGS 38.4; c32_cluster_a B3; selection caveat per CORRECTIONS 3 |
| Was the method-vs-baseline comparison FAIR (matched budget, matched search budget, the paper's own setup)? | n/a — audit of the comparison protocol | whole corpus | Flaw 1 (budget) and flaw 2 (search budget) closed by CORRECTIONS 30/44. **[CORRECTED at cycle 151, CORRECTIONS 204, on 202.1's finding, re-derived from the corpus: flaw 3 AS REGISTERED in CORRECTIONS 10 is a GRANULARITY contrast at AUGMENT=0 (paper config, scalar vs 6-block vs layerwise, n=3). It ran THREE times -- `pp_*` 9, `PP_*` 9 and `ub9` 9 rows, which are ALL 27 augment=0 rows in the corpus, every one MetaOptimize AdamW+Adam -- and CLOSEOUT 5a closed it (CLOSED-WITH-GUARD-CAVEAT). `ub9` has 9 probe directories, a saved launcher (`bin/c88_ub9_widebox.sh`) and a registered scorer. What is OPEN is only the METHOD-vs-TUNED-BASELINE half at AUGMENT=0: zero non-meta rows are recorded augment=0, and CLOSEOUT 5b declined that batch as NOT-WORTH-COMPUTE. It is registered as `cau1` (202), 27 jobs, and NOT submitted. The cycle-150 wording follows, kept verbatim.]** Flaw 3 — the parent paper's own UNAUGMENTED setup — is still open: the only AUGMENT=0 runs (PP-*) were submitted against the standing rule, carry no probe directory, and may not be pooled with augmented rows. | **CONFIRMED, RESCOPED 229** **[SUPERSEDED: OPEN]** (superseded verdict token kept verbatim in the bracket. Moved at cycle 152, CORRECTIONS 229, because `cuc1` (220) closed the two counts that `209` left open; the scope limits that still bind are stated at the head of the next cell and travel with the verdict.) | **[AMENDED at cycle 152, CORRECTIONS 229: BOTH REMAINING COUNTS ARE CLOSED AND THE VERDICT MOVES OPEN -> CONFIRMED, RESCOPED.** `cuc1` (CORRECTIONS 220; row appended at the end of this file) ran the unaugmented comparison on CIFAR-100 (`ResNet18_c100`, `AUGMENT=0` on all 30 runs) against a 5-cell meta-stepsize x alpha0 grid, not two alpha0 cells: a tuned SGD+momentum+cosine `lr` ladder, argmax INTERIOR at `lr` 0.4, beats the best cell by `GAP_END` +12.1780 pp = +26.05 SE (`DEFICIT-HOLDS`). With `cau1` (+3.6173 pp = +9.92 SE, CIFAR-10) every registered fairness flaw is now closed: budget and search budget (30/44), and the parent's own unaugmented setup on both datasets. **What CONFIRMED licenses: the deficit is not an artefact of the three audited unfairnesses. What it does NOT license, and must travel with it:** (1) ONE baseline family, SGD+momentum+cosine (`ONE-BASELINE-FAMILY` on both batches); (2) the paper's own alpha0 1e-6 is UNTESTED on CIFAR-100 (`PAPER-CONFIG-NOT-TESTED`; it is measured only on CIFAR-10, where `m6` sits +15.1160 pp behind); (3) ONE granularity per batch (`chunk771` on CIFAR-100, `resnet18_blocks` on CIFAR-10), so meta-side tuning is BOUNDED, not exhausted -- `cuc1`'s M* sits at the ms-HIGH grid EDGE with a +0.28 SE rise over the centre. Also: no method-side counterweight on either dataset, ResNet18 only, 100 epochs only, and the AUC reading reverses at S* on both datasets. The superseded verdict (OPEN) and wording follow, kept verbatim.]** **[AMENDED at cycle 151, CORRECTIONS 209: `cau1` RAN (27/27) and LANDED `DEFICIT-HOLDS` -- at `AUGMENT=0` a tuned SGDm+cosine ladder beats the method's better `alpha0` cell by +3.6173 pp = +9.92 SE and the parent's own config by +15.1160 pp -- on CIFAR-10 / ResNet18 ONLY. The row STAYS OPEN: the unaugmented comparison has not been run on CIFAR-100, and the method side got two `alpha0` cells and one meta-stepsize, not a tuning ladder. The earlier wording follows, kept verbatim.]** Open on the METHOD half only; registered as `cau1` (202) and not run. **[Superseded: The one remaining fairness objection a referee can raise; cheap to close and never closed.]** | CORRECTIONS 10 (cycle 13), 28.2 (and 202, 204, 209); closed by 220, verdict moved at 229 |

## 2. The method's core axis — step-size granularity

| tested | varied | scale | result | verdict | so what | ref |
|---|---|---|---|---|---|---|
| Does a finer partition of the step size help at all? | granularity {scalar, 6-block, layerwise, nodewise, weightwise} (m = 1 ... 11.17M) | 100 ep, R18/CIFAR-10, SGDm base + Lion meta, a0=1e-3, AUGMENT=1, BETA_CLIP -15:-2.3026, epochs_done=100 | At the shared ms=1e-3: layerwise 91.097 (n=14, sd 0.197) - scalar 87.758 (n=14, sd 0.212) = +3.339 pp, SE 0.077, t=43.2. At each arm's OWN tuned meta-step (ms=1e-4): layerwise 92.795 (n=5, sd 0.177) - scalar 92.231 (n=5, sd 0.190) = +0.564 pp, 95% CI [+0.296, +0.831], t=4.85. | **CONFIRMED** | Real but ~6x smaller than the headline suggested — 81-83% of the apparent gain is meta-step tuning. CORRECTED n: the tuned contrast is n=5 per arm (not n=3 — the quoted CI is itself the df=8 interval) and the ms=1e-3 cells are n=14 per arm. | CORRECTIONS 24; FINDINGS 42.2, 57.2 | **[CORRECTIONS 153 — FIRST WITHIN-BATCH REPLICATE.]** `cfr1` re-measures this row's shared-ms=1e-3 contrast inside ONE submission (ResNet18/CIFAR-10/SGDm+Lion/a0=1e-3/AUGMENT=1/100 ep, canonical box, n=3 v 3): layerwise 91.3133 (sd 0.1406) − scalar 87.8707 (sd 0.2392) = **+3.4427 pp, 19.52 SE_GAP**, against **3.3585 pp** pooled cross-batch over six ARM-BALANCED families (a0L 2 / a0h 2 / ms 3 / mx 5 / rs 2 / tl1 3 per arm) — **0.084 pp = 0.48 SE_GAP apart**, with the cross-batch offset cancelled by design rather than argued away. TRAIN agrees (+5.9933 pp). Metric note: this row's printed 91.097 / 87.758 / +3.339 are the BANNED k=20 `plateau` column; on the PRIMARY plateau5 the same cells read 91.194 / 87.835 / **+3.358** (n=17 each) — the headline is metric-robust, the numerals are not the primary ones.
| Is the granularity axis just a reparameterisation of the meta-stepsize axis? | granularity x meta-stepsize response surface, ms 1e-8 ... 1e-2 | n=3-5/cell, 100 ep, R18/CIFAR-10 (c40, 106 jobs) | Both scalar and layerwise peak at the SAME ms=1e-4. layerwise - scalar = -0.109 / +0.175 / +0.103 / +0.563 / +3.142 / +3.339 pp at ms = 1e-8 / 1e-5 / 3e-5 / 1e-4 / 3e-4 / 1e-3. Falloff above the peak (**PRIMARY `plateau5`**; mean chord slope from each arm's peak to every higher rung of the canonical ladder; canonical box; `analysis/cQ1_row24_falloff_score.py`, 38/38; CORRECTIONS 154): **as of the post-`cfr2` corpus (2,549 rows, CORRECTIONS 155.10)** scalar **5.888 ± 0.155** (peak 92.262 @ ms=1e-4, n=5) vs blk6 **0.915 ± 0.183** (peak 92.530 @ 1e-4, n=5; two of three chords on n=1 cells), layerwise **1.789 ± 0.118** (peak 92.887 @ 1e-4, n=11), nodewise **0.753 ± 0.169** (peak 92.453 @ **3e-4**, n=11; upper chord n=1) pp per decade. *(At CORRECTIONS 154, on the 2,537-row corpus: 5.898 / 0.915 / 1.844 / 0.753 — `cfr2`'s six clamped rows enter the `ms=3e-4` cells, n=5→8 for scalar and layerwise; **these numerals drift on every ingest into the canonical box** and must be re-derived, not quoted stale.)* **Ordering resolved at 2 SE against all three partitions:** scalar − blk6 **+4.983** (2 SE 0.496), − layerwise **+4.054** (2 SE 0.424), − nodewise **+5.145** (2 SE 0.475). **Peak-location-free form, preferred as the headline because it does not rest on an n=1 peak:** at the FIXED over-large ms=1e-3 the loss each arm takes below its own tuned peak is scalar **4.422** ≫ layerwise **1.675** > blk6 **0.914** > nodewise **0.199** pp. *(Printed until CORRECTIONS 154: 5.871 / 1.060 / 1.801 / 2.593 — superseded, see the CORRECTIONS 154 annotation.)* | **CONFIRMED** | THE CORRECT SENTENCE: partitioning buys TOLERANCE to an over-large meta-stepsize, not peak accuracy. The granularity claim is a robustness claim. | FINDINGS 57.2; CORRECTIONS 86 (cycle 57).3 | **[CORRECTIONS 153 — THREATENED -> CONSTRAINED, NOT CLEARED; AND THE FOUR NUMERALS ARE STALE AND OFF-METRIC.]** `cfr1` (12 jobs, ONE submission, granularity {scalar, layerwise} × box {-15:-2.3026, -80:-2.3026} × seeds {0,1,2}, ms=1e-3, ONE FACTOR — only the floor moves) answers CORRECTIONS 148's threat AT THE TOP RUNG: **DID = +0.0320 pp, 0.13 SE_DID, 95% CI [-0.457, +0.521]**, against a registered bar of 0.4988 and a threat magnitude of 2.7343 pp — power ≈ 1.0000, and 80% power against any wall carrying ≥20.6% of the gap. Manipulation verified at the COORDINATE denominator over the FULL trajectory: clamped layerwise hits exactly -15.0000 in 3/3 with 27-30 of 62 coordinates pinned and 83.2-83.4% of records bound (theoretical max 83.8%); released layerwise runs 23.4 nats clear of -80 with n_at_lo=0 at every record; scalar 0/33 corpus-wide. Ceiling never touched (12/12), so the floor is the only wall AT THIS CELL. **DISCHARGED:** the 1e-4 → 1e-3 decade for the scalar/layerwise pair — its lower endpoint is clamp-free by arithmetic (min reachable beta -11.9078 > -15) and its upper endpoint is now box-free to ±0.5 pp. **NOT DISCHARGED:** ms=3e-4, which the census binds 100% (n=15) and where **94.7%** of the ms=1e-3 gap already sits; every rung above 1e-3, where the -80 released floor is ITSELF reachable so the instrument does not transfer; and `blk6` (68.8% bound) + `nodewise`, i.e. two of the four falloff numbers. **THE FOUR NUMERALS DO NOT RE-DERIVE.** No script in `analysis/` computes them; they sit on the BANNED k=20 `plateau` column (peak row 92.231/92.581/92.795/92.547 = the k=20 readings; plateau5 gives 92.262/92.530/92.887/92.453); and at this HEAD peak-to-next-rung on `plateau` gives 3.778/1.119/1.942/0.285 while peak-to-highest-rung on plateau5 gives 4.427/0.761/1.693/1.125 — nodewise moved most because its peak migrated 1e-3 → 3e-4 as n went 1 → 11. **The SENTENCE survives and is what may be quoted:** on plateau5 scalar's drop below its own peak is **3.66×** layerwise's at ms=3e-4 and **2.61×** at ms=1e-3 (57.2 published 3.8× / 2.6×). **Do not re-quote 5.871 / 1.060 / 1.801 / 2.593 until re-derived on plateau5 by a committed script.**  **[CORRECTIONS 154 — THE NUMERALS ARE RE-DERIVED BY A COMMITTED SCORER; THE SENTENCE AND THE NUMERALS ARE SCORED SEPARATELY AND DO NOT AGREE.]** `cQ1` (zero GPU, re-derivation over rows that predate it) registered Test A on the **numerals** and Test B on the **ordering** as independent tests. **Test B (the SENTENCE) HOLDS** pointwise and **at 2 SE** against all three partitions, on both the canonical ladder and the all-rungs set, and in the peak-location-free form — so row 24 stays **CONFIRMED**, and the confirmation attaches to **the sentence, not the numerals**. **Test A (the NUMERALS): 3 of 4 AGREE** at the registered bar (\|Δ\| ≤ 2 SE **and** ≤ 0.50 pp/dec) — scalar +0.027, blk6 −0.145, layerwise +0.043 — while **`nodewise` DISAGREES by −1.840**, ≈5.4× its own 2 SE. **This PARTLY SUPERSEDES the CORRECTIONS 153 annotation below in BOTH directions:** 153's blanket *“THE FOUR NUMERALS DO NOT RE-DERIVE”* was **too broad on the values** (scalar/blk6/layerwise are not materially wrong) and **right about `nodewise` and about the provenance**. **The column swap is NOT what broke the row:** on the banned `plateau` column the same recipe gives 5.8895 / 0.9249 / 1.8207 / 0.6745, every arm within **0.08 pp/decade** of its `plateau5` value; the breakage is **corpus growth** — `nodewise`'s peak migrated 1e-3 → **3e-4** as n went **1 → 11**, the exact fragility FINDINGS 58.8(a) flagged and nothing acted on. **PROVENANCE DEFECT, recorded:** the printed row is a **MIXED-COLUMN** row — blk6/layerwise/nodewise reproduce on `plateau` (residuals ≤0.001) while **scalar reproduces on `best_test`** (5.8706) and FAILS on `plateau` (+0.021) — computed by an **uncommitted ad-hoc script** in cycle 57 (`775eb6b`), whose CSV **has no `plateau5` column at all**. A single hand-arithmetic slip in the scalar cell is not excluded; either way the entry carrying the sentence was off its stated column. CORRECTIONS 153's two trial recipes are **SUPERSEDED, NOT DELETED** — they are `cQ1`'s registered `SECONDARY_1` (peak→next) and `SECONDARY_2` (peak→highest). **NOT CLAIMED:** the sub-ordering *among* partitions (now nodewise 0.753 < blk6 0.915 < layerwise 1.844, i.e. finer is **not** monotonically more tolerant) — both blk6 and nodewise rest on **n=1** cells; only **partition-vs-none** is asserted. **CLAMP SCOPE UNCHANGED:** `cQ1`'s stratum **is** the clamped box `-15:-2.3026`, so the recomputed blk6 and nodewise falloffs are measured on a **still-clamped** surface — and those are the two arms whose numerals moved. **AND THE CENSUS CITATION ABOVE MUST BE NARROWED:** CORRECTIONS 148.4's *“ms=3e-4, n=15, floor 100.0%”*, cited here and in 153.8 as evidence the layerwise arm binds at that rung, is measured on **`m ≥ 777` and `nodewise` runs ONLY** — re-verified this cycle, **all 5 scalar and all 5 layerwise runs at ms=3e-4 carry no `PROBE` at all**, so binding at m=1 or m=62 at that rung is **UNMEASURED IN THE CORPUS**. `cfr2` (launched, **not landed**) is the measurement that settles it; its `R4(ii)` premise gate is therefore **live, not a formality**.  **[CORRECTIONS 155 — `cfr2` LANDS. THE `ms=3e-4` RUNG IS **CONSTRAINED, NOT DISCHARGED**, AND THE DESIGN'S OWN NEGATIVE CONTROL WAS MISLABELLED.]** `cfr2` (12 jobs, ONE submission, ONE factor, `ms=3e-4`, RULE 20 PASS / RULE 21 margin 114 s / `analysis/` diff empty on both hosts): **`G_C` +3.2967, `G_R` +2.9727, `DID` −0.3240 pp = −1.31 `SE_DID`, 95% CI [−0.8106, +0.1626]**, registered bar 0.4966 → registered verdict **`ROW24-3E4-SURVIVES`**. **The label is accepted; its sentence (*“releasing the floor does not move the gap”*) is REFUSED.** What holds: the **wall-driven** account is **refuted at 7.87 SE** (`G_R` 2.9727 vs the 1.5910 bar). What does **not** hold: exclusion — the CI admits the floor carrying **up to 24.6% of the clamped gap** (point **9.83%**), `ATTENUATED` is only **0.70 SE** away (`cfr1`: 1.87 SE), and cfr2 had **80% power only against ≥21.4%** of `G_C` and **24% power against the effect it saw**. **THE REGISTERED NEGATIVE CONTROL IS NOT A CONTROL:** all **3/3 clamped SCALAR** runs reach exactly −15.0000 and sit there **31.6–31.8%** of the trajectory — at the COORDINATE denominator the `m=1` arm is pinned **5.7×** harder than `m=62` (**31.7%** vs **5.53%**) — and released it descends to −19.70 (**85.3%** of maximum travel, vs **8.3%** at `cfr1`'s rung), ending training frozen 0.30 nats from the registered `COLLAPSED-BY-FREEZING` threshold. `DID` = (lay: −0.0140) − (sc: **+0.3100**, **3/3 seeds same sign**, 88% of its own bar): **the arm assumed inert supplies 100% of the estimate.** Binding is **non-monotone in `ms`** for `m=1` (0/33 at `ms=1e-3`, 3/3 at `3e-4`), so CORRECTIONS 148.5's monotone-in-`m` ladder is **measured only at `ms=1e-3`** and may not be extended here; and CORRECTIONS 148's *“the fine arm hits a wall the scalar arm never reaches”* is **inverted** at this rung. `R4(ii)` PASSED (3/3 layerwise at −15, free-fall at **99.5–99.9%** of the earliest possible step, occupancy at this rung's **0.4600** ceiling); ceiling untouched 12/12, so the floor is the only wall and the one-factor release is a **complete** box audit here. **LADDER STATUS:** `ms=1e-4` **DISCHARGED by arithmetic**; `ms=1e-3` **DISCHARGED** (`cfr1`); **`ms=3e-4` CONSTRAINED**; the **peak-location-free headline** (loss at fixed `ms=1e-3` vs own peak) is **DISCHARGED at both endpoints** for scalar/layerwise; `blk6` and `nodewise` **UNAUDITED**, and **every rung above `ms=1e-3` is UNAUDITABLE with this box** (the −80 release is itself reachable there) — which is where `blk6`'s and `nodewise`'s upper chords sit. `cfr1`+`cfr2` are mutually consistent (`Q`=1.02, p 0.31) and their fixed-effect pool (−0.147 ± 0.176, p 0.40) is recorded **as a joint-null test only, never as an effect size** (different exposure: 83.81% vs 46.05% of trajectory). **AND 153.8's CITATION IS AMENDED THERE:** its *“`ms=3e-4` 100% bound”* was an extrapolation from a `m ≥ 777` census (154.5); the claim is now **established by `cfr2`** at `m=62` **3/3** and `m=1` **3/3** — provenance corrected, claim survives.
| Is the difference AMONG partitions (as opposed to partition-vs-none) resolvable? | granularity, each arm at ITS OWN optimal meta-stepsize — layerwise/blk6/scalar at ms=1e-4, nodewise at ms=3e-4 (CORRECTED: nodewise's peak is 3e-4, not 1e-4) | pooled ms+mx+rs on the full 14-column config key, 100 ep, k=5 window | k=5 window, 100 ep: layerwise 92.906 (n=5, sd 0.166) / nodewise 92.656 (ms=3e-4, n=1) / blk6 92.652 (n=2) / scalar 92.262 (n=5). Partitioned arms span 0.254 pp against sd 0.166. The CSV's own k=20 plateau column for the same cells reads layerwise 92.795 (n=5) / blk6 92.581 (n=2) / scalar 92.231 (n=5) / nodewise 92.515 (3e-4) and 92.547 (1e-3), n=1 each. Separately, a 20-epoch layerwise/nodewise/WEIGHTWISE contrast under the documented k=5 window reads 86.79/86.72/82.12 with 0 of 4 strata resolved. | **CONFIRMED** | Partition-vs-none (0.390-0.644 pp) is the resolvable effect; WHICH partition is a coin-flip at n=1 for nodewise. STATE THE WINDOW: both halves of this row are k=5, and the 20-ep contrast is a different arm set (no scalar, no blk6). | CORRECTIONS 87.14 (correcting 87.7); FINDINGS 58.8; re-opened at 20 ep by 96.6 | **[CLOSEOUT, CORRECTIONS 115 — THE 0.254 pp SPAN IS DEMOTED.]** The tuned-peak row pools THREE batch families (ms + mx + rs) with UNBALANCED composition between the arms being differenced: layerwise@1e-4 = 3 ms + 2 rs, blk6@1e-4 = 2 rs ONLY, nodewise@3e-4 = 1 rs. When the arms do not share a composition the batch offset does not cancel. All four cell means reproduce exactly; Welch layerwise-minus-blk6 = +0.254, se 0.093, t 2.73, df 4.3 — but the measured cross-batch floor here is sqrt(2/k_eff)*sd_batch ~ 0.18-0.29 pp, i.e. 70-115% of the gap. **THE 0.254 pp LAYERWISE-OVER-BLK6 SPAN IS UNRESOLVED AT THE CROSS-BATCH FLOOR AND MAY NOT BE REPORTED AS ~3.4 SE.** The row's own headline is unaffected: partition-vs-none is 0.390-0.644 pp, 2-3x the floor, and 'nodewise is n=1 and cannot be placed in that row at all' already stands.
| Does per-weight granularity fail STRUCTURALLY, or does it recover under tuning? | weightwise meta-stepsize grid {3e-5 ... 1e-2} (rw9, 18 jobs) | n=3, 100 ep, R18/CIFAR-10 — WRITTEN, VALIDATED, NEVER SUBMITTED | No number can be given. The one matched 100-epoch plain weightwise run in the corpus is 90.913 (n=1, alpha0 unmatched) and nothing may cite it. The figure used to reason the arm away (68.472, n=10) was a 20-EPOCH plateau read as a 100-epoch one. | **OPEN** | A unit error deleted an entire arm from the campaign's response surface for four cycles; the replacement grid is queued behind a 12-tick cluster outage. | CORRECTIONS 85; 86 (cycle 57).4/.5; bin/c57_rsw_peak.sh |
| H4 — does granularity help under SGDm and do nothing under AdamW (the base-optimizer interaction)? | base optimizer {SGDm, AdamW, Lion} x granularity x alpha0 {1e-6, 1e-3} | pooled cells, 100 ep, R18/CIFAR-10; the retest batches bo6/bo7 are 20-epoch | At a0=1e-6, 100 ep: SGDm granularity span 3.84 pp (scalar 87.750 -> nodewise 91.593) vs AdamW span 0.15 pp (91.555 -> 91.707). CORRECTED — the null is a0-CONDITIONAL: at a0=1e-3 the AdamW-base granularity effect REVERSES SIGN (scalar 92.684 +-0.178 n=3 -> layerwise 90.860 +-0.174 n=8, delta -1.824, t=-15.2), and (Lion,Lion) is null too (delta -0.116, t=-0.6). The AdamW retest at the measurement level (bo6, 20 ep) FAILED its box-free gate on 2 of 3 rungs; bo7 was RUNNING when the cluster went down and has never been read. | **OPEN** | The measured contrast is 'SGDm vs EVERY other base optimizer', not 'SGDm vs AdamW' — so the 'AdamW already normalises per-coordinate' MECHANISM must be dropped as the stated candidate; it is contradicted by the (Lion,Lion) null and the a0=1e-3 sign reversal. Oldest untested claim in the file. | FINDINGS 16.5, 36.4, 54.3; CORRECTIONS 6, 79 |
| Why does per-weight granularity collapse to 10% accuracy — numerics or the method? | meta-optimizer {Lion, Adam} on the weightwise arm; and SwiftTD's beta bound on/off | n=3-4, 100 ep, R18/CIFAR-10, a0=1e-6, ms=1e-3 | ALL FIGURES RESTATED ON PLATEAU (the published ~79.4 and ~12 pp were best_test). Collapse is real and total without the guard: 10.00 in 4/4 (g1_sgdm_weight, d2_collapse, d3_g1, d3_g999). Adam-meta removes it 3/3 (plateau 48.32). The guard removes it 3/3: plateau 10.00 -> 77.821 (d4_clipW, n=3), and is a no-op on layerwise (d4_clipL 90.861 vs unguarded 90.914). Guarded weightwise sits 13.04 pp below guarded layerwise. | **CONFIRMED** | Separates an implementation artefact from the real result — per-weight is genuinely the worst partition, not a numerics bug. | FINDINGS c1 D1 gate, d4-clipW/clipL; metric rule CORRECTIONS 4 |

| **Is the granularity ladder flat once EVERY rung sits at its own optimal ms?** | best-ms plateau5 per rung, 13-axis-matched census of the whole CSV (network, dataset, batch_size, base, meta, alpha0, gamma, augment, beta_clip, hier, lam, eta_ratio, epochs_done) | 100 ep, R18/CIFAR-10, SGDm+Lion; n=2-8 per rung; POST-HOC assembly of cells already on disk | Tuned: scalar 1e-4 92.262 (n=5), nodewise 3e-4 92.450 (n=5), blk6 1e-4 92.652 (n=2), layerwise 1e-4 **92.867** (n=8) — a spread of **0.604 pp**. Mistuning ms by ONE DECADE costs far more: scalar 92.262->87.839 (**-4.424**), layerwise 92.867->91.196 (**-1.670**). Frozen beta (ms=1e-8) 90.316, so tuned-minus-frozen = **+2.551 pp**. **WEIGHTWISE HAS EXACTLY ONE MATCHED RUN IN 1761** (kt2_ww_a1e-3_s0, ms=1e-3, 91.308) and that ms is past every swept rung's optimum. | **OPEN** | **Adaptation is worth ~2.6 pp; choosing the partition ~0.6 pp.** Every 'weightwise is worst' reading in this campaign is confounded with one untuned ms, on the rung the 53.1%-sign-agreement result is about. POST-HOC — no gate rests on it; it is the REASON wm9 was registered, not a result. wm9 (10 jobs, cluster_a, c73) fills the rung with W2 registered in advance: tuned weightwise inside [92.2, 92.9], best ms <= 1e-4. | FINDINGS 72.4; CORRECTIONS 102.6; bin/c73_wm9_weightwise_ms.sh |
| Does a granularity contrast at a SHARED meta-stepsize measure the partition? | the same two partitions compared at a common ms=1e-3 vs at each arm's own tuned ms | hz9 + rs- corpus, 100 ep, R18/CIFAR-10; H2 paired n=2, H1b unpaired n=5 v 5 | At a shared ms=1e-3 nodewise beats layerwise by **+1.138 pp, 2/2 paired**. At each arm's own tuned ms the same contrast is **-0.360 pp** — the sign REVERSES. | **CONFIRMED** | **A granularity contrast at one shared meta-stepsize is confounded with which meta-stepsize that partition prefers** — and that is how nearly every granularity comparison in this campaign, and in the Adam-mini / Adalayer / SGG line, is run. This is the methodological result of cycle 73. | FINDINGS 72.1; CORRECTIONS 102.5 | **[CLOSEOUT, CORRECTIONS 115 — LABEL CORRECTED, VERDICT HELD.]** H2 was scored 'paired within seed' but zips hz9-node-1e3-s{3,4} (jobs 4704502/03) against rs-lay-1e3-s{3,4} (jobs 4700388/92) — two submission waves ~4,100 job-ids apart. THE ONLY GENUINE RULE-14 EXPOSURE IN THE CORPUS. Point estimate IDENTICAL at +1.138 pp; Welch se 0.1365, t 8.34 (df 1.67) against the paired reading's implied t 25.86 — a 3.1x deflation. CONFIRMED STANDS, now qualified 'cross-batch, n=2 v 2, Welch t 8.34'; the '2/2 favouring nodewise' is a SIGN CHECK on two independent arm means, not a per-seed gate. The row's methodological conclusion is untouched.
## 3. The current headline — the accuracy reversal with training horizon

| tested | varied | scale | result | verdict | so what | ref |
|---|---|---|---|---|---|---|
| Does the accuracy-OPTIMAL granularity reverse as the training horizon grows? | training epoch (10/15/20/25/30/40) x granularity {layerwise, nodewise}, paired within seed | br6 n=4 + bl5 n=3 (7 seeds), R18/CIFAR-10, ms=1e-3, alpha0=1e-3, two different beta ceilings | nodewise - layerwise = -3.946 pp at epoch 10 (0 of 7 seeds favour nodewise) -> +1.258 pp at epoch 40 (7 of 7), monotone between, crossing bounded by unanimity to epochs 15-25. Both endpoints are 25-80x the 0.05 pp resolution floor. | **CONFIRMED, RESCOPED c73** | Still a real, reproducible measurement — 58.1's numbers are NOT withdrawn. But it is now established (row 3 below) that it **measures distance-from-optimum, not granularity**: at the shared ms=1e-3 layerwise sits 1.736 pp off its own peak and nodewise 0.098 pp off its, and the trend INVERTS when each arm is tuned. **WRITE IT ONLY AS 'at a shared ms=1e-3'. It is no longer the campaign's headline accuracy claim.** | CORRECTIONS 86.3 (cycle 56), rescoped by 102.5; FINDINGS 58.1, 72.1b |
| Is the U-in-training-time shape general, and is the CROSSING EPOCH general? | ~20 distinct batch-family prefixes, 4 networks, 2 datasets, several meta-stepsizes (post-hoc cuts of data already on disk) | 41 config cells, 20-100 ep; the '5 batches' figure belongs only to the ResNet34 sub-claim (ff5/p6f/p7/uc5/p4) | CORRECTED CELL COUNT: 27 of 41 cells show a positive rise from epoch 10 to 20 (analysis/c58_horizon_reversal.py --slopes 10 20, selftest 42/42) — the 14 negatives are the frozen-beta (fz, fz3), alpha0=1e-6 and ms<=5e-4 cells, which are null or descending BY CONSTRUCTION. Restricted to the ms=1e-3 / a0=1e-3 adapting stratum by FINDINGS 58.3's own \|@10\|>=1 criterion: 24 of 29. The crossing epoch does NOT generalise: 47 at ms=3e-4 vs 20 at ms=1e-3 (both re-derived paired); ResNet34 still -1.082 / -1.452 / -1.125 / -1.106 / -1.192 pp at epoch 20 in 5 of 5 batches; CIFAR-100 crossed by ~16 (p7, n=10). | **CONFIRMED** | 'Between epoch 15 and 25' is a ResNet18/CIFAR-10/ms=1e-3 number and must be written as one. Do NOT quote '~30-41 cells' — that silently promotes null-by-construction controls into the supporting evidence. | FINDINGS 58.3/58.4 | **[CLOSEOUT, CORRECTIONS 115 — CHECKED AND CLEARED.]** analysis/c58_horizon_reversal.py already enforces STANDING RULE 14 by construction, 27 cycles before the rule was written: pool=False is the DEFAULT and the batch family is part of the cell key, so 'runs from two different batches never pair even at identical config'. The task's flagged exposure ('br6 n=4 + bl5 n=3 reported as n=7 paired within seed') is a FALSE ALARM: each of the 7 differences is node-minus-lay taken WITHIN one batch at one seed, so the batch offset cancels before the pooling starts. Re-scored Welch anyway, discarding the pairing: ep10 -3.946 t -14.15 (LARGER than the paired -13.03); ep40 +1.258 t 11.30 (was 16.05); point estimates identical at every epoch; the two batches agree to 0.012 pp at ep40 despite different beta ceilings. CONFIRMED-RESCOPED stands. **STILL OWED, NOT DONE HERE:** probes_br6/ and probes_bl5/ are on disk and no rec_lo/rec_hi was ever reported for the 14 runs; the standing rule requires occupancy beside any field number, and this box class DOES bind elsewhere (ar1 LO 12/12).
| Does the reversal survive letting EACH ARM sit at its own optimal meta-stepsize? | layerwise @ms=1e-4 vs nodewise @ms=3e-4 (each at its own peak), epochs 10->100 (hz9, 9 jobs) | **SUBMITTED c71, LANDED AND SCORED c72/73: 9 jobs, 9/9 at 100/100 epochs.** Scored n=5 vs n=5, UNPAIRED, matched-k=5, pooling hz9 with the rs- corpus under H0.5's licence (max \|z\| = 1.80) | **MEASURED (hz9, matched-k=5 plateau, node@3e-4 - lay@1e-4): +3.444 at ep10, +1.349 at 20, +0.784 at 30, +0.670 at 40, +0.392 at 50, -0.176 at 70, -0.361 at 100** — monotone decay from a large early nodewise lead to a tie, seeds favouring nodewise going 5/5 at ep10-50 to 0/5 at ep80-90. The n=1-vs-n=2 pilot (+3.286 / +1.800 / +1.809 / +0.891 / +0.317 / -0.121 / -0.199 / -0.168) is REPRODUCED throughout. H2, the fixed-ms control, gives +1.138 pp paired 2/2 in the OTHER direction. At the shared ms=1e-3 layerwise sits 1.736 pp off its own peak (pooled ms+mx+rs on the full config key: 92.906 n=5 vs 91.170 n=10) while nodewise sits 0.098 pp off its own (n=1). CORRECTED: 1.86 pp was the superseded rs-only figure. | **CONFIRMED (T-B)** | **THE THREAT IS REALISED AND THE HEADLINE IS RESCOPED.** hz9 ran at cycle 72/73: H1 D = -0.753 against a +-0.30 bar => T-B, the direction registered in advance. Over the same ep10->ep40 window the tuned trend is -2.774 pp against the shared-ms +5.204 pp, and the ep10 sign flips (+3.444 vs -3.946). H1b at ep100: level -0.360, bar +-0.50 => **TIED** (distinguishable from zero at \|level\|/se = 2.32, and SMALLER THAN THE BAR — not 'no difference'). A confirmation REPLICATES the registered pilot and is never a discovery. | FINDINGS 72.1/72.1b; CORRECTIONS 102.5; results/c72_hz9_score.txt |

## 4. Idea 3 — alpha0-robustness, the campaign's one positive method result

| tested | varied | scale | result | verdict | so what | ref |
|---|---|---|---|---|---|---|
| Does a genuinely FIXED learning rate collapse at small initial step sizes (does the robustness claim have a denominator)? | lr over a 7-POINT / 5-DECADE grid {1e-6 ... 1e-1}; arm A = plain AdamW, COS_WARMUP=0, COS_TOTAL=1e8 (verified constant) | n=3/cell, 100 ep, R18/CIFAR-10 | lr=1e-6 -> 68.680 +-0.530 pp (69.162/68.112/68.766, n=3); 1e-5 -> 84.518 +-0.233 (n=3); lr=1e-1 -> NO SURVIVING SEED (3 of 3 collapse; face-value mean 12.598). CORRECTED: 13.896 is a withdrawn survivors-only n=2 figure and may not be quoted. | **CONFIRMED** | The premise holds — a fixed small step size cannot train — so the robustness tradeoff has something to be measured against. Note the grid is 7 points spanning 5 decades, not '7 decades'. | CORRECTIONS 41 (withdrawing 13.896), 44; FINDINGS 47.10; bin/c45_idea3_alpha0_robustness.sh |
| Is MetaOptimize flat over several decades of alpha0 (the parent paper's own claim, never tested here before)? | alpha0 over the same 7-point / 5-decade grid x arm B = AdamW base + Adam meta at m=6 (the paper's own setting) | n=3/cell, 100 ep, R18/CIFAR-10; account-balanced 2:1 so account cannot confound | alpha0=1e-6 -> 91.496, 1e-5 -> 91.535, 3e-4 -> 93.304 (peak), 1e-2 -> 89.987, 1e-1 -> 59.064 (all n=3, 100 ep, re-derived). At alpha0=1e-5 that is +7.65 pp over the tuned cosine's 83.884. | **CONFIRMED** | The bottom end is the durable advantage: 8.7 epochs to 85% vs 89.3 for a fixed LR. | CORRECTIONS 44 |
| Against a TUNED SCHEDULE (not a constant LR), is MetaOptimize still more robust? | arm C = AdamW + horizon-matched cosine over the same 7-point / 5-decade grid | arm C is n=2-4 per cell (3/2/4/3/3/2/3 across the grid) — NOT n=1-3, and there is no n=1 cell; 100 ep, same network/dataset/batch/augmentation | Grid mean over the shared grid: survivors (6 pts) A 82.793 / B 91.723 / C 86.782; face value (7 pts) A 72.765 / B 87.058 / C 81.963. B - C = +4.941 and +5.094 pp (agreeing to 0.153 pp). Peak cost: MetaOptimize 1.113 pp lower at its own best (94.417 - 93.304). | **CONFIRMED** | A statistic with NO free parameter (no threshold, no band edge) — the one number in the campaign that survived three verdict reversals. CAVEAT ADDED: arm C (i3c-*, SW_*) sits ENTIRELY on the account_a account while arms A and B are split 2:1 across accounts, so B-vs-C is the one contrast in this theme that is not account-balanced. | CORRECTIONS 44, 46(1), 52(1) |
| Is that robustness shape an artefact of a 100-epoch budget (arm B pays a startup tax at small alpha0)? | budget 100 -> 300 epochs on the four grid EXTREMES x both arms | n=2/cell, R18/CIFAR-10, account split by seed not by arm | Band membership, both widths and the A-vs-B ordering are IDENTICAL at 100 and 300 epochs on the matched sub-grid. The pre-registered startup-tax mechanism reads +0.228 pp, inside its own 0.3 pp resolution floor — recorded as a null WITH its resolution. | **CONFIRMED** | Lifts the 'NOT FINAL' hold on the Idea 3 verdict; the shape is budget-stable. | CORRECTIONS 42 |
| Earlier width-based readings of Idea 3 ('MetaOptimize is/is not flatter on threshold X') | threshold and band-edge definitions across three successive cycles | same runs, three different reductions | Three consecutive published verdicts (47.1 -> 47.9 -> 48.2) all reversed. Cause: an incomplete arm silently SHORTENS every other arm's band, because a cross-arm width is defined only on the SHARED grid; and one arm's cell moved 90.095 -> 89.987 (0.013 pp under a 90 line) and lost a whole decade. | **WITHDRAWN** | Every threshold result in this project must now carry its sensitivity scan; the dial-free grid mean is the only Idea 3 number to quote. | CORRECTIONS 38, 41, 43, superseded by 44 |

## 5. Scoping the method arms — scale, dataset, budget, initialisation

| tested | varied | scale | result | verdict | so what | ref |
|---|---|---|---|---|---|---|
| Does the granularity gain survive model scale? | network {ResNet10, ResNet18, ResNet34, ResNet50}, matched otherwise | matched n=3 sc- cell (SGDm+Lion, ms=1e-3, a0=1e-6, 100 ep); R50 n=2 | Granularity gain (plateau): +19.877 / +2.867 / +0.902 pp at R10 / R18 / R34. CORRECTED: R18 is +2.867 from the matched n=3 sc- cell (layerwise 90.686 vs scalar 87.819) — the published +3.04 was a pooled n=24-vs-n=18 ROW count across campaigns and is inconsistent with the 0.47 pp layerwise spread quoted beside it. Layerwise is FLAT: 90.619 / 90.686 / 90.220 (0.466 pp spread) while scalar climbs 18.575 pp (70.742 -> 89.317). R50 (n=2) runs the other way: -1.554. | **CONFIRMED** | Reinterpreted: 'granularity stops helping at scale' is mechanically 'the scalar arm stops failing at scale' — a much weaker sales pitch. | FINDINGS 17.1, 19.9, 21.2, 23.2; row-vs-seed caveat FINDINGS 36.2 |
| Is there ANY architecture where the parent paper's premise (layerwise loses to scalar) reproduces on CIFAR-10? | ResNet50 x {scalar, layerwise} x alpha0 {1e-6, 1e-3} | n=2/arm (the sc50 top-up has partly landed), 100 ep, guard -15:-2.3026 | At a0=1e-6: layerwise 87.885 (87.710/88.059, n=2) vs scalar 89.439 (89.103/89.774, n=2) — layerwise LOSES by 1.554 pp. CORRECTED from the stale n=1 figure of 1.394. At a0=1e-3 the reversal disappears: layerwise 90.200 vs scalar 89.223 = +0.978. | **OPEN** | The single data point that would connect our CIFAR-10 results to the paper's ImageNet null — now n=2, still not n=3, and alpha0-conditional. R50 is the sole reversal among R10/R18/R34/R18_c100. | FINDINGS 32.5/32.6; bin/c32_cluster_a.sh B2 |
| Does granularity transfer to a second dataset (CIFAR-100), and does the ORDERING transfer? | CIFAR-100 x {scalar, blk6, layerwise} x alpha0 {1e-6, 1e-3} | n=5/cell, 100 ep, ResNet18_c100 | a0=1e-6: scalar 22.78 -> blk6 52.48 -> layerwise 69.31 (gain +46.52). a0=1e-3: scalar 22.57 -> blk6 51.32 -> layerwise 69.59 (gain +47.02). Essentially insensitive to alpha0. The CIFAR-10 blk6/layerwise ORDER inverts here at both alpha0. | **CONFIRMED** | Largest and most alpha0-robust granularity effect in the campaign — and proof that the CIFAR-10 ordering is not universal. CORRECTED n: n=5 now exists (the published +47.59/+47.64 were n=2 cells). | FINDINGS 20.4, 24.4, 25.8 |
| 'Finer than layerwise costs 17.2 pp on CIFAR-100' | CIFAR-100 x {layerwise, nodewise, weightwise}, byte-matched config | n=3-5, a0=1e-3, 100 ep (c100f-* family, never written into FINDINGS) | REFUTED. The 17.2 pp is layerwise 69.886 minus resnet18_blocks 52.728, and blk6 (m=6) is COARSER than layerwise (m=62). The genuinely finer arms: nodewise 71.184 (n=3) BEATS layerwise (69.586, n=5) by +1.60 pp; only weightwise (66.713, n=3) loses, by -2.87 pp. | **REFUTED** | A sign-and-label error: the arm called 'finer' was the coarser one, and on CIFAR-100 one step finer than layerwise actually HELPS. **UPGRADED cycle 68 (CORRECTIONS 97.5-97.7):** re-scored on `plateau5` in a strictly matched cell it is nodewise 71.415+-0.049 (n=3) vs layerwise 69.048+-0.220 (n=14), **+2.367 pp at 5.24x the resolution gate, window-invariant at k=1/5/10/20, no seed overlap**, and an **interior maximum in u** (weightwise 67.17 < nodewise 71.42 > layerwise 69.05 > blk6 51.58 > scalar 22.21). This row is the resolved `u_train*` that CORRECTIONS 96.7 could not obtain on CIFAR-10; cycles 65-67 cited it zero times. | FINDINGS 24.4/25.8, refuted by the c100f family in results/all_runs.csv |
| Is the granularity result an alpha0=1e-6 startup artefact? | alpha0 {1e-6, 1e-4, 1e-3} x granularity x budget {20, 100 ep} | CORRECTED: the 100-epoch a0 ladder is n=2/cell; the ENTIRE 20-epoch table (p2-* vs p3-*) is n=1/cell, and its blk6/layerwise/nodewise/weightwise arms are FULL-POOLING arms — only 'layerwise plain' is unpooled | At 100 ep every arm is flat to <=0.38 pp across three decades of alpha0 and the gain is +3.3-3.4 pp at every alpha0. At 20 ep alpha0 dominates: the spread over {scalar, blk6, layerwise} collapses 5x from 1e-6 to 1e-3 (21.2 -> 4.2 pp), and nodewise goes from 37.7 pp behind scalar to 0.4 pp. Escape times: scalar 13.8 / blk6 14.0 / layerwise 17.3 / nodewise 25.1 epochs. | **CONFIRMED** | No short-budget granularity comparison at alpha0=1e-6 is admissible — a substantial part of what earlier tables called 'the granularity effect' is arms escaping a crippled init at different rates. | CORRECTIONS 8c, 12; FINDINGS c8 s3/s4 |
| Is granularity a SPEED effect that a longer budget erases (the reading of the parent paper's ImageNet null)? | window E = 100 / 205 / 300 epochs of one SCHED=none trajectory | n=2-3/cell, R18/CIFAR-10 | ON PLATEAU / MATCHED WINDOW THROUGHOUT (re-derived from the e300-* .out files): granularity +3.428 (E=100) / +3.468 (E=205) / +3.450 (E=300) — invariant. Pooling (shrink lam=0.1 minus plain) +0.844 / +0.288 / +0.169 — decays. CORRECTED: the published +3.42 / +0.60 at 300 ep are best_test values; the pooling decay is 3.5x larger than quoted. | **REFUTED** | Kills the campaign's own 'budget explains the ImageNet null' story for granularity — the budget reading applies to POOLING only. Note these are three WINDOWS of one trajectory, exactly the defect CORRECTIONS 86.1 names. | FINDINGS c7 s3 / c8 s6; metric rule CORRECTIONS 4 |
| 'Pooling gains INVERT between 100 and 300 epochs, and scalar overtakes plain layerwise' — once called the strongest result in the project | budget 100 -> 300 ep | re-audit of CORRECTIONS 1's table | REFUTED. The source table was measured on best_test, mixed alpha0, and labelled additive arms as 'zpool' when zero zpool runs exist at 300 epochs. On plateau nothing inverts — gains ATTENUATE — and the actual 300-epoch scalar is 88.287, i.e. 3.450 pp BEHIND layerwise. | **REFUTED** | A headline built on the wrong metric, the wrong stratification and mislabelled arms; the pattern that produced STANDING RULE 1 (re-derive at write time). | FINDINGS 36.2, refuting CORRECTIONS 1 |

## 6. The failed fixes — every direction the project put compute behind and abandoned

| tested | varied | scale | result | verdict | so what | ref |
|---|---|---|---|---|---|---|
| IDEA 1 — put a cosine prior on the learned step size: alpha_t = cosine(t) * exp(beta_t) | granularity {layerwise, blk6} x schedule on/off, plus a byte-matched control | n=2-3/cell, 100 ep, R18/CIFAR-10 | Best Idea-1 arm 92.544 pp plateau (100 ep) = -1.873 pp vs the tuned baseline 94.417 +-0.113 (n=5) and -0.251 pp below the project's own best existing arm. A real +1.664 pp departure from control exists (t=11.6) but the SCHEDULE IS UNIDENTIFIED — no saved submit script, and it is provably not the matched-horizon cosine. | **DEAD** | Stopped by its own pre-registered rule ('worse than the baseline plateau -> record and stop'). Zero jobs in 24 consecutive cycles. | CORRECTIONS 32(1), mechanism killed by 29 |
| IDEA 2 — choose the partition from MEASURED meta-gradient correlation between tensors | per-coordinate sign agreement, architecture-adjacent vs random pairings, alpha0 {1e-6, 1e-3} | kt2 weightwise runs, 11.17M coordinates, 100 ep, R18/CIFAR-10 | Across-tensor pairwise same-sign EXCESS = +0.0001 pp (p=0.225) against a pre-registered kill threshold of 0.1 pp; architecture - random = +0.0002 pp (p=0.29). The kill stands on the TENSOR-level null where the test has power (the per-weight null is under-powered, bound rho <= 1.7e-5). | **DEAD** | The differentiator the idea needed does not exist at the granularity the method would act on. The 'dies by 1000x' framing was itself withdrawn — that ratio compared an observation to a threshold, not to the test's resolution. | CORRECTIONS 32(2), narrowed by 33 |
| M0 SHRINK — pull each group's beta toward the group mean every step, with a tunable coefficient lambda | lambda over six decades {1e-6 ... 1.0} | n=3 ladder, 100 ep, R18/CIFAR-10; the earlier 19-Aug table was n=1 at lambda in {0.001, 0.01, 0.5, 1.0} | CORRECTED. On the completed n=3 ladder (best_test, deltas vs plain layerwise 91.33 +-0.17, n=6): 0.001 +1.21 / 0.01 +1.46 / 0.03 +1.25 / 0.1 +1.21 / 0.3 +1.18 / 0.5 +1.17 / 1.0 +1.20. The admissible statement is the RANGE +1.17 to +1.46, all on best_test. lambda=1.0 is NOT the optimum at n=3 — it is sixth of seven, so 'the optimum is at FULL pooling' must be struck. Shrink applies EVERY STEP, so every lambda >= 0.001 has a half-life <= 693 steps against ~50,000: the sweep measured full pooling three times over. | **WITHDRAWN** | 'Hierarchical shrinkage with a tunable coefficient' was never tested; the operator saturates. Quoting the peak (+1.46) is itself the cherry-pick CORRECTIONS 3 flags. | CORRECTIONS 3, 4; FINDINGS c1, c2 s1, c3 s4 |
| Does pooling CLOSE the per-weight deficit? | pooling strength over six decades on the WEIGHTWISE arm, under three operators (M0 shrink, M1 additive, and the confound-free zpool) | n=1-6/cell, 100 ep, R18/CIFAR-10, a0=1e-6 | NO. Under M0 shrink / M1 additive it is monotone harmful across the whole range (79.38 -> ~49, best_test). CORRECTED: under zpool it is NOT monotone harmful — weightwise goes 77.921 (r=1, plain per-weight) -> 89.052 (r=0.99), an +11.13 pp RESCUE, with every r<=0.7 cell at 87.8-88.1. But 89.052 is still 2.30 pp below plain blk6 (91.350) and only +1.28 pp above plain scalar (87.772). | **REFUTED** | NARROWED: 'no beneficial regime at ANY pooling strength' is false — it holds only for the two operators later shown NOT to be pooling operators (CORRECTIONS 21, 3). Pooling does lift the per-weight arm; it just never reaches a good partition. | FINDINGS c4 s1, 33.2; CORRECTIONS 3, 21 |
| M1 ADDITIVE — is there an INTERIOR pooling optimum (partial pooling beating both endpoints)? | retention ratio r in {0, 0.03, 0.05, 0.07, 0.1, 0.2, 0.3} | n=3-5/cell, 100 ep, R18/CIFAR-10, layerwise, Lion meta, one account, a0=1e-6 | Re-derived exactly from the CSV: 92.200 (n=3) / 92.637 / 93.086 / 93.217 / 92.626 (n=5 each) / 91.387 / 90.957 (n=3). Monotone rise to a plateau at r in [0.05, 0.07], cliff on the high side. | **CONFIRMED** | The curve is real and is the campaign's cleanest empirical shape — but see the next four rows for what it is NOT. | CORRECTIONS 10 (cycle 17); FINDINGS 9.1-10.3, 33.5 |
| Is M1's r actually a POOLING dial (the mechanism the whole hierarchical story rests on)? | r at four granularities spanning 180,000x in group count, plus the true pooling operator (zpool) as a control | n=3 per cell for the additive arms (with one n=8 pooled layerwise cell), n=5 for zpool; 100 ep, a0=1e-6, R18/CIFAR-10 | REFUTED. True zpool r=0 is granularity-INVARIANT to 0.032 pp across 180,000x in m (layerwise 87.740 / weightwise 87.763 / plain scalar 87.772, n=5), while additive r=0 spans 47.20 pp at matched n=3: blk6 91.730 (n=3) / layerwise 92.200 (n=3; 92.587 if pooled to n=8) / nodewise 91.984 (n=3) / weightwise 45.005 (n=3). Cause: under Lion every increment is exactly +-ms, so pooling divides the effective meta-stepsize by \|2p-1\| — measured 10.3x at layerwise, and \|2p-1\| falls monotonically with m (0.395 / 0.128 / 0.0152 / 0.00033). | **REFUTED** | THE HIERARCHICAL IDEA'S MECHANISM IS DEAD. The r-dial is a meta-learning-rate dial wearing a pooling costume. CORRECTED n: the published blk6 91.767 was an n=2 mean (s0 dropped) and the 92.587 layerwise cell is the pooled n=8, not n=3. | CORRECTIONS 21 (also withdrawing 13); FINDINGS 33.1/33.4/34.2 |
| Does FULL pooling help (+1.39 pp), as carried in the handoff doc? | true pooling endpoint (zpool r=0) vs plain layerwise | n=5 (zpool r=0, zsw+zsx) vs n=17 (plain layerwise), 100 ep, a0=1e-6, R18/CIFAR-10 | REFUTED. True full pooling 87.740 +-0.138 (n=5) vs plain layerwise 90.784 +-0.169 (n=17) = -3.044 pp. (The matched zsw n=3 pair r=0 87.825 vs r=1 90.927 gives -3.102 pp — a DIFFERENT derivation; quote one, never both in one sentence.) | **REFUTED** | The +1.39 pp read the additive r=0 cell, which CORRECTIONS 13 had asserted from algebra and never measured at that endpoint. | CORRECTIONS 21; FINDINGS 33.3 |
| Even taking the curve at face value, is pooling worth doing? | best pooled arm vs plain layerwise, each at its own tuned meta-step | n=3/cell, 100 ep, R18/CIFAR-10 | Plain layerwise 92.872 +-0.041 beats the best pooled arm 92.449 +-0.192 by +0.423 pp. | **REFUTED** | Pooling is STRICTLY DOMINATED once the meta-step is tuned — it costs accuracy and adds a hyperparameter. | CORRECTIONS 24 |
| Does the M1 interior optimum survive a non-crippled initialisation? | alpha0 1e-6 -> 1e-3 across the whole r-curve | ac-* batch, k=5 distinct seeds (n=2-12 rows), 100 ep, R18/CIFAR-10 | 76% of the optimum disappears: +1.011 pp over full pooling at alpha0=1e-6 becomes +0.239 pp at 1e-3 — below the campaign's own 0.5 pp materiality bar — and the argmax moves 0.06 -> 0.1. CORRECTED: the a0=1e-3 curve is flat to 0.079 pp (not 0.067) and only over r in [0, 0.07], FOUR cells — the FULL curve still spans 1.272 pp, because r=0.1 (92.431) beats r=1 (91.159) by +1.272 pp, t=26.72. | **REFUTED** | The headline positive result was largely an escape-rate artefact of a deliberately crippled init — but do not overstate: 'the whole curve flattens' is wrong, only its interior does. | FINDINGS 35.1, 37.1/37.2 |
| Does the M1 optimum transfer to another architecture or dataset? | r-curve on ResNet10, ResNet18_c100 at both alpha0; ResNet34 at a0=1e-6 only | n=2-3/cell, 100 ep | No interior optimum anywhere. CIFAR-100 at a0=1e-3: monotone to the boundary, peak at r=1 (69.917); full pooling collapses to 8.98% (rc100-r0, n=2). CORRECTED: at a0=1e-6 CIFAR-100 is monotone then FLAT from r~0.1 with the peak at r=0.2 (69.890 +-0.018, n=2), r=0.4 69.351, r=0.7 69.617, r=1 69.542 — 'monotone to the boundary at BOTH alpha0' is wrong. ResNet10 at a0=1e-3 peaks at r=1 (82.19 -> 91.17 monotone over 6 points, n=3). The CIFAR-10 optimum r=0.06 lands where CIFAR-100 has lost 10-26 pp. | **REFUTED** | It appears in one architecture, one dataset, one initialisation. Not a method. COVERAGE HOLE: ResNet34 at a0=1e-3 has no r-curve at all — one cell (r=0.06, n=3). | FINDINGS 24.3, 28.5, 35.4 |
| Does pooling in META-GRADIENT space (the confound-free operator) have an interior optimum? | zpool r in {0 ... 1} x granularity {layerwise, weightwise} | n=1-6/cell, 100 ep, a0=1e-6, R18/CIFAR-10; zmpool exists ONLY as 4 twenty-epoch endpoint-gate runs — there is no 36-cell zmpool sweep at n=3 | Layerwise: 87.740 (r=0) rising to 90.933 (r=1); max interior 90.971 at r=0.99, i.e. +0.038 pp over r=1, inside the +-0.177 seed band. Weightwise: 87.763 (r=0) ... 89.052 (r=0.99) vs 77.921 (r=1) — an +11.13 pp rescue peaking +1.280 pp above plain scalar (87.772). CORRECTED: the published '+0.36 pp over scalar' and '~9 pp rescue' were best_test on a ladder truncated at r<=0.7. The interior of the sweep is itself confounded — the shared term carries a factor of m, so interior cells train at 3-35x smaller step size. | **REFUTED** | With the meta-stepsize confound removed, pooling rescues over-fine granularity and never improves good granularity. The best true-pooling number anywhere (89.052) is still 2.30 pp below plain blk6 (91.350). | CORRECTIONS 15; FINDINGS 12.1/12.2, 25.4, 33.2 |
| Is there a scale law for the pooling optimum (r* ~ 1/N)? | r-curves at ResNet10 / 18 / 34, with r in {0.005, 0.01, 0.015, 0.025} added to resolve R34 downward | n=3/cell, 100 ep, CIFAR-10, a0=1e-6 (r34f/r34r, 25 jobs — now landed) | CORRECTED: the R34 argmax is INTERIOR and bracketed, not grid-limited. Full curve: 0.005 93.276 / 0.01 93.439 / 0.015 93.779 / 0.02 93.883 / 0.025 93.930 / 0.03 93.813 / 0.04 93.365 / 0.05 93.015. r* ladder R10/R18/R34 = 0.10 / 0.06-0.07 / 0.02-0.025, falling monotonically with model size, but r*.m is NOT scale-invariant and 0.02 / 0.025 / 0.03 are not separable at n=3 (0.117 pp span against per-cell sd 0.10-0.19). | **OPEN** | Left open when the underlying pooling mechanism died; a scale law for a dial that turned out to be a meta-learning-rate knob is not worth resolving. The old 'grid-limited' rationale is obsolete — it is now noise-limited. | FINDINGS 25.6/30.1/31.3/32.7; bin/c30_r34f.sh, c32_cluster_a B4 | **[CLOSEOUT, CORRECTIONS 115 — STAYS OPEN, NOW WITH THE PLATEAU5 NUMBERS.]** The pre-registered r34f-r{0005,001,0015} rungs DID land (jobs 4685602-4685610; note they are r34f-*, NOT r34r-*, which is a different submission at jobs 4684237-4684263) and the registered refutation condition did not fire. On plateau5: r* = 0.10 (ResNet10, N=4.90M) / 0.06 (ResNet18, 11.17M) / 0.025 (ResNet34, 21.28M), giving r*.N = 0.490M / 0.670M / 0.532M and a log-log slope of -0.930, R^2 0.952. **THAT IS NOT EVIDENCE FOR 1/N AND THE VERDICT DOES NOT MOVE.** Every one of the three argmaxes is decided by a CROSS-FAMILY margin far below the cross-batch floor: R34 r=0.025 (r34f) beats r=0.02 (r34r) by 0.055 pp and r=0.03 by 0.118 pp; R18 r=0.06 beats r=0.07 by 0.051 pp across fams bo/mx/sc vs ad/mx/zad; R10 r=0.1 beats r=0.08 by 0.124 pp across r10 vs r10b. All are 2-5x BELOW the 0.20-0.29 pp floor this same closeout establishes. r* falls monotonically with N and any slope in roughly [-1.1, -0.8] is consistent. Scope is alpha0=1e-6 only — at 1e-3 the law breaks outright (R10's interior optimum disappears). NOT WORTH FURTHER COMPUTE: the binding limit is GRID SPACING, not seeds, and even at its own optimum the pooled arm LOSES to the tuned baseline on its own architecture (R34 pooling at r*=0.025 reads 93.967+-0.127 vs f5cos-r34-1e-3 at 94.823+-0.060, deficit -0.857, t -10.54).

## 7. The measurement line — what the meta-gradient field actually looks like

| tested | varied | scale | result | verdict | so what | ref |
|---|---|---|---|---|---|---|
| Do per-weight meta-gradients agree in sign far more than independence allows? ('53.1% of 11.17M against a null of 50.0000 +-0.0015%') | granularity x alpha0, sign-agreement probes | probe runs at m=62 and m=11.17M, R18/CIFAR-10 | REFUTED AS STATED — two arms merged into one sentence. 53.1% is the LAYERWISE (m=62) figure; the weightwise arm reads 50.005-50.028%. And the null is n-dependent: E[max(p,1-p)] = 55.07% at n=62 vs 50.012% at n=11.17M, so the quoted arm sits 1.9 pp BELOW its own floor and was reported as 3.1 pp above it. It is also an alpha0=1e-6 number: at alpha0=1e-3 the excess collapses 16x (6.20% -> 0.38%). | **REFUTED** | This one sentence selected the project's research direction for two cycles. Two independent defects (wrong null, wrong arm) plus a regime qualifier. | CORRECTIONS 26, with 12 |
| Does meta-gradient noise average down like 1/sqrt(N) as the partition gets finer (the premise coarse partitions are sold on)? | TWO DIFFERENT AXES, kept separate: (a) drift vs log(m) and (b) drift vs group size N_b | n=10/rung on R18/CIFAR-10 and R18/CIFAR-100; n=2 with only 3 rungs on R10/CIFAR-10 and R34/CIFAR-10; 20 ep, PROBE=100 | CORRECTED SIGNS. On the drift-vs-log(m) axis the measured slopes are NEGATIVE but nowhere near the -0.500 the sqrt(N) model requires: eight fits across 2 datasets x 3 architectures x 2 windows span -0.0915 to -0.3775, every bootstrap CI excluding -0.500 (best-case upper bound -0.3546). On the reciprocal drift-vs-group-size axis the slope is POSITIVE in 6 of 6 fits (+0.075/+0.110/+0.081 and +0.177/+0.150/+0.205) where the model requires -0.500. Matched n=10 log(m) pair: -0.1398 (CIFAR-10) vs -0.1082 (CIFAR-100). Robustness: 31 of 32 leave-one-rung-out sub-spans stay above -0.500; the ONE exception (R10/STARTUP/node->weight, -0.5248) is a two-point fit at n=2 anchored on a downward-biased weightwise startup point. Slope MAGNITUDES remain unquotable — two published fits ran through Lion-CENSORED points pinned at the meta-stepsize ceiling. | **REFUTED** | The campaign's cleanest structural negative, replicated on a second dataset — but the two axes must never be quoted in one sentence (negative numbers were being offered as examples of positive fits), and the one contrary sub-span must be shown. Its FRAMING as a refutation of Adam-mini/Adalayer/SGG is withdrawn — none of those papers makes this argument. | CORRECTIONS 8b, 17 (censoring), 87.1 (framing withdrawn); FINDINGS 26.5/26.6, 31.2, 32.1/32.3 |
| Are per-weight meta-gradients correlated AT THE ADAPTED OPERATING POINT (not just off-equilibrium)? | beta frozen vs beta free (one field: --alg-meta), granularity ladder | CORRECTED: ONE family — fz-w-a3 (n=5, frozen) vs p7-r18-w (n=10, free), byte-matched except --alg-meta, 20 ep, T=2000 probe records, R18/CIFAR-10 only | Free rho_s(weightwise) = 8.888e-08, at 3.1x its own resolution floor (rho_min 2.90e-08), reproducing an independently measured 8.458e-08 to +5.1%. Frozen rho_s = 1.925e-6 (t=11.7, n=5). Variance budget flips 85.1% bias / 14.6% common mode / 0.7% independent (frozen) -> 4.0 / 45.6 / 52.4 (free). | **CONFIRMED** | Non-independence holds in the regime the literature actually runs in. Every rho_s is a LOWER bound by construction. NOTE: the 4-family generalisation (CORRECTIONS 50) is for the exponent s, NOT for rho_s or the variance budget. | CORRECTIONS 34, 47, 52(2a) |
| How much of the independent information their count implies do per-weight meta-gradients actually carry? (N_eff/m) | granularity x 4 architecture/dataset families x beta free/frozen | n=2-3/arm, 20 ep, box-free in 4 of 4 families | Free: 0.163 (R10) / 0.481 (R18) / 0.552 (R34) / 0.305 (CIFAR-100) — i.e. 16%-55% — against 0.033-0.082 with beta frozen. Box-free range 15.7%-54.7%, so the published figures survive to their significant figures. | **CONFIRMED** | The measurement paper's headline — and it is DEMOTED to a scoped diagnostic: it does NOT predict the accuracy reversal (nodewise has far LOWER effective independence than layerwise at 40 epochs and still WINS by 1.258 pp). | CORRECTIONS 59, 68; demoted by 86.3 |
| Is N_eff/m stationary in training time, or is the headline a 20-epoch sentence? | window: epochs 10-20 vs 20-40, box-free by construction (BETA_CLIP -30:+2.0) | n=4 seeds x 3 granularities, 12 of 12 seeds box-free, R18/CIFAR-10 | layerwise 0.7262 -> 0.6903 (-0.036) — NOT SHOWN TO MOVE, a BOUND rather than an absence, because m=62 is marginal against its own resolution floor (rho_min ~1.1e-3); nodewise 0.4056 -> 0.2796 (-0.126, DRIFTING); weightwise 0.5064 -> 0.1508 (-0.356, COLLAPSING). Pre-registered and replicated. | **CONFIRMED** | The headline is a statement about epochs 10-20. Non-stationarity is GRANULARITY-DEPENDENT, which is the interesting half. Do not write the layerwise cell bare as 'stationary'. | CORRECTIONS 73, 77; renamed from 'budget' to 'window' by 86.1 |
| Is N_eff/m an artefact of the BETA_CLIP guard box every run sits inside? | floor {-15, -30}, ceiling {-4.6052, -2.3026, 0.0, +2.0, +6.0}; full box removal in 3 families | n=2-3/arm, 20 ep, 4 families, ~60 jobs across cl5/uc5/uc6/wc5 | Four boxes at R18 spanning 15 log units of floor and 4.6 of ceiling move N_eff/m by a total span of 0.076 against a +-0.10 bar. Removing the box entirely from arms at 100.0% Q4 ceiling occupancy moves it -0.006 (R10), +0.008 (C100), -0.004 (R34 null control). | **CONFIRMED** | The single biggest threat to the measurement headline, closed by test in four families. Also: the guard is accuracy-neutral (+0.027 pp). | CORRECTIONS 68; FINDINGS 52.2/52.3/52.10 |
| Is a step-size adapter a HIGH-PASS FILTER on meta-gradient correlation — and is it a dial or a switch? | meta-stepsize {frozen, 1e-4, 1e-3, 1e-2} x granularity {blk6, lay, node, w} | n=3/rung, 36 jobs, 20 ep, R18/CIFAR-10 | The profile exponent b runs 0.343 (frozen) -> 0.219 (ms=1e-4) -> 0.081 (ms=1e-3), smooth and monotone; suppression at k=1 goes 1.00x -> 3.37x -> 21.09x; N_eff/m goes 0.042 -> 0.129 -> 0.481. The ms=1e-2 rung is BOUNDARY-DOMINATED (guard binds on 88.0%/86.8% of records) and is quoted by nothing. | **CONFIRMED** | A DIAL driven by the meta-stepsize — the earlier 'switch' reading was withdrawn by its own pre-registered band test, which landed inside the registered [0.10, 0.30] interval. | CORRECTIONS 58, withdrawing 53 |
| Off the meta-optimum, does the correlation have a RANGE (short-range, channel-scaled) or is the falling profile an estimator artefact? | block size k from 1 to layerwise x 4 architecture/dataset families, with a heterogeneity-corrected independence floor | n=2/arm, 20 ep, PROBE5 per-coordinate counts | The profile falls >= 10x in every family (R10 264.6x, R18 69.3x, C100 23.3x, R34 16.6x) AFTER heterogeneity correction, which itself removes 58% of the raw excess. Per-leg exponents: within-channel 0.069-0.181 vs beyond-channel 0.393-0.816, steeper in 4 of 4. A parameter-free exchangeable model over-predicts layerwise by 13.6x-127x. | **CONFIRMED** | The project's structural result: correlation is nearly scale-free within a channel and collapses beyond it. At the adapted equilibrium the profile FLATTENS and exchangeability is not rejected — so this is an off-equilibrium statement. | CORRECTIONS 45, 50, 59; narrowed by 47 |
| Adam-mini's ACTUAL premise: does the row mean represent its row (so a per-row step size is a good approximation)? | row / column / spatial groupings, each against a size-preserving within-tensor regroup null; and the nodewise arms where the row IS the adapted unit | 58 weightwise + 67 nodewise arms, 13 batch families, 4 architectures, 2 datasets, frozen and free beta | CLEAN NEGATIVE FROM BOTH SIDES. The row explains 0.083%-0.612% of within-tensor structure (median 0.190% all tensors; 0.063% conv-only) against a null of 0.069%-0.175% — 97-99% is WITHIN the row. At the nodewise arms rows are not distinguishable from each other either: R_tensor_cor 86.88%-97.06% (median 93.50%) against a null of 0.19%-0.50%. | **REFUTED** | The premise the whole coarse-partition line rests on fails on the quantity our partition aggregates. NOTE: we measure z (the meta-gradient), not G — no document may write 'we refuted Adam-mini'. | CORRECTIONS 88.5/88.16, sharpened by 89.3 |
| Do 10 anomalous arms carry genuine conv-row structure, and is it architectural or dynamical? | 1-D-tensor share (H_A), saturation/dead channels (H_C), genuine conv structure (H_B) — all three registered before scoring | 58 weightwise arms; plus cross-seed reproducibility | H_A refuted and BACKWARDS (clean arms draw a median 50.6% of their numerator from 1-D tensors); H_C refuted by one number (conv coordinates with p_i in {0,1} = 0.000% in all 58 arms); H_B holds with no overlap and a 16x gap (conv-only R_row_cor: exceptions 7.216-49.412% vs clean 0.000-0.448%). But WHICH rows carry is at the null — one seed inverts by 500x in the same config. | **CONFIRMED** | Stated against our own interest: within a single run that structure is real. It does not rescue the premise, because the premise is a claim about row IDENTITY and the statistic measuring exactly that sits at the null. | CORRECTIONS 89.2/89.4/89.5/89.9 |
| Is the 3x3 convolution FILTER a coherent structural object in the meta-gradient field? | output-channel x input-channel interaction term F_int, block-size ladder, contiguous vs semantic (o,i) partitions | 50 clean weightwise arms, 13 families, 4 architectures, 2 datasets | F_int = 0.811-1.041 across ALL 50 arms against a registered threshold of 1.10 — not reached in a single arm. The unrestricted argmax is g <= 9 coordinates in 17 of 17 arms, and a NON-CONTIGUOUS (o,i) partition beats the output channel in 17 of 17. | **REFUTED** | No filter object exists; the structure is additive row + input-channel and lives at the KERNEL scale, not at contiguity. Marked DONE, do not re-run. | CORRECTIONS 91.6, 91.7, 92.2, 93.5 |
| Where in block size is the meta-gradient field's structure maximal, and is that location trajectory-invariant? | block size u across the full ladder x beta frozen/free x meta-stepsize x base optimizer | 33 arms (19 across four trajectory classes + 14 on the ms axis), 20-80 ep | CORRECTED PRECISION. 14 of 14 arms on the ms axis peak at exactly u*=1/512 (spread 0.000 rungs); the other 19 of 19 peak at u* in {1/1024, 1/512} — ONE rung of spread, not an exact rung. 4 of 17 arms peak on U_LADDER's bottom boundary rung 1/1024 and are therefore CENSORED FROM BELOW — bounds, not located peaks. Registered POWER CONTROL, both values: amplitude ratio 2.192x geometric mean on the 19-arm set (corpus E* span 1059x) and 1.524x on the clean ms dial, the latter clearing its 1.50 bar by only 0.024 and only because of the unquotable rung. Of the 19-arm tests A1/A3 are POST-HOC-INFORMED; only B1-B4 are blind. | **CONFIRMED** | The peak LOCATION is invariant to one rung; the AMPLITUDE is not — so the invariance is a real measurement and not a vacuous one. Do not write '33 of 33 at exactly 1/512'. | CORRECTIONS 92.3, 95.3/95.4/95.5/95.6, 96.2 |
| THE BRIDGE — does 'the field has structure at scale X' imply 'partition at scale X' (the only way the measurement line becomes a method)? | field peak u* vs training-accuracy-optimal partition, on ONE common axis, across 21 budget-matched accuracy cells and 5 meta-stepsize rungs | 17 field arms x 21 accuracy cells; positive control on frozen-beta arms | MEASURED NEGATIVE at >= 2.71 decades (>= 512x). The field peaks at u* ~ 0.0017 in 17/17 arms; training peaks at layerwise (u ~ 440) in 16 of 21 cells. Coincidence fires 0/21; the >=2-decade separation fires 20/21 as registered and 14/14 after gating unscorable cells. The field peak sits 0.74 decades from WEIGHTWISE, the worst partition (-4.6 pp, n=26). Positive control: frozen-beta granularity span 0.078 pp median vs free-beta 5.662 pp. | **REFUTED** | STANDING RULE 13: direction C is DIAGNOSTIC, NOT PRESCRIPTIVE. Capturing more field structure monotonically HURTS training. The 5.41-decade point estimate was itself withdrawn; the verdict survives at >= 2.71. | CORRECTIONS 94.2/94.8, revised by 96.7, strengthened by 96.8 |
| What causes the late-training (Q4) rebound in per-weight correlation? | clip saturation removed by test (Q4 ceiling occupancy 100.0% -> 0.0%) in two families; beta-velocity collapse | n=2/arm, 20 ep, R10 and CIFAR-100 | Clip saturation EXCLUDED BY TEST: rebound ratio 2.690 -> 2.701 at R10 (unchanged to 0.5%) and 2.577 -> 2.583 at CIFAR-100 (0.2%) while occupancy went from 100% to 0%. Beta-velocity collapse also dead (flat to +-30%). | **OPEN** | An unexplained effect recorded AS unexplained, with two mechanisms genuinely killed rather than merely 'unsupported'. This took four bookkeeping reversals to get right. | CORRECTIONS 69, reinstating 61(5) on correct evidence |
| Does the frozen-vs-free scaling exponent contrast survive a change of estimator? | raw agreement vs debiased agreement vs variance instrument, on a byte-matched frozen/free pair | p5 vs fr5, n=2-3, 20 ep, R18/CIFAR-10 | Raw gap 0.343 (frozen 0.618 / free 0.961); debiased 0.178 (0.754 / 0.932); variance 0.188 (0.759 / 0.947). The two clean instruments agree to 0.010 — so ~48% of the published effect was the bias channel. | **CONFIRMED** | Direction survives on two independent clean instruments; the published MAGNITUDE was half instrument artefact and 0.343 may never be quoted again. | CORRECTIONS 56, cause identified in 55 |

| Does the argmin of N_eff/m track BUDGET, or ADAPTATION EXTENT (span)? | span (c55 3-rung steady defn) across 8 cells differing in network, dataset, budget and ms | ns5/uc5/uc6/cl5/br6 + sp8 (9 jobs, 40 ep, submitted and scored in one tick) | Argmin is `w` below span ~6.3, `node` in ~[8.9,12.6], `w` again above ~17.5. sp8 is the FIRST 40-epoch cell INSIDE the node band (span 9.96, argmin node, gap/SE 7.35) — breaking the confound cycle 55 flagged against itself, where every in-band cell was 20-epoch and every 40-epoch cell sat above it. Neither budget nor ms gives two argmins at its own matched value; span does. | **CONFIRMED** | Collapses two of CORRECTIONS 70's three scopes (ms, budget) into ONE SCALAR. The base-optimizer scope does NOT collapse. `w` is the argmin BOTH below and above the node band — an INTERIOR BAND, never a threshold. 2 of 8 cells UNDECIDED; cl5 cD UNINTERPRETABLE and excluded, not counted. | FINDINGS 71.7/71.8; CORRECTIONS 101.9 |
| Does the N_eff/m ordering across partitions depend on the BASE optimizer? | SGDm vs AdamW, same three rungs, same budget | bo7, 12 jobs, 20 ep, R18/CIFAR-10; W0 12/12, W0.3 box-free 12/12 at 0.00% | The ordering **INVERTS**: SGDm `node 0.4016 < w 0.5045 < lay 0.7258`; AdamW **`w 0.01725 < node 0.11432 < lay 0.43608`**. Weightwise under AdamW is **0.110x the lower endpoint** of the published [0.1571, 0.5474] range — OUTSIDE it. | **CONFIRMED** | **The nodewise minimum is an SGDm phenomenon**, so the Adam-mini sentence suspended since cycle 54 is DELETED, not hedged. The campaign's headline range must be written 'at a 20-epoch budget, under SGDm'. ms/alpha0 are UNTUNED for AdamW — this is an operating point, not a property of the optimizer. | FINDINGS 71.1/71.2; CORRECTIONS 101.2/101.3 |
## 8. Instrument and methodology — the corrections that cost the campaign real results

| tested | varied | scale | result | verdict | so what | ref |
|---|---|---|---|---|---|---|
| Is the `plateau` column comparable across budgets? | audit of aggregate.py:85 — plateau_of(tests, k=20) | whole CSV, 1707 rows; 144 runs hand-checked with 0 disagreements | On a 20-epoch run tests[-20:] IS the whole run, so `plateau` is an area-under-the-curve SPEED statistic there and an asymptote at 100 epochs, under one column name. 380 of 1675 runs (22.7%) are affected; mean(last 5) minus recorded plateau has median +13.479 pp. Live consequence: a 1.67 pp layerwise-over-nodewise advantage at 20 epochs (4 of 4 strata resolved) becomes 0.07 pp with 0 of 4 strata resolved under the documented k=5 window, on n=27/25/26. | **CONFIRMED** | The defect sits UNDER the headline, was found once (86.2) and then ignored by five consecutive cycles before being rediscovered. aggregate.py is deliberately NOT patched — that is an operator decision. | CORRECTIONS 86.2, rediscovered and quantified by 96.4-96.6; STANDING RULE 14 |
| Were the c40 response-surface rows measured on completed runs? | audit of epochs_done vs epochs_requested by granularity | rs-* family, 100 ep, R18/CIFAR-10 | A truncation contaminant that is GRANULARITY-ASYMMETRIC: 0 of 22 scalar and 0 of 22 layerwise runs truncated, but 6 of 13 blk6 (46%) and 6 of 10 nodewise (60%). Depression is monotone in severity (24 ep -> 76.81, 29 ep -> 78.73, 36 ep -> 82.18). The published blk6 row's SHAPE inverts when they are dropped. | **REFUTED** | A response surface reporting an arm's shape from runs that stopped at a quarter of their budget. The one analysis that had already filtered correctly (42.2) re-derives exactly. | FINDINGS 57.1 |
| Was the weightwise arm correctly dropped from the response surface? | audit of the number used to justify the drop | n=10 (all 20-epoch) vs the single matched 100-epoch run | A UNIT ERROR: '68.471 +-0.245 (n=10)' is p7-r18-w, and all ten are 20-EPOCH runs, read as a 100-epoch plateau. The one matched 100-epoch plain weightwise run is 90.913 — +22.4 pp above the figure used to reason the arm away. | **REFUTED** | An entire granularity was deleted from the campaign's central experiment by a units mistake, and the replacement grid is still unrun. | CORRECTIONS 85, with 86 (cycle 57).4 |
| Are DERIVED probe fields safe to compute statistics from? | audit of block_sizes.json n_b, agree2's count cap, drift_extract's `spread`, the `collapsed` column | all probe directories, whole CSV (1707 rows) | Four independent failures. (i) n_b reports 11,173,962 for NODEWISE arms that have 14,420 nodes — z-scores overstated by 27.8x. (ii) counts above 20,000,000 were CENSORED (limit_denominator), so R34 weightwise (21,282,122) reported at exactly 20,000,000, moving affected cells 10-35% relative. (iii) `spread` read two fields written identically — 0.0000 by construction, a check that could never fail. (iv) CORRECTED: `collapsed` is NOT 0 on every row — it fires on 12 of 1707 rows (i3a300-1e1-s0, i3a-1e1-s1, g1_sgdm_weight_s0/s1/s2, d3_g999_s0/s1, d2_collapse_s0/s1, d3_g1_s0/s1, d1_metaLion_s3). The live defect is narrower: the predicate `tests[-1] <= 11.0 and max(tests) > 20.0` cannot by construction flag a PARTIAL collapse, so the FINDINGS 23.2b run that plateaus at 20.5 is unflagged — and that one run out of 24 more than doubled a reported granularity gain (+1.73 -> +4.60 pp). | **REFUTED** | STANDING RULE: a probe field derived at write time is a CLAIM, not a measurement. Write 'the detector misses partial collapses — filter on plateau > 50 explicitly', never 'collapsed is 0 on every row'. | CORRECTIONS 16, 19; FINDINGS 23.2b |
| Is it safe to tabulate a probe while its job is still running? | same probe read at 89 vs 100 records | p7-r34-lay-s1 | The STEADY window is 'the last 50% of records that EXIST', so the headline excess reads 0.9879 at 89 records and 1.4690 at 100 — a 49% change from nothing but reading early. Stdout buffering means an in-flight probe shows 0 epochs and does not announce itself. 8 of 32 probe directories in one cycle's table were affected. | **REFUTED** | Also cost the project once via a related bug: an M1 'collapse chasm' at ResNet34 turned out to be cells read at 18-36 of 100 epochs. | CORRECTIONS 20; FINDINGS 36.8 |
| Is the BETA_CLIP guard occupancy being measured at the resolution the clamp operates at? | per-TENSOR beta mean vs PER-COORDINATE clip counts (PATCH_CLIPCOUNT) | 36-job ml5 batch + the 18-job wc5 control it justified | The published 0.00% high-guard occupancy came from the per-TENSOR beta list (one mean per tensor) while the clamp applies per coordinate. At coordinate resolution the HIGH guard binds on 95.4% of records and binds EARLIER than the low guard. Cycle 50 therefore spent 18 jobs MOVING THE WRONG WALL, and its primary test came back UNINTERPRETABLE by its own registered fallback rule. | **UNINTERPRETABLE** | STANDING RULES 5 and 6: a registered fraction must name its DENOMINATOR and be measured at the resolution the mechanism operates at. Three denominators on the same bytes disagreed by two orders of magnitude and landed on opposite sides of 50%. | CORRECTIONS 62, 63; FINDINGS 51.1 |
| Can a box-free (guard-free) configuration exist at this budget? — an extrapolation used as a design premise | beta velocity measured on the startup segment vs the segment being extrapolated to | 5 'WILL BIND' predictions vs 5 'will not bind' | REFUTED BY MEASUREMENT and perfectly one-sided: 0 of 5 'will bind' rows correct, 5 of 5 'will not bind' rows correct. The 92-100%-of-maximum-Lion-speed figure was the STARTUP velocity; the top coordinate decelerates ~9x. Then the FIX was still not enough — the guard bound at 10.4 extra epochs against a worst-seed projection of 22.8 epochs of headroom, i.e. 12.4 epochs EARLIER than projected (optimistic by 22.8/10.4 = 2.2x), because one seed RE-ACCELERATED 3.1x (0.123 -> 0.379 v/vmax between epochs 10-20 and 20-30). | **REFUTED** | STANDING RULE 7 and its corollary: a rate used to extrapolate must be measured on the segment extrapolated FROM, and a monotone trend in a rate is not a property of the rate. CORRECTED DIRECTION: the bind came EARLIER than projected, not after — the opposite reading would make the projection conservative and would not support the rule. | CORRECTIONS 67, 71 (late addendum), 72 |
| Can a pooled gate statistic be trusted for a binary event? | pooled binding fraction vs per-seed binding fraction | bl5, 9 jobs, 40 ep | A reported 'weightwise 8.03% at the guard' was ONE seed at 24.07% and two at 0.00%. The pooled row hid a run that spent 96.3% of its last quarter pinned. The affected gate's primary result was recorded UNINTERPRETABLE, and the record index at which it first bound became the actual finding. | **REFUTED** | STANDING RULE 8: score binary gates PER SEED, never on a pooled fraction. A mean over a binary event is not a diagnostic. | CORRECTIONS 72; FINDINGS 53.1-53.3 |
| Can a summary statistic be read past its own uncertainty flag? | argmin verdicts where one or more ranked rungs failed their interpretability gate | 25 argmin cells audited; 12 recorded UNINTERPRETABLE once the rule was applied uniformly | Four separate occurrences of the same failure, including a scorer that printed 'the Adam-mini sentence must be DELETED' from an argmin computed over two rungs that had FAILED the box-free gate. Verdict refused; the dependent sentence is SUSPENDED — it may not be written and may not be deleted — pending a batch (bo7) that has never been read. | **UNINTERPRETABLE** | An argmin inherits the interpretability of EVERY rung it ranks, not just the one it selects. And CORRECTIONS 76(1)/79 is SYMMETRIC: deleting a claim on unregistered evidence is the same error as keeping one. | CORRECTIONS 75, 79, 83 |
| Is 'byte-matched to batch X' a safe thing to write in a submit script header? | audit of two batches whose headers claimed byte-match | uc5 (6 of 18 jobs), plus the earlier cancelled-queue incident | 6 jobs died within ~5 minutes: the header claimed byte-match to a CIFAR-100 batch but passed --NN-name ResNet18 with --dataset CIFAR100, i.e. a 10-class head against 100-class labels. Separately, an ad-hoc queue trim selected by RECENCY and cancelled 50 pending jobs that a cycle had named in writing as 'the two things this cycle turns on' — decisive experiments are BY CONSTRUCTION always the newest jobs in the queue. | **REFUTED** | Produced two operational rules: 'byte-matched' is a claim and must be DIFFED by a guard, and queue trimming goes from the BACK by Nice with a protected-prefix file, printing every name. | CORRECTIONS 71; FINDINGS 52.8; bin/safe_trim.sh, bin/PROTECTED.txt |
| Does the FairShare >= 0.35 submission floor do anything? | measured RawUsage decay over a full idle cycle | both accounts, cluster-wide | PriorityDecayHalfLife = 14 days with no usage reset, so a full cycle with both queues at zero moved FairShare by less than the 6th decimal (0.333054 -> 0.333054, RawUsage -0.0859%). Separately, 93 of 94 pending jobs were QOSMaxGRESPerUser-bound, not priority-bound. | **REFUTED** | A self-imposed gate that had been blocking submissions for cycles was measured to be inert and formally retired, replaced by a batch-size rule. | CORRECTIONS 35, 37 |
| Operational state — what is actually left to run? | n/a | CSV frozen at 1707 rows since cycle 54; cluster down 12 consecutive ticks | Four guard-validated batches are WRITTEN AND UNSUBMITTED: hz9 (9 jobs — the paired tuned test of the horizon reversal, the campaign's top open question), rw9 (18 jobs — the weightwise peak grid deleted by the unit error), cp9 (8 jobs — the per-coordinate sign probe), sp8 (9 jobs — the span/budget dissociation), plus a 1-job PROBE7 gate. Two batches (bo7, bd7) were RUNNING when the outage began and their survival is unknown and deliberately not guessed. | **OPEN** | Every remaining open question in this table is blocked on the same outage; the zero-compute reduction of bytes already on disk has produced a result in each of the last 12 cycles. | CORRECTIONS 96.0/96.13 |

| **Is a guard that an instrument EXISTS a guard that it is ENABLED?** | PATCH_PROBE5 present in HF.py (bf8's guard 1) vs PROBE5=1 present in the sbatch --export line | bf8, 12 jobs, 80 ep, cluster_b — the whole batch | **NO.** patch_probe5.py:55 gates the per-coordinate sign-count writer on `PROBE5=='1'`; bin/c71_floor_budget.sh:306 exports `PROBE=5` (the probe STRIDE) and never `PROBE5=1` (the INSTRUMENT); bd7 exports both. Guard 1 checked the patch was present. It was present. It was OFF. No neg_counts.npy was written, neff_instrument.reduce_root:162 skips any dir lacking neg_counts.json, so F0.5/F1/F2 were never scored. **12 GPU-jobs, zero N_eff data.** Then the scorer compounded it: F0 read n_beta from the missing file and printed 'a n_beta mismatch means the NETWORK changed: the batch is VOID' — but n_beta is a probe.jsonl FIELD, present and constant across all 8000 records, and **F0 re-scored is 12/12**. | **REFUTED** | **STANDING RULE (19): check the switch, not just the wiring; and a validity gate must read the quantity it NAMES from the source that carries it.** A gate that silently degrades to a file-presence test returns VOID for a healthy run and PASS for a broken one. bf9's guard 1b reads the PROBE5 gate out of HF.py, asserts its OWN --export line sets stride+instrument+interval, and diffs all three against bd7 — and it caught itself twice on the first two dry runs (a self-reading guard must exclude its own source line). | CORRECTIONS 102.2/102.3; FINDINGS 72.2; analysis/c73_bf8_ceiling.py (15/15) |
| Does a box-free gate scored at BOTH guards actually catch the bind? | LO occupancy vs HI occupancy, per seed, at LO in {-60,-90} with HI fixed +2.0 | bf8, 12 arms, 80 ep | **The gate worked and the prose then misnamed it.** STANDING RULE (18) caught a bind — but at the CEILING, while the printout said 'AN ARM BINDS AT LO EVEN AT THIS FLOOR' and pointed at first_lo, which is None on 12/12. Floor fully freed (bd7: 10/12 bound at LO=-30 -> 0/12 here); deepest beta_true_min **-46.744** against a pre-registered projection of **-46.4**, error 0.34 log units. Ceiling bind is seed-0-only, at both floors: rec_hi 52.23%/50.68% but **coord_hi 0.0178%/0.0329%** — ~2-4k of 11,173,962 weights. | **CONFIRMED** | Reporting only rec_hi on an 11.17M-coordinate arm **overstates the bind by ~3000x** (STANDING RULE 6). Both columns reported, neither averaged, and the published rec_-based gate is NOT re-thresholded after the fact. The registered 'binds at LO => next batch is an ms ladder' branch DID NOT FIRE and may not be cited as discharged. | CORRECTIONS 102.4; FINDINGS 72.3 |
| Is the ~290-run orphan backlog real? | generous STRICT vs LOOSE citation rules over 109 families / 1707 runs, with corpus ablation | whole CSV; analysis/c69_orphan_census.py, 24/24 selftests, registered before any family was classified | **WITHDRAWN as a search artefact.** 102 families / 1676 runs STRICT-cited, 6 / 27 AMBIGUOUS, **1 family / 4 runs ORPHAN**. The ~290 figure came from ablating MASTER-TABLE.md AND FINDINGS.md out of the corpus (which gives 342). True uncited set is **7 runs**, all 2-5 epoch smoke tests at chance accuracy, now named and abandoned. | **WITHDRAWN** | A per-tick operator instruction was discharged at campaign scale, and the instruction's own premise was refuted by measuring it. **Never quote a ~290-run orphan backlog again.** | CORRECTIONS 98.2/98.3; FINDINGS 69.5 |
| How often is a published headline contrast contaminated by an unmatched axis? | full-axis reconstruction of every published contrast | 56 headline contrasts; analysis/c70_composition_audit.py, 31/31 selftests, registered before any cell was scored | 6 of 56 contaminated (**by ONE cell** — 5/56 would read localised), 2 of 5 material. Pair-level post-hoc **13/56 (23.2%)**, with a verdict flip in **8 of 13**. All four published 65.3 cells reconstruct clean; 68.6 remains the only contaminated published cell. G6 ('100% shrink') WITHDRAWN as a rule by the same pass — 5/5 became 29/45. | **CONFIRMED** | **Any unaudited contrast now carries a prior: ~23% contaminated, ~62% flip if so.** Reproduced live at cycle 73: on a loose 8-axis match nodewise@1e-3 reads n=8/92.808 and would have overturned hz9's tuned arm; under the full 13-axis match it is n=3/92.254 and hz9 was right. | CORRECTIONS 100; FINDINGS 70.x; re-demonstrated in 72.4 |

## 9. Cycles 130–149 — the mechanism hunt: WHERE in the network the granularity gap lives

**How to read this section.** Cycles 130–139 were paper-integration and correction cycles and ran **ZERO GPU jobs**; they produced no experiment rows and are recorded in CORRECTIONS 130–139 only. Every row below is a batch (or a zero-GPU re-derivation over runs already on disk) from cycles 140–148. **The verdict column here carries the REGISTERED SCORER'S OWN TOKEN, verbatim, in backticks** — not a paraphrase, and not one of the six prose verdicts sections 1–8 use. That is deliberate: these batches were pre-registered with branch tokens, and the token is the result. Tokens are therefore counted separately from the CONFIRMED/REFUTED/OPEN/WITHDRAWN/DEAD/UNINTERPRETABLE tally in the header. **[SUPERSEDED at cycle 150: 'The three cycle-148 batches in flight at compile time (`cdep1`, `cvg1`, `ciso2`) are NOT here — they were not scored by this pass and their rows are theirs to add.']** Two of those three have since landed, been scored by their registered scorers and been ingested, and their rows are the **last two rows of this section**, added at cycle 150 (CORRECTIONS 197). **`ciso2` is still not here** — it was scored by a concurrent track in the same cycle and its row is that track's to add; every reader used for the two new rows excludes `ciso2-`, and both scorers print bit-identical output with and without `ciso2` in the corpus.

| tested | varied | scale | result | verdict | so what | ref |
|---|---|---|---|---|---|---|
| Is the hierarchical `r`-dial just a meta-stepsize dial in a pooling costume? | `r` x meta-stepsize — the first time `r` was tested off the single `ms=1e-3` that all 637 prior hierarchical runs shared | `g4m`, 72 runs, 100 ep, ONE batch | Interior peak in `r` SURVIVES ms-optimisation WITHIN each `r` at **+0.821 pp**, against a registered bar of 0.50. | `REFUTED` | Overturns the audit's central mechanism claim. "r is a step-size dial in a pooling costume" had never actually been tested, because every prior hierarchical run sat at one `ms`. | CORRECTIONS 141.1 |
| Does the two-level proposal `beta_g = s + d_g` (partial shrinkage) beat plain layerwise? | shrinkage strength, at `alpha0` {1e-6, 1e-3} | `tl1`, 42 runs, 100 ep | PEAK **-0.253 pp** at `alpha0=1e-6` and **-0.819 pp** at 1e-3. Plain layerwise beats EVERY partial-shrinkage setting. | `NULL at both alpha0` | The proposal as written is dead in both directions; shrinkage toward a shared level costs accuracy at both `alpha0`. | CORRECTIONS 141.1 |
| Does an empirical-Bayes / James-Stein lambda recover the pooling benefit? | EB/JS lambda | `eb1`, 18 runs, 100 ep | LIFT **+0.041 pp** against a registered bar of **1.16**. | `FAILURE` | The last principled pooling estimator the project funded. With `g4m`, `tl1` and this, no hierarchical-pooling fix survived its own controls. | CORRECTIONS 141.1 |
| Does the `r`-ladder transfer to ResNet-50? | `r` ladder x `alpha0` {1e-6, 1e-3} x two initialisations | `a2g3`, 36 runs, 100 ep, ResNet-50 | The two initialisations DISAGREE at both `alpha0`. | `SPLIT / UNRESOLVED` | Reported unresolved rather than softened. No ResNet-50 `r` claim may be made in EITHER direction. | CORRECTIONS 141.1 |
| Does the scalar-to-layerwise gap replicate on a third dataset? | granularity {scalar, layerwise} on Tiny-ImageNet | `tn1`, 12 runs, 100 ep | GRAN = layerwise - scalar = **+40.975 pp**. | `REPLICATES` | The gap is not a CIFAR-10 artefact. Note the axis: this is DATASET, not ARCHITECTURE — the corpus remained 100 % ResNet until `cvg1` was launched at cycle 148. | CORRECTIONS 141.1 |
| Is the gap carried by the HEAD — the last two tensors? (The first mechanism the data actively suggested.) | partition {scalar, `[2,60]` stem, `[60,2]` head, `[31,31]` mid, `resnet18_blocks`, layerwise} | `hb1`, 18 runs, CIFAR-100/`ResNet18_c100`, 100 ep, ONE batch | Capture of the in-batch 46.904 pp scalar-to-layerwise gap: head **+0.0412**, stem -0.0028, mid **+0.2165**, blocks +0.6486. `SPEC` = head - max(control) = **-0.1753** — the head is BEATEN by an arbitrary control partition. Re-derived from the corpus exactly as the scorer reported it. | `REFUTED-SMALL` | Kills the head hypothesis at its first direct test, and starts the cut-position line that runs through the rest of this section. | CORRECTIONS 141.7, 142.2; `analysis/cH1_hb1_score.py` |
| Is the surviving peak Lion-specific? | meta-optimizer on the additive two-level arm | `lsm1`, 24 runs, 100 ep | All 24 jobs reached `RUN_DONE`; the registered scorer, run UNEDITED with its documented arguments, returned the truncation branch. | `UNRESOLVED-TRUNCATED` | The Lion-specificity question is UNANSWERED, not answered negatively. Nothing here licenses a meta-optimizer claim. | CORRECTIONS 142.1; `analysis/cA3_lsm1_score.py` |
| Is the operative variable the NUMBER of groups, or the SIZE-BALANCE of the groups? | `m=2` cut position {`[45,17]`, `[31,31]`, `[17,45]`, `[60,2]`, `[55,7]`} — including a MIRROR arm added BEYOND the briefed design | `cbl1`, 33 runs, CIFAR-100/`ResNet18_c100`, `ms=1e-3`, `alpha0=1e-6`, 100 ep, ONE batch | `[45,17]` and `[17,45]` have **IDENTICAL** normalised entropy (0.84742) and differ by **+16.539 pp, se 0.799, t +20.71 (df 4)** — capture 0.4202 vs 0.0679. `BAL_RANGE` 0.4095 against `CNT_RANGE` 0.7841, ratio 0.522. | `COUNT-LAW-UNRESOLVED-BALANCE-COMPARABLE` + `BALANCE-MATTERS` | **The mirror control DISQUALIFIED the batch's own registered statistic and was NOT replaced by a better-fitting one** — that refusal is the point. What survives is sharper than either hypothesis and is ASYMMETRIC IN DEPTH: cutting at tensor 45 captures 42 % of a 46.5 pp gap, the mirror 7 %, the head 4.5 %. The LATE layers need their own step size. No optimum in `m` is named: CAPTURE is 0 at `m=1` and 1 at `m=62` BY CONSTRUCTION. | CORRECTIONS 143; `analysis/cJ1_cbl1_score.py` |
| Does the parent paper's blockwise claim hold at 489 ImageNet classes? | granularity {scalar, blockwise, layerwise, nodewise} | `in489g1`, 12 runs, ImageNet-489 | Registered branches: BLOCK `UNRESOLVED` / GRAN `UNRESOLVED` / NODE `FINER_HELPS`. Blockwise shows **no improvement over scalar** (+0.4126 pp at E=80, both arms at ~1 %). | `UNRESOLVED` (`BLOCK UNRESOLVED / GRAN UNRESOLVED / NODE FINER_HELPS`) | The parent's blockwise claim is CORROBORATED and its scope BROKEN. **[SUPERSEDED IN PLACE by CORRECTIONS 163.6.** The superseded wording is kept verbatim: *"SIX GROUPS ARE NOT ENOUGH AT 489 CLASSES AND SIXTY-TWO ARE: a 47.4 pp cliff between two adjacent rungs of the ladder."* It fails on two grounds — (1) NOT A CLIFF: `in489g2` fills the interior and the rise is DISTRIBUTED, largest adjacent capture rise +0.4316 under the registered `CLIFF` bar of 0.60; (2) NOT ABOUT SIX: at `m=8` capture ranges +0.0080 to +0.1497 depending only on WHERE the cuts fall. **The parent-paper corroboration is UNTOUCHED.]** | CORRECTIONS 144, superseded in part at 163.6 |
| Where does the cut have to fall for an `m=2` partition to capture the gap? | contiguous `m=2` cut position `k` over a grid, with the parameter-balance rival pre-registered as a point prediction | `cpk1`, 39 runs, CIFAR-100/`ResNet18_c100`, 100 ep | ARGMAX at **`k*=49`** (`[49,13]`, plateau5 55.447, capture **+0.6965**); `k=17` +0.0687, `k=55` +0.0139. RANGE 0.6826, PEAK SET = {49} ALONE, half-maximum span 7 tensors (hump covers `k` in [42,49]). Four registered predictions REFUTED including P1 (`k*=45`, the stage boundary) and P5 (the balance rival). The step `k=49 -> k=52` is **-0.3640 CAPTURE, -17.105 pp, t -25.55 (df 4)** — capture halves. Mirror pairs break at identical entropy: `k=17` vs `k=45` differ by 16.290 pp (t +23.78). | `SHAPE POSITION-SINGLE-PEAKED` + `BOUNDARY BOUNDARY-REFUTED` | The pre-registered RIVAL won by predicting a different grid point. **[TWO SUPERSESSIONS, both in place. (a) CORRECTIONS 160: the wording *"Position is settled as the operative variable"* is kept verbatim and read instead as "the operative variable AMONG THE `m=2` ARMS" — the "cut position dominates group count" form REPRODUCES AS AN OVERCLAIM. (b) CORRECTIONS 156.1: the capture curve, its single-peakedness and `k*=49` are **100-EPOCH MEASUREMENTS** and must be written as such; `cpk1` was never run past 100 epochs.]** | CORRECTIONS 146; rescoped 156.1; overclaim corrected 160 |
| Which single tensor carries the `k=49 -> k=50` cliff? | the five consecutive single-tensor steps around the cliff | `cts1`, 18 runs, 100 ep, ONE batch | In-batch GAP 46.951 pp. `k=49->50` moves `layer4.0.bn2.weight` (**512 params**) for **+24.834 pp, se 0.944, t +26.32 (df 4)**, dCAPTURE +0.5289; the neighbouring steps give -0.233 (`bn2.bias`, 512) and -7.707 (`shortcut.0.weight`, **131,072** params). | `CLIFF CLIFF-REPRODUCES` + `SPLIT SINGLE-TENSOR-MAJORITY` + `TENSOR layer4.0.bn2.weight` + `A1 MASS-REFUTED` + `A2 BN-LEVERAGE-FAVOURED` + `SCALE-SHIFT SCALE-SHIFT-ASYMMETRIC` | **The briefing's MASS account is dead**: the 512-parameter BN scale moves the cliff and the 131,072-parameter conv weight moves it BACKWARDS. The winning account's class-level form was withdrawn by the parent batch's own data at 147.6. **[RESCOPED IN PLACE by CORRECTIONS 156.1: the 24.834 pp numeral is a 100-EPOCH object and every quotation must carry "at 100 epochs" in the same sentence.]** | CORRECTIONS 147, rescoped 156.1 |
| Is the cliff an artefact of the `BETA_CLIP` clamp? | box {clamped, released} x cut {49, 50}, one factor | `cts2`, 12 runs, 100 ep | `D_R` = **24.7673 pp** against a survive bar of 12.4170; `D_C` 25.163. NO MODERATION at 2 `SE_INT` (`INT` 0.3953 pp <= 2.0598). Scorer run UNEDITED on `cluster_b` (exit 0) and re-run on the Mac against the local backup, reproducing every line. | `SURVIVED` | The cycle-123 mechanism objection is tested and does not survive: the pinning is real, the DEPENDENCE is not. CORRECTIONS 147 is a statement about step-size allocation. **[RESCOPED IN PLACE by 156.1 — both numerals are 100-epoch objects.]** | CORRECTIONS 150, rescoped 156.1 |
| Do the corpus's two box-released scalar-containing cells rescue MASTER-TABLE row 24? | box {released `-60:6.0`, canonical} x arm {scalar, `resnet18_blocks`, layerwise}, on runs ALREADY ON DISK | `cN1`/`cN2`, **ZERO GPU**, 0 rows ingested (corpus unchanged at 2,501) | `resnet18_blocks` vs scalar: `D_GAP` +1.8523 pp, `SE_D` 8.4944, **0.22 SE**. layerwise vs scalar: `D_GAP` +1.9497, **0.23 SE**. Modifiers fired: `NULL-AMBIGUOUS-TWO-WALL`, `OCCUPANCY-FLAGGED`, `CROSS-BATCH`, `CROSS-ACCOUNT`. | `UNRESOLVED` (both arms); roll-up `VOID-OCCUPANCY` on both `tc1` cells | **The rescue does not happen, and it fails for a MEASUREMENT reason rather than a statistical one** — one of the two "box-released" cells is not actually box-released, and every box ladder the campaign ran varied {layerwise, nodewise, weightwise} only, never the SCALAR axis that row 24 is about. Precedent for RULE 16: `cN1` was frozen and `cN2` registered beside it rather than edited. | CORRECTIONS 149 |
| Does the SECOND cliff have the same single-tensor structure? | the consecutive single-tensor steps at the second cliff site | `scl1`, 12 runs, 100 ep | The second cliff is carried by ONE 512-parameter normalisation scale, `layer4.0.shortcut.1.weight`, worth **15.8080 pp**. The arithmetic survives five independent lenses. | `CLIFF CLIFF-REPRODUCES` + `SPLIT SINGLE-TENSOR-MAJORITY` + `TENSOR layer4.0.shortcut.1.weight` + `MECHANISM SHARED-MECHANISM` | Two sites, two 512-parameter BatchNorm/normalisation scales, one mechanism. **The scorer's own remark that "the withdrawal was too cautious" DOES NOT survive — CORRECTIONS 147.6 stands as written.** A registered scorer's prose is not privileged over the audit. **[RESCOPED IN PLACE by 156.1: a 100-epoch attribution.]** | CORRECTIONS 152, rescoped 156.1 |
| Is the cliff a property of the partition, or of stopping the clock at 100 epochs? | horizon 100 -> **772 epochs**, with the 100-epoch control IN BATCH (epochs 95-99 of the same six runs) | `cts3`, 6 runs, **772 ep** — 7.72x the horizon of every other `ResNet18_c100` run in the corpus | TWO verdicts on two axes, neither quotable without the other. SIZE: `DE` = **19.3427 pp = 26.00 `SE_ARM_DIFF`**, 1.56x the survive bar and 13.00x the collapse bar. DIRECTION: `dD` = **-5.6747 pp = -5.39 `SE_dD`** against a direction bar of +/-2.1040. Decomposition: `d(k49)` +1.0133, `d(k50)` +6.6880 — **the shrinkage is the LOSING arm improving**, not the winning arm decaying. | `SURVIVED` + `SHRINKS` | **Saying only "SURVIVED" overstates; saying only "SHRINKS" understates.** `SHRINKS` was the REGISTERED PREDICTION (-7.3320 predicted, -5.6747 measured, 1.58 `SE_dD` on the conservative side) and may NEVER be retold as a surprise or as a null. The consequence is the RESCOPING that this section applies to `cpk1`, `cts1`, `cts2` and `scl1`: all are 100-epoch objects. The 100-epoch figure may NOT be called an upper bound on the converged cliff — that assumes monotonicity in budget, and this batch has two points. | CORRECTIONS 156 |
| Does the argmax `k*=49` move at a converged budget on fresh seeds? | five cut positions at **772 epochs**, seeds {3,4,5} | `cpk2`, 18 runs, 772 ep | The argmax **DOES NOT MOVE**. The pre-registered branch is confirmed; its CURVE MODEL is refuted — the forecast predicted **three rank swaps** and **ZERO** occurred. | `k*-UNMOVED` + `CONVERGED` | `k*=49` becomes a CONVERGED-BUDGET, FRESH-SEED object **among the five cuts sampled** — the qualifier is load-bearing, because a curve was not run. A confirmed branch whose own model failed is recorded as both. | CORRECTIONS 158 |
| Is the `k50`/`k52` rate-law break a difference in RATE or in KIND? | re-derivation over `.out` files already on disk | `cS1`, **ZERO GPU**, nothing submitted or ingested | The break is a difference **in kind**; the law is non-exponential. | `DIFFERENT-KIND` + `NON-EXPONENTIAL` | Removes the rate-law reading of the cliff, which would have made it a smooth phenomenon measured coarsely. | CORRECTIONS 159 |
| Is the headline CIFAR-100 granularity contrast confounded by `alpha0`? | `alpha0` x granularity on the headline cell, from the corpus | `cU1`, **ZERO GPU**, corpus unchanged at 2,573 | The confound **does not exist at the size alleged**; ORDER is preserved and the assessment's claim does not reproduce. | `INTERACTION-ALPHA0-x-GRANULARITY` + `ORDER ORDER-PRESERVED` + `ASSESSMENT-DOES-NOT-REPRODUCE` | An external assessment's central objection to the headline cell, tested and not reproduced. The same entry CORRECTED IN PLACE this campaign's own "cut position dominates group count" overclaim — the audit cut both ways in one cycle. | CORRECTIONS 160 |
| What happens across FIVE CONSECUTIVE single-tensor steps, measured in one batch? | consecutive cut grid `k` = 45..50 at 772 epochs, seeds {6,7,8} | `cpk3`, 21 runs, 772 ep | Peak confirmed at 49 at the converged budget. A TENSOR-CLASS SIGN PATTERN appears in the five single-tensor steps. | `PEAK-CONFIRMED-AT-49` + `CONVERGED` | The sign pattern is admitted **ONLY as a sign census**, because **class is PERFECTLY ALIASED with ordinal position mod 3** on this architecture — the batch cannot tell "BatchNorm scale" from "every third tensor". `147.6` stands as written; one of its three grounds is retired. | CORRECTIONS 161 |
| Is the ImageNet-489 transition located by GROUP COUNT? | the `m` axis filled in: `m` = 1, 8, 16, 32, 45, 62 | `in489g2`, 14 runs, ImageNet-489 | Capture +0.0000, +0.0080, +0.0250, +0.4566, +0.8762, +1.0000. Largest adjacent capture rise **+0.4316**, UNDER the registered `CLIFF` bar of 0.60 — R3 does not fire. At `m=8` capture ranges +0.0080 to +0.1497 depending only on WHERE the cuts fall. | `COUNT-DISQUALIFIED-POSITION-DOMINATES` | The disqualification is confirmed from an INDEPENDENT re-parse. **The verdict's second half (`POSITION-DOMINATES`) is a SCORER TOKEN and is BARRED FROM PROSE by CORRECTIONS 160's own arithmetic** — the table records the token and the bar together, which is the only honest way to carry it. Read instead: blockwise (1.4433 pp) and the eight-group balanced partition (1.3450 pp) are both dead at ~1.4 pp while layerwise reaches ~49 pp, and the transition between them is NOT located by group count. | CORRECTIONS 163 |
| Is the operative thing the tensor's IDENTITY, or its position in the name list? | name-list PERMUTATION control (`PATCH_NAMESETS`, proved additive against the unpatched code path) | `cpr1`, 15 runs, 100 ep | The registered `CLASS-OPERATIVE` branch is **ARITHMETICALLY UNREACHABLE** — it could not have fired whatever the data did. The `DC` miss REFUTES SINGLE-TENSOR ADDITIVITY. | `UNRESOLVED-PATTERN-UNREGISTERED` | A pre-registered branch that no outcome could reach is a design defect, recorded rather than repaired after the fact. The `DS` point agreement is **WITHDRAWN as evidence**. Single-tensor additivity — the premise under `161`'s census bookkeeping — falls here. | CORRECTIONS 164 |
| Is CONTIGUITY of the group operative, or is it coarse MASS? | co-grouping, holes, and an **EXACT 3,956,288-parameter** mass match | `cpg1`, 15 runs, 100 ep | CO-GROUPING dies at **-10.22 SE**; INERT-BEYOND-FIRST-HOLE at **+11.89 SE**; coarse mass refuted at the exact match. | `CONTIGUITY-OPERATIVE` + `INTERACTION-ABSENT` + `COARSE-MASS-REFUTED-AT-EXACT-MATCH` + `KC-REPLICATES` | **The token is NOT separable from a no-contiguity "is `conv2` coarse" account on this batch's own arms** — stated with the result, not discovered later. **[SUPERSESSION, in place: CORRECTIONS 166.6 supersedes `164.5`'s *"CONTIGUITY-DEGRADATION is REFUTED WITH THE SIGN REVERSED"*, whose cited evidence points the OPPOSITE way; it conflated hole count with tensor identity. The prior author's self-correction is UPHELD and the superseded wording kept verbatim at `164.5`.]** | CORRECTIONS 166, superseding 164.5 |
| Is `conv2` privileged, as `166.7`'s rival account claimed? | re-derivation over material already on disk | `CONV2` check, **ZERO GPU**, nothing scored or ingested; corpus 2,638 | `conv2` is not privileged. | `CONV2-NOT-PRIVILEGED` | `166.7`'s rival account MUST BE RENAMED; **its substance is untouched**. A naming correction recorded as a result rather than folded silently into prose. | CORRECTIONS 170 |
| **Is there a tuned non-meta baseline on CIFAR-100 at all, and does MetaOptimize clear it?** | plain SGD+momentum+cosine `lr` ladder {0.01, 0.02, 0.05, 0.1, 0.2, 0.3} vs the MetaOptimize arm, plus a 200-epoch SGD arm | `cdn1`, 24 runs, CIFAR-100/`ResNet18_c100`, 100 ep (plus 200 ep), ONE batch so every contrast is WITHIN batch | Ladder plateau5 (re-derived INDEPENDENTLY from the CSV this cycle, matching the scorer to four decimals): 75.3733 / 76.3660 / 77.1567 / **77.5927** / 77.0900 / 76.3700; **INTERIOR** peak at `lr=0.1`. MetaOptimize arm **71.8933** (n=3, sd 0.0731). **`GAP_in` = 77.5927 - 71.8933 = +5.6993 pp = +18.80 SE** (registered SE 0.3032). SGD at 200 ep: 78.8280. | `BRANCH: BELOW-BY-A-LOT` + `VERDICT: USABLE` (`cdn2`, `V2 PASS`) | **This is the campaign's strongest denominator result and it is on CIFAR-100, not CIFAR-10 — more than 3x the 1.46 pp CIFAR-10 deficit the old bottom line led with.** EVERY rung of the ladder beats the meta cell; the smallest margin is +3.480 pp. RULE 16 precedent: the registered `cdn1` scorer **CRASHES UNCONDITIONALLY in its scoring path**, was LEFT UNEDITED, hand-derived at 171, and a NEW scorer `cdn2` registered beside it reproduced the hand derivation LINE FOR LINE. Also: the 100-epoch cell UNDERSTATES the plain-SGD ceiling — quote both horizons. | CORRECTIONS 171 (hand), 175 (`cdn2`); `analysis/cdn1_denominator_score.py` frozen, `analysis/cdn2_denominator_score.py` registered |
| Does the REDUCTION itself — how per-tensor terms are composed into a group's update — carry the effect? | composition of the reduction, with a null and a cliff arm | `crn1`, 18 runs, moved off `cluster_b` and re-submitted on `cluster_a` with all 18 originals cancelled at **0 started, 0 rows** | Composition is operative and the cliff is amplified. The cycle brief's COLLAPSE account is excluded at **-75.53 SE**. | `COMPOSITION-OPERATIVE` + `CLIFF-AMPLIFIED` | **The fourteenth candidate is the first not to die** — and the audit narrows it TWICE: the `COMPOSITION-OPERATIVE` token rests on **ONE** clean pair (the null is excluded on all three), and `CLIFF-AMPLIFIED` is **NOT separable from a one-sided death** — `k50n` lands BELOW every one of the 192 same-cell rows the corpus owns, and `abs(D_TN)` is 99.39 % of the largest value available if it is pinned there. The account move also forced the discovery that `cluster_a`'s shared harness is not the same tree. | CORRECTIONS 169, 172, 173 |
| RULE 11 — does the granularity gap survive giving EACH arm its own tuned meta-stepsize, on the headline CIFAR-100 cell? | `ms` ladder x granularity {scalar, layerwise} at `alpha0` {1e-6, 1e-3}, with a negative control `F` | `cru1`, 60 runs, CIFAR-100/`ResNet18_c100`, 100 ep | **The gap SURVIVES tuning**: `+47.4607 -> +35.2907 pp`, `RATIO 0.7436`. `ACCOUNT T SUPPORTED / ACCOUNT D REFUTED`. The negative control **`F` FAILS at `alpha0=1e-3`** — the most consequential result in the batch. | `1e-3:GAP-SHRINKS` + `1e-6:UNRESOLVED-OPTIMUM-AT-LADDER-EDGE` + `ALIAS-BROKEN` | RULE 11 is CLOSED for scalar-vs-layerwise on this cell at 100 epochs. **`hz9` IS NOT OVERTURNED — IT WAS NOT TESTED**: different dataset, different contrast, and a gap 41x smaller. **[SUPERSEDED IN PLACE by CORRECTIONS 177.7.** The superseded wording is kept verbatim at `174.8`: *"the gap is not a step-size-adaptation phenomenon at all and both T and D are incomplete."* Account `F`'s PREMISE is FALSE on `cru1`'s own probe — `beta` DID move, every coordinate, at the maximum rate, in both directions. `F-FAILS` stands as a MEASUREMENT and licenses NOTHING about mechanism.**]** | CORRECTIONS 168, 174; superseded in part 177, 180 |
| Is the CIFAR-100 `ms=1e-4` stratum what the prose said it was? | census of the stratum against the corpus | `cms1`, **ZERO GPU**, corpus unchanged at 2,740 | The stratum is **35 rows, not 20 and not 20+12**: `cdn1` had already added three at `171`, unrecorded. | `HEADLINE-ARMS-AT-MS1E4:PRESENT` + `MS1E4-ALPHA0-LEVELS=2` + `ALIAS:BROKEN` + `BLK6-AT-MS1E4:ABSENT` + `CUTPOS-AT-MS1E4:ABSENT` | Prose that `cru1` falsified BY CONSTRUCTION is rewritten in place. The census exists because an unrecorded ingest silently changed a stratum a claim rested on — the same failure mode INGEST ORDER guards against. | CORRECTIONS 176 |
| H-DISAGREE (the fifteenth mechanism, and the first DYNAMICS candidate) — do the two groups of an `m=2` partition disagree most where capture peaks? | per-meta-step sign disagreement against `cpk1`'s cut-position curve | **ZERO GPU**, from probe traces on disk; nothing submitted or ingested | Disagreement rate is **MONOTONE-DECREASING in `k`** — 0.3834 at `k=1`, 0.0615 at `k=49`, 0.0414 at `k=53-57` — with **rho -0.0455** against the capture curve. On the one TRUE `m=2` trajectory on disk the two groups of `[49,13]` disagree on **0.45 %** of meta-steps. | `PRIMARY H-DISAGREE-REFUTED-PEAK-ELSEWHERE` + `CONTROL-NUMEL H-DISAGREE-REFUTED-PEAK-ELSEWHERE` + `WEIGHTING-ROBUST` + `SEEDS-DISAGREE` | Refuted on its first run, against a pre-registered curve. **The cycle brief's recipe was WRONG TWICE, from the source, and both corrections were registered BEFORE the first run** — the layerwise `z` is an UNNORMALISED per-tensor SUM, not a mean. | CORRECTIONS 179 |
| H-DOMINATE (E) — is the scalar arm's sign set by an ELEMENT-COUNT majority? | element-level `frac_neg` statistic, as briefed | **ZERO GPU**, registered then found unrunnable on the record | In the scalar arm `frac_neg` is the indicator `1[z_step < 0]` of the **ONE** reduced aggregate (`n_tot = 1` on all 42 scalar probes), not an element-count majority — so the brief's `R_E` is a **TAUTOLOGY** (0.0000 on 42/42 files) and **no element-level statistic exists anywhere on disk**. | `UNRESOLVED-FRAC-NEG-NOT-THE-RIGHT-STATISTIC` + `ELEMENT-STATISTIC-NOT-ON-DISK` + `BARS-NOT-APPLIED` | **A reasoned refusal delivered as the result.** The brief was wrong on three further counts, from the source and the corpus. Descriptively, the standard cell's scalar `beta` is driven by a UNANIMOUS per-step Lion vote. | CORRECTIONS 181, 183 |
| H-DOMINATE (T) — at the TENSOR level, which tensors carry the scalar meta-gradient's sign? | per-tensor probe logging (`PATCH_PROBE_TENSOR`, proved inert bit-for-bit on the live tree) under scalar dynamics | `ctd1`, 6 runs, 100 ep | **THREE 512-parameter BatchNorm scales — `layer4.1.bn2.weight`, `layer4.0.bn2.weight`, `layer4.0.shortcut.1.weight`, 0.014 % of the parameters — carry the sign against the other 59 tensors and vote `beta` DOWN on EVERY pinned record.** `PINNED-R_T=1.0000`, `PINNED-FRAC=0.6298`, `PINNED-AGG-NEG=0.0000`. Every section-[B] number re-derives EXACTLY with an independent parser. | `PRIMARY DOMINATION-BY-OTHER-TENSORS` + `SECONDARY-z:DOMINATION-BY-OTHER-TENSORS` + `PATH-DEPENDENT-MIXED` + `FLOOR-OK` + `FLOOR-NOT-INGESTED` | The same three tensors the cliff work found by a completely different route. **The briefing's reading of the narrowing is WRONG** — the layer-4 convs sit in the early 21-tensor prefix with the OPPOSITE sign, as OPPONENTS, and never vote down in any phase. The "coherent story" BREAKS at `linear.weight`, which pins at the floor under its own beta. **H-DOMINATE AS NAMED is DEAD (184, 186); nothing later revives it.** | CORRECTIONS 182, 184, 186 |
| H-ISOLATE — does lifting the three carrier tensors out of the shared step-size group CAUSE the rescue? | step-size group membership: `ISO` (the 3 carriers isolated), `CTRL` (their layer-2 structural homologues), `ONE` (tensor 50 alone), against scalar and layerwise anchors | `ciso1`, 15 runs, 100 ep, ONE batch; ingest 2,746 -> 2,761 (added 15, changed 0, removed 0), 12.82 GPU-h | `ISO` lifts plateau5 from 23.2807 to 70.2113 = **+46.9307 pp = +62.10 SE**, the WHOLE in-batch scalar-to-layerwise gap. `CTRL` (the layer-2 homologues) gives **-0.0633 pp = -0.08 SE**. `ONE` (tensor 50 alone, 512 params, 0.0046 % of the model) ALSO rescues, **+41.4693 pp**. `ISO` is **statistically INDISTINGUISHABLE from the layerwise ceiling**, not better than it (+1.184 pp = 1.57 SE, inside the 2-SE read bar). | `UNRESOLVED-PARTIAL` + `ISO-RESCUES-BUT-59-NOT-FREE` + `ISO-59-NEITHER` + `ISO-3-PINS` + `CTRL-59-PINS` + `ONE-61-FREE` + `PREMISE-REPLICATES` + `ONE-RESCUES` + `SPECIFIC:CTRL-NULL` + `FLOOR-NOT-INGESTED` | `ONE` rescuing is NOT a paradox: the operative quantity is the **SIGN OF THE REMAINDER SUM**, not domination, and removing tensor 50 alone flips it on **1.0000** of the pinned records. **The design CANNOT separate tensor IDENTITY from network DEPTH** — `ISO` and `CTRL` differ in identity AND depth AND isolated numel at once — which is exactly what `cdep1` (cycle 148, in flight, NOT in this table) was registered to control. `ISO-59-NEITHER` is NOT a collapse: the 59-group peaks at -4.95, is still descending at the horizon, and first crosses -14.0 at epoch 92-98. **The registered "DOMINATION" account is NOT the operative mechanism.** | CORRECTIONS 185, 187 |
| H-ISOLATE's IDENTITY leg — is the rescue caused by THOSE THREE TENSORS, or by any three matched `layer4` 512-parameter BatchNorm parameters at that depth? | WHICH three `layer4` 512-parameter BN parameters are lifted out of the shared step-size group: `ISO` (the `ctd1`/`ciso1` carriers, isolated 50, 53, 59) vs `DEPTH` (a NON-carrier triple matched on group sizes [59,3], isolated numel **1,536 EXACTLY**, width, stage and normalisation-layer type; isolated 47, 48, 56) vs `DEPTH2` (class-pure, sizes [60,2]), against `ONE`, `k01` (`scalar`) and `k62` (`layerwise`) anchors | `cdep1`, 18 runs, 100 ep, CIFAR-100/`ResNet18_c100`, ONE batch, FRESH seeds {24,25,26}; ingest 2,761 → 2,779 (added 18, changed 0, removed 0); **16.05 GPU-h**, re-summed here over its own 18 `wallclock_min` values (minute-rounded, so marginally below `sacct`) | **PRIMARY `DELTA_ID` = plateau5(`ISO`) − plateau5(`DEPTH`) = 70.0440 − 23.5207 = +46.5233 pp = +61.56 SE.** The matched non-carrier triple recovers **0.36 %** of the in-batch gap (`D_DEPTH` **+0.1687 pp = +0.22 SE**); class-pure `DEPTH2` +0.0960 pp; `DELTA_BIAS` = `DEPTH` − `DEPTH2` = +0.0727 pp. The positive control FIRED (`D_ISO` **+46.6920 pp = +61.79 SE**, `D_ISO`/`D_CEIL` = 1.0194) and `ISO` REPLICATED `ciso1` to **0.1673 pp**. `ONE` rescued **+41.4747 pp** at a different set AND a different size. TRAIN beside TEST at every arm: `k62` 98.895, `ISO` 94.164, `ONE` 83.464, `DEPTH` 23.535, `DEPTH2` 23.491, `k01` 23.471. | `IDENTITY-OPERATIVE` + `ISO-59-NEITHER` + `ISO-3-PINS` + `DEPTH-59-PINS` + `DEPTH-3-PINS` + `DEPTH2-60-PINS` + `DEPTH2-2-PINS` + `ONE-61-FREE` + `PREMISE-REPLICATES` + `H-BROKEN-DEAD` + `ONE-RESCUES` + `DEPTH-FLOOR-SATURATED` + `DEPTH2-FLOOR-SATURATED` + `ISO-REPLICATES` + `BIAS-NULL` + `DEPTH2-FLOOR` | **The identity leg of H-ISOLATE CLOSES and DEPTH is excluded** — matching a triple on sizes, isolated numel, width, stage and normalisation type does NOT reproduce the rescue, and H-BROKEN is DEAD IN BATCH because `ONE` rescues at a different set and size. **BOUND BY `188.3`, WHICH WAS REGISTERED BEFORE THE ARM RAN AND IS NOT NEGOTIABLE: this batch CANNOT separate tensor IDENTITY from term MAGNITUDE.** The carriers rank 1/2/3 of 62 by mean absolute `L_i`, every 512-parameter `layer4` non-carrier sits below the median, and the best admissible carrier-free triple is still orders of magnitude short — so IDENTITY-OPERATIVE means 'not ANY three matched `layer4` 512-parameter BN parameters', and the magnitude leg is **architecturally unreachable on this network**. `DEPTH` and `DEPTH2` both sit INSIDE the registered saturation band, so their LEVELS are a BOUND, not a magnitude. ResNet-only. **[AMENDED at cycle 152, CORRECTIONS 229: 'ResNet-only.' is RESCOPED to THIS batch's DEPTH control.** The pattern it measured -- the nominated set rescues and a matched non-carrier set does not -- has since been measured IN BATCH on three more networks: `VGG11_bn_c100` (`cvi1`, 203: bn8.weight alone vs its twin bn7.weight, `DELTA_ID` +31.3033 pp), `ResNet18_gn_c100` with GroupNorm (`cgn2`, 225: ISO {50,53,59} vs the numel-, width-, stage- and owner-matched CTL {47,48,56}, `DELTA_ID` +41.5167 pp) and `PlainNet18_c100` with no residual additions (`cpl2`, 226: layer4.1.bn2.weight alone vs its bn1 twin, `DELTA_HEAD` +52.1367 pp). Still bound: the tensor LIST does not transfer (3 here and on GN, 1 on VGG and on PlainNet); 'DEPTH is excluded' is a ResNet-BatchNorm measurement only; and the magnitude leg is NOT lifted but now unreachable by set-matching on every network examined (x225 here, x19.1 on GN at 225.6, x691 on PlainNet at 226.4). `cvt1` (227) is registered to intervene on the vote itself and has NOT landed. The superseded wording, 'ResNet-only.', is kept verbatim immediately before this bracket.]** | CORRECTIONS 193; scope bound registered at 188.3; cross-batch reading at 195.6 |
| Does the scalar→layerwise `plateau5` gap — the gap the whole granularity programme is about — exist AT ALL off ResNet, on a plain BatchNorm conv stack with no residual addition? | ARCHITECTURE (`VGG11_bn_c100`, the campaign's first non-ResNet network) x granularity {`scalar`, `layerwise`}; nothing else varies | `cvg1`, 6 runs, 100 ep, CIFAR-100, seeds {31,32,33}, ONE batch; ingest 2,779 → 2,785 (added 6, changed 0, removed 0); **4.27 GPU-h**, re-summed here over its own 6 `wallclock_min` values | `k01` (`scalar`) **35.0173**, `kL` (`layerwise`) **66.2860** → **`D`(TEST) = +31.2687 pp = +65.42 SE**, with TRAIN AGREEING at **+64.1587 pp** (35.1233 → 99.2820). All gates PASS: 6/6 `RUN_DONE`, 100 epoch lines, 0 tracebacks, seed ranges 1.1140 and 0.1380, ONE ENV line x6 (`AUGMENT=1`, `BETA_CLIP=-15:-2.3026`, `PROBE=100`, `PROBE_TENSOR: on` x6); `G-STRUCT` 26 tensors / 9,274,532 parameters / **no parameter name contains `shortcut`**. | `GAP-REPLICATES` + `GATES-CLEAN` + `SIGMA-PRIOR-DOMINATES` + `TRAIN-AGREES` + `FAMILY-NOT-VARIABLE` | **The campaign's FIRST non-ResNet result.** The corpus is no longer 100 % ResNet (12 `network` values, 6 non-ResNet rows) and **the ResNet scope objection is answered FOR THE GAP.** It is **NOT** answered for the ISOLATION, and the two claims must never travel together: no isolation arm has run on VGG, and this batch's OWN probe records nominate a carrier set of **cardinality ONE** — `bn8.weight`, 512 parameters, 0.5784 of the pinned mean-absolute-`L_i` mass, flipping the remainder sign alone on **1.0000** of pinned records at all three seeds — against ResNet's THREE. That nomination is a **HYPOTHESIS for a future batch, not a result**, and it REFUTES the pre-registered guess that VGG's carriers would be the four 512-wide BN scales at 14/17/20/23: only `bn8.weight` (23) ranks 1, while the other three rank **19, 20 and 11 of 26**. `VGG11_bn` is **non-RESIDUAL, NOT non-BatchNorm**. **[AMENDED at cycle 152, CORRECTIONS 229: two clauses above are overtaken by registered results, and are kept verbatim.** (a) 'no isolation arm has run on VGG': `cvi1` (203) isolated bn8.weight alone against its exact twin bn7.weight and returned `IDENTITY-OPERATIVE-VGG`, `DELTA_ID` +31.3033 pp = +65.40 SE, and `cvh1` (219) holds it to 328 epochs (`RHO` 1.0001, a level frozen at the pin). (b) the bn8.weight nomination is therefore no longer only a HYPOTHESIS: the intervention it proposed has run and confirmed it. What still binds: the gap and the isolation remain SEPARATE claims for the tensor LIST (1 on VGG, 3 on ResNet) and for identity vs MAGNITUDE (bn8 holds 0.5857 of the mass, bn7 0.0023; not separated on this or any network); 'non-RESIDUAL, NOT non-BatchNorm' stands.]** | CORRECTIONS 194, 195.8; carrier nomination re-derived at 197 |
| H-ISOLATE's HORIZON leg — does the rescue survive to a horizon at which the isolated 59-group has had time to pin, or is +46.93 pp merely a DELAY of the scalar collapse? | HORIZON (100 -> 250 epochs) at a BYTE-IDENTICAL partition, with the 100-epoch control read IN-RUN and PAIRED from epochs 95-99 of the same runs; arms `ISO` (the 3 carriers isolated, [59,3]), `ONE` (tensor 50 alone, [61,1]), against `k01` (`scalar`) and `k62` (`layerwise`) anchors | `ciso2`, 12 runs, **250 ep** (the corpus's FIRST 250-epoch rows), CIFAR-100/`ResNet18_c100`, ONE batch, FRESH seeds {31,32,33}; ingest 2,785 -> 2,797 (added 12, changed 0, removed 0); **21.65 GPU-h**, re-summed here over its own 12 `wallclock_min` values | **The registered scorer SHORT-CIRCUITS at its pinning gate and emits TWO TOKENS ONLY** (106 PASS / 0 FAIL / 0 SKIP, exit 0, sha `ab935f68...`, run unedited, re-run and reproduced at cycle 151). `ISO`'s 59-group fails `PIN_OCC_MIN` = 0.50 exact-equality dwell on 3/3 seeds (tail-occ 0.3974 / 0.3365 / 0.4295), so the primary bar is never reached and the branch map stops there. **UNDERNEATH the gate the primary is measured, three-seed replicated and unambiguous: `RHO` = `D_ISO`@250 / `D_ISO`@100 = 0.997737**; `D_ISO`@250 = **+46.7393 pp = +61.85 SE**, `D_ISO`@100 = +46.8453 pp; per-seed paired `RHO` 0.997855 / 0.998962 / 0.996407. TEST @100 -> @250: `k01` 23.0640 -> 23.1573, `k62` 69.0813 -> 70.3667, `ISO` 69.9093 -> 69.8967, `ONE` 64.7960 -> 64.9440. TRAIN beside TEST: `k01` 23.1413 -> 23.2533, `k62` 98.8413 -> 99.9260, `ISO` 94.3260 -> 94.5627, `ONE` 83.6573 -> 84.5540. Null model checked FIRST: `k01` moves +0.0933 pp = +0.12 SE over 150 further epochs. | `UNRESOLVED-NOT-PINNED` + `ISO-59-NOT-PINNED-AT-250` *(the COMPLETE emitted set — the registered code returns at its line 993, BEFORE the block that computes FROZEN-AT-PIN / CEIL-\* / ORDER-\* / FLOOR-\* / ISO-TRAIN-\* / TRAIN-GAP-\* / REPLICATES-CISO1. Any further stamp quoted for this batch is COUNTERFACTUAL arithmetic and is labelled as such at 196.6; none is carried into this cell.)* | **The rescue is INTACT at 2.5x the horizon: `ISO` loses one tenth of one percent of it over 150 further epochs.** The `RESCUE-COLLAPSES` account registered at `190` — that +46.93 pp is a ~68-epoch DELAY — is **REFUTED ON THE DATA**: `ISO` freezes on schedule at epoch 99.4-103.0 and then holds 69.9 % for ~148 epochs while the floor holds 23.1 %. Freezing at a good point and collapsing to the floor are different events and this batch separates them. **The gate that fired is DEFINITIONAL, not physical, and the defect is now measured**: `PIN_OCC_MIN` scores exact-equality dwell, not step-size magnitude — at a threshold 0.05 nats away `ISO` occupies 0.936 / 0.930 / 0.917 and at 0.50 nats 1.0000 on 3/3, its step size never exceeds 1.12x / 1.19x / 1.13x the floor, while `k62` sits at 0.0000 at EVERY threshold and runs at 114.8x / 85.7x / 131.0x. The registered gate puts `ISO` and `k62` in the same bucket while their step sizes differ by ~100x. `cIS2` is FROZEN; fix it by registration, never by editing it. **`187`'s `UNRESOLVED-PARTIAL` is NOT revised by this batch** — that token belongs to `ciso1`'s own run and BATCH is the unit of replication. **Magnitude confound untouched, ResNet-only, no claim beyond 250 epochs.** | CORRECTIONS 190 (registration), 196 (landing), 199 (reconciliation, gate defect re-derived independently) |
| H-ISOLATE OFF ResNet — does isolating the ONE tensor that `cvg1`'s records nominate (`bn8.weight`) rescue the scalar collapse on VGG, and does its exact twin (`bn7.weight`) NOT? | step-size group membership on `VGG11_bn_c100`: `ISO` (`sets:1-22,24-26/bn8.weight`, `[25,1]`) vs `CTL` (`sets:1-19,21-26/bn7.weight`, `[25,1]`) — same numel (512), width, class, adjacent depth, group sizes byte-identical, spec strings differing in one tensor — against `k01` (`scalar`) and `kL` (`layerwise`) IN-BATCH anchors | `cvi1`, 12 runs, 100 ep, CIFAR-100, ONE batch, FRESH seeds {41,42,43}; ingest 2,797 → 2,809 (added 12, changed 0, removed 0); **6.65 GPU-h** re-summed over its own 12 `wallclock_min` values (`sacct` 6.80) | **PRIMARY `DELTA_ID` = plateau5(`ISO`) − plateau5(`CTL`) = 66.8507 − 35.5473 = +31.3033 pp = +65.40 SE.** `D_ISO` **+31.2833 pp**, `D_CTL` **−0.0200 pp = −0.04 SE**, in-batch gap `D_GAP` **+30.7360 pp**; RECOVERY 1.0178 vs RECOVERY_CTL −0.0007 (descriptive). TRAIN beside TEST: `kL` 99.3367, `ISO` 95.9733, `k01` 35.5640, `CTL` 35.4893. Seed ranges 0.49 / 0.45 / 0.72 / 0.50. Registered scorer run UNEDITED, **35 PASS / 0 FAIL / 0 SKIP, exit 0**; an independent parser reproduces every number digit for digit. Anchors vs `cvg1` (between-batch, descriptive): `k01` +0.5500 pp, `kL` +0.0173 pp. | `IDENTITY-OPERATIVE-VGG` + `GATES-CLEAN` + `MAGNITUDE-NOT-SEPARATED` + `NOT-A-ONE-VARIABLE-ABLATION` + `CARDINALITY-ONE-vs-THREE` + `MISTUNING-NOT-EXCLUDED` + `SIGMA-NARROW-LIVE` + `TRAIN-AGREES` + `ISO-TRACKS-KL` + `CTL-AT-FLOOR` + `GAP-REPLICATES-CVG1` | **The campaign's FIRST isolation result off ResNet.** One 512-parameter BN scale (0.0055 % of the model) carries the whole in-batch gap, and its twin carries none of it. The nomination premise **REPLICATES IN BATCH** on this batch's own scalar records (`bn8` rank 1, share 0.5857, remainder-sign flip 1.0000; `{14,17,20}` flip 0.0000), so the record statistic predicted the intervention on a second architecture family. Mechanism, from the probe records: `ISO`'s isolated `bn8` pins at epoch ~20 while its 25-tensor complement **never pins in 100 epochs** (terminal −13.26 to −13.53); `CTL`'s complement pins at epoch 35.8-36.0, exactly like `k01`. **NOT licensed:** identity vs MAGNITUDE (`bn8` 0.5857 of the mass, `bn7` 0.0023); the tensor LIST (1 here, 3 on ResNet); anything about residual connections; anything about non-BatchNorm nets; any horizon beyond 100 epochs — the complement is still descending at −0.13 to −0.15 per epoch and extrapolates to the clamp at epoch ~109-113, unmeasured. `ISO` − `kL` = +0.5473 pp (+1.14 SE) is NOT a claim that isolation beats layerwise. | CORRECTIONS 198 (registration), 200 (launch), 203 (landing) |
| Does the denominator survive in the parent paper's OWN, UNAUGMENTED setting -- does a TUNED non-meta baseline still beat MetaOptimize when `AUGMENT=0` on every arm? | a 7-rung plain SGD + momentum 0.9 + wd 5e-4 + cosine-to-zero `lr` ladder {0.005, 0.01, 0.02, 0.05, 0.1, 0.2, 0.4} (`cdn1` arm-A recipe) vs MetaOptimize AdamW+Adam `resnet18_blocks` at the parent's own config `m6` (`alpha0` 1e-6, box `-60:6.0`, = `ub9-b6` spec) and at the CIFAR-10 denominator's own meta cell `m6t` (`alpha0` 3e-4, box `-15:-2.3026`, = `i3b-3e4` spec); `M*` = the better of the two | `cau1`, 27 runs, ResNet18 / CIFAR-10, 100 ep, `AUGMENT=0` on every arm, ONE batch, FRESH seeds {50,51,52}; ingest 2,809 -> 2,863 together with `cvk1` (added 54, changed 0, removed 0); **13.47 GPU-h** re-summed over its own 27 `wallclock_min` values (`sacct` 13.76) | **PRIMARY `GAP_END` = plateau5(`S*`) - plateau5(`M*`) = 89.6153 - 85.9980 = +3.6173 pp = +9.92 SE** (SE 0.3648; the frozen prior sigma 0.4468 dominates the in-batch 0.3717). Ladder plateau5 85.385 / 87.085 / 88.409 / 88.799 / 89.489 / **89.615** / 88.667 -- argmax INTERIOR at `lr=0.2`; 6 of 7 rungs beat `M*` by >= 2 SE. `m6` (the parent's config) **74.4993**, `GAP_END` vs `m6` +15.1160 pp = +41.44 SE. Seed sets DISJOINT (min `S*` 89.3800 > max `M*` 86.4140). Secondary `GAP_AUC` +0.8448 pp = +2.68 SE_AUC (best-AUC rung `lr=0.01` 85.6494 vs `m6t` 84.8046); read at `S*` the AUC gap is -4.1099. TRAIN beside TEST: TRAIN plateau5 99.995-100.000 on every arm (at ceiling). Registered scorer run UNEDITED on `cluster_b`, exit 0; `FINAL:` identical to the one Codex saved (one descriptive TRAIN-AUC digit differs by interpreter float `sum()`, 209.4); an independent parser, calibrated on `ub9-b6`, agrees digit for digit. | `DEFICIT-HOLDS` + `LADDER:INTERIOR` + `M*=m6t` + `SIGMA-PRIOR-DOMINATES` + `CURVE-DEFICIT` + `TRAIN-AT-CEILING` + `ONE-BASELINE-FAMILY` + `COUNTERWEIGHT-NOT-TESTED` + `ALPHA0-TWO-CELLS` + `CIFAR10-ONLY` | **The augmentation-artefact account of the denominator is refuted at this cell**: in the parent's own unaugmented setting a tuned scheduled baseline still beats the method, and the parent's own config is 15 pp behind. **NOT licensed:** CIFAR-100 (`cdn1`'s `GAP_in` +5.699 stays `AUGMENT=1`); any baseline family but SGDm+cosine; meta-side tuning (`alpha0` two cells, meta-stepsize 1e-3 only) -- the FAIR row above STAYS OPEN on both; the fixed-step counterweight (+1.551) not re-measured unaugmented; anything about granularity. The curve reading is weaker than the endpoint (only 2 of 7 rungs beat `m6t` on AUC). A registered prediction MISSED in the method's favour: 202.6 put `m6t` in [72, 80]; it measured 85.998. **[AMENDED at cycle 152, CORRECTIONS 229: two NOT-licensed clauses above are answered by a LATER batch, not by this one, and are kept verbatim.** CIFAR-100 at `AUGMENT=0`: `cuc1` (220, row appended at the end of this file) measures `GAP_END` +12.1780 pp = +26.05 SE against a 5-cell meta-stepsize x alpha0 grid; and the FAIR row above no longer stays open -- it moved to CONFIRMED, RESCOPED at 229. What THIS row licenses is unchanged: `cau1`'s method side is still two alpha0 cells at one meta-stepsize, on CIFAR-10. Still binding on both batches: one baseline family, one granularity per batch, alpha0 1e-6 measured on CIFAR-10 only, no method-side counterweight.]** | CORRECTIONS 202 (registration), 206 (launch), 209 (landing) |
| Does the ResNet cut-position peak transfer to VGG as a CARRIER rule -- is `k*` the last cut before the carrier `bn8.weight` (k = 22), against the parameter-balance rival (16) and depth-fraction (20/21)? | contiguous `m=2` cut position k in {13, 16, 19, 20, 21, 22, 23} (`[k, 26-k]`) on `VGG11_bn_c100`, with `k01` (`scalar`) and `kL` (`layerwise`) IN-BATCH anchors; SGDm+Lion, ms 1e-3, `alpha0` 1e-6, `AUGMENT=1`, bars FROZEN (O2, 208) | `cvk1`, 27 runs, CIFAR-100, 100 ep, ONE batch, FRESH seeds {55,56,57}; launched on `cluster_b` by Codex (another AI the user ran) with the registered successor launcher, unedited (210.1); ingest 2,809 -> 2,863 together with `cau1` (added 54, changed 0, removed 0); **15.07 GPU-h** re-summed over its own 27 `wallclock_min` values (`sacct` 15.31) | **PRIMARY: argmax `k_hat` = 19 (68.6933); `PEAK_SET` = {16, 19, 22}** at `PEAK_BAR` 0.9573 pp (sigma 0.586232 frozen, SE 0.4787). TEST (TRAIN): k13 61.9987 (77.6553), k16 68.2760 (90.3820), k19 68.6933 (82.7313), k20 67.3247 (80.9980), k21 67.7180 (81.5707), k22 68.0533 (98.7887), k23 34.9120 (34.9753); anchors `k01` 35.4480, `kL` 66.2760, `D_GAP` +30.8280. `D22-16` -0.2227 pp = -0.47 SE (tied). CLIFF 22->23 (moves `bn8.weight`) +33.1413 pp = +69.24 SE, but k23 is FLOORED (-0.5360 vs `k01`), so it is a BOUND. k21 misses `PEAK_SET` by 0.0180 pp. Registered scorer run UNEDITED on `cluster_b`, exit 0, whole output byte-identical to Codex's; two independent parsers agree digit for digit. | `UNRESOLVED-TIE-WITH-22` + `CARRIER-CUT-UNRESOLVED` + `TIE-16-19-22` + `GATES-CLEAN` + `ALIAS-CLASS-IS-INDEX-MOD-3` + `MAGNITUDE-NOT-SEPARATED` + `HORIZON-100-ONLY` + `SHARED-MS-1E-3` + `NOT-A-ONE-VARIABLE-ABLATION` + `SIGMA-NARROW-PINNED` + `TRAIN-ARGMAX-DISAGREES(22)` + `D22-16-TIED` + `CLIFF-IS-A-BOUND` + `GAP-REPLICATES-CVG1` | **The carrier-cut and balance predictions are NOT separated** (22 and 16 tie, -0.47 SE), so 204's link between ResNet's `k*=49` and VGG's carrier stays a PREDICTION, not evidence. The peak set {16, 19, 22} is exactly the conv->BN-scale junctions among the cuts at k >= 16 (cuts after `conv6`/`conv7`/`conv8.weight`) -- THE CLASS ALIAS: junction class is index mod 3, so the batch cannot tell a boundary before a BN scale from every third index; the grid's fourth junction, k13, is NOT in the peak set (-6.6947 pp). What the batch shows: a PLATEAU over junction cuts 16-22 and a floor-level collapse once `bn8.weight` joins the large group. **NOT licensed:** identity vs class or vs magnitude; the isolation line; any horizon but 100 ep; any ms but 1e-3; residual connections; group count; position beats balance. | CORRECTIONS 201 (registration), 207-208 (successor, O2), 210 (launch record and landing) |

## 10. Cycles 75–110 — the count-matched partition audit: does the partition matter once the group COUNT is held fixed?

**How to read this section (added at cycle 151, CORRECTIONS 211, Track T; ZERO GPU).** This audit is the campaign's one clearly successful line and the basis of the paper, and until this pass it had **no row in this file**. It is **appended at the end rather than inserted in cycle order** because the research-notebook site derives experiment ids from this file's line numbers; appending leaves every existing row on its line. **Batches:** `tw0`, `at1`, `ck1`, `cx2`, `mm1`, `pp1`, `bn1`, `ar1`, `cc1`, `fa1`, `g3m`, `gc1`, `gn1` (cycles 75–88, CORRECTIONS 104–119) **and** `rl3`, `aw1`, `gm2`, `hz3`, `nl1`, `r50`, `ml2`, `sm3`, `bm2`, `sm4`, `rp1`, `hz3q` (cycles 89–110, CORRECTIONS 120–134) — thirteen of the headline's twenty cells come from the second group, so the audit cannot be recorded from 104–119 alone — plus three pre-data withdrawals (`sf1`, `sl1`, `gf1`/`gf2`) and the zero-GPU analyses that decided a question. Rows run in cycle order after the first, which is the headline. **Verdict column:** the registered scorer's own token, verbatim, in backticks, as in section 9; where a batch has no registered scorer (five do: `ml2`, `nl1`, `r50`, `gm2`, `sm3`) or the row is a pre-data withdrawal or a zero-GPU analysis, the record's own verdict in quotation marks with its entry number. Neither form joins the six prose families in the header. **Numbers:** every plateau5 number in the result column was re-derived for this pass from the raw `.out` epoch series by `analysis/cT211_partition_audit_rederive.py` (new; imports nothing from `analysis/`, no `csv` module; every one of the 368 runs it reads matches `results/all_runs.csv` to 0.000000, and `--check` asserts the numerals printed here); anything marked *(quoted)* needs probe records or a simulation this pass could not re-run, and carries its entry number. **Scorer re-runs:** the local mirrors hold no `probe.jsonl` for these batches, so, run unedited here, every probe-gated scorer VOIDs, stays PENDING or stops on a missing file; only `aw1`'s (CSV-based) and `hz3q`'s (`.out`-based) reproduce their registered verdicts locally. Every other token below is the one the scorer printed on the cluster, as the cited entry records it, except three tokens in two rows (`g3m` H1 and H3, `gc1`), each marked in its cell as the scorer's own branch function applied to the re-derived numbers, because the entry that scored the batch never quoted them.

| tested | varied | scale | result | verdict | so what | ref |
|---|---|---|---|---|---|---|
| **POST-HOC SYNTHESIS — the audit's headline.** At a fixed group count, does the uniform partition beat the architecture-aligned one in every count-matched cell, and by how much? | uniform `chunk<K>` minus architecture-aligned `nodewise`, count-matched (R18 14,421 v 14,420; R34 25,562 v 25,556; C100 14,595 v 14,600), ONE batch per cell; across cells: network (R18 / R34 / R50), dataset (C10 / C100), base (SGDm / SGD / RMSProp / AdamW), meta (Lion; RMSProp in `sm4` only), ms (1e-4 / 3e-4), budget (100 / 300 ep), three boxes | the 20 cells of the paper's Table 2 (`analysis/c98_figures.py` `CELLS`, the `gn1`-GroupNorm cell excluded): 18 batches, 154 runs, n 3 v 3 to 9 v 9 | **D > 0 in 20 of 20 cells; t ≥ 3.0 in 18** (the two below are `ml2` t 2.34 and `sm3` t 2.22); D runs from +0.141 (`sm3`) to +1.640 (`gc1`). **Eight SGDm cells** (`cc1`, `mm1`, `pp1`, `gn1`-BN, `rl3` ×2, `fa1`, `hz3`): fixed-effect pool **+0.5556 ± 0.0448**, Q 4.21 on 7 df, τ 0. **Fourteen byte-identical-contrast cells:** +0.5297 ± 0.0294, Q 102.47 on 13 df, τ 0.2948. TRAIN agrees in sign in 20 of 20. The simulation-calibrated 95 % half-width **±0.121** and the SGDm null median **9.4** are NOT re-derived by the independent parser; `analysis/c98_reproduce.py`, run unedited, reproduces both (PASS), although that audit exits 1 on ten stale corpus-count checks. | "wins all twenty count-matched cells" *(MAIN-IDEA-REPORT §5 and the paper's abstract; a pool no scorer registered a bar for, paper §3.4)* | The partition matters at fixed count, by about 0.6 pp — small next to the method's own deficit against tuned SGD: −1.807 pp as published and **−1.796** as `c98_reproduce.py` now derives it on CIFAR-10, **−5.699 pp on CIFAR-100** (`cdn1`), outside the "1.8–4.2 pp" MAIN-IDEA-REPORT quotes. **Six of the twenty cells come from the five batches with no scorer registered before their runs** (`ml2`, `nl1` ×2, `r50`, `gm2`, `sm3`), and two of those (`ml2`, `sm3`) were VOID on their registered meta axis and enter as Lion replicates. The effect is heterogeneous across base optimisers (the moderator row). | MAIN-IDEA-REPORT §5; paper `tab:D`, §4.3–§4.4; CORRECTIONS 124.2, 126.1–126.2, 128.1; `analysis/cT211_partition_audit_rederive.py` |
| Is the weightwise accuracy deficit at its own tuned meta-stepsize a box (clamp) artefact — and where does the fully tuned granularity ladder peak? | granularity {layerwise, nodewise, weightwise} with the probe on, at `wm9`'s argmax ms 1e-4, box -15:-2.3026 | `tw0`, 9 runs, R18/C10, SGDm+Lion, 100 ep, seeds 0–2 | plateau5 layerwise 92.939, nodewise 91.961, weightwise 91.234 (n=3 each). T1: weightwise box-free on 3/3 seeds, and every arm 0.0000 on all four occupancy columns *(quoted, 104.2)*. With the 13-axis census the tuned ladder reads frozen 90.384 → scalar 92.262 → blk6 92.652 → **layerwise 92.887** → nodewise 92.450 → weightwise 91.146, an interior optimum at layerwise *(quoted, 104.4)*. T4: N_eff/m 0.7458 / 0.0549 / 0.0157, argmin weightwise at gap/SE 22.75 *(quoted)*. | T1 `CONFIRMS` + T4 `DECIDED` | The weightwise deficit is not a clamp artefact and ceiling-runaway is ruled out as its mechanism, which leaves it unexplained. The gate was easy — every arm passed at the maximum margin, which the record says against itself (104.2). Its nodewise arm is the cross-batch anchor `ck1` and `mm1` were first read against. | CORRECTIONS 104.1–104.5; FINDINGS 74.1–74.4; `analysis/c75_tw0_score.py` |
| Out-of-sample prediction A2: is sign agreement `a_raw` monotone DECREASING in group count m across the tuned ladder? | the two rungs `tw0` left unprobed or off their argmax: blk6 (`resnet18_blocks`) at ms 1e-4 and nodewise at ms 3e-4, probe on | `at1`, 6 runs, R18/C10, 100 ep, seeds 0–2 (seeds that already existed: report distinct seeds, not independent draws, 104.7) | A1 accuracy reproduction: blk6 92.449 against its registered 92.652 (−0.203), nodewise@3e-4 92.564 against 92.450 (+0.114), both inside ±0.50. A2: blk6 `a_raw` 0.70432 > layerwise 0.65693; nodewise@3e-4 0.53368, strictly inside (0.50117, 0.65693) *(quoted, 105.5)*. A0.3: nodewise@3e-4 BOUND at the lower guard on 3/3 seeds, `rec_lo` ≈ 0.455 *(quoted)*. | A1 `REPRODUCES` + A2 `CONFIRMS` | A field result — and the field was later shown not to predict accuracy (`cc1` C1 below). A2's nodewise point sits in a box-BOUND cell; a free cell at the same m agrees on `a_raw` to 0.005, which is carried with the result rather than used to excuse it. | CORRECTIONS 104.7, 105.5; FINDINGS 74.7, 75.5; `analysis/c75_at1_score.py` |
| At (nearly) fixed group count, does the PARTITION matter — is granularity one variable (m) or two (count, partition)? | contiguous flat chunks `chunk1024` (m 10,944) vs output-channel `nodewise` (m 14,420) at the same ms; anchor `chunk1` ≡ weightwise | `ck1`, 15 runs (chunk K ∈ {1, 2, 16, 128, 1024} × 3 seeds), R18/C10, SGDm+Lion, ms 1e-4, 100 ep, read against `tw0` (CROSS-batch) | chunk1024 92.526 vs `tw0` nodewise 91.961: **+0.565 pp, se 0.089, t 6.38** (cross-batch, and 24 % apart in m). Post-hoc decomposition: count +0.049, partition +0.517 *(quoted, 105.3)*. K1: chunk1 − `tw0` weightwise = −0.255, se 0.146, t −1.74, inside ±0.50. | K1 `REPRODUCES` + K2 `REFUTES` | K2 refuted its own registered prediction that the partition does not matter at fixed m, which made the axis (count, partition) and opened this audit. It had been registered as the more interesting outcome, with a symmetric ±0.50 bar. Not yet an alignment claim: at matched m the two partitions still differ in size distribution (105.3). `mm1` re-measured it at matched count. | CORRECTIONS 104.8–104.9, 105.2–105.3, 105.6; FINDINGS 74.9, 75.0–75.2; `analysis/c75_ck1_score.py`, `analysis/c76_partition_vs_count.py` |
| Is there structure in the three-decade hole between nodewise (m 14,420) and weightwise (m 11,173,962)? | chunk K ∈ {1, 2, 16, 128, 1024} (m 11,173,962 → 10,944), one batch | `ck1`, 15 runs (as above) | plateau5 90.979 → 91.095 → 91.411 → 92.159 → 92.526: every adjacent step positive, about 0.51 pp per decade, no knee *(slope quoted, 105.4)*. The chunk1 → chunk2 step — 100 % → 0 % singleton groups — is +0.115, se 0.133, t 0.87. | K3 `CONFIRMS` | The fine end slopes; it does not turn anywhere — a negative about shape. Two later uses: its count slope was DELETED as an import (117.10: four of the seven `ck1`+`cx2` rungs have K ≤ 512 and split the 1-D tensors), and its chunk1 → chunk2 step is the datum that refuted the size-1 carrier (119.0 K4, row below). | CORRECTIONS 105.4, 117.10, 119.0; FINDINGS 75.3 |
| Does the chunk curve keep rising to the coarse end, and does any chunk size beat the tuned ladder's best rung? | chunk K ∈ {8192, 65536} (m 1,407 and 220) | `cx2`, 6 runs, R18/C10, ms 1e-4, 100 ep | chunk8192 92.763, chunk65536 92.858; **within the batch 65536 − 8192 = +0.095, se 0.107, t 0.88**; the rise from `ck1`'s chunk1024 (92.526) rides on the cross-batch link. Both rungs sit under the 93.037 ceiling *(quoted, 106.4)*. | X1 `CONFIRMS` + X2 `CONFIRMS` *(X2's confirming branch was registered as uninteresting in advance; the scorer returns interesting=False on it)* | No architecture-free chunk size beats the tuned ladder's best rung. X1 confirms on paper, but its within-batch content is a tie — do not lean on it. | CORRECTIONS 105.6, 106.4; FINDINGS 76.3–76.5; `analysis/c76_cx2_score.py` |
| Does the partition effect survive a MEASURED matched-count contrast rather than an interpolated, cross-batch one? | `chunk777` (m 14,421) vs `nodewise` (m 14,420), one batch, one ms | `mm1`, 6 runs, R18/C10, SGDm+Lion, ms 1e-4, box -15:-2.3026, 100 ep, seeds 0–2 | 92.529 vs 92.044: **D = +0.485, se 0.161, t 3.01**; TRAIN D +0.437. M2: `mm1` nodewise − `tw0` nodewise = +0.083 — with `ck1`'s −0.255, a cross-batch offset with no stable sign. | M1 `CONFIRMS` + M2 `REPRODUCES` | The first measured count-matched D. It cleared the pre-registered UNDECIDED band (+0.15, +0.30] by ONE standard error at n=3, which 106.2 says; M2 was declared unable to void M1 before the data existed. Nineteen more cells have since matched it (first row). | CORRECTIONS 105.6, 106.2–106.3; FINDINGS 76.0–76.2; `analysis/c76_mm1_score.py` |
| Which property of the partition carries D — architecture ALIGNMENT, or the group-SIZE distribution? | `nodewise` vs `permnode<S>` (nodewise's exact count and per-tensor size multiset, membership randomised WITHIN each tensor) vs `chunk777`, one batch | `pp1`, 9 runs, R18/C10, ms 1e-4, 100 ep, seeds 0–2 (the permutation seed IS the run seed) | **A = permnode − nodewise = −0.009, se 0.157, t −0.06** (alignment); **B = chunk777 − permnode = +0.590, se 0.107, t 5.53** (size distribution, about all of D); D = +0.581, se 0.141, t 4.11. 66.64 % of nodewise's 14,420 groups are size 1 — the 41 BatchNorm scale/shift and `linear.bias` tensors, 9,610 scalars on 0.09 % of the weights *(quoted, 107.4)*. | P2 `NULL` + P1 `REPLICATES` | The rival the literature would assume, alignment, is a NULL — a **bounded** null, not a refutation: its 95 % CI [−0.317, +0.298] spans −55 % to +51 % of D and the NULL band (±0.15) was narrower than the realised se, so "ALIGNMENT IS REFUTED" may not be written (125.1, 126.1). The permutation is within-tensor only, so it is silent on layer boundaries. `rp1` replicated it at twice the power. | CORRECTIONS 106.6, 107.2–107.4, 125.1, 126.1; FINDINGS 77.1–77.6; `analysis/c77_pp1_score.py` |
| The degenerate-tail test: with only the 9,610 size-1 groups merged away, does the matched-count gap collapse? | `nodewise1d` (nodewise on every tensor with ndim ≥ 2, ONE group per 1-D tensor) vs `chunk2325`, m 4,851 EXACTLY; `nodewise` anchor | `bn1`, 9 runs, R18/C10, ms 1e-4, 100 ep, seeds 0–2 | **G = chunk2325 − nodewise1d = +0.295, se 0.048, t 6.15** — inside the band (+0.15, +0.30] registered UNDECIDED, 0.005 pp short of SURVIVES. T = nodewise1d − nodewise = +0.427, t 10.43, count-confounded (14,420 → 4,851). T3: nodewise 92.184 reproduces `mm1` and `pp1`. | T1 `UNDECIDED` + T2 `CONFIRMS` + T3 `REPRODUCES` + T5 `NOT APPLICABLE` | The bands were not moved and no seeds were added after seeing which side it fell on (108.6a). T2 was registered as count-confounded before submission and is not evidence about the tail. Later SGDm cells put G near zero (`cc1` +0.011, `fa1` −0.001, `rl3`@1e-4 −0.011), but `bn1`'s T1 stays UNDECIDED. | CORRECTIONS 107.6, 108.2–108.4, 108.6; FINDINGS 78.1–78.5; `analysis/c78_bn1_score.py` |
| Is D an argmax artefact (every D so far read at ms 1e-4, 0.42 pp below nodewise's better stepsize)? And with the tail removed, does the gap collapse? | {nodewise, chunk777, nodewise1d, chunk2325} at ms 3e-4 | `ar1`, 12 runs, R18/C10, box -15:-2.3026, 100 ep, seeds 0–2 | A1 **D′ = 93.045 − 92.347 = +0.697, se 0.118, t 5.90**; A2 **G′ = −0.139, se 0.072, t −1.94**; A3: 0 of 12 arms box-free, `rec_lo` 0.452–0.460 *(quoted)*. | A1 `SURVIVES` + A2 `COLLAPSES` + A3 `VOID` | At the time, "the gap lives entirely in the size-1 tail". **Later removed from the primaries:** the corrected occupancy instrument (117.1) finds `ar1` floor-bound asymmetrically — nodewise 0.110 vs chunk777 0.0014 of coordinates, 75× — and the floor holds nodewise's step sizes UP, which inflates D; `ar1` is box-VOID and is not among the twenty cells. `fa1` re-read the same cell with a floor that cannot bind. | CORRECTIONS 108.6, 109, 109.2–109.5, 117.1; FINDINGS 79.1–79.4; `analysis/c79_ar1_score.py` |
| Direction C — does the meta-gradient sign-agreement field (N_eff/m) predict accuracy at matched count? The pre-registered test that decides it. | {nodewise, chunk777, nodewise1d, chunk2325} at the only stepsize where the field is readable (ms 1e-4), fresh seeds | `cc1`, 12 runs, R18/C10, 100 ep, seeds 3–5; 12/12 box-free, `rec_lo` = `rec_hi` = 0.0000 *(quoted)* | C2 contrast: accuracy +0.727 (t +3.63) against d(N_eff/m) −0.0237 (t −11.14), ANTI-CONCORDANT. C3 contrast: accuracy +0.011 (t +0.08) against −0.0529 (t −23.26), DISSOCIATION *(field numbers quoted, 110.2; accuracy re-derived)*. | C1 `MIXED` → direction C `IS DROPPED` | The field resolves at t −11 and −23 and is decisively wrong about accuracy on one contrast and silent on the other: the correlation statistic the Adam-mini / Adalayer / SGG line would reach for does not track the outcome. This is mechanism M2, refuted on a gate registered before its data. A second VOID had been costed as DROP in advance, so this cannot be reread as "try again". | CORRECTIONS 108.5–108.6, 109.4, 109.6, 110.2, 110.5; FINDINGS 78.6, 80.2; `analysis/c81_cc1_score.py` |
| At fresh seeds, does D replicate — and does G (tail removed, exact count) stay null inside the same batch? | as the row above | `cc1`, 12 runs | **D = +0.727, se 0.200, t 3.63; G = +0.011, se 0.147, t 0.08; D − G = +0.715**; TRAIN D +0.501. | C2 `REPLICATES` + C3 `COLLAPSES` | D's fourth reading (+0.485 / +0.581 / +0.697 / +0.727) and, inside ONE batch, tail present +0.727 against tail removed +0.011 — the ResNet-18 mechanism anchor `g3m` was built to replicate. C3 was declared unable to amend `bn1`'s T1. | CORRECTIONS 110.3; FINDINGS 80.3–80.4 |
| Does the partition gap SCALE with the singleton fraction f — the registered law gap(f) = −0.139 + 1.2544·f and its `sf1` ladder — and does it differ across architectures? | (registered, never run) an f-ladder of `nodewisep<k>` rungs, and a cross-architecture f comparison | zero runs; `docs/REGISTER-c82-singleton-law.md`, 36 jobs planned | Withdrawn on three defects before any run: frozen in a cell (ms 3e-4, box -15) that `ar1` measured bound on 12/12 (`rec_lo` 0.4521–0.4597), a guaranteed VOID; its R5 fires REFUTED exactly when the law is true (deep-first k=21 gives f 0.614, not 0.311); and f is collinear with m and with the comparator. Across architectures f is pinned near 2/3 by the conv→norm idiom (66.290 %–66.662 % over seven networks), about 11,000 seeds per arm to resolve *(quoted, 111.1–111.2)*. | "WITHDRAWN" *(CORRECTIONS 111.2)* + cross-architecture test "UNTESTABLE — DO NOT RUN" *(111.1)* | `ar1`'s A1/A2 survive as measurements; only their use as a two-point anchor for a law is withdrawn. No form of "the gap scales with the singleton fraction" may be written — the same collinearity then killed `sl1`, and 124.2 found that regressing D on f is regressing D on the dataset dummy. | CORRECTIONS 111.1–111.2, 124.2; `docs/REGISTER-c82-singleton-law.md`, `docs/REGISTER-c82b-singleton-law-WITHDRAWAL.md` |
| The ms/box deconfound: re-read `ar1`'s cell with a floor that cannot bind — does the box change the optimiser, and do D and G survive in a free box? | box -25:-2.3026 against `ar1`'s -15:-2.3026 (only the floor moves), {nodewise, chunk777, nodewise1d, chunk2325}, ms 3e-4 | `fa1`, 24 runs in two waves of 12, R18/C10, 100 ep, seeds 0–5 | F3, `fa1` − `ar1` per arm: nodewise −0.020 (t −0.15), chunk777 −0.088 (t −0.83), **nodewise1d −0.177 (t −2.22)**, chunk2325 −0.039 (t −0.57). F2: **D_w = +0.629, se 0.123, t 5.11**; **G_w = −0.001, se 0.076, t −0.01**. Floor free on 24/24; the ceiling binds on the nodewise arm on 5 of 6 seeds under the corrected instrument *(both quoted, 113, 117.1)*. | F3 `THE BOX CHANGED THE OPTIMISER, NOT MERELY THE INSTRUMENT.` + F2 `SURVIVES` + G_w `CONSISTENT-WITH-BOX-FREE (UNDERPOWERED)` + F1 `DISSOCIATION` | The registered expectation — F3 unresolved, below 0.10 pp — was defied on one arm, so D_w may not be pooled with `ar1`'s and every sentence about D carries its box. The tail story survives without gaining a replication. It is the corpus's first box-free ms 3e-4 cell and one of the eight SGDm cells, and it produced STANDING RULE 14 (seed labels do not pair across batches). | CORRECTIONS 111.3–111.6, 112, 113, 117.1; `analysis/c82_fa1_score.py` |
| Can a FIXED-m ladder measure a singleton dose-response — a plateau5 slope in singleton fraction at constant group count? | (built, validated, never run) seven `chunkvec` rungs holding m ≈ 14,420 while f sweeps 0 → 0.666 | zero runs; 48 jobs planned; `PATCH_CHUNKVEC` suite 68/0, scorer 105/105 | Killed before a GPU-hour: over the rungs corr(S, G2) = −0.9999975 (fixing m relocates the collinearity into the treatment arm); the registered OLS gate scores a pure step and a zigzag CONFIRMED and a genuine saturating dose-response not; both endpoints were already in the CSV *(quoted, 114.1)*. | "WITHDRAWN AND DELETED" *(CORRECTIONS 114.1)* | The first completed batch the campaign killed before it ran. It is the origin of the standing rules that a primary must be shown to DISCRIMINATE before the runs exist, and that arms measuring a quantity the registration forbids reporting must be cut (114.1). | CORRECTIONS 114.1; `docs/REGISTER-ideas-ABC.md` |
| Does D — and the mechanism "D large AND G null" — generalise to a second architecture? | ResNet-34: `nodewise` (m 25,556) vs `chunk835` (25,562); `nodewise1d` (8,595) vs `chunk2500` (8,587); ONE batch and one ms, so D − G is within-batch | `g3m`, 36 runs (4 arms × 9 seeds), CIFAR-10, SGDm+Lion, ms 1e-4, box -15:-2.3026, 100 ep | **D = +0.666, se 0.094, t 7.08** (t 0.64 against the R18 pool +0.5805 ± 0.0939); **G = +0.171, se 0.073; D − G = +0.495** (t 4.15, quoted); TOST on G at ±0.30, p 0.0488 *(quoted, 116)*. T = nodewise1d − nodewise = +0.758, count-confounded (0.4732 decades). | H1 `GENERALISES` + H2 `NULL REPLICATES` + H3 `MECHANISM CONFIRMED ON ResNet34` *(H2 as 116 records it; H1 and H3 are the scorer's `band_h1()` and H3 branch applied to the re-derived D and D − G — 116 quotes neither token)* | The cleanest generalisation in the audit: nine seeds per arm and both legs in one batch, which the closeout redesign bought for the same 36 jobs (CLOSEOUT §4). Equivalence is to ±0.30 — the ±0.15 band was unreachable at this n, and said so in advance. RULE 11 stays OPEN on ResNet-34 (ms pinned to the R18 anchor's). | CLOSEOUT §3–§4; CORRECTIONS 115, 116, 117.10; `analysis/c83_gen_score.py` |
| Does D survive a second dataset — a CIFAR-100 screen with a pre-registered stopping rule? | ResNet18_c100: `nodewise` (m 14,600) vs `chunk771` (m 14,595), one batch | `gc1`, 8 runs (4 seeds), CIFAR-100, SGDm+Lion, ms 1e-4, box -15:-2.3026, 100 ep | chunk771 71.952 (sd 0.183) vs nodewise 70.311 (sd 0.454): **D = +1.640, se 0.245, t 6.71**; TRAIN D +3.859. | `CLOSE-CONFIRMED` *(the scorer's own `band()` applied to the re-derived D, se and arm sds; 116 does not quote the printed token, and the scorer cannot run here without probe records)* | "C100 gives the LARGEST D in the corpus" was WITHDRAWN at 117.11: a percentage point is not commensurable across a 3.7× error budget, and on D/headroom (0.055) CIFAR-100 is among the smallest cells (fourth of twenty, 128.2). No G on CIFAR-100 until `gm2`. | CLOSEOUT §3; CORRECTIONS 116, 117.11, 128.2; `analysis/c83_gc1_score.py` |
| Was box occupancy — the gate every D reading rests on — read from the right array? | occupancy read from the 62-entry per-tensor `beta` summary vs the per-coordinate rails `n_at_lo` / `n_at_hi` | zero GPU; every D-carrying batch's probe records, each run's own plateau5 window | The three cycle-87 scorers read the 62-entry summary: blind where clipping was total (`fa1` nodewise `rec_hi` 1.0000 on s1/s4/s5 read as 0.000000) and sign-inverted where it fired (`ar1`: chunk777 7× more bound on the summary, nodewise 75× more bound on the rails). Corrected: `mm1`, `pp1`, `cc1`, `bn1`, `g3m`, `gc1` clean on both rails; `fa1` nodewise ceiling-bound on 5 of 6 seeds; `ar1` VOID *(all quoted, 117.1)*. | "THE BOX INSTRUMENT WAS READING THE WRONG ARRAY" *(CORRECTIONS 117 heading; the fix is enforced by `bin/c87_an0_analysis.sh` GUARD 3c)* | It removes `ar1` from the primaries and makes `fa1` travel with a ceiling disclosure; every later scorer gates on the coordinate fraction and prints the record fraction beside it. | CORRECTIONS 117.1, 109.6(g) |
| Is there ONE count slope that can be imported to correct count-mismatched contrasts — e.g. the practitioner's T = nodewise1d − nodewise? | the in-batch count axis U = chunk2325 − chunk777 (0.4732 decades) against four imported slopes (−0.4070, −0.4906, −0.5100, −0.5361 pp per decade) | zero GPU for the audit; in-batch U from `rl3` and `hz3` | The four imported values span 0.129 pp per decade against a 0.10 bar, and four of the seven `ck1`+`cx2` rungs split the 1-D tensors. In-batch U: `rl3` +0.064 (ms 1e-4) and +0.017 (3e-4); `hz3` +0.399 / −0.052 / −0.108 at 100 / 200 / 300 epochs — the sign changes with budget. T's count share runs 2.9 %–34.7 % across its two exact decompositions *(all quoted, 117.10, 120, 122)*. | "THE IMPORTED SLOPE IS DELETED" *(CORRECTIONS 117.10)* | A count correction must be measured in-batch at the budget being corrected, and the paper quotes the RANGE over both decompositions (T = (D − G) + U and T = D + C), never one share (RULE 23). | CORRECTIONS 117.10, 120, 122 |
| Is the carrier the degenerate size-1 tail per se, or BatchNorm specifically — does D survive GroupNorm at byte-identical singleton structure? | normaliser BatchNorm (`ResNet18`) vs GroupNorm(32, C) (`ResNet18_gn`) × {nodewise, chunk777}, one batch | `gn1`, 24 runs (BN 4+4, GN 8+8), R18/C10, ms 1e-4, box -15:-2.3026, 100 ep | BN: **D = +0.587, se 0.153, t 3.83**. GN at face value: D = +0.202, se 0.137. T0.6 commensurability: BN level 92.293 vs GN 89.431, **a difference of −2.862 pp against a registered bar of 2.0** (error-budget ratio 1.37×). | `NO TRANSFER VERDICT IS ISSUED` + `THIS IS NOT A NULL` | Neither D_GN nor the BN–GN difference may be quoted as a transfer result, and "the gap collapses under GroupNorm" may not be written. The GN cell is removed from every pool (126.1); it had been the within-batch test of M7, which is therefore NOT SEPARABLE. The BN arm is one of the twenty cells. Run unedited on today's local mirror the scorer prints "THE WHOLE BATCH IS VOID", because `runs_cluster_b/gn1` holds no probe dirs — a sync fault, not a finding (110.1). | CORRECTIONS 114.2, 118, 124.3, 125.1, 126.1; `analysis/c84_gn1_score.py` |
| Do degenerate size-1 groups CAUSE the parent paper's granularity non-monotonicity — are size-1 groups the carrier as a general size law (mechanism M3)? | four checks on runs already on disk: the parent's own text, its §9 clause, layerwise → nodewise at the parent's η 1e-3 in five batches, and `ck1`'s chunk1 (100 % singletons) → chunk2 (0 %) | zero GPU; `ck1` in-batch; `br6`, `bl5`, `bf8`, `bf9`, `sp8` | K4: **chunk1 90.979 → chunk2 91.095, +0.115, se 0.133, t 0.87**, against a count-only prediction of +0.131 to +0.161 — removing every singleton buys nothing, and the ladder has no kink where f collapses. K3: at η 1e-3 the 66.6 %-singleton partition WINS by +0.76 to +1.25 pp in 5 of 5 batches *(quoted, 119.0)*. | "REFUTED" *(CORRECTIONS 119.0: "Size-1 groups cause the non-monotonicity" is REFUTED; the paper's M3)* | What survives is narrower and better measured: shattering the 41 one-dimensional normalisation tensors while the weights they scale stay grouped costs 0.43–0.82 pp at matched count — an architectural prescription, not a size law and not a variance law. "Variance" as the mechanism is struck too. | CORRECTIONS 119.0–119.2; paper §sec:size1 (M3) |
| Graded dose: is the harm specific to size-1 groups, smooth in 1-D group size, or tensor-level? | (registered, never run) `gf2`: nodefloor2 (m 9,615) vs chunk1167 (9,619); nodefloor4 (7,212) vs chunk1560 (7,212) | zero runs; `gf1` withdrawn; `gf2` 20 jobs registered and BLOCKED on `PATCH_NODEFLOOR`; zero `gf2` rows in the corpus | `gf1`'s TOST bound was 0.30 − 1.746 × 0.1883 = −0.0287, so its equivalence branch could never fire; `gf2` registers S = D(2) + D(4) against three points (0.20 / 0.69 / 1.19), 2.60 and 2.66 se apart *(quoted, 119.9–119.10)*. | `gf1` "WITHDRAWN" *(119.9)* + `gf2` "BLOCKED on PATCH_NODEFLOOR" *(119.10)* | The dose question is open and unfunded. The design that would break the tail/alignment confound is the additive tail test (124.6, R5), which no reviewer asked for. | CORRECTIONS 119.9–119.10, 124.6, 125.3; `analysis/c88_scorers.py` |
| RULE 11 — does D survive giving each arm its own tuned meta-stepsize, in a box both of whose rails are provably unreachable? | {nodewise, chunk777, nodewise1d, chunk2325} × ms {1e-4, 3e-4}, box -30:9.0 | `rl3`, 24 runs, R18/C10, one batch, 100 ep, seeds 0–2 | Welch: **D @1e-4 = +0.681, se 0.173, t 3.93; D @3e-4 = +0.591, se 0.096, t 6.18**; G @1e-4 −0.011, @3e-4 +0.217. Matched-optima dD = −0.090, se 0.178, t −0.51 on 16 df, in the scorer's pooled-sd form, whose per-D se is 0.126 *(quoted, 120)*. | `RULE 11 CLOSED ON R18/CIFAR-10` | "D is unchanged at matched optima to within 0.356 pp" — the referee objection ranked most likely to sink the paper, answered on R18/C10 only; RULE 11 stays OPEN on ResNet-34 and CIFAR-100. The first provably box-free measurement of D; not poolable by level with the -15-box cells. | CORRECTIONS 117.3, 117.7, 120, 131.3; `analysis/c87_rl3_score.py` |
| Does D transfer to an AdamW base — the largest external-validity hole, when every count-matched row was SGDm? | `cc1`'s four arms with ONE variable changed (base SGDm → AdamW), one batch | `aw1`, 12 runs, R18/C10, ms 1e-4, box -15:-2.3026, 100 ep; 3 of the 6 registered seeds | **D = +0.279, se 0.087, t 3.19**, 95 % lower bound 0.108 *(quoted, 123.1)*; **G = +0.232, se 0.089; D − G = +0.047**. | `UNRESOLVED at n=6. Report the interval. Do NOT re-cut the data, and do NOT describe it as 'partially transferring'.` | The cycle-91 headline "D TRANSFERS TO AdamW" (+0.361 on the banned `plateau` column, against a weaker restatement of the hashed rule) was WITHDRAWN at 123.1 — the origin of "run the registered scorer; do not hand-roll one". With `sm3`'s replicate, the size-1 tail carries essentially none of D under AdamW + Lion (M4 narrowed). The scorer is CSV-based and, re-run unedited here, prints this token byte-identically. | CORRECTIONS 119.10, 121, 123.1, 125.2; `analysis/c88_scorers.py` |
| On CIFAR-100: does D replicate, and does the tail mechanism (G null) hold on a second dataset? | ResNet18_c100: `nodewise` vs `chunk771` (D); `nodewise1d` vs `chunk2293` (G); one batch | `gm2`, 12 runs, CIFAR-100, SGDm+Lion, ms 1e-4, box -15:-2.3026, 100 ep, 3 seeds | **D = +1.485, se 0.238, t 6.24; G = +0.068, se 0.069; D − G = +1.417**; TRAIN D +3.647. | "NO REGISTERED VERDICT" *(paper §3.4: one of five batches with no scorer registered before its runs)* | D replicates `gc1` on the same CIFAR-100 configuration, and under SGDm "D large and G null" holds on a second dataset. Its launch script's header is a verbatim copy of `aw1`'s (CORRECTIONS 211), so the question it was built to ask is recorded nowhere but in its runs' ARGS lines. | paper `tab:D` row 16; CORRECTIONS 124.2, 211 |
| The horizon objection: is D a last-fifth-of-training artefact — does it survive a 3× budget? | budget 100 → 300 epochs, read within-run at epochs 100 / 200 / 300, {nodewise, chunk777, nodewise1d, chunk2325}, box -30:9.0 (both rails provably free at 300 ep) | `hz3`, 24 runs, R18/C10, ms 1e-4, 300 ep, seeds 0–5 (seed 5's three non-nodewise runs were later found in box -15:-2.3026, on another GPU class) | plateau5 **D(300) = +0.428, se 0.086, t 4.94**; G(300) −0.057. Within-run D(300) − D(100): on the scorer's registered 50-epoch window **+0.229, se 0.070, t 3.27** *(quoted, 131.3)*; on plateau5 **−0.149, se 0.105, t −1.42** (re-derived by `c99_hz3q_score.py`, run unedited here). | `MECHANISM SURVIVES THE HORIZON` + `GROWS` *(the 50-epoch window)* | "THE GAP GROWS WITH BUDGET" was WITHDRAWN at 123.2: at B = 100 the 50-epoch window contains the mid-training trough (`cc1` −0.560 at epoch 55) and at B = 300 it does not. The level survives three times the budget; the trend was repaired by `hz3q`. Three `hz3` seed-5 CSV rows carry the wrong `beta_clip` (124.4a). | CORRECTIONS 117.2–117.6, 122, 123.2, 124.4, 131.3; `analysis/c87_hz3_score.py` |
| Does D move with the base optimiser's normalisation — SGD and RMSProp bases? | base SGD vs RMSProp × {nodewise, chunk777, nodewise1d, chunk2325}, one submission | `nl1`, 24 runs, R18/C10, ms 1e-4, box -15:-2.3026, 100 ep, seeds 0–2 | **D(SGD) = +1.035, se 0.109, t 9.54; D(RMSProp) = +0.973, se 0.251, t 3.87**; G(SGD) +0.525, G(RMSProp) +0.020. | "NO REGISTERED VERDICT" *(paper §3.4; CORRECTIONS 123.4 registered an analysis rule, not a scorer)* | It supplies both single-batch levels of the base-optimiser candidate moderator — in the paper's words the newest headline's weakest flank is also its unregistered one; `bm2` replicated both levels under a registered scorer. One batch, not two (124.4c). Its launch script's header is `aw1`'s, copied (CORRECTIONS 211). | CORRECTIONS 123.4, 124.2, 124.4, 125.3; paper `tab:D` rows 11–12 |
| Does D survive the bottleneck block family (ResNet-50) — the network the closeout had declared structurally excluded? | ResNet-50: `nodewise` vs `chunk295` (D); `nodewise1d` vs `chunk884` (G); one batch | `r50`, 12 runs, CIFAR-10, SGDm+Lion, ms 1e-4, box -15:-2.3026, 100 ep, 3 seeds | **D = +0.881, se 0.261, t 3.37; G = +0.153, se 0.314; D − G = +0.729**; TRAIN D +1.112. | "NO REGISTERED VERDICT" *(paper §3.4)* | The exclusion — a chunk arm "would SPLIT" the BatchNorm tensors — was ruled PREMATURE at 117 (objection 3) and the batch ran; D survives on a third architecture at n=3. CLOSEOUT §6 and CORRECTIONS 116 still print the exclusion unamended. Its launch script's header is `aw1`'s, copied (CORRECTIONS 211). | CLOSEOUT §6; CORRECTIONS 116, 117 (objection 3), 211; paper `tab:D` row 14 |
| The meta axis — does D survive an Adam or RMSProp META-optimiser, when every partition row was meta = Lion? | intended: meta {Adam, RMSProp} at an SGDm base; what ran: meta = Lion on all 24 (argparse last-wins on a repeated `--alg-meta`) | `ml2`, 24 runs = 12 distinct configurations (the two nominal halves share the whole command line, `--seed` included) | Every one of the 24 ARGS lines repeats `--alg-meta`. As a Lion replicate, collapsed to three seed groups: **D = +0.456, se 0.195, t 2.34**; G = +0.173; D − G = +0.282. | "voided `ml2`'s meta axis" *(CORRECTIONS 125.2; no registered scorer, paper §3.4)* | The origin of STANDING RULE 20 — a batch's science is what its runs' own ARGS line says — and of `analysis/argsline_guard.py`. It enters the twenty cells as an SGDm + Lion cell at its collapsed se (3 v 3, not 6 v 6) and stays outside the fourteen-cell pool. Its launch script's header is `aw1`'s, copied (CORRECTIONS 211). | CORRECTIONS 125.2, 125.4, 126.3; paper §sec:silent-failures |
| Which property of the group-size distribution carries D (mechanism M8)? | twenty candidate statistics over size multisets CONSTRUCTED from the optimiser's own allocated state (163 network × granularity cells, 150 built) | zero GPU; the count-matched design | `frac_groups_size1` takes two values (0.6664 on nodewise, 0 elsewhere); the "best predictor" fit (β −0.986, t −12.5) is the three-arm equality test relabelled (χ² 7.5665 on 10 df, identical for an arbitrary arm dummy); the design has rank 3, so at most two statistics are identifiable; `ck1` refits the singleton coefficient at +0.106 ± 0.18 *(all quoted, 124.1)*. | "NOT IDENTIFIABLE FROM THIS CORPUS" *(CORRECTIONS 124.1)* | β = −0.986 may not be written in any role. The size distribution remains the surviving carrier; WHICH property of it is an open question that needs new runs — the additive tail test (124.6). | CORRECTIONS 124.1, 124.5–124.6 |
| Can D be predicted from a measurable property of the configuration — accuracy level, headroom, count, parameters, stepsize, dataset? | leave-one-design-point-out over the fifteen-cell D table (11 design points), one predictor each; the aligned-arm level slope separately | zero GPU | The corpus mean's LOO RMSE is 0.4005; k·log(headroom) 0.3284 (−18 %), all of it the single CIFAR-100 point — on the ten CIFAR-10 folds the mean wins (0.2791 vs 0.2863); aligned-arm level is 33 % worse. The level slope (−0.392) was never significant (exact permutation p 0.167), and `gn1`'s within-batch test went the wrong way by 3.6–7.0 se *(all quoted, 124.2–124.3)*. | "NO. THE PREDICTIVE FRAMING IS DEAD BEFORE IT REACHED A DRAFT" *(124.2)* + "THE LEVEL SLOPE IS DEAD" *(124.3)* | D varies genuinely (τ 0.285 against 0.137 pp noise at the time) and nothing measured predicts it out of sample better than its own mean. **Later amended:** once the `gn1`-GroupNorm cell was removed (126.1) the within-CIFAR-10 slope moved to −0.161 ± 0.067, so M7 is NOT SEPARABLE rather than dead. | CORRECTIONS 124.2–124.3, 126.1 |
| The second-moment corner — AdamW base × RMSProp meta: does D shrink when both carry a second moment? | intended meta RMSProp; what ran: meta = Lion (the `ml2` flag bug, repeated one cycle later) | `sm3`, 12 runs, R18/C10, AdamW base, ms 1e-4, box -15:-2.3026, 100 ep, seeds 0–2 | All 12 ARGS lines repeat `--alg-meta`. As a byte-identical replicate of `aw1`: **D = +0.141, se 0.064, t 2.22** (the smallest D of the twenty); **G = +0.296, se 0.096; D − G = −0.155**. Pooled with `aw1`, 6 v 6: D +0.210 ± 0.056, G +0.264 ± 0.060, D − G −0.054 *(pool quoted, 125.2)*. | "VOID AS DESIGNED" *(CORRECTIONS 125.2; no registered scorer)* | The second-moment candidate (M9) stayed untested, not refuted, until `sm4`. As an `aw1` replicate it made G under AdamW + Lion resolved in two batches — the tail carries essentially none of D there (M4 narrowed to a base–meta pairing). Its launch script's header is `aw1`'s, copied (CORRECTIONS 211). | CORRECTIONS 125.2, 125.4, 128.1 |
| Is the base-optimiser moderator replicated — a second independent batch at the SGD and RMSProp levels? | base {SGD, RMSProp} × {nodewise, chunk777}, fresh seeds, one submission | `bm2`, 12 runs, R18/C10, ms 1e-4, box -15:-2.3026, 100 ep, seeds 3–5 | **D′(SGD) = +0.978, se 0.086, t 11.40; D′(RMSProp) = +0.631, se 0.149, t 4.23**. Level pools with `nl1`: SGD **+1.000 ± 0.067**, Q 0.17; RMSProp **+0.720 ± 0.128**, Q 1.37. | `REPLICATES` (SGD) + `REPLICATES` (RMSProp) + `REPLICATED (Q <= 3.841)` (both levels) | Both single-batch moderator levels now rest on two batches. It could not make the base optimiser an IDENTIFIED moderator (the moderator row). Its occupancy gate reads probe records, so the verdict was issued on the cluster and quoted verbatim into the manuscript (131.3); run unedited here it VOIDs every arm. | CORRECTIONS 125.3, 126.5, 127.5, 131.3; `analysis/c97_bm2_score.py` |
| M9 — does a second-moment normaliser anywhere in the loop shrink D, at the corner where base AND meta carry one (AdamW base × RMSProp meta)? Registered refutation bar D ≥ +0.55. | meta Lion → RMSProp against `aw1` / `sm3`, one flag | `sm4`, 12 runs, R18/C10, AdamW base, ms 1e-4, box -15:-2.3026, 100 ep, seeds 0–2 — the corpus's only non-Lion partition cell | **D = +0.889, se 0.228, t 3.89; G = +0.261, se 0.081; D − G = +0.629**; 95 % CI for D [+0.4415, +1.3371] *(CI quoted, 131.3)*. | H1 `REFUTED. D >= +0.55 ...` + H2 `G PERSISTS` + H3 `MECHANISM CONTRAST RESOLVED` | The one refutation designed, registered, run and read in that order: D is the LARGEST in the AdamW family, not the smallest. Kept out of every pool by its own scorer's scope note. One cell is not an axis, so nothing general about the meta-optimiser follows. | CORRECTIONS 127.5, 128.1–128.3, 130.4 (item 8), 131.3; `analysis/c97_sm4_score.py` |
| Is the heterogeneity of D attributable to the base optimiser? | the fourteen byte-identical-contrast cells, partitioned by base optimiser vs by batch | zero GPU; the fourteen-cell pool | Fixed-effect pool **+0.5297 ± 0.0294**, Q **102.47** on 13 df; level pools **SGD +1.000, RMSProp +0.720, SGDm +0.556, AdamW +0.189**; RMSProp − AdamW **+0.531 ± 0.138, t 3.84** (all re-derived). Between-base Q 95.12 on 3 df (92.8 %), between-batch 98.16 on 10 df (95.8 %); dQ(base given batch) = 4.108 on 2 df, p 0.128 *(quoted, 128.1; reproduced by `c98_reproduce.py`)*. | "candidate moderator" *(CORRECTIONS 128.1, which withdrew "identified")* | The base optimiser is not separated from the submission it arrived in, so the paper says "candidate moderator" with the batch rival in the same sentence. M6, the base's own second-moment normalisation, is refuted post-hoc on these level pools. What would settle it: a second independent batch at each level within ONE submission. | CORRECTIONS 125.1, 126.2, 128.1; paper §4.4; `analysis/c98_reproduce.py` |
| Does the alignment null replicate at twice the power, with the permutation draw decoupled from the run seed? | `nodewise` vs `permnode{101, 202, 303}` (three fixed draws) × run seeds 6–11, fully crossed, one batch | `rp1`, 24 runs, R18/C10, ms 1e-4, box -15:-2.3026, 100 ep | **A = permnode − nodewise = −0.018, se 0.079, t −0.23 on 5 df** (paired on run seed); 95 % CI [−0.222, +0.185]; draw F(2,10) 0.175, p 0.842; run seed F(5,10) 4.634, p 0.019 *(the last three quoted, 129.4)*. | T1 `NULL` + T1b `CONSISTENT WITH NULL, UNDERPOWERED` + T2 `THE PERMUTATION DRAW IS EXCHANGEABLE AT THIS RESOLUTION` + `RUN SEED IS A REAL EFFECT HERE` | The paper's demotion clause (an interval spanning half of D) did not fire, so the alignment leg is a null — still not an equivalence claim: the open share narrows from about half of D to about a third. The run-seed effect contradicts the corpus seed null and is unresolved (A.3b). | CORRECTIONS 125.3, 129.1–129.4, 129.7, 131.3; `analysis/c97_rp1_score.py` |
| Repair `hz3`'s doubly confounded seed 5 (box and GPU class): is D flat from 100 to 300 epochs? | all four arms at seed 5, 300 ep, box -30:9.0, one submission on one GPU class | `hz3q`, 4 runs, R18/C10, ms 1e-4 | Within-seed D(300) − D(100) on plateau5: as published n=6 −0.149 (t −1.42); seed 5 dropped n=5 −0.207 (t −1.94); **repaired n=6 −0.238, se 0.093, t −2.57** on 5 df; repaired **D(300) = +0.394, se 0.093, t 4.25**. GPU-class control +0.134 pp against a 1.00 bar. | `NOT FLAT -- D DECLINES WITH BUDGET` | "D does not grow … we cannot resolve whether it decays" was WITHDRAWN (134): D shrinks with budget and does not go away — all six seeds still favour chunk777 at 300 epochs. Not a cell: Table 2 keeps `hz3`'s +0.428 with the repair disclosed. The scorer reads `.out` series and, re-run unedited here, prints this verdict (its occupancy gate needs probe records this mirror lacks). | CORRECTIONS 131.4, 134; `analysis/c99_hz3q_score.py` |
| **[Mechanism-hunt row in section 9's convention, appended at the file's end so that no existing line moves -- CORRECTIONS 217]** Is the scalar→layerwise gap a BatchNorm phenomenon -- does it exist on a network with no batch statistics? | every `BatchNorm2d(C)` → `GroupNorm(32, C)` on ResNet18_c100 (the same 62-tensor list, bitwise-identical initial point, no buffers): `k01` scalar vs `kL` layerwise, ONE batch | `cgn1`, 6 runs, `ResNet18_gn_c100` / CIFAR-100, SGDm+Lion, ms 1e-3, α₀ 1e-6, box -15:-2.3026, `AUGMENT=1`, 100 ep, fresh seeds {60,61,62}; ingest 2,863 -> 2,884 together with `cpl1` (added 21, changed 0, removed 0); **7.43 GPU-h** re-summed over its own 6 `wallclock_min` values (`sacct` 7.49) | **PRIMARY `D` = plateau5(`kL`) - plateau5(`k01`) = 51.8173 - 14.4340 = +37.3833 pp = +63.08 SE** (SE 0.5927; the in-batch sigma 0.7259 dominates the frozen prior 0.5862). TRAIN 60.5467 vs 16.5167, `D(TRAIN)` +44.0300. Seeds `k01` 14.236 / 14.380 / 14.686, `kL` 52.200 / 50.682 / 52.570. `D` >= 23.274 -> FULL-SIZE; `D_rel` 0.4369 against the BN in-batch median 0.6045 (descriptive). Account (B) (abs D <= 2) refuted; (A)'s D band hit, both of (A)'s levels missed LOW. **Two bounds:** the scalar `beta` never reaches the -15 clamp inside 100 epochs (0 of 500 records on every seed; it ends at -12.30 / -11.78 / -11.55), and `kL` is still rising at +0.29 to +0.34 pp/epoch over epochs 80-99 (epoch-to-epoch jitter 1.5-3.0 pp), so D is a 100-epoch snapshot, not a converged gap. Registered scorer run UNEDITED on `cluster_b`, exit 0; an independent parser agrees digit for digit. | `GAP-REPLICATES` + `HARNESS-CLEAN` + `FULL-SIZE` + `SIGMA-INBATCH-DOMINATES` + `TRAIN-AGREES` + `K01-BELOW-BN-FLOOR-BAND` + `GN32-ALSO-CHANGES-PER-CHANNEL-INVARIANCE` + `HORIZON-100-ONLY` + `PROBE-RECORDS-PRESENT` | **The gap does not need batch statistics**: at this cell it exists, full-size by the registered stamp, on a network with none, the same tensor list and the same initial point. **NOT licensed:** that GN's carriers are ResNet's -- the scalar arm's records nominate {50, 53, 59}, the same three indices, but only under a DISCLOSED post-peak deviation because the 194.5 pinned-record set is EMPTY on GN (a HYPOTHESIS; nothing submitted); other normalisers; per-channel invariance (GN(32) changes it too); 'the same mechanism as BatchNorm' (the GN scalar arm fails without reaching BN's clamp); the size of the GN gap at convergence; RULE 11. | CORRECTIONS 212 (registration), 216 (launch), 217 (landing) |
| **[Mechanism-hunt row in section 9's convention, appended at the file's end so that no existing line moves -- CORRECTIONS 218]** Once ResNet-18's residual additions are removed, and nothing else, do the surviving ex-carrier BN scales still carry the gap -- the one-variable test of 189.2's residual account? | `PlainNet18_c100` = ResNet18_c100 with every `out += shortcut(x)` removed and the 3 projection shortcuts not constructed (53 tensors): `k01`, `kL`, ISO {`layer4.0.bn2.weight`, `layer4.1.bn2.weight`} [51,2], its exact bn1 twin CTL {`layer4.0.bn1.weight`, `layer4.1.bn1.weight`} [51,2], ONE {`layer4.0.bn2.weight`} [52,1]; ONE batch | `cpl1`, 15 runs, `PlainNet18_c100` / CIFAR-100, SGDm+Lion, ms 1e-3, α₀ 1e-6, box -15:-2.3026, `AUGMENT=1`, 100 ep, fresh seeds {69,70,71}; ingested with `cgn1` (2,863 -> 2,884, added 21, changed 0); **9.82 GPU-h** re-summed over its own 15 `wallclock_min` values (`sacct` 9.95) | **PRIMARY `DELTA_ID` = ISO - CTL = 67.5427 - 11.6967 = +55.8460 pp = +98.49 SE**; KEY `D_ONE` = ONE - `k01` = 11.6840 - 11.7867 = **-0.1027 pp = -0.18 SE**; `D_GAP` +57.1933 (`kL` 68.9800); `RECOVERY` 0.9749, `RECOVERY_ONE` -0.0018 (ResNet's ONE ~0.91, between-batch). TRAIN: `k01` 11.80, `kL` 97.65, ISO 82.36, CTL 11.71, ONE 11.79; `DELTA_ID(TRAIN)` +70.65. SE 0.5670 (the frozen prior; in-batch 0.2218). On `k01`'s own pinned records `layer4.1.bn2.weight` holds 0.6201 of the mass and removing it flips the remainder sign on 1.0000 (non-gating). **Two bounds:** `k01` floors at 11.79, below every collapse account's registered 20-36; and ISO's rescue is a DELAY -- its complement group pins at the clamp at epoch 60-62 (`k01`'s at 36), so ISO's level is frozen and matches `kL` on TEST (-1.44 pp) but not on TRAIN (-15.29 pp). Registered scorer run UNEDITED on `cluster_b`, exit 0; an independent parser agrees digit for digit. | `HEAD-CARRIES-PLAIN` + `GATES-CLEAN` + `ONE-VARIABLE-RESIDUAL-ABLATION` + `THIRD-CARRIER-ABSENT` + `MAGNITUDE-NOT-SEPARATED` + `MISTUNING-NOT-EXCLUDED` + `HORIZON-100-ONLY` + `SIGMA-PRIOR-FROZEN` + `H-BROKEN-EXCLUDED` + `FLOOR-READINGS-ARE-BOUNDS` + `DECOMPOSITION-BY-SUBTRACTION` + `TRAIN-AGREES` + `ISO-TRACKS-KL` + `CTL-AT-FLOOR` + `ONE-AT-FLOOR` | **One-variable support for 189.2 for `layer4.0.bn2.weight` ONLY**: removing the addition it fed removed its carrier status, and ISO's in-batch rescue kills H-BROKEN, so ONE's null is real. **And 189.2 as a characterisation of WHICH tensors carry does not survive**: its two literal forms (R-ONLY, R-NOGAP) missed their registered branches -- a rescue exists with no addition anywhere, and by subtraction it rides on `layer4.1.bn2.weight`, a summand of nothing, as PATH (a conv-free path to the classifier) predicted. **NOT licensed:** `layer4.1.bn2.weight` as a measured carrier (never isolated alone); identity vs magnitude; anything about `layer4.0.shortcut.1.weight` (absent); PATH as confirmed (its `k01` level missed); any horizon but 100 epochs; RULE 11. **[AMENDED at cycle 152, CORRECTIONS 229: `cpl2` (226) ISOLATED `layer4.1.bn2.weight` ALONE, so 'by subtraction' and 'never isolated alone' above are superseded, and kept verbatim.** HEAD lifts the scalar arm from 11.8120 to 64.1767 pp, its bn1 twin stays at 12.0400 (`DELTA_HEAD` +52.1367 pp = +91.95 SE), and this batch's pair reproduces at 67.7707 as the in-batch positive control (`HEAD-CARRIES-ALONE-PLAIN`). 'Alone' is bar-relative: ISO - HEAD = +3.5940 pp = +6.34 SE, a small real increment inside the frozen 5.0 pp bar; and HEAD's rescue is also a DELAY (its complement pins at epoch 98.2 / 98.8). Still NOT licensed: identity vs magnitude (the twin's term is x691 smaller; `cvt1`, 227, is registered and has NOT landed); PATH as confirmed; any horizon but 100 epochs; RULE 11.]** **[UPDATE at cycle 152, CORRECTIONS 231: `cvt1` LANDED at 230 (`STEP-SIZE-NEEDED-VOTE-SUFFICES`), so 'has NOT landed' above is superseded and kept verbatim. What it found about `layer4.1.bn2.weight`: silencing its term in the shared meta-gradient sum while it keeps the shared step size does NOT rescue (MUTE 10.9860 vs `k01` 11.9493; on MUTE's records the DOWN vote is re-carried by other BN scales, 47, 44, 41, 38), while isolating it does (HEAD 64.7940, the positive control); and its bn1 twin's term x691 inside HEAD's complement re-pins that complement at epoch 37.6 and drops the arm to 30.1400 (0.344 of the gap kept). So its rescue needs its OWN STEP SIZE, not merely the removal of its vote, and a carrier-sized vote cast by one other tensor (its bn1 twin) collapses that complement. **[CORRECTED IN PLACE at cycle 152, CORRECTIONS 236: the same wording slip as line 5 -- one other caster was run; SUPERSEDED wording, kept verbatim: 'and a carrier-sized vote collapses a complement whoever casts it.']** Still NOT licensed: magnitude separated from identity in LEVEL (INJECT is a partial collapse; timing vs identity UNSURE), any dose-response, any network but PlainNet, anything past 100 epochs, RULE 11.]** | CORRECTIONS 215 (registration), 216 (launch), 218 (landing) |
| **[Mechanism-hunt row in section 9's convention, appended at the file's end so that no existing line moves -- CORRECTIONS 219]** Does the VGG isolation rescue survive a LONG horizon, or is `cvi1`'s +31.28 pp a 100-epoch artefact of a complement that had not yet pinned? | horizon alone: the four `cvi1` arms, spec strings BYTE-IDENTICAL to `cvi1`'s, run to **328 epochs** with the 100-epoch control read IN-RUN from the same runs (`SCHED=none`, so `num_epochs` is only the loop bound); `k01` scalar floor, `kL` layerwise ceiling, ISO `sets:1-22,24-26/bn8.weight` [25,1], CTL `sets:1-19,21-26/bn7.weight` [25,1]; ONE batch | `cvh1`, 12 runs, `VGG11_bn_c100` / CIFAR-100, SGDm+Lion, ms 1e-3, alpha0 1e-6, box -15:-2.3026, `AUGMENT=1`, `PROBE=100` + `PROBE_TENSOR=1` on every arm (1,640 records/run), **328 ep**, fresh seeds {63,64,65}; horizon E = ceil(`PIN_BOUND` 114.7865 + `W_MAX` 213.0) = 328 by the `190` method; ingest 2,884 -> 2,926 together with `cuc1` (added 42, changed 0, removed 0); **17.32 GPU-h** re-summed over its own 12 `wallclock_min` values (`sacct` 17.44) | **PRIMARY `RHO` = D_ISO@328 / D_ISO@100 = +32.0093 / +32.0053 = 1.0001**, both IN BATCH, `D_ISO` = plateau5(ISO) - plateau5(`k01`); +42.36 SE and +42.35 SE on SE 0.755682 (the frozen sigma 0.925518 dominates the in-batch 0.244624, df 16). Levels TEST @100 -> @328: `k01` 35.3627 -> 35.4020, `kL` 66.1867 -> 66.9140, ISO 67.3680 -> 67.4113, CTL 35.2393 -> 35.3087; TRAIN 35.5227 -> 35.6293 / 99.2493 -> 99.8767 / 95.8907 -> 96.1573 / 35.4553 -> 35.5400. Paired per seed rho 1.0008 / 1.0012 / 0.9985. `DELTA_ID` @328 = ISO - CTL **+32.1027 pp = +42.48 SE**; ISO - `kL` @328 +0.4973 (+0.66 SE); NULL MODEL `k01` @328 - @100 +0.0393 (+0.05 SE); `kL` 12/13/13 of 26 groups FREE. Every arm's seed range <= 0.564. **All six registered point predictions landed IN band** (rho [0.96,1.04], the four levels, and the pin bound). **The re-registered freedom gate, graded:** ISO's complement is PINNED on 3/3 by step-size MAGNITUDE (`MED_R` 1.0010 / 1.0020 / 1.0020, `MAX_R` 1.197 / 1.273 / 1.234) while `cIS2`'s superseded exact-clamp-dwell rule scores 0.4195 / 0.3780 / 0.3610 and would have called it NOT PINNED -- the `196.5` defect, reproduced and removed -- and the gate sits AFTER the primary, so no `RHO` can be lost to it. Pin (r<=2) at epoch 104.8 / 106.4 / 107.8, post-pin window **220.2 ep >= `W_MAX` 213.0**, and ISO's TEST over epochs 113-117 is 67.3433 against 67.4113 at the end: **+0.0680 pp = +0.09 SE**. Registered scorer run UNEDITED on `cluster_b`, exit 0, **105 PASS / 0 FAIL / 0 SKIP**; an independent parser agrees digit for digit and re-grades the branch map to the same branch. | `RESCUE-SURVIVES` + `RHO:1.0001` + `COMPLEMENT-PINNED` + `ISO-PIN-EPOCH:106.3` + `POST-PIN-WINDOW:220.2` + `WINDOW-MEETS-WMAX` + `FROZEN-AT-PIN` + `CEIL-TRACKS` + `KL-STILL-TRAINING` + `FLOOR-HOLDS` + `CTL-AT-FLOOR-AT-E` + `IDENTITY-HOLDS-AT-E` + `ISO-TRAIN-FROZEN` + `SIGMA-FROZEN-DOMINATES` + `REPLICATES-CVI1` + `MAGNITUDE-NOT-SEPARATED` + `NOT-A-ONE-VARIABLE-ABLATION` | **`cvi1`'s `HORIZON-100-ONLY` bound is retired for this arm**: the VGG rescue is unattenuated at 328 epochs, and the complement pinned inside `cvi1`'s own extrapolated 109-113 window, so the `190` extrapolation method has now bounded a measured pin three times. **But 'survives' means A LEVEL FROZEN AT THE PIN, NOT CONTINUED LEARNING** (+0.0680 pp over 220 post-pin epochs; `ISO-TRAIN-FROZEN` says the same on TRAIN), and rho ~ 1 is not a bound -- `213`'s own floor gate predicts 1.00-1.07 under the NEVER-PINS account too, which is why the pin state is reported beside it. **NOT licensed:** any other architecture (`ciso2`'s ResNet rho 0.9977 at 250 ep is a different batch and is not pooled); the tensor LIST (cardinality 1 on VGG against ResNet's 3); IDENTITY vs MAGNITUDE; any ordering of ISO against `kL` (+0.4973 pp, inside `READ_BAR` 1.5114, and `kL` is still training); convergence of either arm; any horizon beyond 328 ep; `AUGMENT=0`, another meta-optimiser or the paper configuration; `RULE 11`. | CORRECTIONS 213 (registration), 216 (launch), 219 (landing) |
| **[Denominator row in section 9's convention, appended at the file's end so that no existing line moves -- CORRECTIONS 220]** Does the denominator hold in the parent's own UNAUGMENTED setting on CIFAR-100 -- the two counts `cau1` left open (CIFAR-100, and meta-side tuning beyond two alpha0 cells)? | `AUGMENT=0` on EVERY arm: a 5-rung plain-SGD `lr` ladder (0.05 / 0.1 / 0.2 / 0.4 / 0.8, momentum 0.9, wd 5e-4, cosine `COS_TOTAL=50000` `COS_WARMUP=1000`) against a 5-cell meta-stepsize x alpha0 plus-grid around the corpus-best augmented cell; one batch, one granularity | `cuc1`, 30 runs, `ResNet18_c100` / CIFAR-100, 100 ep, fresh seeds {66,67,68}; 15 baseline + 15 method (`HF` SGDm+Lion, `chunk771`, box -15:-2.3026, `PROBE=5`); grid `m1e4a1e3` 1e-4/1e-3 (REF) / `m3e4a1e3` 3e-4/1e-3 (CENTRE) / `m1e3a1e3` 1e-3/1e-3 / `m3e4a1e4` 3e-4/1e-4 / `m3e4a1e2` 3e-4/1e-2; ingested with `cvh1` (2,884 -> 2,926, added 42, changed 0); **22.95 GPU-h** re-summed over its own 30 `wallclock_min` values (`sacct` 23.27) | **PRIMARY `GAP_END` = plateau5(S\*) - plateau5(M\*) = 65.0573 - 52.8793 = +12.1780 pp = +26.05 SE** (SE 0.4674; the frozen prior 0.572475 dominates the in-batch 0.5555, df 18). **THE LADDER IS INTERIOR** -- 62.658 / 63.097 / 64.774 / **65.057** / 59.848, argmax the 4th of 5 rungs, falling 5.209 pp = 11.14 SE at the top -- **so the token does NOT take `\| LOWER-BOUND`**. Grid: 48.457 / 52.749 / **52.879** / 47.057 / 14.195, the last `DOES NOT TRAIN`. M\* is an EDGE cell (ms-HIGH) but beats the CENTRE by only +0.1307 pp = +0.28 SE, so the registered map gives plain `DEFICIT-HOLDS` with `METHOD:FLAT-TOP-MS-HIGH`, not `DEFICIT-HOLDS-METHOD-EDGE`. Every rung beats every cell (smallest margin `lr08` - M\* = +6.969 pp). `PEAK` +9.1087 pp = +25.37 SE_PEAK (endpoint and early-stopping AGREE); `TUNING` M\* - REF **+4.4220 pp = +9.46 SE** (the augmented cell spec is genuinely mistuned here, and the gap is measured against the retuned cell); `CURVE` +6.9710 pp = +17.62 SE_CURVE at the best-AUC rung but **-2.3489 pp at S\***, hence `CURVE-DEFICIT-LOWER-BOUND`. TRAIN plateau5 >= 99.96 on every training arm (TRAIN-AUC gap -22.9231; epoch-to-99 % 18-19 for M\* against 80 for S\*). Selection: S\* and M\* are both max-of-5, worst-case bias ~0.384 pp EACH in OPPOSITE directions; adversely +11.41 pp = 24.4 SE. **`214`'s disclosed adverse sensitivity model, which predicted every cell at 4-15 pp and a `GATED-METHOD-AT-FLOOR` non-answer, DID NOT FIRE: four of five cells train at 47.06-52.88 pp and only the alpha0-HIGH corner floors.** RULE 20 at full 30/30 per family AND per cell, the separate ENV audit (`AUGMENT=0` in the ENV line AND the `NODE=` header of all 30), the **30/30 `SGD_*` runtime `/proc/environ` witness** (`SGD_count=0` + positive control) and a `build_optimizer.py` sha256 **and** mtime re-check against `PROVENANCE.txt` all PASS before any number. Registered scorer run UNEDITED on `cluster_b`, exit 0; an independent parser agrees digit for digit. | `DEFICIT-HOLDS` + `LADDER:INTERIOR` + `METHOD:FLAT-TOP-MS-HIGH` + `S*=lr04` + `M*=m1e3a1e3` + `SIGMA-PRIOR-DOMINATES` + `TUNING-GAIN-RESOLVED` + `CURVE-DEFICIT-LOWER-BOUND` + `PEAK-DEFICIT` + `ENDPOINT-PEAK-AGREE` + `TRAIN-AT-CEILING` + `BARS-FROZEN` + `ONE-BASELINE-FAMILY` + `ONE-GRANULARITY-CHUNK771` + `PAPER-CONFIG-NOT-TESTED` + `COUNTERWEIGHT-NOT-TESTED` + `CIFAR100-RESNET18-ONLY` | **Row 19's two remaining counts are CLOSED**, and the unaugmented CIFAR-100 deficit is **the largest the campaign has measured**: +12.178 pp here against +5.699 augmented on the same network (`cdn1`) and +3.617 unaugmented on CIFAR-10 (`cau1`) -- **so removing augmentation does not rescue the method on CIFAR-100; it widens the deficit** (that three-way reading is BETWEEN batches and is orientation, not a measurement). **NOT licensed:** the paper's own alpha0 1e-6 (not on the grid; `214`'s model says it might floor, which is a prediction, not a measurement); any granularity but `chunk771`; any baseline family but SGD+momentum+cosine; 'the method needs augmentation' (no counterweight was swept); meta-side tuning as exhausted (M\* sits at a grid edge whose rise is inside noise, so ms-HIGH under-tuning is BOUNDED by that flat top, not excluded); any horizon but 100 ep, and no convergence claim -- every training arm is at the TRAIN ceiling, so the endpoint compares the generalisation of interpolating solutions; the AUC reading as a clean win (it REVERSES at S\*); `RULE 11`. | CORRECTIONS 214 (registration), 216 (launch), 220 (landing) |
| **[Mechanism-hunt row in section 9's convention, appended at the file's end so that no existing line moves -- CORRECTIONS 225]** Does the ISOLATION -- not only the gap -- transfer off BatchNorm: on the GroupNorm twin of ResNet18_c100, do ResNet's three carriers still carry the scalar collapse, and does `layer4.0.bn2.weight` alone still rescue? | `ResNet18_gn_c100` at `cgn1`'s cell, spec strings BYTE-IDENTICAL to `ciso1`/`cdep1`: `k01` scalar, `kL` layerwise, ISO {50 `layer4.0.bn2.weight`, 53 `layer4.0.shortcut.1.weight`, 59 `layer4.1.bn2.weight`} [59,3], CTL {47, 48, 56} [59,3] (numel-, width-, stage- and owner-matched, `cdep1`'s DEPTH set), ONE {50} [61,1]; ONE batch | `cgn2`, 15 runs, `ResNet18_gn_c100` / CIFAR-100, SGDm+Lion, ms 1e-3, alpha0 1e-6, box -15:-2.3026, `AUGMENT=1`, `PROBE=100` + `PROBE_TENSOR=1` on every arm, 100 ep, fresh seeds {72,73,74}; ingest 2,926 -> 2,956 together with `cpl2` (added 30, changed 0, removed 0); **11.08 GPU-h** re-summed over its own 15 `wallclock_min` values (`sacct` 11.20) | **PRIMARY `DELTA_ID` = ISO - CTL = 55.7147 - 14.1980 = +41.5167 pp = +67.23 SE** (SE 0.6176; the in-batch sigma 0.7564 dominates the frozen prior 0.7259); TRAIN +50.2820. KEY `D_ONE` = ONE - `k01` = 40.2853 - 14.0940 = **+26.1913 pp = +42.41 SE**; `D_ISO` +41.6207, `D_CTL` +0.1040 (+0.17 SE), `D_HEAD3` = ISO - ONE +15.4293, `D_GAP` +39.8107 (`kL` 53.9047); RECOVERY 1.0455, RECOVERY_ONE 0.6579 (ResNet BN's ONE ~0.91, between-batch). TRAIN `k01` 16.15, `kL` 63.03, ISO 66.56, CTL 16.28, ONE 45.59. Every level of the registered T-IDENT account landed in its band. **Three bounds:** 0 of 500 records at the clamp for `k01` or any complement, and ISO's complement `beta` is still FALLING (-0.023 to -0.029 per epoch over 80-99; the isolated carriers' own group pins at epoch ~22), so the rescue is a 100-epoch level and a delay is not excluded; ISO (+0.38 to +0.44 pp/ep) and ONE (+0.32 to +0.38) are still rising and ISO sits +1.81 pp above `kL`; and magnitude is unreachable by set-matching here too (every DOWN-voting non-carrier together holds 0.0324 of `k01`'s post-peak mass against the carriers' 0.4653). Registered scorer run UNEDITED on `cluster_b`, exit 0; an independent parser agrees digit for digit. **[RESCOPED at cycle 152, CORRECTIONS 234: 'ISO sits +1.81 pp above `kL`' above, and the `ISO-TRACKS-KL` stamp, held AT 100 EPOCHS ONLY; both are kept verbatim.** `cgn3` (231) ran this cell to 430 epochs with the 100-epoch level read in-run: `kL` kept climbing (TEST 66.3853, TRAIN 99.79) and passes ISO for good at epoch ~174 (the first window-end epoch from which `kL`'s 5-epoch arm-mean TEST stays above ISO's), and at 430 ISO - `kL` = **-3.0233 pp = -3.88 SE** (`CEIL-BELOW`) against +2.4933 in-run at 100. The rescue itself is not a delay at that horizon (`RESCUE-SURVIVES`, `RHO` 1.2235, the complement pinned at ~225 and ISO's TEST flat after it); what does not survive is the ordering against layerwise.]** | `IDENTITY-TRANSFERS-GN` + `HARNESS-CLEAN` + `K01-IS-A-LEVEL-NOT-A-CLAMP` + `NOMINATION-WAS-POST-PEAK-NOT-PINNED` + `CTL-MATCHED-ON-NUMEL-NOT-CLASS` + `MAGNITUDE-NOT-SEPARATED` + `MISTUNING-NOT-EXCLUDED` + `HORIZON-100-ONLY` + `RESCUE-PERSISTENCE-NOT-TESTED` + `GN32-ALSO-CHANGES-PER-CHANNEL-INVARIANCE` + `SIGMA-INBATCH` + `H-BROKEN-EXCLUDED` + `FLOOR-READINGS-ARE-BOUNDS` + `TRAIN-AGREES` + `ISO-TRACKS-KL` + `CTL-AT-K01` + `ONE-RESCUES` | **The BN isolation transfers whole to GroupNorm at this cell: the carrier SET, not only the gap, does not need batch statistics**, and `217`'s post-peak GN nomination is supported by intervention. **NOT licensed:** identity vs magnitude (unreachable by set-matching on this net too); persistence past 100 epochs, or rescue as opposed to delay (no complement pins; all still descending); anything about the clamp; the ISO > `kL` ordering or any converged size; 'the same mechanism as BatchNorm' (ONE recovers 0.66 here vs ~0.91 on BN, and GN's collapse is a descent, not a pin); other normalisers (LayerNorm, GN(C), none); RULE 11. | CORRECTIONS 221 (registration), 223 (launch), 225 (landing) |
| **[Mechanism-hunt row in section 9's convention, appended at the file's end so that no existing line moves -- CORRECTIONS 226]** On the residual-free ResNet-18, does `layer4.1.bn2.weight` ALONE carry the rescue `cpl1` attributed to it only by subtraction? | `PlainNet18_c100` at `cpl1`'s cell (53 tensors, every index re-derived by name): `k01` scalar, `kL` layerwise, ISO {44 `layer4.0.bn2.weight`, 50 `layer4.1.bn2.weight`} [51,2] (BYTE-IDENTICAL to `cpl1`'s ISO: the positive control), HEAD {50} [52,1], TWIN {47 `layer4.1.bn1.weight`} [52,1] (its exact bn1 twin); ONE batch | `cpl2`, 15 runs, `PlainNet18_c100` / CIFAR-100, SGDm+Lion, ms 1e-3, alpha0 1e-6, box -15:-2.3026, `AUGMENT=1`, `PROBE=100` + `PROBE_TENSOR=1`, 100 ep, fresh seeds {75,76,77}; ingested with `cgn2` (2,926 -> 2,956, added 30, changed 0); **9.58 GPU-h** re-summed over its own 15 `wallclock_min` values (`sacct` 9.74) | **PRIMARY `DELTA_HEAD` = HEAD - TWIN = 64.1767 - 12.0400 = +52.1367 pp = +91.95 SE** (SE 0.5670, the frozen prior; in-batch 0.5498); TRAIN +67.0587. KEY `D_HEAD` = HEAD - `k01` **+52.3647 pp = +92.35 SE**; `D_PAIR` = ISO - HEAD **+3.5940 pp = +6.34 SE** (bar 5.0; the nearest margin in the branch, 1.41 pp); positive control `D_ISO` +55.9587 (ISO 67.7707; `cpl1`'s 67.5427, between-batch); `D_TWIN` +0.2280; `D_GAP` +57.1260 (`kL` 68.9380); RECOVERY_HEAD 0.9358. TRAIN `k01` 11.76, `kL` 97.29, ISO 82.58, HEAD 78.95, TWIN 11.89. Every level of the registered SUBTRACT account landed in its band. Pins: `k01` and TWIN's complement at epoch 36.2-36.4 (identical to the record), ISO's complement at 62.4-63.0 (`cpl1` 60.0-62.4), HEAD's complement at 98.2 / 98.8 and at -14.999 on the last record of s75; HEAD's and ISO's TEST are flat from epoch ~50. On `k01`'s 956 pinned records `layer4.1.bn2.weight` holds 0.6143 of the mass and removing it flips the remainder sign on 1.0000 (44: 0.0000). Registered scorer run UNEDITED on `cluster_b`, exit 0; an independent parser agrees digit for digit. | `HEAD-CARRIES-ALONE-PLAIN` + `HARNESS-CLEAN` + `SINGLE-TENSOR-DECOMPOSITION-OF-cpl1` + `THIRD-CARRIER-ABSENT` + `MAGNITUDE-NOT-SEPARATED` + `MISTUNING-NOT-EXCLUDED` + `HORIZON-100-ONLY` + `SIGMA-PRIOR-FROZEN` + `POSITIVE-CONTROL-REPRODUCES` + `SINGLETON-BROKEN-EXCLUDED` + `FLOOR-READINGS-ARE-BOUNDS` + `TRAIN-AGREES` + `HEAD-BELOW-ISO` + `TWIN-AT-K01` + `ISO-TRACKS-KL` | **`cpl1`'s decomposition-by-subtraction is now a measurement:** on a network with no residual addition anywhere, the rescue rides on `layer4.1.bn2.weight` alone -- the one BN scale with no conv between it and the classifier, PATH's location and VGG's `bn8` position -- and its exact twin does not; H-SINGLETON is dead in batch. **NOT licensed:** '44 contributes nothing' (`D_PAIR` +3.59 pp is resolved at +6.34 SE and merely inside the frozen 5.0 bar); identity vs magnitude (HEAD holds 0.6143 of the pinned mass and the best DOWN-voting non-carrier, TWIN, is 691x smaller); that 50 carries on ResNet (a different network); anything past 100 epochs (HEAD's complement pins at ~98); RULE 11. | CORRECTIONS 222 (registration), 223 (launch), 226 (landing) |
| **[Mechanism-hunt row in section 9's convention, appended at the file's end so that no existing line moves -- CORRECTIONS 230]** On the residual-free ResNet-18, is `layer4.1.bn2.weight`'s rescue the removal of its VOTE from the shared meta-gradient sum or its OWN STEP SIZE -- and does a vote act by its SIZE or by its OWNER? | `PlainNet18_c100` at `cpl2`'s cell with PATCH_VOTEWEIGHT (`VOTE_W=<tensor>:<w>` multiplies one tensor's term inside the unnormalised shared sum, before the sign; proved inert bitwise before launch, 227.4): `k01` scalar; HEAD {50} [52,1] (`cpl2`'s HEAD, BYTE-IDENTICAL: the positive control); MUTE = scalar grouping with 50's term x0 (50 keeps the shared step size and stops voting); DOSE = the same x0.1; INJECT = HEAD's grouping with the twin 47 `layer4.1.bn1.weight` x691 (fixed K, sign kept) inside the complement; ONE batch | `cvt1`, 15 runs, `PlainNet18_c100` / CIFAR-100, SGDm+Lion, ms 1e-3, alpha0 1e-6, box -15:-2.3026, `AUGMENT=1`, `PROBE=100` + `PROBE_TENSOR=1`, 100 ep, fresh seeds {78,79,80}; ingested alone (2,956 -> 2,971, added 15, changed 0); **10.22 GPU-h** re-summed over its own 15 `wallclock_min` values (`sacct` 10.38). **The 9 MUTE / DOSE / INJECT rows carry `k01`'s / HEAD's CSV cell key (no `VOTE_W` column) and are listed in `results/CORPUS-EXCLUSIONS.tsv`; [SUPERSEDED, not true when written: every cell-pooling reader drops them.]** **[AMENDED at cycle 152, CORRECTIONS 231: no file in `analysis/` imports `analysis/corpus_exclusions.py`, and at least one existing reader pools them -- `cPL2_plainnet_head_score.py --selftest` re-derives SIGMA_PLAIN on the live corpus as 8.6778 (df 24), against 0.4107 (df 15) with the 9 rows dropped; it prints a NOTE and feeds no bar (O2). The rule now reads: every FUTURE noise-floor or corpus-sigma derivation must drop them with `corpus_exclusions.filter_rows` first, and say so in its registration entry (docs/OPERATIONS.md, registration checklist); frozen-literal bars of already-registered scorers are unaffected.]** | **CO-PRIMARY `P_VOTE` = HEAD - MUTE = 64.7940 - 10.9860 = +53.8080 pp = +94.90 SE; CO-PRIMARY `P_INJECT` = HEAD - INJECT = 64.7940 - 30.1400 = +34.6540 pp = +61.12 SE** (SE 0.5670, the frozen prior; in-batch 0.5959); TRAIN +68.7867 / +48.9500. KEY `D_HEAD` +52.8447 pp = +93.20 SE (positive control; `cpl2` +52.3647, between-batch); `D_MUTE` -0.9633 pp = -1.70 SE; `D_DOSE` -0.0007 pp; `D_INJ` +18.1907 pp = +32.08 SE. Levels (TEST / TRAIN): `k01` 11.9493 / 11.85, HEAD 64.7940 / 79.72, MUTE 10.9860 / 10.94, DOSE 11.9487 / 12.06, INJECT 30.1400 / 30.77; every seed range <= 1.37 pp. **G-BITE PASS on 15/15** (500 records each; intervened runs 457-485 informative records, unweighted-sum match on 0); an independent check adds that every interior beta step follows the WEIGHTED Lion direction, and on the 9-113 records per run where weighted and unweighted directions disagree it follows the weighted one on all and the unweighted on none. Pins: `k01` 36.2-36.6; MUTE 40.2-40.6; DOSE never continuously (at the clamp 0.12-0.19 of records, -14.96 to -15.00 at the end); INJECT's complement 37.6 on all seeds (HEAD's 99.8), its isolated 50 at 20.8-23.6 as HEAD's. On MUTE's post-epoch-36 records the DOWN vote is re-carried by other BN scales (47 0.20-0.29 of the mass, 44, 41, 38), every record DOWN. Registered scorer run UNEDITED on `cluster_b`, exit 0; an independent parser agrees on every level, contrast, branch and stamp. | `STEP-SIZE-NEEDED-VOTE-SUFFICES` + `HARNESS-CLEAN` + `PATCH-BITES` + `K-FIXED-691` + `ONE-NETWORK-PLAINNET` + `HORIZON-100-ONLY` + `SIGMA-PRIOR-FROZEN` + `POSITIVE-CONTROL-REPRODUCES` + `DOSE-AT-K01` + `INJECT-PARTIAL` + `MUTE-BELOW-HEAD` + `FLOOR-READINGS-ARE-BOUNDS` + `TRAIN-AGREES` | **The first intervention on the vote itself, and it separates the two halves of isolation:** silencing 50's vote while it keeps the shared step size does NOT rescue (MUTE at `k01`), so the rescue needs 50 on its own step size; and a carrier-sized vote cast by a DIFFERENT tensor into HEAD's rescued complement pins that complement within ~1.4 epochs of `k01`'s pin (37.6 vs 36.2-36.6) and costs 34.65 pp, so a vote's collapsing effect is at least partly its SIZE, not only its owner. **Bounded, and led with (230.4):** INJECT is a PARTIAL collapse -- 18.19 pp (+32 SE) above `k01`, 0.344 of the HEAD gap kept, 0.14 pp above the top of the registered 8-30 band; the branch is bar-relative (`P_INJECT` 24.65 pp over COLLAPSE). **NOT licensed:** that magnitude alone collapses (INJECT stays well above `k01`), that identity is irrelevant, or which property of 50 needs the own step size; that x691 is minimal or record-matched (one K); any dose-response (one dose, and DOSE sits at `k01` like MUTE, so it cannot separate threshold from graded); ResNet (BN or GN) or VGG; anything past 100 epochs; RULE 11. | CORRECTIONS 227 (registration and launch), 230 (landing) |
| **[Mechanism-hunt row in section 9's convention, appended at the file's end so that no existing line moves -- CORRECTIONS 231]** Is the GroupNorm isolation rescue (`cgn2`, 225) a rescue or a delayed collapse that 100 epochs could not see -- and what happens to ONE's partial rescue? | `ResNet18_gn_c100` at `cgn2`'s cell to E = 430 epochs, spec strings BYTE-IDENTICAL to `cgn2`'s: `k01` scalar, `kL` layerwise, ISO {50 `layer4.0.bn2.weight`, 53 `layer4.0.shortcut.1.weight`, 59 `layer4.1.bn2.weight`} [59,3], ONE {50} [61,1]; no CTL arm; the 100-epoch control read IN RUN from epochs 95-99 of the same runs (proved on the tree and the live train.py / HF.py before launch); ONE batch | `cgn3`, 12 runs, `ResNet18_gn_c100` / CIFAR-100, SGDm+Lion, ms 1e-3, alpha0 1e-6, box -15:-2.3026, `AUGMENT=1`, `PROBE=100` + `PROBE_TENSOR=1`, 430 ep, fresh seeds {81,82,83}; ingested alone (2,971 -> 2,983, added 12, changed 0); **38.70 GPU-h** re-summed over its own 12 `wallclock_min` values (`sacct` 38.81). These rows are 430-epoch measurements (`epochs_done` 430) and must not be pooled into a 100-epoch GN cell. | **PRIMARY `RHO` = D_ISO@430 / D_ISO@100 = +48.5193 / +39.6573 = 1.2235** (D_ISO +62.34 SE at 430, +50.95 SE at 100; SE 0.7783, the frozen prior 0.9532 dominating the in-batch 0.7036); per-seed paired rho 1.2376 / 1.2035 / 1.2302; TRAIN RHO 1.6144 (+47.37 -> +76.48). Levels TEST @100 -> @430 (TRAIN @430): `k01` 14.4460 -> 14.8427 (16.93), `kL` 51.6100 -> 66.3853 (99.79), ISO 54.1033 -> 63.3620 (93.41), ONE 39.8920 -> 56.6187 (81.56); every seed range <= 2.93 pp. ONE: `RHO_ONE` 1.6418 (+25.4460 -> +41.7760), share of the `kL` gap 0.6847 -> 0.8105, ISO - ONE +14.21 -> +6.74. ISO - `kL` +2.49 at 100, **-3.02 pp (-3.88 SE) at 430**; `kL` passes ISO for good at epoch 174. Pin gate (step-size magnitude, last quarter): ISO's complement PINNED on 3/3 (median r 1.03 / 1.02 / 1.03), first r <= 2 at epoch 238.4 / 221.0 / 215.2 (inside the 335.5 bound), never re-freed (<= 0.4 % of later records above r 2); ONE's complement PINNED at 282.6 / 282.8 / 312.6; `k01` PINNED at 195-219; the isolated groups on the clamp from epoch ~20. Post-pin: ISO TEST 63.34 at epochs 244-248 vs 63.36 at the end (+0.03 SE), per-seed OLS -0.05 to +0.03 pp per 100 epochs over 186-209 epochs; ONE -0.07 to +0.03 over 112-142. **But ISO's TEST settled (within 0.5 pp of its end, for good) at epoch 162, while its complement r was still 49-76 -- 53-76 epochs BEFORE the pin -- and TRAIN kept rising (87.0 at 150 -> 93.0 at 225 -> 93.4).** Registered scorer run UNEDITED on `cluster_b`, exit 0, 87 PASS / 0 FAIL; an independent parser reproduces every level, contrast, the branch and all 20 conditional stamps, byte-identical on cluster_b and the Mac. | `RESCUE-SURVIVES` + `RHO:1.2235` + `TRAIN-AGREES` + `COMPLEMENT-PINNED` + `ISO-PIN-EPOCH:224.9` + `POST-PIN-WINDOW:191.6` + `WINDOW-MEETS-W100` + `PIN-INSIDE-BOUND` + `FROZEN-AT-PIN` + `RHO-ONE:1.6418` + `ONE-SURVIVES` + `ONE-SHARE-RISES` + `ONE-COMPLEMENT-PINNED` + `HEAD3-NARROWS` + `CEIL-BELOW` + `KL-STILL-TRAINING` + `K01-PINNED` + `FLOOR-HOLDS` + `ISO-TRAIN-MOVES` + `SIGMA-FROZEN-DOMINATES` + `DIFFERS-FROM-CGN2` + `MAGNITUDE-NOT-SEPARATED` + `NO-IN-BATCH-CTL` + `GN32-ALSO-CHANGES-PER-CHANNEL-INVARIANCE` + `ONE-CELL-ONLY` | **On GroupNorm the isolation rescue is a regime, not a delayed collapse: the complement's step size DOES pin (at ~225, inside the extrapolated bound, not in the never-pins tail 228 feared), and the rescued level holds flat for ~190 epochs after it**, as on VGG (`cvh1`) and ResNet BN (`ciso2`). ONE's partial rescue is not partial at long horizon: 50 alone reaches 0.81 of the `kL` gap. **Bounded, and led with:** (1) the survival is of a LEVEL that stopped rising at ~162 -- before the pin, while TRAIN kept climbing -- so the plateau is not caused by the pin in time, and 'the pin freezes the level' is not licensed; (2) the rescued arm does NOT track the layerwise ceiling at long horizon: `kL` climbs on to 66.39 and ISO sits 3.02 pp below it (`CEIL-BELOW`), so 'isolating three tensors recovers layerwise' holds at 100 epochs only; (3) D_ISO@100 here is 1.96 pp below `cgn2`'s (`DIFFERS-FROM-CGN2`, between-batch, descriptive). **NOT licensed:** persistence past 430 epochs or a decay slower than 1.56 pp per ~190 epochs; identity vs magnitude (`MAGNITUDE-NOT-SEPARATED`); `DELTA_ID` at 430 (no CTL arm); any other normaliser (GN(32) also changes per-channel invariance), network or cell; the mechanism of the pre-pin TEST plateau (UNSURE); RULE 11. | CORRECTIONS 228 (registration and launch), 231 (landing) |
| **[Mechanism-hunt row in section 9's convention, appended at the file's end so that no existing line moves -- CORRECTIONS 235]** `cvt1`'s MUTE failed while the DOWN vote moved to other BN scales: does silencing `layer4.1.bn2.weight` AND the whole DOWN coalition -- every tensor still on ONE shared step size -- rescue the scalar arm, or is the own step size needed after all? | `PlainNet18_c100` at `cvt1`'s cell, from `cvt1`'s unchanged PATCH_VOTEWEIGHT tree (no new harness code): `k01` scalar; HEAD {50} [52,1] (BYTE-IDENTICAL to `cvt1`/`cpl2`: the positive control); MUTE50 = scalar with 50's term x0 (`cvt1`'s MUTE, replicated); MUTEDOWN = scalar with {50} + RULE C's 20-tensor DOWN coalition x0 (47 44 41 38 35 32 29 26 23 20 17 14 40 46 43 11 37 8 5 49: 15 BN scales + 5 convs, 0.828 of the vote mass on `cvt1` MUTE's post-epoch-36 records); MUTECTL = scalar with {50} + RULE K's count- and class-matched NON-DOWN set x0 (15 BN biases of the same modules + 5 smaller convs, 0.028 of the mass, NOT mass-matched); G-RUNAWAY registered against an UP-runaway; ONE batch | `cvt3`, 15 runs, `PlainNet18_c100` / CIFAR-100, SGDm+Lion, ms 1e-3, alpha0 1e-6, box -15:-2.3026, `AUGMENT=1`, `PROBE=100` + `PROBE_TENSOR=1`, 100 ep, fresh seeds {84,85,86}; ingested with `cvt2` (2,983 -> 3,019, added 36, changed 0); **9.92 GPU-h** re-summed over its own 15 `wallclock_min` values (`sacct` 10.11). **The 9 MUTE50 / MUTEDOWN / MUTECTL rows carry `k01`'s CSV cell key and are listed in `results/CORPUS-EXCLUSIONS.tsv`** (`corpus_exclusions.py --check` PASS) | **PRIMARY `P_COAL` = MUTEDOWN - MUTECTL = 11.3293 - 7.8213 = +3.5080 pp = +6.19 SE** (SE 0.5670, the frozen prior; in-batch 0.4309); TRAIN +3.5593. KEY `D_DOWN` -0.2427 pp = -0.43 SE; `D_CTL` -3.7507 pp = -6.61 SE; `D_MUTE50` -0.2773 pp; `D_HEAD` +53.0240 pp = +93.52 SE; `G_DOWN` +53.2667 pp. Levels (TEST / TRAIN): `k01` 11.5720 / 11.65, HEAD 64.5960 / 79.25, MUTE50 11.2947 / 11.17, MUTEDOWN 11.3293 / 11.29, MUTECTL 7.8213 / 7.73; every seed range <= 1.38 pp. **G-BITE + G-BITE-SET PASS on 15/15** (intervened runs 453-500 informative records, unweighted match 0; MUTEDOWN / MUTECTL 320-497 set-informative records, 50-only and other-set match 0); every interior beta step follows the WEIGHTED Lion direction, and on the 10-34 records per run where weighted and unweighted directions disagree it follows the weighted one on all. **G-RUNAWAY: 0 of 15 runs** (0.000 of post-epoch-17 records at the ceiling; highest shared beta -4.110, MUTECTL). Shared-beta pins: `k01` 36.4; MUTE50 40.2-40.6; MUTECTL 41.2-41.4; MUTEDOWN at or below -14 from epoch 40.4-43.0 on 0.95-0.99 of later records (continuous pin only at 90.6-99.6); HEAD's complement 96.4-99.8. On MUTEDOWN's post-epoch-36 records the sum votes DOWN on 0.91-0.99 of records, re-carried by convs 28, 16, 25 and `bn1.weight` (0.55-0.60 of the weighted mass DOWN outside the coalition). Registered scorer run UNEDITED on `cluster_b`, exit 0; an independent parser agrees on every level, contrast, the runaway gate, branch and stamp | `OWN-STEP-NECESSARY` + `HARNESS-CLEAN` + `PATCH-BITES` + `SETS-FROM-CVT1-MUTE` + `CTL-COUNT-CLASS-NOT-MASS` + `NO-TIME-GATE` + `ONE-NETWORK-PLAINNET` + `HORIZON-100-ONLY` + `SIGMA-PRIOR-FROZEN` + `POSITIVE-CONTROL-REPRODUCES` + `MUTE50-AT-K01` + `CTL-BELOW-K01` + `FLOOR-READINGS-ARE-BOUNDS` + `TRAIN-AGREES` | **Silencing the whole DOWN coalition while every tensor shares the step size does not rescue: `cvt1`'s 'the rescue needs 50 on its own step size' survives the re-carriage objection at the coalition level (STRENGTHENED, as registered).** The OWN-STEP-NECESSARY account hits 5 of its 6 predictions (MUTECTL misses its 8-16 band by 0.18 pp, below); COALITION and NONSPECIFIC miss MUTEDOWN / MUTECTL by >= 44 pp. **Bounded, and led with (235.4):** (1) the vote was re-carried AGAIN by the next tier the eligibility bar left in, so 'no silenced set rescues' is NOT licensed; (2) `P_COAL` is positive only because the control fell 3.75 pp BELOW `k01` (MUTEDOWN - MUTE50 = +0.03 pp), and why is UNSURE; (3) descriptive, UNSURE: every scalar arm's shared step size over epochs 15-30 was LARGER than HEAD's complement's (10,749-13,845 vs 8,672 x the floor) and TEST settled by epoch 21-25 while it was still large, so the scalar arms do not fail for want of shared step size in the window where the level is set. **NOT licensed:** that no other silenced set rescues; WHY the own step size is needed; a mass-matched control; any time-gated silence; ResNet (BN or GN) or VGG; anything past 100 epochs; RULE 11. | CORRECTIONS 232 (registration and launch), 235 (landing) |
| **[Mechanism-hunt row in section 9's convention, appended at the file's end so that no existing line moves -- CORRECTIONS 236]** Is `cvt1`'s injected collapse of HEAD's complement a THRESHOLD or GRADED in the twin's weight K, and was the 0.344 residue a matter of dose? | `PlainNet18_c100` at `cvt1`'s cell, from `cvt1`'s unchanged PATCH_VOTEWEIGHT tree (no new harness code): `k01` scalar; HEAD {50} [52,1] (K = 1, BYTE-IDENTICAL to `cvt1`/`cpl2`); HEAD's grouping with `layer4.1.bn1.weight` (idx 47) x K inside the complement at K = 13, 33 (the 'initiate-but-not-sustain' interval in log thirds), 152, 691 (`cvt1`'s INJECT byte for byte) and 2000; K derived from `cvt1`'s probe records (233.3); no time gate (dose and onset confounded, disclosed); ONE batch | `cvt2`, 21 runs, `PlainNet18_c100` / CIFAR-100, SGDm+Lion, ms 1e-3, alpha0 1e-6, box -15:-2.3026, `AUGMENT=1`, `PROBE=100` + `PROBE_TENSOR=1`, 100 ep, fresh seeds {87,88,89}; ingested with `cvt3` (2,983 -> 3,019, added 36, changed 0); **15.68 GPU-h** re-summed over its own 21 `wallclock_min` values (`sacct` 15.90). **The 15 K13-K2000 rows carry HEAD's CSV cell key and are listed in `results/CORPUS-EXCLUSIONS.tsv`** (`corpus_exclusions.py --check` PASS) | **PRIMARY SHAPE from K13 / K33 / K152 against HEAD and K691: HEAD - K13 = +12.0693 pp, K13 - K691 = +22.0720 (BETWEEN); HEAD - K33 = +28.8253, K33 - K691 = +5.3160 (BETWEEN, 0.32 pp outside MATCH); HEAD - K152 = +30.0767, K152 - K691 = +4.0647 (AT-PLATEAU). PRIMARY `TOP` = K691 - K2000 = +5.2593 pp = +7.39 SE** (SE 0.7121, in-batch sigma 0.8721 over the 0.6944 prior); TRAIN +5.8187. `D_HEAD` +52.9420 pp = +74.35 SE; `P691` = HEAD - K691 = +34.1413 pp. Levels (TEST / TRAIN): `k01` 11.4767 / 11.29, HEAD 64.4187 / 78.83, K13 52.3493 / 62.02, K33 35.5933 / 36.87, K152 34.3420 / 35.38, K691 30.2773 / 30.89, K2000 25.0180 / 25.07; share of the HEAD gap kept 0.772 / 0.456 / 0.432 / 0.355 / 0.256; every seed range <= 2.80 pp. **G-BITE PASS on 21/21** (ladder runs 408-498 informative records, unweighted match 0; every interior step follows the weighted Lion direction, on 90-236 disagreeing records per ladder run). Complement pins: HEAD 99.6-99.8; K13 never (still descending, arm mean -12.16 at epoch 100; TEST still rising +0.015 to +0.028 pp per epoch); K33 at or below -14 from 36.4-37.8 (continuous pin 38.4 / 85.8 / 97.4); K152 38.2-38.4; K691 37.6-37.8; K2000 36.8-37.0. DOM epoch HEAD 44.2-49.8, K13 20.0 / 89.0 / never, K33 18.2-18.6, K152 17.0-17.2, K691 16.6-16.8, K2000 16.2-16.4. Registered scorer run UNEDITED on `cluster_b`, exit 0; an independent parser agrees on every level, contrast, shape state, TOP token and stamp | `GRADED` + `TOP-ATTENUATED` + `HARNESS-CLEAN` + `PATCH-BITES` + `ONE-NETWORK-PLAINNET` + `HORIZON-100-ONLY` + `DOSE-ONSET-CONFOUNDED` + `SIGMA-INBATCH` + `POSITIVE-CONTROL-REPRODUCES` + `K13-BETWEEN` + `K33-BETWEEN` + `K152-AT-PLATEAU` + `TRAIN-AGREES` | **The injected collapse is graded in K, and `cvt1`'s 0.344 residue is not a fixed floor: it falls to 0.256 at K2000.** `cvt1`'s INJECT replicates (K691 30.28 vs 30.14, between batch). No registered account returns this pair of words exactly: SIGN-SAT-GRADED and KNEE-AT-INITIATION hit 6 of 7 levels (K33 below its band by 2.41 pp; K13 above by 14.35), and the registered prior `TOP-FLAT` missed by 0.26 pp. **Bounded, and led with (236.4):** (1) both words sit on near bars (K33 0.32 pp and `TOP` 0.26 pp past MATCH), so `GRADED` rests on K13 alone, and K13 is still rising with an unpinned complement at 100 epochs -- its level may be a delay; (2) the sign-saturation derivation failed at K33: on each arm's own records the K needed to hold the collapse scales with K (median 12.9 at K13, 26-30 at K33, 78-96 at K691, 197-279 at K2000), so K33 held on 0.92-1.00 of its records; (3) from K33 up the complement's beta falls at Lion's maximum rate on every arm and the rungs differ by ONSET (first beta <= -8 at epoch 24.4 / 23.6-23.8 / 22.8-23.0 for K152 / K691 / K2000), so the dose effect above 152 is consistent with onset bought by dose (confounded, UNSURE); level ranks with the complement's step-size multiplier over epochs 15-30 (Spearman 0.965, descriptive). **NOT licensed:** a functional form or knee location; that the grading is a vote equilibrium; separating dose from onset; any caster but the twin; ResNet (BN or GN) or VGG; anything past 100 epochs; RULE 11. | CORRECTIONS 233 (registration and launch), 236 (landing) |
| **[Mechanism-hunt row in section 9's convention, appended at the file's end so that no existing line moves -- CORRECTIONS 240]** With `layer4.1.bn2.weight`'s vote already out of the complement's sum (HEAD's grouping), is the MAGNITUDE of its own step size the lever: does a large step size on 50 stall the network, and does a small one keep the rescue? | `PlainNet18_c100` at `cvt1`'s cell, from an isolated tree = `cvt1`'s + the additive, opt-in `PATCH_BETAHOLD` (overwrites one group's beta after every meta update and its clamp, and at init; the held group's momentum is dead state): `k01` scalar; HEAD {50} [52,1] (BYTE-IDENTICAL: the positive control); HOLDLOW (50's beta at the -15 floor from init); HOLDSHARED (50's beta := the complement's live beta; `cvt1`'s MUTE in exact arithmetic); HOLDHIGH (exogenous max-rate replay of MUTE's measured shared trajectory, `tri:9428`: peak -4.39 at epoch 18.9, clamp at 40.1); HOLDHEAD (replay of HEAD's own median 50-trajectory, `tri:5041`: the hold's positive control); no time gate; ONE batch | `cvt4`, 18 runs, `PlainNet18_c100` / CIFAR-100, SGDm+Lion, ms 1e-3, alpha0 1e-6, box -15:-2.3026, `AUGMENT=1`, `PROBE=100` + `PROBE_TENSOR=1`, 100 ep, fresh seeds {90,91,92}; ingested with `cvt5` (3,019 -> 3,049, added 30, changed 0); **14.77 GPU-h** re-summed over its own 18 `wallclock_min` values (`sacct` 14.96; four [HOST_3c1b003035] runs took 4,941-5,334 s). **The 12 HOLDLOW / HOLDSHARED / HOLDHIGH / HOLDHEAD rows carry HEAD's CSV cell key and are listed in `results/CORPUS-EXCLUSIONS.tsv` with their `BETA_HOLD` witness** (`corpus_exclusions.py --check` PASS, completeness 51/51) | **CO-PRIMARY `P_HIGH` = HEAD - HOLDHIGH = 64.7347 - 11.1267 = +53.6080 pp = +94.55 SE** (TRAIN +68.8720; per seed +54.00 / +53.24 / +53.59); **`P_COUP` = HOLDHIGH - HOLDSHARED = -0.2673 pp = -0.47 SE** (TRAIN -0.1633); **`P_LOW` = HOLDLOW - HEAD = +0.7260 pp = +1.28 SE** (TRAIN +1.5193; per seed +0.92 / +1.14 / +0.12) (SE 0.5670, the frozen prior; in-batch 0.4638). KEY `D_HEAD` +53.0400 pp = +93.54 SE; `P_CTL` = HEAD - HOLDHEAD +0.0687 pp = +0.12 SE. Levels (TEST / TRAIN): `k01` 11.6947 / 11.51, HEAD 64.7347 / 79.99, HOLDLOW 65.4607 / 81.51, HOLDSHARED 11.3940 / 11.28, HOLDHIGH 11.1267 / 11.12, HOLDHEAD 64.6660 / 79.25; every seed range <= 1.22 pp. State margins: HOLDLOW 5.73 pp and HOLDHEAD 4.93 pp inside AT-HEAD; HOLDSHARED 2.30 pp and HOLDHIGH 2.57 pp inside AT-K01. **G-BITE PASS on 18/18** (500 records each; every unheld group equals Lion recomputed, 0 mismatches, worst 4.0e-07; the held group on its schedule at every record in both beta and beta_pre; the patch's own `bh_*` record audits; no `bh_*` key on `k01` / HEAD); read against any other hold mode, every run fails on 101-398 records. Complement pins: HEAD 97.8-99.6; HOLDLOW 99.6 / never; HOLDSHARED and HOLDHIGH 40.2-40.4 (MUTE's path, within 0.06-0.16 of the replay); HEAD's own 50 pins at 20.2-22.8. Registered scorer run UNEDITED on `cluster_b`, exit 0; an independent parser rebuilds 154/154 of its lines verbatim | `OWN-STEP-MAGNITUDE` + `HARNESS-CLEAN` + `PATCH-BITES` + `HOLD-FROM-INIT` + `REPLAY-MAX-RATE-TRIANGLE` + `SHARED-IS-MUTE-ARITHMETIC` + `ONE-NETWORK-PLAINNET` + `HORIZON-100-ONLY` + `SIGMA-PRIOR-FROZEN` + `POSITIVE-CONTROL-REPRODUCES` + `HOLDLOW-AT-HEAD` + `HOLDSHARED-AT-K01` + `HOLDHIGH-AT-K01` + `HOLDHEAD-AT-HEAD` + `FLOOR-READINGS-ARE-BOUNDS` + `TRAIN-AGREES` | **Confirmed by intervention, as registered: with its vote out of the complement's sum either way, 50 on a large step size (MUTE's trajectory, replayed) stalls the arm at `k01`, and 50 frozen at the floor keeps HEAD's level; `cvt1`'s 'the rescue needs 50 on its own step size' sharpens to 'on a SMALL step size'.** OWN-STEP-MAGNITUDE hits all 9 of its registered bands; the next-closest account (OWN-STEP-GRADED) misses HOLDHIGH by 8.87 pp. **Bounded, and led with (240.4):** (1) 'complement free' is not 'complement like HEAD's' -- in HOLDHIGH the unforced complement followed MUTE's collapse path and pinned at 40.4 (HEAD 97.8-99.6), so the batch cannot say whether 50's large step size stalls the network directly or through the complement's early collapse; descriptive, UNSURE: the TEST gap is already +2.05 pp after 17 epochs while the two complements' beta differ by 0.001 (by > 0.1 only from epoch 18.0-18.2), so the stall begins before the complements part; (2) no time gate and one shape per arm, so which window of the trajectory does the damage, and any dose curve, are untested; (3) the floor readings are locations, and `P_LOW` (+0.73 pp, not a registered effect) speaks only to the first ~22 epochs, after which HEAD's own 50 is at the floor too. **NOT licensed:** that the stall is direct rather than via the complement; which window matters; a dose curve; why a large step size on this BN scale stalls; that HOLDSHARED and MUTE are the same run; any other tensor, network or horizon; RULE 11. | CORRECTIONS 237 (registration and launch), 240 (landing) |
| **[Mechanism-hunt row in section 9's convention, appended at the file's end so that no existing line moves -- CORRECTIONS 241]** Does PlainNet's HEAD rescue survive 300 epochs past its own complement's pin, and is `cvt2`'s `GRADED` (K13 between HEAD and the plateau at 100 epochs, still rising, complement unpinned) a delay or a K-dependent level? | `PlainNet18_c100` at `cvt1`'s cell, from `cvt1`'s unchanged PATCH_VOTEWEIGHT tree (no new harness code), `--num-epochs 300` with the 100-epoch control read in run: `k01` scalar; HEAD {50} [52,1]; K13 and K33 (HEAD's grouping with `layer4.1.bn1.weight` x13 / x33 in the complement, `cvt2`'s strings byte for byte); E = 300 derived from `cvt2`'s K13 records (latest predicted r <= 2 at 223.5); pin gate by step-size MAGNITUDE (median r = exp(beta+15) over the last quarter of records), read as stamps after the branches; no time gate; ONE batch | `cvt5`, 12 runs, `PlainNet18_c100` / CIFAR-100, SGDm+Lion, ms 1e-3, alpha0 1e-6, box -15:-2.3026, `AUGMENT=1`, `PROBE=100` + `PROBE_TENSOR=1`, 300 ep, fresh seeds {93,94,95}; ingested with `cvt4` (3,019 -> 3,049, added 30, changed 0); **25.03 GPU-h** re-summed over its own 12 `wallclock_min` values (`sacct` 25.15). **The 6 K13 / K33 rows carry HEAD's CSV cell key and are listed in `results/CORPUS-EXCLUSIONS.tsv`** (`corpus_exclusions.py --check` PASS); `k01` / HEAD are ordinary 300-epoch rows, kept out of 100-epoch cells by `epochs_requested` | **PRIMARY (a) `G13` = K13@300 - K33@300 = 53.0807 - 37.1347 = +15.9460 pp = +23.04 SE** (TRAIN +25.7233; +15.7167 at 100); **`DROP13` = K13@300 - K13@100 = +0.4193 pp = +0.61 SE** (TRAIN +1.5847; per seed +0.45 / +0.48 / +0.33). **PRIMARY (b) `RHO` = D_HEAD@300 / D_HEAD@100 = 52.6447 / 52.5807 = 1.0012** (TRAIN 1.0043; per seed 0.9995 / 1.0050 / 0.9992) (SE 0.6920, the frozen prior 0.8475; in-batch 0.6684). Levels at 100 / 300 (TEST; TRAIN at 300): `k01` 11.6280 / 11.6700 (11.67), HEAD 64.2087 / 64.3147 (78.87), K13 52.6613 / 53.0807 (64.15), K33 36.9447 / 37.1347 (38.43); every seed range <= 1.77 pp at either horizon. **G-BITE PASS on 12/12** (1,500 records each; K13 / K33 1,409-1,414 informative records, unweighted match 0; every interior step follows the weighted Lion direction, including the 88-723 records per K run where weighted and unweighted disagree). Pin gate, first r <= 2: `k01` 34.8-35.0; HEAD 62.2-64.0 (0 records above r 2 after; at the exact clamp on 0.71-0.79 of them); K13 159.2-161.4 (median r 1.51 / 1.62 / 1.22, but back above r 2 on 74 / 104 / 5 later records; s94 ends at r 2.59); K33 37.0-69.4 (max r after epoch 100 1.23). K13's arm-mean TEST within 0.5 pp of its end from epoch 105. Registered scorer run UNEDITED on `cluster_b`, exit 0; an independent parser rebuilds 115/115 of its printed lines and the 32-token FINAL byte for byte | `K-DEPENDENT-EQUILIBRIUM` + `RESCUE-SURVIVES` + `K33-HOLDS-PINNED` + `HARNESS-CLEAN` + `PATCH-BITES` + `ONE-NETWORK-PLAINNET` + `HORIZON-300` + `NO-TIME-GATE` + `ONE-CELL-ONLY` + `SIGMA-PRIOR-FROZEN` + `RHO:1.0012` + `TRAIN-AGREES-A` + `TRAIN-AGREES-B` + `K13-LEVEL-HOLDS` + `HEAD-LEVEL-HOLDS` + `K33-LEVEL-HOLDS` + `K13-COMPLEMENT-PINNED` + `K13-PIN-EPOCH:160.3` + `K13-POST-PIN-WINDOW:138.6` + `K13-WINDOW-MEETS-W100` + `K13-PIN-INSIDE-BOUND` + `K13-FROZEN-AT-PIN` + `HEAD-COMPLEMENT-PINNED` + `HEAD-PIN-EPOCH:63.3` + `HEAD-POST-PIN-WINDOW:236.0` + `HEAD-WINDOW-MEETS-W100` + `HEAD-FROZEN-AT-PIN` + `K33-COMPLEMENT-PINNED` + `K33-MAXR-POST100:1.2325` + `K33-EXACT-CLAMP-CONTINUOUS` + `K01-PINNED` + `FLOOR-HOLDS` | **(a) `cvt2`'s `GRADED` is NOT a delay at 300 epochs: K13 holds 15.95 pp above K33 and moved +0.42 pp from its in-run 100-epoch level, so the partial level is K-dependent. (b) PlainNet's HEAD rescue survives: RHO 1.0012, the level frozen for the 236 epochs after its complement's magnitude pin -- `cvh1`'s / `cgn3`'s sentence on a fourth network. (c) K33 stays pinned.** EQUILIBRIUM + FREEZE-HOLDS hits all 8 registered bands; every other account misses at least one band by >= 6.92 pp. **Bounded, and led with (241.4):** (1) no branch bar is near (closest: `DROP13` 4.58 pp under +5), but every level-holds and frozen-at-pin stamp sits only 0.96-1.34 pp inside READ_BAR 1.384 -- locations, not tight nulls; (2) K13's 'pinned' is a median hovering near the floor, not a clamp: arm-mean beta rises -14.95 -> -14.54 over epochs 200-299, s94's complement ends above r 2, and the DOWN fraction stays near 0.5 -- its step size is small, not frozen; (3) the level stopped moving before the pin (settled at 105, first r <= 2 at ~160, TRAIN rising to ~160), so `K13-FROZEN-AT-PIN` is true as registered but 'the pin froze the level' is not licensed. The scorer's printed RESCUE-SURVIVES licence says the exact clamp came 'at ~96-100'; in this batch HEAD's exact-clamp dwell starts at 299.8 / never / 299.8 (registered text, not edited; no token reads it). **NOT licensed:** anything past 300 epochs; any K but 13 and 33 at long horizon; that K13's step size is frozen; dose vs onset; the ceiling (no `kL`); ResNet (BN or GN) or VGG; RULE 11. | CORRECTIONS 238 (registration and launch), 241 (landing) |
