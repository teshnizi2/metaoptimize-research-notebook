#!/usr/bin/env python3
"""g3b_design.py -- THE FROZEN DESIGN TABLE OF `g3b` (CORRECTIONS 299; ICML-PLAN s4a rank 1, "G3b"):
DO `SCALAR-BEATS-BEST` AND THE COUNT-MATCHED SIGN HOLD ON THE AUDIT'S SECOND DATASET, AND DOES THE AUDIT'S CIFAR-100
MetaOptimize ARM AT 5e-4 REMOVE 1.16's DENOMINATOR DECAY CONFOUND?

WHY THIS BATCH EXISTS.  `cgw1` (CORRECTIONS 296) is the ONLY evidence under the rewritten TMLR headline (297): on
ResNet18 / CIFAR-10 the count-matched sign (chunk777 - nodewise) is resolved at alpha-scaled wd 0.1 (+0.3693) and 1e-2
(+0.4533) and UNDECIDED at 5e-4 (+0.2410, +/-2 SE [-0.0120, +0.4940]); at 5e-4 plain scalar beats both partitions by
2.6-2.8 pp (SCALAR-BEATS-BEST).  One cell.  The audit's second dataset is CIFAR-100 (`gc1` D +1.640, `gm2` D +1.485 at
wd 0.1; fixed-effect pool +1.5602 +/- 0.1705, re-derived below from the filtered corpus), and its denominator result
(`cdn1`: tuned SGD + cosine at lr 0.1 is +5.699 pp above the in-batch MetaOptimize arm `cdn1-m`, 175) compares a
MetaOptimize arm run at alpha-scaled 0.1 against an SGD run at the standard 5e-4 (ICML-PLAN 1.16, PARTIAL).  `g3b`
re-runs the audit's CIFAR-100 cell (`gc1` / `gm2` / `cdn1-m`) at 0.1 (in-batch anchor) and 5e-4 (primary) with all four
grains in batch at both rungs, so (i) the count-matched sign, (ii) scalar against the better partition, and (iii) the
`cdn1-m` arm (chunk771) at 5e-4 against the landed tuned-SGD level are read side by side with `cgw1`.

THE CELL, VERBATIM from the landed ARGS lines (`gc1-ch-s0`, `gm2-*`, `cdn1-m-*`, `cru1-*-m1e-4-a1e-3-*`, read at
registration): ResNet18_c100 / CIFAR-100 / bs 100 / SGDm 0.99 / Lion 0.99 / beta2 0.9 / meta wd 0 / gamma 1 /
ms 1e-4 / alpha0 1e-3 / 100 epochs / AUGMENT=1 / BETA_CLIP=-15:-2.3026 / PROBE=5 -- EXCEPT `--weight-decay-base`,
which is the axis, and `--stepsize-groups`, the grain.  20 ARGS flags in the audit's own order.  No patch, no hold
variable, no mask.  (The landed scalar / layerwise arms at this cell are `cru1`'s, run at PROBE=100; that difference
is disclosed and touches only the non-gating level stamps.)

THE HARNESS'S DECAY IS ALPHA-SCALED (CORRECTIONS 292/293): `SGDm_base_update` is  delta = a*(m + wd*w);  w <- w - delta;
h <- gamma*(1 - wd*a)*h - delta.  It is not L2 and not Loshchilov & Hutter's decoupled decay.  Every rung is this
decay; alpha-independent decay is not tested (bound on every FINAL).

THE TREE is an ISOLATED byte copy of the live tree (bin/cG3B_stage_harness.sh), pinned to the SAME six shas `cgw1`
ran on (re-verified on cluster_b at registration: the live tree has not moved).  The premise (the passed decay reaches the
update bitwise at 0.1 and 5e-4) is inherited by SOURCE-TEXT identity of `SGDm_base_update` with harness_cwd1's HF.py,
whose landed real-GPU bite log (`cwd5`, RW2) proved it on an L4 -- `cgw1`'s route (291.3), re-checked by the launcher's
guards 4e / 4f.  Why a copy: a PENDING job imports what is on disk when it STARTS, and three other tracks share $WS.

Stdlib only (no torch).  Imported, UNEDITED, by analysis/cG3B_c100wd_score.py, analysis/g3b_live_check.py and
bin/cG3B_c100wd.sh.   python3 analysis/g3b_design.py   prints the manifest.
"""
import hashlib
import os

HERE = os.path.dirname(os.path.abspath(__file__))
REPO = os.path.dirname(HERE)

PREFIX = "g3b"
NET = "ResNet18_c100"
DSET = "CIFAR100"
EPOCHS = 100
BATCH = 100
STEPS_PER_EPOCH = 50000 // BATCH             # 500
PROBE = 5
N_RECORDS = EPOCHS * STEPS_PER_EPOCH // PROBE   # 10,000
CLIP = "-15:-2.3026"
LO, HI = -15.0, -2.3026
MST = "1e-4"
A0 = "1e-3"
AUG = "1"
GAMMA = "1"
WALL = "02:00:00"
GPU_CONSTRAINT = "L4"
GPU_NAME = "NVIDIA L4"
NTENS = 62
TOTPAR = 11220132

# the tree: the SAME pinned bytes as cgw1 (analysis/cgw1_design.py), re-verified on cluster_b at registration
HF_LIVE_SHA = "4732b74aa3a10508896e92eccced5c89051c353fa8a01a3af21aab9aeda0cecd"
TRAIN_SHA = "3fea309e172609c1ee1be143d5f1847404426df31869a63c4cefbb1ebddfcab7"
BN_SHA = "c79988833c4399a06cc84d15c0830f54d86f59feeec345f47a2287c1cf2ba77d"
BUILDOPT_SHA = "25a899b3745e9d66fbf630795a1202c6075e2067daffa077e4a54c28e54c7ec2"
LOADDATA_SHA = "b52b58a35dcecd80ce6d8f8033b09c20dad9f4827b808f4ea2a828ce88138c46"
TINDATA_SHA = "9e0f3322e5025e966aa18e6ac39c1d1b347c9ce2bf6e50423f301e965a6eecb7"
RUNNER_SRC_SHA = "a0d0a1b904e64ea5a3de8c0649b58454beff0322419c97ea9e6d77481cfd9f3a"   # $WS/jobs/run_cifar.sh
TREE = "harness_g3b"
RUNNER_NAME = "run_cifar_g3b.sh"

# ---- THE LADDER: two rungs.  The CLI token is what the ARGS line echoes and what --expect / corpus_exclusions compare.
RUNGS = (("W1", "0.1", 0.1, "the audit's own value -- gc1 / gm2 / cdn1-m ran here; the IN-BATCH anchor"),
         ("W4", "5e-4", 0.0005, "the standard CIFAR value, 200x below the anchor; THE PRIMARY RUNG; cdn1's SGD ran here"))
RUNG_IDS = tuple(r[0] for r in RUNGS)
WD_TOKEN = dict((r[0], r[1]) for r in RUNGS)
WD_VALUE = dict((r[0], r[2]) for r in RUNGS)
ANCHOR = "W1"
PRIMARY = "W4"
STANDARD_WD_TOKEN = "0.1"      # corpus_exclusions ARGS_KINDS standard for ARGS_WD_BASE (CORRECTIONS 263)

# ---- THE GRAINS.  (arm prefix, --stepsize-groups token, registered group count m on the live ResNet18_c100) ----------
GRAINS = (("ch", "chunk771", 14595),     # uniform contiguous chunks of 771 -- the audit's CIFAR-100 uniform partition
          ("nd", "nodewise", 14600),     # one step size per output channel -- the architecture-aligned partition
          ("k01", "scalar", 1),          # plain shared step size
          ("kL", "layerwise", 62))       # one step size per tensor
GRAIN_IDS = tuple(g[0] for g in GRAINS)
SPEC_OF_GRAIN = dict((g[0], g[1]) for g in GRAINS)
M_OF_GRAIN = dict((g[0], g[2]) for g in GRAINS)

# ---- SEEDS: the assigned block {170..173}, verified FREE at registration (0 corpus rows carry any of 170-173, the
# ---- corpus max seed is 162; 0 `.out` ARGS lines under $WS/runs on cluster_b carry --seed 170..173; 0 g3b-* jobs).
# ---- 4 seeds at BOTH rungs, all four grains.  WHY 4 (299.4): at the frozen floor 0.2734 a 4 v 4 contrast has SE 0.1933;
# ---- P(SURVIVES | D = +1.56) ~1.000, P(SURVIVES | D = +0.60) 0.865, P(VANISHES | D = 0) 0.781.  3 v 3 gives
# ---- 1.000 / 0.754 / 0.749; 5 v 5 needs seed 174, outside the block.  No seed may be added after any run is read (108.6a).
SEED_BLOCK = (170, 171, 172, 173)
SEEDS_OF_RUNG = {"W1": (170, 171, 172, 173), "W4": (170, 171, 172, 173)}
UNUSED_SEEDS = ()

ARMS = tuple("%s%s" % (g, r) for r in RUNG_IDS for g in GRAIN_IDS)
RUNG_OF = dict(("%s%s" % (g, r), r) for r in RUNG_IDS for g in GRAIN_IDS)
GRAIN_OF = dict(("%s%s" % (g, r), g) for r in RUNG_IDS for g in GRAIN_IDS)
SPEC = dict((a, SPEC_OF_GRAIN[GRAIN_OF[a]]) for a in ARMS)
M_OF_ARM = dict((a, M_OF_GRAIN[GRAIN_OF[a]]) for a in ARMS)
WD = dict((a, WD_TOKEN[RUNG_OF[a]]) for a in ARMS)
WDF = dict((a, WD_VALUE[RUNG_OF[a]]) for a in ARMS)
SEEDS = dict((a, SEEDS_OF_RUNG[RUNG_OF[a]]) for a in ARMS)
RUNS = tuple((a, s) for a in ARMS for s in SEEDS[a])
NJOBS = len(RUNS)                                            # 16 + 16 = 32
ARGS_DEVIATING = tuple(a for a in ARMS if WD[a] != STANDARD_WD_TOKEN)
N_EXCLUSION_ROWS = sum(len(SEEDS[a]) for a in ARGS_DEVIATING)   # 16


def run_name(arm, seed):
    return "%s-%s-s%d" % (PREFIX, arm, seed)


def expected_args(arm, seed, save_dir):
    """The run's ARGS line as (flag, value) pairs IN ORDER -- 20 flags, every value ONE shell token (gc1's order)."""
    return [("optimizer", "HF"), ("alg-base", "SGDm"), ("momentum-param-base", "0.99"),
            ("weight-decay-base", WD[arm]), ("alg-meta", "Lion"), ("momentum-param-meta", "0.99"),
            ("Lion-beta2-meta", "0.9"), ("weight-decay-meta", "0"), ("dataset", DSET), ("NN-name", NET),
            ("batch-size", str(BATCH)), ("max-time", "999:00:00"), ("gamma", GAMMA), ("meta-stepsize", MST),
            ("alpha0", A0), ("num-epochs", str(EPOCHS)), ("stepsize-groups", SPEC[arm]), ("seed", str(seed)),
            ("save-directory", save_dir), ("run-name", run_name(arm, seed))]


def args_line(arm, seed, save_dir):
    return "ARGS: " + " ".join("--%s %s" % kv for kv in expected_args(arm, seed, save_dir))


# the runner's ENV line with the PROBE_DIR VALUE replaced by <P> (run_cifar.sh's, byte-equal to cdn1-m / gm2 on cluster_b)
ENV_TEMPLATE = ("ENV: AUGMENT=1 BETA_CLIP=-15:-2.3026 HIER=none LAM=na ETA_RATIO=na COS_TOTAL=default "
                "COS_WARMUP=default SCHED=none SCHED_TOTAL=none SCHED_WARMUP=none SCHED_MIN=none PROBE=5 "
                "PROBE_DIR=<P> EB_RHO=na EB_LOG=0")


def probe_dir_name(arm, seed):
    return "probe_%s" % run_name(arm, seed)


# ---- LANDED at THIS EXACT CELL at wd 0.1, FILTERED corpus (3,383 rows -> 3,115 through corpus_exclusions.filter_rows;
# ---- identical unfiltered: no exclusion row touches this cell).  BETWEEN-BATCH; for predictions and NON-GATING stamps.
LANDED_W1 = {"ch": 71.9648, "nd": 70.4220, "k01": 35.7920, "kL": 71.0827}   # n 10 / 7 / 3 / 3
LANDED_W1_N = {"ch": 10, "nd": 7, "k01": 3, "kL": 3}      # ch = gc1 4 + gm2 3 + cdn1-m 3; nd = gc1 4 + gm2 3; k01/kL = cru1
# the landed CIFAR-100 count-matched pool: per batch D = mean(ch) - mean(nd), SE from the batch's pooled within-arm sd
LANDED_D = {"gc1": (1.6400, 0.2446), "gm2": (1.4847, 0.2378)}
C100_POOL_D = 1.5602            # fixed-effect over gc1 + gm2, +/- 0.1705 (MASTER-TABLE rows 191 / 199 quote 1.640 / 1.485)
C100_POOL_SE = 0.1705
# the denominator: cdn1's tuned SGD + cosine at its argmax lr 0.1 (plateau5 77.526 / 78.048 / 77.204; sd 0.4259) and
# its in-batch MetaOptimize arm cdn1-m (chunk771, THIS cell at wd 0.1: 71.814 / 71.958 / 71.908) -> GAP_in +5.6993
SGD_LANDED = 77.5927
SGD_LANDED_SD = 0.4259
SGD_LANDED_N = 3
CDN1_M_LANDED = 71.8933
CDN1_GAP_IN = 5.6993
# the cgw1 side-by-side (CORRECTIONS 296.3), DESCRIPTIVE ONLY, never read by a bar
CGW1_D = {"W1": 0.3693, "W4": 0.2410}
CGW1_T4 = {"ch": 2.5590, "nd": 2.8000}

# ---- COST (299.4).  Per-run L4 minutes at THIS cell from the corpus's own wallclock_min, L4 nodes 880-887 only:
# ---- chunk771 51 (gc1 / gm2 / cdn1-m, n 6 on 880-887: 49-65), nodewise 43 (n 3: 39-46), scalar 42 (cru1 n 2, PROBE=100),
# ---- layerwise 31 (cru1 n 3, PROBE=100).  cgw1 ran +10 % over its estimate (32.4 v 29.3), and PROBE=5 costs more than
# ---- PROBE=100, so each is padded: +4 min.
MIN_PER_RUN_L4 = {"ch": 55, "nd": 47, "k01": 46, "kL": 35}
EXPECTED_GPU_H = sum(MIN_PER_RUN_L4[GRAIN_OF[a]] for a, _s in RUNS) / 60.0      # 24.40
HARD_BOUND_GPU_H = NJOBS * 2.0                                                 # WALL x NJOBS = 64

# ---- ACCOUNTS AND PREDICTED LEVELS (299.5).  PRIORS, UNSURE: no MetaOptimize run at THIS cell has ever run at a decay
# ---- other than 0.1.  Anchored on (i) the landed W1 levels above; (ii) cgw1's 5e-4 ladder on CIFAR-10 (partitions
# ---- -4.4 to -4.5 pp, scalar -1.7, layerwise -3.0 from 0.1); (iii) cwd5's k01W4 72.408 / kLW4 67.881 on this network at
# ---- the mechanism cell (ms 1e-3 / alpha0 1e-6, a DIFFERENT cell).  k01W1 is PREDICTED COLLAPSED (cru1: 35.79) under
# ---- every account; it enters no gated contrast.  (lo, hi) plateau5 per arm at W4.
ACCOUNTS = ("AUDIT-SURVIVES", "AUDIT-VANISHES", "AUDIT-REVERSES", "SCALAR-TIES-OR-BEATS-BEST")
PRED_W1 = {"ch": (71.4, 72.5), "nd": (69.9, 70.9), "k01": (25.0, 50.0), "kL": (70.5, 71.6)}
PRED_W4 = {
    "AUDIT-SURVIVES": {"ch": (66.5, 71.5), "nd": (65.0, 70.5), "k01": (68.0, 73.5), "kL": (66.0, 70.5)},
    "AUDIT-VANISHES": {"ch": (65.5, 70.5), "nd": (65.5, 70.5), "k01": (68.0, 73.5), "kL": (66.0, 70.5)},
    "AUDIT-REVERSES": {"ch": (65.0, 70.0), "nd": (65.5, 70.5), "k01": (68.0, 73.5), "kL": (66.0, 70.5)},
    "SCALAR-TIES-OR-BEATS-BEST": {"ch": (65.5, 71.0), "nd": (65.0, 70.5), "k01": (69.0, 73.5), "kL": (66.0, 70.5)},
}
PRED_D4 = {"AUDIT-SURVIVES": (0.30, 1.80), "AUDIT-VANISHES": (-0.15, 0.15), "AUDIT-REVERSES": (-1.00, -0.15)}
PRIOR_P = {"AUDIT-SURVIVES": 0.55, "AUDIT-VANISHES": 0.15, "AUDIT-REVERSES": 0.05, "AUDIT-UNDECIDED": 0.25,
           "SCALAR-TIES-OR-BEATS-BEST": 0.55, "DENOM-HOLDS": 0.85}      # judgements, not measurements
CHANCE = 1.0                                            # CIFAR-100 chance, the floor for the 164.6 gate


def manifest_text():
    L = ["BATCH %s" % PREFIX, "NETWORK %s" % NET, "DATASET %s" % DSET, "NUM_PARAM_TENSORS %d" % NTENS,
         "TOTAL_PARAMS %d" % TOTPAR, "CLIP_C %s" % CLIP, "MS %s ALPHA0 %s" % (MST, A0), "EPOCHS %d" % EPOCHS,
         "META_STEPS %d" % (EPOCHS * STEPS_PER_EPOCH), "PROBE %d N_RECORDS %d" % (PROBE, N_RECORDS),
         "WD_LADDER %s" % ",".join("%s=%s" % (r, WD_TOKEN[r]) for r in RUNG_IDS)]
    for g, spec, m in GRAINS:
        L.append("GRAIN %s SPEC %s M %d" % (g, spec, m))
    for a in ARMS:
        L.append("ARM %s RUNG %s WD %s SPEC %s M %d SEEDS %s" % (a, RUNG_OF[a], WD[a], SPEC[a], M_OF_ARM[a],
                                                                ",".join(str(s) for s in SEEDS[a])))
    L.append("NJOBS %d" % NJOBS)
    return "\n".join(L) + "\n"


def file_sha(path):
    return hashlib.sha256(open(path, "rb").read()).hexdigest()


if __name__ == "__main__":
    print(manifest_text())
    print("ARGS-DEVIATING ARMS (owe an ARGS_WD_BASE exclusion row per seed, %d rows): %s"
          % (N_EXCLUSION_ROWS, ARGS_DEVIATING))
    print("EXPECTED %.2f GPU-h, HARD BOUND %.0f GPU-h" % (EXPECTED_GPU_H, HARD_BOUND_GPU_H))
