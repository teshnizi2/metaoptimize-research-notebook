#!/usr/bin/env python3
"""cgw1_design.py -- THE FROZEN DESIGN TABLE OF `cgw1` (CORRECTIONS 291, gate G3 of docs/ICML-PLAN.md):
DOES THE COUNT-MATCHED PARTITION AUDIT SURVIVE AT STANDARD WEIGHT DECAY?

WHY THIS BATCH EXISTS.  The count-matched partition audit (MASTER-TABLE section 10, CORRECTIONS 211) -- uniform
`chunk777` beats architecture-aligned `nodewise` at matched group count (m 14,421 v 14,420) in 20 of 20 cells, SGDm
fixed-effect pool +0.5556 +/- 0.0448 pp -- ran EVERY cell at `--weight-decay-base 0.1`.  In this harness that decay
is ALPHA-SCALED: `SGDm_base_update` is  delta = a*(m + wd*w);  w <- w - delta;  h <- gamma*(1 - wd*a)*h - delta,
i.e. the decay sits OUTSIDE the momentum buffer (so it is not L2) and is multiplied by the LEARNED step size a.  It is
NOT Loshchilov & Hutter's decoupled decay in their own parameterisation (arXiv:1711.05101, Algorithm 1 line 9:
theta <- theta - m - eta_t*lambda*theta), where the decay is scaled by the SCHEDULE MULTIPLIER eta_t and NOT by the
step size alpha (CORRECTIONS 291.2, the verifier's V3; this paragraph's first draft carried the same slip).
`cwd5` (CORRECTIONS 285) showed that on the mechanism cell the scalar-vs-layerwise ranking REVERSES between 0.1 and
5e-4, and plain scalar at 5e-4 (`cwd5` k01W4 72.408, ResNet18_c100 at ms 1e-3 / alpha0 1e-6) sits ABOVE the audit's
best CIFAR-100 partition (`gm2` chunk771 72.054; next `gm2` chunk2293 72.000; both at ms 1e-4 / alpha0 1e-3, so the
comparison is CROSS-CELL and CROSS-BATCH).  Re-derived at 291.2 from results/all_runs.csv (the "72.41" that
ICML-PLAN printed as an audit figure is the scalar value itself; V4).  So the audit's +0.56 pp may itself be a 0.1
artefact.  `cgw1` re-runs the audit's CORE cell at 5e-4 and 1e-2 with the 0.1 anchor IN BATCH, and puts plain SCALAR
and LAYERWISE arms in the SAME batch at every rung so "plain scalar ties the audit's best partition" is read WITHIN
batch and within cell.

WHY THIS CELL AND ONLY THIS ONE (291.3).  It is the audit's most load-bearing cell: the headline pool the TMLR draft
quotes (+0.5556, 8 SGDm cells) takes 4 of its 8 cells from this exact configuration (`mm1`, `pp1`, `cc1`, `gn1`-BN),
the other four are one-axis variants of it (`rl3` box, `fa1` ms/box, `hz3` budget), and 13 further audit batches
(`pp1`, `bn1`, `ar1`, `aw1`, `nl1`, `bm2`, `sm4`, `rp1`, ...) change ONE variable from it.  If the gap vanishes here at
standard decay, no other audit cell can carry the headline.  The CIFAR-100 cells (`gc1`, `gm2`; the largest D) are
NOT in this batch: a second cell at the same three rungs and four grains costs a second ~29 GPU-h and breaks the
~30 GPU-h ceiling.  Bound `ONE-CELL` is printed on every FINAL, and the CIFAR-100 threat is answered here only in
kind (within-cell scalar vs best partition), never for CIFAR-100 itself.

THE CELL is the SGDm-pool cell VERBATIM (`mm1` / `pp1` / `cc1` / `gn1`-BN, four of the eight SGDm pool cells, all
this exact configuration): ResNet18 / CIFAR-10 / bs 100 / SGDm 0.99 / Lion 0.99 / 0.9 / meta wd 0 / gamma 1 /
ms 1e-4 / alpha0 1e-3 / 100 epochs / AUGMENT=1 / BETA_CLIP=-15:-2.3026 / PROBE=5 -- EXCEPT `--weight-decay-base`,
which is the axis.  No patch, no hold variable, no mask on any arm.

THE ONE AXIS IS AN EXISTING REGISTERED CLI FLOAT (`train.py --weight-decay-base`, default 0.1; `cmo1` ran 0, `cwd5`
ran 1e-2 / 1e-3 / 5e-4).  The tree is an ISOLATED byte-copy of the live tree (`bin/cGW1_stage_harness.sh`): HF.py
4732b74a... (the `HF_LIVE_SHA` of analysis/cwd_design.py), train.py 3fea309e..., build_network.py c7998883....
WHY A COPY AND NOT THE LIVE TREE: a PENDING Slurm job imports whatever HF.py is on disk when it STARTS, and three
other tracks share this workspace; the copy freezes the bytes the guards tested for all 40 jobs.  No patch is applied.
`SGDm_base_update` (compared as SOURCE TEXT by the live check), `build_optimizer.py` and `train.py` are identical to
the tree `cwd5`'s bite job ran on (harness_cwd1; its HF.py differs from the live one ONLY by env-gated PATCH regions
that are absent here), and that job's RW2 proved on the real GPU path that the passed weight decay reaches the update
BITWISE at 0.1, 1e-2, 1e-3 and 5e-4 -- a superset of this ladder's three tokens.  The premise therefore carries over
by code identity (291.3, re-checked by the launcher's guard 4f against the landed log), and every run's own ARGS line
pins its token (RULE 20).

Stdlib only (no torch).  Imported, UNEDITED, by
    analysis/cGW1_auditwd_score.py   (the registered scorer)
    bin/cGW1_auditwd.sh              (the launcher's guards 4 / 4c')
    python3 analysis/cgw1_design.py  # prints the manifest
"""
import hashlib
import os

HERE = os.path.dirname(os.path.abspath(__file__))
REPO = os.path.dirname(HERE)

PREFIX = "cgw1"
NET = "ResNet18"
DSET = "CIFAR10"
EPOCHS = 100
BATCH = 100
STEPS_PER_EPOCH = 50000 // BATCH             # 500
PROBE = 5
N_RECORDS = EPOCHS * STEPS_PER_EPOCH // PROBE   # 10,000 (records at counter % 5 == 0)
CLIP = "-15:-2.3026"
LO, HI = -15.0, -2.3026
MST = "1e-4"
A0 = "1e-3"
AUG = "1"
GAMMA = "1"
WALL = "02:00:00"
GPU_CONSTRAINT = "L4"
GPU_NAME = "NVIDIA L4"
NTENS = 62
TOTPAR = 11173962

# the tree: an isolated byte copy of the live tree (the audit's lineage); pinned
HF_LIVE_SHA = "4732b74aa3a10508896e92eccced5c89051c353fa8a01a3af21aab9aeda0cecd"
TRAIN_SHA = "3fea309e172609c1ee1be143d5f1847404426df31869a63c4cefbb1ebddfcab7"
BN_SHA = "c79988833c4399a06cc84d15c0830f54d86f59feeec345f47a2287c1cf2ba77d"
BUILDOPT_SHA = "25a899b3745e9d66fbf630795a1202c6075e2067daffa077e4a54c28e54c7ec2"
LOADDATA_SHA = "b52b58a35dcecd80ce6d8f8033b09c20dad9f4827b808f4ea2a828ce88138c46"
TINDATA_SHA = "9e0f3322e5025e966aa18e6ac39c1d1b347c9ce2bf6e50423f301e965a6eecb7"
RUNNER_SRC_SHA = "a0d0a1b904e64ea5a3de8c0649b58454beff0322419c97ea9e6d77481cfd9f3a"   # $WS/jobs/run_cifar.sh
TREE = "harness_cgw1"
RUNNER_NAME = "run_cifar_cgw1.sh"

# ---- THE LADDER.  The CLI token is what the ARGS line echoes and what --expect / corpus_exclusions compare. ----------
RUNGS = (("W1", "0.1", 0.1, "the audit's own value -- every one of the 20 cells ran here; the IN-BATCH anchor"),
         ("W2", "1e-2", 0.01, "one decade down -- the rung at which cwd5's collapse had already vanished"),
         ("W4", "5e-4", 0.0005, "the standard CIFAR value -- 200x below the anchor; THE PRIMARY RUNG"))
RUNG_IDS = tuple(r[0] for r in RUNGS)
WD_TOKEN = dict((r[0], r[1]) for r in RUNGS)
WD_VALUE = dict((r[0], r[2]) for r in RUNGS)
ANCHOR = "W1"
PRIMARY = "W4"
STANDARD_WD_TOKEN = "0.1"      # corpus_exclusions ARGS_KINDS standard for ARGS_WD_BASE (CORRECTIONS 263)

# ---- THE GRAINS.  (arm prefix, --stepsize-groups token, registered group count m on the live ResNet18) --------------
GRAINS = (("ch", "chunk777", 14421),     # uniform contiguous chunks of 777 weights -- the audit's winning partition
          ("nd", "nodewise", 14420),     # one step size per output channel -- the architecture-aligned partition
          ("k01", "scalar", 1),          # plain shared step size
          ("kL", "layerwise", 62))       # one step size per tensor
GRAIN_IDS = tuple(g[0] for g in GRAINS)
SPEC_OF_GRAIN = dict((g[0], g[1]) for g in GRAINS)
M_OF_GRAIN = dict((g[0], g[2]) for g in GRAINS)

# ---- SEEDS: fresh block {152..159} (verified free at 291.3: 0 corpus rows and 0 .out ARGS lines on cluster_b carry any
# ---- of 149-159; caw1 holds 149-151).  4 seeds at the PRIMARY rung (all four grains), 3 at W1 and W2.  WHY 4 AND NOT 5
# ---- (291.4): at the frozen floor 0.1789 a 4 v 4 primary contrast has SE 0.1265; P(SURVIVES | D = +0.5556) 0.978,
# ---- P(SURVIVES | D = +0.40) 0.785, P(VANISHES | D = 0) 0.882.  5 v 5 buys 0.988 / 0.812 / 0.908 for +2.9 GPU-h and
# ---- breaks the ~30 GPU-h ceiling; 3 v 3 gives 0.960 / 0.753 / 0.848.  The anchor W1 only has to clear +0.15:
# ---- P(D1_hat > +0.15 | D1 = +0.5556) at 3 v 3 is 0.997.  Seeds may NOT be added after any run is read (108.6a).
SEED_BLOCK = tuple(range(152, 160))
SEEDS_OF_RUNG = {"W1": (152, 153, 154), "W2": (152, 153, 154), "W4": (152, 153, 154, 155)}
UNUSED_SEEDS = (156, 157, 158, 159)

ARMS = tuple("%s%s" % (g, r) for r in RUNG_IDS for g in GRAIN_IDS)
RUNG_OF = dict(("%s%s" % (g, r), r) for r in RUNG_IDS for g in GRAIN_IDS)
GRAIN_OF = dict(("%s%s" % (g, r), g) for r in RUNG_IDS for g in GRAIN_IDS)
SPEC = dict((a, SPEC_OF_GRAIN[GRAIN_OF[a]]) for a in ARMS)
M_OF_ARM = dict((a, M_OF_GRAIN[GRAIN_OF[a]]) for a in ARMS)
WD = dict((a, WD_TOKEN[RUNG_OF[a]]) for a in ARMS)
WDF = dict((a, WD_VALUE[RUNG_OF[a]]) for a in ARMS)
SEEDS = dict((a, SEEDS_OF_RUNG[RUNG_OF[a]]) for a in ARMS)
RUNS = tuple((a, s) for a in ARMS for s in SEEDS[a])
NJOBS = len(RUNS)                                            # 12 + 12 + 16 = 40
ARGS_DEVIATING = tuple(a for a in ARMS if WD[a] != STANDARD_WD_TOKEN)
N_EXCLUSION_ROWS = sum(len(SEEDS[a]) for a in ARGS_DEVIATING)   # 12 + 16 = 28


def run_name(arm, seed):
    return "%s-%s-s%d" % (PREFIX, arm, seed)


def expected_args(arm, seed, save_dir):
    """The run's ARGS line as (flag, value) pairs IN ORDER -- 20 flags, every value ONE shell token."""
    return [("optimizer", "HF"), ("alg-base", "SGDm"), ("momentum-param-base", "0.99"),
            ("weight-decay-base", WD[arm]), ("alg-meta", "Lion"), ("momentum-param-meta", "0.99"),
            ("Lion-beta2-meta", "0.9"), ("weight-decay-meta", "0"), ("dataset", DSET), ("NN-name", NET),
            ("batch-size", str(BATCH)), ("max-time", "999:00:00"), ("gamma", GAMMA), ("meta-stepsize", MST),
            ("alpha0", A0), ("num-epochs", str(EPOCHS)), ("stepsize-groups", SPEC[arm]), ("seed", str(seed)),
            ("save-directory", save_dir), ("run-name", run_name(arm, seed))]


def args_line(arm, seed, save_dir):
    return "ARGS: " + " ".join("--%s %s" % kv for kv in expected_args(arm, seed, save_dir))


# the runner's ENV line with the PROBE_DIR VALUE replaced by <P>; identical on every arm (the runner carries no wd)
ENV_TEMPLATE = ("ENV: AUGMENT=1 BETA_CLIP=-15:-2.3026 HIER=none LAM=na ETA_RATIO=na COS_TOTAL=default "
                "COS_WARMUP=default SCHED=none SCHED_TOTAL=none SCHED_WARMUP=none SCHED_MIN=none PROBE=5 "
                "PROBE_DIR=<P> EB_RHO=na EB_LOG=0")


def probe_dir_name(arm, seed):
    return "probe_%s" % run_name(arm, seed)


# ---- landed levels at THIS EXACT CELL at wd 0.1, from the FILTERED corpus (3,316 rows -> 3,094), for the predicted
# ---- levels and ONE non-gating replication stamp.  BETWEEN-BATCH; no bar, sigma, state or contrast reads them. ----
LANDED_W1 = {"ch": 92.562, "nd": 92.044, "k01": 92.262, "kL": 92.887}   # n 19 / 32 / 5 / 11
LANDED_W1_N = {"ch": 19, "nd": 32, "k01": 5, "kL": 11}
SGDM_POOL_D = 0.5556            # MASTER-TABLE line 175 / CORRECTIONS 211.2, fixed-effect, 8 cells, +/- 0.0448

# ---- COST (291.4).  Per-run L4 minutes at THIS cell from the corpus's own wallclock_min (nodes 880-887, 100 ep,
# ---- PROBE=5): chunk777 52 (n 10), nodewise 44 (n 20), scalar 42 (n 6), layerwise 34 (n 17), +1 min start-up each.
MIN_PER_RUN_L4 = {"ch": 53, "nd": 45, "k01": 43, "kL": 35}
EXPECTED_GPU_H = sum(MIN_PER_RUN_L4[GRAIN_OF[a]] for a, _s in RUNS) / 60.0      # 29.33
HARD_BOUND_GPU_H = NJOBS * 2.0                                                 # WALL x NJOBS = 80

# ---- ACCOUNTS AND PREDICTED LEVELS PER ARM (291.5).  PRIORS, UNSURE: no MetaOptimize ResNet18 / CIFAR-10 run has ever
# ---- run at a weight decay other than 0.1, so every W2 / W4 level below is a judgement, anchored on (i) the landed
# ---- W1 levels above and (ii) the realised per-step shrink a*wd, which at a ~ 1e-3 falls from ~1e-4 at 0.1 to
# ---- ~5e-7 at 5e-4 (near-undecayed training; a CIFAR-10 ResNet18 typically loses 0.5-1.5 pp without decay).
# ---- (lo, hi) plateau5 per arm at W4.  W1 is predicted AT the landed level +/- 0.5 under every account.
ACCOUNTS = ("AUDIT-SURVIVES", "AUDIT-VANISHES", "AUDIT-REVERSES", "SCALAR-TIES-OR-BEATS-BEST")
PRED_W4 = {
    "AUDIT-SURVIVES": {"ch": (91.0, 92.4), "nd": (90.5, 91.9), "k01": (90.5, 92.2), "kL": (91.0, 92.6)},
    "AUDIT-VANISHES": {"ch": (90.5, 92.0), "nd": (90.5, 92.0), "k01": (90.5, 92.2), "kL": (91.0, 92.6)},
    "AUDIT-REVERSES": {"ch": (90.3, 91.7), "nd": (90.6, 92.0), "k01": (90.5, 92.2), "kL": (91.0, 92.6)},
    "SCALAR-TIES-OR-BEATS-BEST": {"ch": (90.5, 92.2), "nd": (90.3, 92.0), "k01": (90.9, 92.6), "kL": (91.0, 92.6)},
}
PRED_D4 = {"AUDIT-SURVIVES": (0.30, 0.80), "AUDIT-VANISHES": (-0.15, 0.15), "AUDIT-REVERSES": (-0.80, -0.15)}
PRIOR_P = {"AUDIT-SURVIVES": 0.45, "AUDIT-VANISHES": 0.30, "AUDIT-REVERSES": 0.10, "AUDIT-UNDECIDED": 0.15,
           "SCALAR-TIES-OR-BEATS-BEST": 0.35}           # judgements, not measurements
CHANCE = 10.0                                           # CIFAR-10 chance, the floor for the 164.6 gate


def manifest_text():
    L = ["BATCH %s" % PREFIX, "NETWORK %s" % NET, "DATASET %s" % DSET, "NUM_PARAM_TENSORS %d" % NTENS,
         "TOTAL_PARAMS %d" % TOTPAR, "CLIP_C %s" % CLIP, "MS %s ALPHA0 %s" % (MST, A0), "EPOCHS %d" % EPOCHS,
         "META_STEPS %d" % (EPOCHS * STEPS_PER_EPOCH), "PROBE %d N_RECORDS %d" % (PROBE, N_RECORDS),
         "WD_LADDER %s" % ",".join("%s=%s" % (r, WD_TOKEN[r]) for r in RUNG_IDS)]
    for g, spec, m in GRAINS:
        L.append("GRAIN %s SPEC %s M %d" % (g, spec, m))
    for a in ARMS:
        L.append("ARM %s RUNG %s WD %s SPEC %s M %d SEEDS %s" % (a, RUNG_OF[a], WD[a], SPEC[a], M_OF_ARM[a],
                                                                ",".join(str(s) for s in SEEDS[a])))
    L.append("NJOBS %d" % NJOBS)
    return "\n".join(L) + "\n"


def file_sha(path):
    return hashlib.sha256(open(path, "rb").read()).hexdigest()


if __name__ == "__main__":
    print(manifest_text())
    print("ARGS-DEVIATING ARMS (owe an ARGS_WD_BASE exclusion row per seed, %d rows): %s"
          % (N_EXCLUSION_ROWS, ARGS_DEVIATING))
