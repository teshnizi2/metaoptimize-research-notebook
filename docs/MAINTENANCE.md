# Maintaining the research notebook

The website publishes a versioned research snapshot. Imported evidence is stored in `public/data/research.json` and `public/data/runs.json`; maintainer notes are stored in `content/journal.json`. The browser reads these published files. It does not write research history or contact ALICE.

Run the commands below from the website project directory. Use Node.js 22.12 or later and install the locked dependencies with `npm ci`. Evidence import also requires Python 3, Pillow, and pypdf; the exporter can use the configured Codex bundled Python when those libraries are absent from the current interpreter.

## Append a note, correction, or warning

Use the experiment IDs shown in the experiment register. The current snapshot contains 179 IDs (161 research questions and 18 method checks). The command validates against the actual imported register, including `CVK2`, rather than a hard-coded count.

```sh
npm run log -- --type note --title "Interpretation note" --text "Describe the observation and its evidence here." --experiments CVK2
npm run log -- --type correction --title "Correction to an earlier note" --text "Reference the earlier entry ID, explain the correction, and cite the supporting source." --experiments MT098,CVK2
npm run log -- --type warning --title "Scope limitation" --text "Describe the limitation that qualifies this experiment." --experiments CVK2
```

These are syntax examples; replace the example content with a real update before executing. The command generates a unique entry ID and the actual UTC recording time. Optional `--id J-UNIQUE-ID` and `--date 2026-09-15T09:00:00.000Z` let a maintainer retain a documented historical ID/date. Use an explicit historical date only when supported by the record. Valid dates are `YYYY-MM-DD` or UTC ISO timestamps ending in `Z`.

Entries require a nonempty title, nonempty text, and at least one existing experiment ID. Only `note`, `correction`, and `warning` are accepted. Unknown IDs, duplicate entry IDs, duplicate linked experiment IDs, invalid dates, unsupported options, and corrupt stored JSON cause a nonzero exit without modifying the journal. Obvious private paths, credentials, contact addresses, IP addresses, and infrastructure account assignments are also rejected before append or publication; entries are never silently rewritten. These patterns support maintainer review and do not establish that arbitrary prose is safe to publish. An ID must begin with a letter and contain only letters, numbers, underscores, or hyphens, with at most 80 characters. Titles allow 160 characters and text allows 20,000.

`npm run log` saves immediately to the local file, independently of the browser. Each write preserves previous entries, uses an exclusive writer lock, and atomically replaces the complete JSON file. `npm run build` validates the journal, requires already published entries to remain an unchanged prefix, and copies it to `public/data/journal.json`. It also validates and synchronizes the artifact date catalog before compiling the website. An initial `[]` journal is valid. Commit both journal files to retain the durable research history in version control.

Append a new correction instead of editing or deleting an earlier entry. A journal note does not revise an imported experiment outcome, registered score, or numerical table. Those changes require verified evidence and a new import. Journal warnings appear in both **Warnings & limits** and **Research log**, linked to their experiments. They carry a journal label, with no invented resolution state.

For a note-only update:

```sh
node scripts/sync-journal.mjs
python3 scripts/package_notebook.py --verify
npm run build
npm run preview -- --port 4173
```

Review `/activity` and any linked experiment pages, then follow the deployment steps below. The package command creates the source download and verifies `npm ci`, `npm test`, and `npm run build` in an isolated extraction. The final local build then includes the refreshed source download. Recreate the source ZIP after the last journal, interface, or evidence change; otherwise the download can lag behind the website.

## Import a verified evidence update

Prepare the verified campaign workspace first. The import expects its `outputs/tables/complete_experiment_register.csv`, `complete_run_inventory.csv`, `complete_figure_index.csv`, `research_timeline.csv`, supporting evidence JSON files under `work/`, the completed PDF, and supporting tables/assets. It imports the established evidence schema; it does not infer new scientific verdicts from raw jobs.

Set the two paths for the machine performing the import. Also set `RESEARCH_REVISION` and `ARCHIVE_REVISION` to verified full 40-character commit SHAs available on GitHub. The research revision identifies the original code tree; the archive revision must already contain the exact exported `public/source` bytes. For new evidence, first commit and push those sanitized source snapshots on a protected nonproduction branch. Then generate the mappings, package and test the complete update before publishing `main`. Never claim that a changed local file is identical to the pinned original. `PUBLICATION_DATE` is the date of this publication snapshot; measurement windows and historical phase dates remain in the source evidence.

```sh
RESEARCH_WORKSPACE="/path/to/verified-campaign-workspace"
RESEARCH_REPO="/path/to/hierarchical-metaoptimize"
PUBLICATION_DATE="$(date -u +%F)"

python3 scripts/export_sources.py --repo "$RESEARCH_REPO" --workspace "$RESEARCH_WORKSPACE" --public ./public --audit "$RESEARCH_WORKSPACE/work/portal_source_audit.json"
python3 scripts/export_research.py --workspace "$RESEARCH_WORKSPACE" --public-dir ./public --as-of "$PUBLICATION_DATE"
python3 scripts/export_research.py --public-dir ./public --check
npm run copy:check
npx tsx scripts/export_github_links.ts --research-repo "$RESEARCH_REPO" --research-revision "$RESEARCH_REVISION" --archive-revision "$ARCHIVE_REVISION"
npm run dates:refresh -- --evidence "$RESEARCH_WORKSPACE/work/artifact-date-evidence.json"
npm run dates:check
npm run test:data
node scripts/sync-journal.mjs
python3 scripts/package_notebook.py --verify
npm run build
```

The source import must run first because evidence export validates its source catalog. The source repository is read only. The exporters replace generated public snapshots/assets and produce audit receipts in the campaign workspace. Review `work/portal_source_audit.json` and `work/portal_data_audit.json`, including coverage, references, hashes, privacy checks, and explicit missing evidence. Inspect the version-control diff before publishing.

Record the evidence update with a separate `npm run log` entry linked to the affected experiments, then repeat journal synchronization, source packaging, and the final build after appending it. Import regenerates the base publication history, while `content/journal.json` preserves maintainer history. Do not remove old experiment IDs that existing journal entries reference; a removed ID will block synchronization until the historical reference is restored in the evidence index.

The current validation checks the established 179-record, 3,437-run snapshot (161 research questions, 18 method checks, 10 areas; `EXPECTED_STATS` in `export_research.py`) and its registered CVK2 result. If a later campaign changes the evidence schema or coverage, update the importer and its tests deliberately from verified source evidence before publishing. Do not bypass failed validation to make an import pass.

## Maintain reader-facing experiment copy

The fields in `public/data/research.json` are the exact research register: they preserve scorer tokens, formulas, registered bands, and audit wording. They are evidence, not website prose. The interface reads its concise question, result, explanation, comparison, and rationale from `content/experiment-copy.json`; the experiment page keeps the evidence fields under **Exact registered record**.

Every imported MASTER-TABLE record from MT175 onward (MT175-MT242 today) needs all five editorial fields: `question`, `result`, `explanation`, `comparison`, and `why`. Keep the numbers and scientific outcome faithful to the register, use ordinary sentences, and never edit the evidence JSON merely to improve wording.

Run `npm run copy:check` after every evidence import. The check rejects missing or stale IDs, blank required fields, oversized summaries, and visible scorer/formula notation. `npm run build` runs this check first, so a newly imported row cannot be published until its reader-facing copy is complete.

## Maintain later-evidence cross-links

A later batch can weaken, supersede, refute, replicate or qualify an earlier record. MASTER-TABLE lines are pinned and the importer lets only header line 3 differ between pins, so the earlier row cannot be edited to point forward. `content/later-evidence.json` records the relation as data instead: one object per pair with `earlier`, `later`, `relation` (exactly one of `weakens`, `supersedes`, `refutes`, `replicates`, `qualifies`), `source` (`CORRECTIONS <n>` or `CORRECTIONS <n>.<m>`), `quote` and a one-line `reason`. The earlier record's page shows a **Later evidence** note and the later record's page a **Bears on** note; neither record's registered text, outcome or badge changes.

Seed a pair only from a CORRECTIONS entry that says, in its own words, that the later batch weakens, supersedes, refutes, replicates or qualifies the earlier one, and quote those words verbatim (Markdown marks removed, spaces collapsed). Never infer a relation. A registered licence is quoted exactly as registered, with its "at this cell" wording. CORRECTIONS 229 names amended rows by MASTER-TABLE line number (162 is MT163, 163 is MT164, 166 is MT020); map them by batch and say so in the reason. `validateLaterEvidence()` in `src/lib/later-evidence.ts`, run by `npm run copy:check` before every build and by `tests/later-evidence.test.ts`, rejects unknown records, self-links, relations outside the vocabulary, duplicates, multi-line or missing reasons, a source entry that is absent from the published CORRECTIONS copy (`docs/CORRECTIONS.md` in `public/data/source-index.json`), and a quote that is not in the cited entry or subsection. Relations the campaign states in other words (for example "strengthened", "supported", "transfers") are not in the vocabulary and are left out rather than relabelled.

## Outcome model and the partition audit

`scripts/register_model.py` is the single, reviewable place where register rows become published outcomes. Both exporters import it.

- Research questions carry one of four outcomes: `success` (Goal met), `fail` (Goal missed), `mixed` (Mixed) or `unresolved` (Open). `corrected` is a separate badge with a `correction` record (note and source); it never replaces the outcome.
- `CORRECTION_REMAP` maps each of the 23 rows the register exported with outcome `correction`: eight research questions get a real outcome, and fifteen process checks become `method-check` records with no research outcome. An unmapped `correction` row stops the export.
- `PARTITION_AUDIT` imports MASTER-TABLE section 10 (lines 175-211) from the pinned campaign commit `d69b23a`, whose file hash is checked. Each row has an explicit rule, outcome, batches, optional correction note and one-line reason. New IDs are `MT<line>` in that commit (MT175-MT211); existing IDs are never renumbered.
- `APPENDED_ROWS` imports the rows appended after the audit (MASTER-TABLE lines 212-217, CORRECTIONS 217-226) from the pinned campaign commit `64e4f47`, whose file hash is checked; lines 1-211 must match the partition-audit commit except the header lines 3 and 5. Each row has an explicit rule, research area, batches, figure page and one-line reason that starts with the row's first registered verdict token: MT212 cgn1, MT213 cpl1, MT214 cvh1, MT215 cuc1 (Baseline comparisons), MT216 cgn2, MT217 cpl2. The import itself does not rewrite earlier records these rows bear on; `APPENDED_BEARS_ON` lists them (MT019, MT020, MT162, MT163, MT166, MT213), and changes to earlier records go through `ROW_AMENDMENTS` below. Their 93 runs (cgn1 6, cpl1 15, cvh1 12, cuc1 30, cgn2 15, cpl2 15) link through their batches once the run inventory includes them; see the run-inventory note below.
- `ROW_AMENDMENTS` applies rows the campaign amended in place, pinned to campaign commit `6e33fd8` (CORRECTIONS 229), whose MASTER-TABLE hash is checked; it may differ from `64e4f47` only on header line 3 and rows 19, 162, 163, 166 and 213, and no line may move. Each entry names the record (row 19 is MT019, 162 is MT163, 163 is MT164, 166 is MT020 and 213 is MT213; the older IDs came from a MASTER-TABLE snapshot one line longer, and the cau1 row keeps its original ID), its outcome before and after, and a one-line reason. An outcome may change only where the row's verdict column changed: MT019 moved from Open to Goal met (row 19: OPEN -> CONFIRMED, RESCOPED 229) and gains the `cuc1` batch; the other four keep their outcome and gain the amendment note in their scope. A drifted outcome stops the export. The previous outcome and wording stay on the record as an `amendment` warning; a result moved by later data is not a corrected claim, so the Corrected badge is not set. The ID fixture `tests/fixtures/register-ids-2026-09-16.json` records the MT019 change explicitly as `outcomeChange`.
- `LANDED_ROWS` imports the `cvt1` landing (MASTER-TABLE line 218, CORRECTIONS 230) from the pinned campaign commit `82867bb`, whose file hash is checked; it may differ from `6e33fd8` only on header line 3 and row 19's verdict-cell formatting fix (the superseded token OPEN moved into a bold `[SUPERSEDED: OPEN]` bracket; every other row-19 cell and the verdict wording from "Moved at" on must be unchanged), and no line may move. That fix changes no record's text; it shows only in the published MASTER-TABLE source copy. MT218 is Mixed under `VERDICT_RULES`: the returned branch `STEP-SIZE-NEEDED-VOTE-SUFFICES` answers both halves of the question, but its registered account missed INJECT's 8-30 band (30.14). `LANDED_INTERVENTIONS` names its vote-weight arms from the campaign's hash-pinned `results/CORPUS-EXCLUSIONS.tsv`: the record gains an intervention warning, and each of the 9 MUTE / DOSE / INJECT runs carries `intervention`, `interventionWitness` and `interventionNote` parameters, because the run inventory gives those rows the cell key of the plain `k01` / HEAD arm. The export stops if the list and the inventory disagree. The extended run inventory appends the 15 `cvt1` corpus rows after the 93 earlier ones (2,971 rows).
- `CGN3_ROWS` imports the `cgn3` landing (MASTER-TABLE line 219, CORRECTIONS 231) from the pinned campaign commit `e3a43da`, whose file hash is checked; it may differ from `82867bb` only on header line 3 and rows 213 and 218, and no line may move. MT219 is Goal met under `VERDICT_RULES`: the registered branch `RESCUE-SURVIVES` answers the question, and every arm and RHO land in the tested FREEZE-HOLDS account's registered bands; the level settling before the pin and `CEIL-BELOW` stay in its reason and scope. Its 12 runs are 430-epoch runs. `CGN3_AMENDMENTS` applies the two in-place amendments of the same commit, each checked to change one column and keep the superseded wording: MT218's scope takes row 218's amended scale cell verbatim (the pooling claim bracketed as not true when written), and MT213 gains an `Amended at CORRECTIONS 231` note that cvt1 has landed, with the pending-cvt1 clause of its CORRECTIONS 229 note retired. Neither moves an outcome; each adds a `warning-<id>-amendment-231` record. Because the live corpus was re-aggregated at that ingest, the extended run inventory keeps the published 2,971 rows byte for byte and appends the 12 `cgn3` rows (2,983 rows).
- `CVT23_ROWS` imports the `cvt3` and `cvt2` landings (MASTER-TABLE lines 220-221, CORRECTIONS 235 and 236) from the pinned campaign commit `9c5d72a`, whose file hash is checked; it may differ from `e3a43da` only on header lines 3 and 5 and rows 213 and 216, and no line may move. Line 5 (the bottom-line paragraph, amended at CORRECTIONS 234 and 236) feeds no record and appears only in the published MASTER-TABLE source copy. MT220 (`OWN-STEP-NECESSARY`) and MT221 (`GRADED` + `TOP-ATTENUATED`) are Mixed under `VERDICT_RULES`, as MT218: each branch answers its question, but a registered expectation was defied (cvt3's own account misses its MUTECTL band by 0.18 pp and `P_COAL` comes from the control falling below `k01`; for cvt2 no registered account predicted the pair of words and the stated `TOP-FLAT` prior missed by 0.26 pp). Each reason quotes all FINAL tokens. `CVT23_AMENDMENTS` applies the two row amendments of the same pin, each checked to be one contiguous bracketed edit of one column that keeps any replaced wording: MT216's result takes row 216's amended cell verbatim (CORRECTIONS 234: 'ISO above `kL`' held at 100 epochs only), and MT213 gains an `Amended at CORRECTIONS 236` note ('whoever casts it' corrected to the one caster that was run). Neither moves an outcome; each adds a `warning-<id>-amendment-<number>` record. `CVT23_INTERVENTIONS` names the 24 intervened runs (cvt3 MUTE50 / MUTEDOWN / MUTECTL, cvt2 K13-K2000) from `results/CORPUS-EXCLUSIONS.tsv` at `9c5d72a`, whose hash is checked and whose cvt1 pin must be a byte prefix; they carry the same run parameters as cvt1's. The extended run inventory keeps the published 2,983 rows byte for byte and appends the 36 new corpus rows (3,019 rows).
- `CVT45_ROWS` imports the `cvt4` and `cvt5` landings (MASTER-TABLE lines 222-223, CORRECTIONS 240 and 241) from the pinned campaign commit `643264c`, whose file hash is checked; it may differ from `9c5d72a` only on header lines 3 and 5 (line 5 again feeds no record), and no line may move. No existing row was amended, so no earlier record changes. MT222 (`OWN-STEP-MAGNITUDE`) and MT223 (`K-DEPENDENT-EQUILIBRIUM` + `RESCUE-SURVIVES` + `K33-HOLDS-PINNED`) are Goal met under `VERDICT_RULES`, as MT219: each registered branch answers its question and the tested account is in band on every registered band (cvt4's 9, cvt5's 8), with the bounds of CORRECTIONS 240.4 and 241.4 kept in the reason and scope. Neither corrects an earlier claim, so neither carries the Corrected badge. `CVT45_INTERVENTIONS` names the 18 intervened runs (cvt4's `BETA_HOLD` step-size holds HOLDLOW / HOLDSHARED / HOLDHIGH / HOLDHEAD, cvt5's `VOTE_W` arms K13 / K33) from `results/CORPUS-EXCLUSIONS.tsv` at `643264c`, whose hash is checked; each pinned copy of the list must be a byte prefix of the next. The run note names the intervention kind (step-size hold or vote weight). cvt5's runs are 300-epoch runs. The extended run inventory keeps the published 3,019 rows byte for byte and appends the 30 new corpus rows (3,049 rows). `jobs/run_cifar_cvt4.sh` was then archived at campaign commit `0ade9cc` (CORRECTIONS 244.1, sha256 equal to the registered runner), so MT222 links it; the source-unavailable warning it carried before is gone.
- `C244_AMENDMENTS` applies CORRECTIONS 244's in-place wording amendments of rows 222 and 223, pinned to campaign commit `0ade9cc`, whose MASTER-TABLE hash is checked. It may differ from `643264c` only on lines 5, 222 and 223, and only by inserted `[AMENDED at cycle 152, CORRECTIONS 244: …]` brackets: `strip_inserted_brackets()` must give the pinned lines back byte for byte, and in each row exactly the so-what cell changes. MT222's scope takes row 222's amended cell (sufficiency at this cell; `P_COUP` a floor location, not an absence of coupling) and MT223's takes row 223's (the bound first; `K-DEPENDENT-EQUILIBRIUM` an equilibrium of the level only). Neither moves an outcome; each adds a `warning-<id>-amendment-244` record.
- `CVT67_ROWS` imports the `cvt6` and `cvt7` landings (MASTER-TABLE lines 224-225, CORRECTIONS 246 and 247) from the pinned campaign commit `dae2a49`, whose file hash is checked; it may differ from the CORRECTIONS 244 pin `0ade9cc` only on header line 3 and on line 5, where removing the inserted `CORRECTIONS 246` and `CORRECTIONS 247` brackets must give the pinned line back byte for byte, and no line may move. Line 5 feeds no record, and no existing row was amended, so no earlier record changes (MT222's direct-vs-complement bound, which line 5 now calls partly resolved, stays as row 222 reads). MT224 (`GRADED`) is Mixed under `VERDICT_RULES`, as MT218 and MT221: the branch answers the 2x2 question, but no registered account fits every band (CORRECTIONS 246.3: GRADED's own account hits 3 of 5). MT225 (`TRANSFERS-GRADED`) is Goal met, as MT219 and MT222: its account hits every registered band, the positive control reproduces, G-BITE passes and no numeric prior was registered; the dose / held-set / complement bounds of 247.4 stay in the reason and scope. Neither carries the Corrected badge. `CVT67_INTERVENTIONS` names the 24 intervened runs (cvt6's `BETA_HOLD` arms HOLDLOW / HOLDHIGH and its forced arms HIGHHEADPATH / LOWMUTEPATH / LOWHEADPATH, which carry `BETA_HOLD` and `COMP_HOLD` at once; cvt7's `GROUP_HOLD` arms HOLDLOW / HOLDHIGH / HOLDISO) from `results/CORPUS-EXCLUSIONS.tsv` at `dae2a49`, hash checked, the earlier pins byte prefixes. `intervention_kinds()` reads one `PATCH=value` per hold from the list's intervention cell and stops on an unknown patch; the run note names every kind (the 51 earlier notes are unchanged), and a run with a second hold carries `interventionAdditionalWitness`, the one matching `<PATCH>: on` line of its own raw log (`additional_witness()`), because the list carries the first hold's witness only. `cvt67_corrections()` checks that `docs/CORRECTIONS.md` at `dae2a49` differs from the ingest commit `45eac02` only by the appended entries 246-247 and by inserted `CORRECTIONS 246` / `CORRECTIONS 247` brackets on seven listed lines (242's RULE H run count in four places, 242.3(2), 242.14, 243.4); `CVT67_REGISTRATION_CORRECTIONS` shows them on MT224 / MT225 as `warning-<id>-registration-<number>` records (registration text, not a Corrected badge). The extended run inventory keeps the published 3,049 rows byte for byte and appends the 36 new corpus rows (3,085 rows). `jobs/run_cifar_cvt6.sh` and `jobs/run_cifar_cvt7.sh` were then archived at campaign commit `4ff0891` (CORRECTIONS 250.1, sha256 equal to the registered runners), so MT224 and MT225 link them; the source-unavailable warnings they carried before are gone. The cvt6 intervention note says all seven arms ran `harness_cvt6` and `COMP_HOLD` was set only on the three forced arms (HOLDLOW and HOLDHIGH ran with it off).
- `CVT89_ROWS` imports the `cvt8` and `cvt9` landings (MASTER-TABLE lines 226-227, CORRECTIONS 252 and 253) from the pinned campaign commit `ba01f54`, whose file hash is checked; it may differ from the cvt6/cvt7 pin `dae2a49` only on header line 3 and on line 5, where removing the inserted `CORRECTIONS 252` and `CORRECTIONS 253` brackets must give the pinned line back byte for byte, and no line may move. No existing row was amended, so no earlier record changes (MT225's dose confound, which 252 resolves toward dose, and MT224's reading, which 253 replicates, stay as their rows read; `CVT89_BEARS_ON` records the relationship). Both are Mixed under `VERDICT_RULES`, as MT218, MT221 and MT224: each branch answers its question, the positive control reproduces and G-BITE passes 21/21, but no registered account fits every band. MT226 (`DOSE-FULL+ROUTE-PARTIAL+BIGROUTE-DIRECT`): of the four DOSE-family accounts three hit 6 of 7 and DOSE x VIA hits 5 (CORRECTIONS 252.3), and both big-dose arms returned `BELOW-K01`, so at PlainNet's dose a recovery under 5 pp cannot be seen and forcing the complement's path is not evidence that the free complement plays no role (252.8 item 3); the scorer's two RULE 16 text defects stay in the reason. MT227 (`DOSE-GRADED` + `WINDOW-GRADED`): DOSE-GRADED's account misses MIDDOSE's band by 5.86 pp and WINDOW-GRADED's misses EARLY by 1.63 pp (descriptive; the tokens come from the bars), and one 2.92 pp move would turn the FINAL into REPLICATE-FAILED. Neither carries the Corrected badge. `CVT89_INTERVENTIONS` names the 33 intervened runs (cvt8's `GROUP_HOLD` arms HOLDHIGH / HOLDBIG, and HIGHISOPATH / BIGISOPATH / LOWISOPATH with `GROUP_HOLD` + `REST_HOLD`; cvt9's LOWHEADPATH / HIGHHEADPATH / MIDDOSE / RESDOSE with `BETA_HOLD` + `COMP_HOLD`, and EARLY / LATE with a third hold, `WINDOW_HOLD`) from `results/CORPUS-EXCLUSIONS.tsv` at `ba01f54`, hash checked, the earlier pins byte prefixes. `intervention_kinds()` knows `REST_HOLD` (rest-group step-size hold) and `WINDOW_HOLD` (update-window hold); a three-kind note reads 'a, b and c interventions' (the two-kind wording is unchanged), and `interventionAdditionalWitness` joins every further hold's own log line in the listed order with ` | ` (`additional_witness()` checks a `WINDOW_HOLD` value `<n0>:<n1|end>` against the line's `n0=` / `n1=`). The 75 earlier intervened runs keep their notes and witnesses byte for byte (`tests/fixtures/intervention-notes-57b9ad5.json`). `cvt89_corrections()` checks that `docs/CORRECTIONS.md` at `ba01f54` differs from the ingest commit `1cb52f7` only by the appended entries 252-253 and one inserted `CORRECTIONS 253` bracket (249.3's RULE E integral), and that every line of the cvt6/cvt7 pin is unchanged; MT227 shows it as `warning-MT227-registration-253`. The runners `jobs/run_cifar_cvt8.sh` and `jobs/run_cifar_cvt9.sh` were archived at registration, so both records link them. The extended run inventory keeps the published 3,085 rows byte for byte and appends the 42 new corpus rows (3,127 rows). `tests/fixtures/register-ids-57b9ad5.json` pins every earlier record's ID, section, area, title, kind, outcome, Corrected badge and order.
- `CVT89_AMENDMENTS` applies the landing's final audit (CORRECTIONS 253.15), pinned to campaign commit `3cf4201`, whose MASTER-TABLE and CORRECTIONS hashes are checked; the CVT89 rows, exclusion list and registration correction stay pinned to `ba01f54`. 253.15 found that row 227's bound named one single-arm route out of `DOSE-GRADED` (RESDOSE rising ~39 pp) where the registered `decide()` has two (MIDDOSE falling ~39 pp also gives `DOSE-NONMONOTONE`), and row 227 was corrected in place. `cvt89_audit_master_table()` lets the file differ from `ba01f54` only in row 227's so-what cell, and `check_in_place_correction()` only by that one correction: the two-route sentence plus one `[CORRECTED IN PLACE at cycle 153, CORRECTIONS 253.15: …]` bracket that quotes the superseded words verbatim; removing the bracket and restoring the words must give the pinned cell back byte for byte. `cvt89_audit_corrections()` lets `docs/CORRECTIONS.md` differ from `ba01f54` only by the 253.15 section, with its one-line note that the row was amended, inserted before the closing `Next free number` line. MT227 stays Mixed without the Corrected badge; its scope takes the corrected cell (the old words stay in the bracket) and it gains `warning-MT227-amendment-253`. The same audit fixed two wording slips in MT226's reason: it overstated the route result at PlainNet's dose, and it counted three DOSE-family accounts where 252.3 lists four. `tests/fixtures/register-ids-2969a1c.json` pins every record's ID, section, area, title, kind, outcome, Corrected badge and order across the audit.
- `MUST_ROWS` imports the four MUST-tier landings (MASTER-TABLE lines 228-231, CORRECTIONS 264, 265 and 266) from the pinned campaign commit `40d29cf`, whose file hash is checked; it may differ from the cvt8/cvt9 final-audit pin `3cf4201` on header line 3 ALONE, and no line may move. Unlike every landing since `cvt3`, line 5 (the bottom-line paragraph) was deliberately left unamended: CORRECTIONS 266.12 records that `cmo1`'s `W0k01` 71.5600 is still 6.03 pp below that cell's tuned baseline peak 77.5927, so `GAP_in` stands. No existing row was amended and no registration text was corrected in place, so no earlier record changes and none of the four gains an amendment or registration warning (`MUST_BEARS_ON` records which earlier records they bear on: MT162, MT163 and MT155). The outcomes follow `VERDICT_RULES`: MT228 (`cmo1`) is Mixed, as MT218 and MT226 — the momentum leg answers both halves, but at base weight decay 0 the scalar arm is the best arm in the batch (`W0k01` 71.5600, +48.7713 pp above the anchor), so there is no gap to close and the registered ISO reading is stamped `W0-ISO-AT-REF` and not read; MT229 (`cst1`) is Open, because the registered scorer STOPPED at `G-DECOMP` on all nine runs and exited 1, so the batch reaches no branch and owns no licence sentence (the gate is a scorer tolerance defect, reported under RULE 16 and not fixed); MT230 (`cct1`) is Goal met, as MT219 and MT225 — both registered words are returned, every gate passes, no registered band is missed and neither bar is near; MT231 (`cmg1`) is Mixed, because no registered account's levels are reproduced (A3 predicts `MCAR` 68.4166 against the observed 65.4335) and the verdict turns on a bar with 0.8850 pp of margin. None carries the Corrected badge: the two refuted sentences (264.6 W1's epoch-duplicate claim, 266.6's N-pin clause) were claims of the landing reports themselves, corrected inside the same entries before any of this reached the register.
- `MUST_ARGS_DEVIATIONS` names `cmo1`'s 18 deviating runs, which are a NEW kind of exclusion row (CORRECTIONS 263). Its M9 and W0 arms ran the live harness with no patch and deviate from the standard cell in a base-optimiser CLI FLAG alone (`--momentum-param-base 0.9`, `--weight-decay-base 0`), which no patch announces and no column of `results/all_runs.csv` carries, so the inventory writes those rows with the plain 0.99 / 0.1 cell key. The exclusion row's intervention cell is the flag as written on the command line and its witness is `<KIND>: <flag>=<value>`; `ARGS_KINDS` holds the two kinds with their standard values, `args_deviation()` reads the row and refuses an unknown kind, a witness that disagrees with its own intervention cell, or a listed value that is the standard one, and `args_witness_line()` checks it against the run's own `ARGS:` line read with argparse last-wins semantics (`analysis/argsline_guard.py` is a registered file and is not imported; its rule is re-typed, as `analysis/corpus_exclusions.py` re-types it on the campaign side). Those runs carry `argsDeviation`, `argsDeviationKind`, `argsDeviationWitness`, `argsDeviationArgsWitness` and `argsDeviationNote`, never the patch `intervention` parameters, so the 108 patch-intervened runs of the earlier landings keep their marks and their count. The published ARGS witness quotes the checked flag alone, because the raw line also carries the run's private save directory; the sanitized raw log carries the whole line. MT228 gains `warning-MT228-args-deviation`. `cst1`, `cct1` and `cmg1` own no exclusion row at all (CORRECTIONS 265.1, 266.1): none of their runs prints an intervention line, and their arms are separated by columns the corpus already carries. `must_corrections()` checks that `docs/CORRECTIONS.md` at `40d29cf` is the ingest commit `77c6de9`'s file with its closing `Next free number` trailer replaced by entries 264-266 and a new trailer, every earlier line unchanged, and the whole cvt8/cvt9 audit pin still an exact prefix. The extended run inventory keeps the published 3,127 rows byte for byte and appends the 54 new corpus rows (3,181 rows). `tests/fixtures/register-ids-474fc17.json` pins every earlier record's ID, section, area, title, kind, outcome, Corrected badge and order.
- `MECH4_ROWS` imports the last four MUST-tier landings (MASTER-TABLE lines 232-235, CORRECTIONS 270, 271, 272 and 273) from the pinned campaign commit `2972d48`, whose file hash is checked; it may differ from the MUST-tier pin `40d29cf` on header line 3 and on row 229, which the same landing amended in place, and no line may move. Line 5 was again deliberately left unamended (273.10: no verdict of this cycle moves the denominator result). All four are Goal met under `VERDICT_RULES`, as MT219, MT222, MT223, MT225 and MT230: each returns its registered branch in the registered direction, each scorer exits 0 with every gate passing, each positive control reproduces, G-BITE excludes the batch's own broken-patch null on records, and — the test the MT218-MT231 precedent applies before Goal met is granted — NO registered account misses a band and NO control fails. MT232 (`cvt10`, `ONE-SUFFICES+…+SPLIT-NO-EFFECT`): each of the three carriers held alone on PlainNet's dose stalls the run while its free twin rescues, which closes 252.8(1)'s held-set confound, but the same hold does nothing once the other two leave the shared vote. MT233 (`cwd1`, `COLLAPSE-VANISHES`): masking the coupled weight decay on the 20 BatchNorm scales removes the collapse entirely. MT234 (`csv1`, `BOTH-ROUTES`): both the applied step and the vote carry part of the damage. MT235 (`cwd2`, `WD-ROUTE` + `SCALAR-NEEDS-CARRIER-WD`): the held step's damage runs through the coupled decay on that one scale. What each row bounds — sufficiency only, one dose or one tensor set, one cell, one horizon, three or four seeds, heterogeneous ungated GPU hardware on `cwd1` and `cwd2`, the counterfactual shadow vote on `csv1`, and the RULE 16 defects each reports and does not fix — is SCOPE in the reason and the record's scope, not a band the result missed, exactly as MT230's bounds are. None carries the Corrected badge: the wording each landing fixed was the wording of its own report, fixed inside the same entry before any of it reached the register.
- `MECH4_INTERVENTIONS` names the 45 intervened runs of that ingest (`cvt10`'s five held arms under the existing `GROUP_HOLD` kind; `cwd1`'s two masked arms and `cwd2`'s twelve under `DECAY_MASK`; `csv1`'s nine shadow-vote arms under `SHADOW_VOTE` and its three MUTE arms under the existing `VOTE_W`) from `results/CORPUS-EXCLUSIONS.tsv` at `2972d48`, hash checked, every earlier pin still a byte prefix; the file is byte-identical at the ingest `66a19fb`, where the rows were appended, and at the landing, which did not touch it. `INTERVENTION_KINDS` learns `DECAY_MASK` (coupled weight-decay mask) and `SHADOW_VOTE` (shadow-vote), and `additional_witness()` learns their value grammars — `<spec>` for `DECAY_MASK`, `<vote>:<applied>:<name>` for `SHADOW_VOTE` — refusing a value written for another kind rather than guessing. `cwd2`'s HIGHWD0 and LOWWD0 are the first rows in the corpus to carry three kinds ending in a mask (`BETA_HOLD` + `COMP_HOLD` + `DECAY_MASK`), and each further hold is witnessed by its own line in the run's log. `cvt10`'s ISOSPLIT is the one listed arm with NO plain twin at its cell key, so `intervention_opening()` gives it its own note wording instead of naming an arm. The 126 rows of the earlier landings keep their notes and witnesses byte for byte. `mech4_corrections()` checks that `docs/CORRECTIONS.md` is append-only across both commits: the ingest added entries 267-269 below the MUST-tier landing's closing `Next free number` trailer, and the landing added 270-273 below the ingest's. The extended run inventory keeps the published 3,181 rows byte for byte and appends the 72 new corpus rows (3,253 rows). `tests/fixtures/register-ids-2cf35f1.json` pins every earlier record's ID, section, area, title, kind, outcome and Corrected badge.
- `MECH5_ROWS` imports the `cwd3` landing (MASTER-TABLE line 236, CORRECTIONS 278) from the pinned campaign commit `97eb049`, whose file hash is checked; it may differ from the `MECH4` pin `2972d48` on header line 3 ONLY — this landing amended no earlier row and did not touch line 5 — and no line may move. MT236 (`cwd3`, `CARRIER-DECAY-SUFFICES` + `CAR-REC+CTL-NULL+CTL2-NULL`) is Goal met under `VERDICT_RULES`: it returns its registered branch AND its registered state word in the registered direction, which is the best of the accounts CORRECTIONS 275.4 wrote down before any run existed; the scorer exits 0 with every gate passing; the in-batch reference `NWD` replicates `cwd1`'s network-wide result; `G-BITE` excludes the batch's own broken-mask null on records with a positive decay term on 6,000 of 6,000 masked probe records and a cross-read that refuses every wrong mask size; and NO registered account misses a band and NO control fails. Removing the coupled weight decay from the three `ctd1` carriers alone (1,536 of 11,220,132 parameters) removes the scalar collapse as fully as removing it from all twenty BatchNorm scales, while a count-matched non-carrier triple and a class-pure non-carrier pair both stay on the floor. What the row bounds — two floor readings that are BOUNDS and not measured zeros, a residual that is a bound in the other direction whose sign is horizon-dependent, a descriptive ratio that drifts through 1, position class AND count/dose both unseparated from carrier identity, one switch changing both routes, one network, one cell, one horizon, three seeds, and a RULE 16 defect reported and not fixed — is SCOPE in the reason and the record's scope, exactly as MT235's bounds are. It carries no Corrected badge: the wording the landing fixed was its own report's, fixed inside the same entry (278.6) before any of it reached the register.
- `MECH5_INTERVENTIONS` names the 12 intervened runs of that ingest — `cwd3`'s four masked arms `CARWD0`, `CTLWD0`, `CTL2WD0` and `NWD`, three seeds each, all under the EXISTING `DECAY_MASK` kind — from `results/CORPUS-EXCLUSIONS.tsv` at `97eb049`, hash checked, every earlier pin still a byte prefix; the file is byte-identical at the ingest `064dff6`, where the rows were appended, and at the landing, which did not touch it. The batch adds no kind and no multi-kind row: every listed run carries exactly one intervention, and the three `k01` runs print `DECAY_MASK: off` and own no row. `mech5_corrections()` checks that `docs/CORRECTIONS.md` is append-only across both steps: entries 274-277 were appended below the `MECH4` landing's closing `Next free number` trailer, and 278 below theirs. The extended run inventory keeps the published 3,253 rows byte for byte and appends the 15 new corpus rows (3,268 rows). `tests/fixtures/register-ids-98b0e8d.json` pins every earlier record's ID, section, area, title, kind, outcome and Corrected badge, and the import moves none of them.
- `MECH6_ROWS` imports the `cwd4` and `cwd5` landings (MASTER-TABLE lines 237 and 238, CORRECTIONS 283 and 285) in TWO steps, from the pinned campaign commits `26baf4a` and `8554afa`, both file hashes checked. Each step may differ from the previous pin on header line 3 ONLY — neither landing amended an earlier row and neither touched line 5 — and no line may move. **Both rows are Mixed, and neither is Goal met by default.** The MT218-MT236 precedent grants Goal met only when the fired account returns its registered branch AND its registered state word in the registered direction, NO registered account misses a band, and NO control fails. MT237 (`cwd4`, `ONE-SUFFICES-PARTIAL` + `TWO-REC+CTL2-NULL+ONE50-REC+ONE53-PART+ONE59-REC`) answers its question and REFUTES the count / dose rival at a matched count of two, but the fired account's own registered LEVEL BAND for `ONE53` is K01 (18-28) and `ONE53` landed at 58.5780 — a defied registered expectation, exactly as at MT231, where `ACCOUNT-A3-MERGE-HARMLESS` was ruled a BRANCH match only — and the verdict's second token turns on a near bar, `ONE50`'s `REC` at +0.40 SE, which the row itself labels DESCRIPTIVE / UNSURE. MT238 (`cwd5`, `THRESHOLD-W1-W2` + `W1-COLLAPSE+W2-NOGAP+W3-NOGAP+W4-NOGAP+CAR-UNREADABLE`) hits EVERY registered band on the ladder and no control fails, but the batch's SECOND registered question — whether the carrier account still holds wherever the collapse still exists (281.3) — is unanswerable at this cell: `CARW2` was pre-registered to be read ONLY if rung W2 collapses, W2 is `NOGAP`, and the registered scorer itself calls this `cmo1`'s `ISO-UNREADABLE` at wd 0 (MT228) repeating one rung higher. What the two rows bound — floor readings that are LOCATIONS and therefore bounds, four rungs that BRACKET a transition between 1e-2 and 0.1 and locate nothing inside that decade, a `NOGAP` that is a bound and never "the grains are equal", `G_W2` unresolved and unpoolable, the magnitude and position-class rivals still unseparated, decoupled decay untested — is SCOPE that the reason and scope carry. Neither corrects an earlier PUBLISHED notebook claim, so neither carries a Corrected badge.
- `MECH6_INTERVENTIONS` names the 18 intervened runs of the `cwd4` ingest — its six masked arms `CARWD0`, `TWOWD0`, `CTL2WD0`, `ONE50`, `ONE53` and `ONE59`, three seeds each, all under the EXISTING `DECAY_MASK` kind — from `results/CORPUS-EXCLUSIONS.tsv` at `8b9fbd2`; the three `k01` runs print `DECAY_MASK: off` and own no row. `MECH6_ARGS_DEVIATIONS` names the 21 ARGS-value rows of the `cwd5` ingest at `91fcd57` — its seven non-anchor arms × 3 seeds under the `ARGS_WD_BASE` kind — and the six anchor runs at the standard 0.1 own none. Every earlier pin is still a byte prefix of both. **`cwd5`'s three `CARW2` rows are the corpus's first TWO-AXIS rows** (CORRECTIONS 284): a run that BOTH deviates on an ARGS value AND prints an ON `<KIND>` line, which one witness column cannot describe. `args_axes()` splits the intervention cell on the spaced separator `" + "` (a patch spec's own `+` is not a separator), `args_extra_kinds()` returns the patch axes, and the export reads each one back from the run's OWN log as `interventionAdditionalWitness`, publishing `argsDeviationAxes` beside it. `mech6_corrections()` checks that `docs/CORRECTIONS.md` is append-only across all four steps of the cycle: 279-282 at `7ff4685`, 283 at `26baf4a`, 284 at `5045875` and 285 at `8554afa`, each below the previous closing `Next free number` trailer; the two ingest commits touched `results/` alone and no entry at all. The extended run inventory keeps the published 3,268 rows byte for byte and appends the 48 new corpus rows (3,316 rows). `tests/fixtures/register-ids-eddc014.json` pins every earlier record's ID, section, area, title, kind, outcome and Corrected badge, and the import moves none of them.
- `MECH7_ROWS` imports the `caw2` and `cgw1` landings (MASTER-TABLE lines 239 and 240, CORRECTIONS 295 and 296) in ONE step, from the pinned campaign commit `8a99001`, whose file hash is checked; it may differ from the `MECH6` pin `8554afa` on header line 3 ONLY — neither landing amended an earlier row and neither touched line 5 — and no line may move. CORRECTIONS 297 (the campaign's path decision) is a planning entry and owns no row. **MT239 (`caw2`, `NO-COLLAPSE-CONTROL-DOSE-NOT-REACHED` + `M-COLLAPSE+L-NOGAP+A-NOGAP+X-NOGAP+K-COLLAPSED` + `ATTR-BASE-PROTECTS` + `GATE-DOES-NOT-FIRE`) is Mixed, as MT238**: its co-primary is answered in the registered direction — the harness's standard AdamW + Adam does not collapse at the mechanism cell — the registered prediction is returned, every band of the fired account is hit and the control reproduces, but the dose half returned NOT-REACHED (`RHO_X` 0.0591 against a 0.5 bar), so immunity at the control's dose is undecided and the attribution word is confounded with the dose. **MT240 (`cgw1`, `AUDIT-UNDECIDED` + `SCALAR-BEATS-BEST` + `W1-SURVIVES+W2-SURVIVES+W4-UNDECIDED`) is Open, as MT183 (`bn1`)**: its primary lies between the registered bars and its licence is "report the interval"; the co-reported scalar reading is resolved and carried in the reason. MT240 is the first appended row in the count-matched partition audit's area (section 10, `page-8`): `AREAS` gains section 10. What the rows bound — one cell each, the dose not reached, a `NOGAP` that is a bound, and a harness 5e-4 far less decayed than a standard recipe, so no reading settles whether the audit's effect depends on non-standard decay — is SCOPE in the reason and scope. Neither corrects an earlier PUBLISHED notebook claim, so neither carries a Corrected badge. MT240 discloses one unavailable source, `jobs/run_cifar_cgw1.sh`, a runner kept in the campaign workspace and not in the repository.
- `MECH7_ARGS_DEVIATIONS` names the 46 ARGS-value rows of the joint ingest `477a853` (the landing left the list byte-identical): `caw2`'s 18 — 12 `ARGS_MOMENTUM_BASE` rows (the AdamW base's own β1 0.9 against the per-flag standard 0.99) and **6 TWO-ARGS rows (CORRECTIONS 294), the corpus's first: `XS` / `XL` deviate on `--momentum-param-base 0.9` AND `--weight-decay-base 1.0` at once, are listed ONCE by their weight-decay witness, and name both flags in the intervention cell** — and `cgw1`'s 28 `ARGS_WD_BASE` rows (1e-2 and 5e-4); the control, the meta-swap arms and the twelve anchor runs at 0.1 own none. Every earlier pin is still a byte prefix. `args_clauses()` reads every `--<flag> <value>` axis of an ARGS-value row (ARGS axes before any patch axis, no flag twice, every flag a registered ARGS kind), `args_extra_args()` returns the ARGS axes beyond the witness's kind, and `MECH7_MULTI_ARGS` re-types CORRECTIONS 294's registry: a row with a second ARGS axis outside it stops the export. The export publishes `argsDeviationAdditionalArgs`, reads each further kind back from the run's OWN `ARGS:` line as `argsDeviationAdditionalArgsWitness`, and adds a TWO-ARGS clause to the run note. One-axis rows and CORRECTIONS 284's ARGS + patch rows keep exactly their earlier reading. `mech7_corrections()` checks that `docs/CORRECTIONS.md` at `8a99001` keeps every line above the `cwd5` landing's closing trailer and appends entries 286-297 below it, closed by a fresh trailer. The extended run inventory keeps the published 3,316 rows byte for byte and appends the 67 new corpus rows (3,383 rows). `tests/fixtures/register-ids-429e0c2.json` pins every earlier record's ID, section, area, title, kind, outcome and Corrected badge, and the import moves none of them.
- `MECH8_ROWS` imports the `crt1` and `csh1` landings (MASTER-TABLE lines 241 and 242, CORRECTIONS 309 and 310) in ONE step, from the pinned campaign commit `a53bce1`, whose file hash is checked; it may differ from the `MECH7` pin `8a99001` on header line 3 ONLY — neither landing amended an earlier row and neither touched line 5 — and no line may move. (The landing was written at `c06738d`; `a53bce1` then corrected row 241's stamp count from 14 to 12 and named two licence citations as scorer-log lines, so the pin is the corrected file.) Entries 298-308 between the two pins are audits, registrations, patches and code debts and own no row. **MT241 (`crt1`, `WEAKENED-TO-TIE` + `TUNED-SCALAR-TIES-BEST` + `ORACLE-SCALAR-TIES-BEST` + `M2-BEATS+A2-BEATS+M3-BEATS+M1-BEATS`) is Mixed**: it returns a definite registered branch with its licence — after per-grain re-tuning at 5e-4, cgw1's scalar-beats-both reading is rewritten as "ties (a bound) after re-tuning" — but its registered clauses split (the tuned reading ties while every per-config reading still beats), the favoured account was not returned, six of its twelve registered level bands were missed, and the branch rests on the in-batch sigma lying above a break-even value (at the frozen floor the same rule reads the opposite branch). It is the second appended row in the partition-audit area (section 10, `page-8`). It bears on MT240, but row 240 was NOT amended in place, so MT240 keeps its record and its Open outcome (`MECH8_BEARS_ON`). **MT242 (`csh1`, `HORIZON-DOES-NOT-REPRODUCE` + `A-NOGAP+M-NOGAP+P-NOGAP`) is Mixed, as MT237**: it returns the registered prediction, but the fired account's own level band for `GPL` (60-76) was missed at 56.9707, whose reference margin over REF_MIN is 1.97 pp. What the rows bound — one cell each, a four-point grid selected at its edge, an expected-bias selection bound, a constant discount that is not the learned horizon, sufficiency and not necessity, a tie and a NOGAP that are bounds — is SCOPE in the reason and scope. Neither corrects an earlier PUBLISHED notebook claim, so neither carries a Corrected badge. MT241 discloses one unavailable source, `jobs/run_cifar_crt1.sh`, a runner kept in the campaign workspace on the cluster and not in the repository, as MT240 does for cgw1's.
- `MECH8_ARGS_DEVIATIONS` names the 54 ARGS-value rows of the joint ingest `5db62be` (the landing left the list byte-identical): `crt1`'s 36 and `csh1`'s 18, every one `ARGS_WD_BASE: weight-decay-base=5e-4`, one kind per run (the meta step size, alpha0 and gamma are CSV columns, so no run is two-ARGS or two-axis). `csh1`'s rows name the corpus lookalike arm (`k01` / `kL`) and are registered at the landing entry, CORRECTIONS 310, as the campaign's refute pass required. Every earlier pin is still a byte prefix. `mech8_corrections()` checks that `docs/CORRECTIONS.md` at `a53bce1` keeps every line above the `caw2` / `cgw1` landing's closing trailer and appends entries 298-310 below it, closed by a fresh trailer. The extended run inventory is the campaign workspace inventory, then the landed corpus rows exactly as published before (sanitizing them gives the published 3,383 rows byte for byte), then the 54 new corpus rows (3,437 rows). New source files that mention an earlier batch by name add related-source links to those earlier records, as every earlier source import did; no ID, title, area, kind, outcome or badge moves. `tests/fixtures/register-ids-60dc6ba.json` pins every earlier record's ID, section, area, title, kind, outcome and Corrected badge, and the import moves none of them.

- `MECH4_AMENDMENTS` applies the one row this landing amended in place, row 229 (`cst1`, MT229), and it is the first amendment since CORRECTIONS 229 to MOVE an outcome: Open → Mixed. CORRECTIONS 268 registered and pushed a frozen SUCCESSOR scorer, `cST2_carriervote_score.py`, before it was run on a single `cst1` record; its one change is an ms-aware `G-DECOMP` tolerance, every other bar is the literal frozen at 256.5, and reading the same nine runs it reaches `NOMINATION-PARTIAL+ISO-RESCUES+CTL-NULL`. The registered clauses then split — the isolation rescue transfers to meta step 3e-4 (+41.1567 pp = +74.00 SE) while the vote-dominance nomination misses its frozen 750-record bar by 41 records of 1,500 — which is `VERDICT_RULES` "mixed" and not "met". `check_row229_amendment()` checks the shape of the edit: the question, comparison and scale cells may not change, the result and so-what cells may only gain one appended bracket carrying the mark, the verdict cell must open with that bracket and keep the predecessor's verdict verbatim inside a `[SUPERSEDED …]` bracket, and the ref cell may only gain citations. The REGISTERED predecessor `cST1_carrier_contrast_score.py` is UNEDITED and still frozen under RULE 16, and 265.3's reading stands as the correct output of a defective check, so no earlier published claim is corrected: the Corrected badge stays off and the superseded wording is kept in the record and in `warning-MT229-amendment-273`, whose title is "Outcome moved by a later result".
- `ADDED_PHASES` adds research phases the campaign's `research_timeline.csv` does not have, in the same format (phase-09, 15-17 Sep 2026, linked to MT212-MT227; cvt4 and cvt5 were launched on 16 Sep and landed at 22:10 UTC the same day; cvt6 and cvt7 were registered and launched from 23:19 UTC on 16 Sep and landed at 01:54 UTC on 17 Sep; cvt8 and cvt9 were registered from 03:21 UTC on 17 Sep and landed at 06:50 UTC the same day; phase-10, 17-18 Sep 2026, linked to MT228-MT231, the MUST-have tier of `docs/LIMITS-PREP.md` section 5.1, registered as a block on 17 Sep and landed on 18 Sep; phase-11, 18-19 Sep 2026, linked to MT232-MT235, the rest of that tier — R1, S1b, N1 and N4 — registered and launched from 18:12 UTC on 18 Sep and landed at 00:17 UTC on 19 Sep; phase-12, 19 Sep 2026, linked to MT236, the `cwd3` batch registered, proved, launched, scored and landed the same day; phase-13, 20 Sep 2026, linked to MT237, the `cwd4` batch registered, proved, launched, scored and landed the same day; phase-14, 20-21 Sep 2026, linked to MT238, the `cwd5` weight-decay ladder registered and launched on 20 Sep and landed at 22:41 UTC the same day; phase-15, 22 Sep 2026, linked to MT239, the `caw2` standard-recipe cell registered, proved, submitted, scored and landed the same day; phase-16, 22 Sep 2026, linked to MT240, the `cgw1` audit-at-standard-decay batch landed the same day; phase-17, 22 Sep 2026, linked to MT241, the `crt1` retune registered, submitted, scored and landed the same day; and phase-18, 22 Sep 2026, linked to MT242, the `csh1` short-horizon control, the same day. phase-11 is a separate phase because its four batches ask WHICH TENSORS carry the collapse and THROUGH WHICH ROUTE, rather than what the carrier account needs; phase-12 is separate again because it asks whether the three NOMINATED carriers' own decay is enough, against two count-matched non-carrier sets at the same depth; phase-13 because it asks whether the rescue is a matter of WHICH tensors or of HOW MANY; phase-14 because it is a SCOPE question rather than a decomposition — does the collapse exist at all away from the campaign's own weight decay; phase-15 because it asks whether the collapse reaches the standard recipe rather than a lower decay on the same base; phase-16 because it asks about the partition audit's own headline, not the mechanism; phase-17 because it re-tunes that headline's scalar reading rather than measuring it; and phase-18 because it tests a rival cause of the collapse, the short meta-horizon). The timeline CSV itself is published unchanged.
- The published `assets/tables/complete_experiment_register.csv` is regenerated from this register: one row per record, with `kind`, the four-way `outcome` and `outcome_label`, and `corrected` / `correction_note` as separate columns. The campaign's 111-row export is an input, not the published table.
- `outcomes_by_area.csv` and `goal_outcomes.csv` are regenerated from the register too: outcome counts by area (four outcomes, method checks and Corrected in separate columns; the retired `correction` column is gone), and the report's page goal table with each page's current linked records and their outcome counts.
- MASTER-TABLE cells are Markdown. `clean()` in `register_model.py` removes bold, emphasis and code marks and turns backslash escapes into the literal character (`S\*` becomes `S*`, `\|` becomes `|`), so no escape reaches the published text; `tests/markdown-escapes.test.ts` fails if one does. The campaign's register export copies the same cells with their marks, so `remap_base_row()` passes its text fields (`BASE_TEXT_FIELDS`) through `clean()` too (39 of its 111 rows carried `**` or backticks into titles, scopes and verdict warnings), and `export_research.py` cleans the table titles and scopes it takes from the workspace panel indexes. `tests/markdown-marks.test.ts` fails if any published text (hrefs excepted) carries `**`, `__` or a backtick, and checks against `tests/fixtures/register-ids-fe542ec.json` that no ID, section, area, kind, outcome or Corrected badge moved and that a title changed only by losing marks.
- A MASTER-TABLE ref cell is split into citations by `split_references()`: on `;` outside brackets only, because rows 224-225 put a `;` inside parentheses (`CORRECTIONS 242 (registration and launch; ...), 246 (landing)`), and splitting there published a label cut off at the open bracket. A cell whose brackets do not balance stays one citation. `tests/source-labels.test.ts` fails if any published source label leaves a bracket open, and `tests/fixtures/register-ids-ad152c3.json` pins every record's ID, section, area, title, kind, outcome and Corrected badge across that fix.
- MASTER-TABLE source anchors are resolved by row content. The original IDs came from an uncommitted MASTER-TABLE snapshot that was one line longer from line 20 on, so anchoring by line number alone would point most records at their neighbouring row.

Run the source export before the research export (the research export requires a source link for every record). `export_research.py --audit PATH` writes its receipt outside the campaign workspace when that workspace must stay read only. `--run-inventory PATH` publishes an extended copy of `complete_run_inventory.csv` kept outside that workspace: the campaign inventory unchanged, followed by the corpus rows (`results/all_runs.csv`) of landed batches, with raw logs found in the archived `runs/` and `runs_alice2/` `.out` files and sanitized by the same exporter. The Python data tests accept `NOTEBOOK_WORKSPACE`, `NOTEBOOK_RESEARCH_REPO`, `NOTEBOOK_DATA_AUDIT` and `NOTEBOOK_RUN_INVENTORY` when the portal is not checked out inside the campaign workspace. Most of them read only the published snapshot; the tests that also read one of those four inputs (`tests/external_inputs.py`) run when its variable is set or its default path exists, and are skipped with a reason naming the variable otherwise, so `npm run test:data` in a plain clone reports skips instead of errors. A set variable always runs its tests, so a wrong path fails instead of skipping. Set all four before an evidence import, and check that the run reports no skips.

## Review and deploy

Start `npm run preview -- --port 4173`, then inspect:

- `/runs`: search and combine model, dataset, batch, execution-state, and experiment filters; advance beyond the first 50 rows and reset filters.
- `/runs/run-<jobId>`: use a real ID from the ledger. Check full parameters, accuracy window, completed epochs, experiment links, and explicit missing-log messages. CVK runs with a published `logHref` should show and download their raw log.
- `/warnings?experiment=CVK2` and `/activity`: test filters, linked experiments, dated entries, and empty results.
- A direct reload of an experiment/run route, the PDF, a source text download, a CSV, and the evidence bundle.

Deployment is a maintainer action after review. Use the existing Vercel project association and configured account; do not create a replacement project accidentally. Check `.vercel/project.json` locally if present and confirm the destination in the Vercel CLI before publication. Keep account credentials and local association files out of public assets and version control.

```sh
npm run deploy
```

The local `npm run deploy` command runs the JavaScript tests, validates and synchronizes the journal and date catalog, regenerates and verifies the source ZIP, builds production assets, then calls `vercel --prod --archive=tgz`. This order keeps the downloadable source aligned with the publication and uploads the thousands of assets as an archive. The packaging command writes `public/assets/notebook-source.zip` plus the source deliverable in the campaign outputs and records hashes, privacy coverage, and portability checks in `work/notebook_source_package_report.json`. Vercel's remote build runs `npm test` followed by `npm run build`; the Python packager is local tooling and is excluded from the hosted deployment. Run the Python data checks above first whenever imported evidence changes. Commit the reviewed source and generated data using the project's normal version-control workflow. Once Vercel reports a successful deployment, open its reported production URL and verify the snapshot ID, a new journal entry, direct route reloads, and downloads. Build success alone does not verify a deployment.

## Metric and provenance boundaries

- Run execution state describes inventory completion or supersession. Scientific success/failure belongs to the linked experiment. A completed job can have a collapsed trajectory or a failed scientific objective.
- `Run.epochs` is `epochs_done`, the number of completed training epochs. `parameters.epochs_requested` holds the requested budget.
- Displayed run accuracy is `plateau5`, the mean test accuracy over the final five completed epochs, only when `window_ok=1`. Missing/invalid windows are not replaced by final or best accuracy. The legacy `plateau` field uses a different historical window and remains explicitly separate in source parameters.
- An experiment/run association identifies the documented batch family. It does not assert that every run participated in every reported contrast.
- All 3,437 runs in this snapshot have a sanitized raw log linked by `logHref`, with original/public SHA-256 pairs. If a future snapshot omits an archived log, retain its explicit archived-but-unpublished availability label rather than implying that no log exists.
- Source downloads can be redacted research records. Their original and public hashes distinguish the two copies. A related current source file is not automatically the exact historical executable; use explicit frozen-source verification when available.
- Activity dates identify documented phase windows, completion events, publication snapshots, or the maintainer's recording time. They are not inferred per-run launch times or a live cluster feed.

## Recover a failed journal operation

Read the command's error before retrying. Validation failures leave the previous journal intact. Restore malformed JSON or a changed published prefix from version control, then append a correction. If `.journal.lock` remains after an interrupted process, inspect its recorded PID and verify that no journal or build-time sync writer is running before removing that specific stale lock. Never clear another active writer's lock.

The two JSON files and version control provide the persistence boundary. There is no browser-local storage, collaborative editor, or backend database. The hosted notebook uses native Vercel access protection. A saved local entry becomes visible to readers only after a successful build and publication.

## Research group access and GitHub

The hosted notebook and downloads served by Vercel require site access. Keep **Vercel Authentication → All Deployments** enabled in the project settings, including after redeployment.

Both GitHub repositories are public and readable without invitations. The notebook repository includes published source and evidence; site login does not make those files private. Never place access links, passwords, tokens or professor contact details in the journal, code archive or repository.

The existing Vercel project is connected to the notebook GitHub repository with `main` as its production branch. After a verified update, synchronize the journal, regenerate the source ZIP, test, build, commit the reviewed changes and push `main`; Vercel starts the build automatically. Confirm the new deployment is ready and still denies anonymous HTML, data, source, log and download requests before sharing it. The CLI deployment command remains available for an authorized manual release. Do not disable protection to make a browser check or automated verification pass.

The research group uses one revocable Vercel shareable link attached to the stable production alias. It is stored separately from this repository. Both professors can use the same link without a Vercel account. Anyone holding it can read the notebook; distribute it only within the group. The link remains valid until revoked, and its continued access must be checked after a production update. Access protection stays enabled for every deployment.

## Artifact dates

The UI reads `public/data/artifact-dates.json`; its maintained source is `content/artifact-dates.json`. The build validates complete artifact coverage, date precision, links, snapshot hashes and current evidence bytes before synchronizing the reader copy. `npm run dates:check` verifies both copies without changing them.

- **Created** requires a dated producer record for the exact original artifact. Unknown creation remains explicitly unrecorded.
- **First recorded** and **Updated** identify the documented file or notebook-record scope. Git dates do not prove original creation. Copied protocols use their own path history.
- **Exported** dates the public export of the bytes; it does not date model training.
- **Submitted**, **Started** and **Finished** come from retained scheduler accounting. Offset-qualified timestamps display in UTC.
- **Linked run window** gives the start/finish range and dated coverage for runs linked through the experiment register. It is not an asserted timestamp for every figure measurement.
- Relative age uses the reader's current clock. Age alone does not mark a scientific result failed or automatically obsolete. ZIP member dates are fixed packaging metadata, not research dates.

A website rebuild or journal note does not refresh unchanged artifact dates. Keep both date-catalog files together with their evidence. If validation detects stale hashes or missing records, supply a verified metadata update; never silence the error by changing dates to today's date.

For the maintained repository, after updating the scientific export and verifying new date evidence, refresh the catalog from the local evidence fixture:

```sh
npm run dates:refresh
npm run dates:check
```

The default fixture is `../artifact-date-evidence.json` relative to the portal. Supply `npm run dates:refresh -- --evidence /path/to/verified-artifact-date-evidence.json` on another machine. The importer records the time a changed notebook record was saved; it preserves unchanged dates and only retains historical facts when their source evidence matches. Review the catalog diff, then synchronize the journal, package, test, build and publish. Keep the raw evidence fixture outside public assets and version control.
