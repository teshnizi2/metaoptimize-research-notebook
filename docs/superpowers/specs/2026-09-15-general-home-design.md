# General research homepage

The user identified the large CVK2 comparison as one experiment rather than a general homepage review. The homepage will cover the complete research register; the existing CVK2 chart remains on its experiment detail page, with dates and links intact.

Use the established simple, private research-group layout and fonts. Keep readable bold values, the existing outcome colors and compact tables. Preserve the publication date, downloads and their artifact dates, and all navigation. Introduce no database, new dependency or scientific data changes.

Homepage order: research notebook header and publication/download dates; inventory totals; overall research outcome distribution across every question; a nine-area table with actual total and five outcome counts per area; recent dated notebook notes/corrections/warnings linked to the research log. Remove the hard-coded Recent results selection and single-experiment feature panel. Outcome totals represent heterogeneous research goals and audits, not a comparable method win rate or average accuracy. The page is an overview; it must not invent professor or peer reviews.

Area counts are derived from the experiment register, preserving known area order and including any new area found in the register. Five categories remain separate: Goal met, Goal missed, Mixed, Open and Corrected. Each count links to the existing area/outcome filters. Show zero counts neutrally and label colors with text.

Recent updates use real, validly dated note/correction/warning entries, newest first with deterministic ties. Imported phase windows and publication events do not masquerade as new reviews. Titles, types and dates are enough on the homepage; full entries remain in the log.

Verify grouping and total invariants, unknown/empty areas, event selection and stable date ordering without mutating inputs. Inspect desktop and mobile layout, count/filter links, log links and continued access to the detailed CVK2 plot. Existing scientific assets and the artifact date catalog must remain byte-identical. Package source, publish through connected GitHub main, and verify live content and access protection.
