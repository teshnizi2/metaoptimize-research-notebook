# Private research group notebook

## User-authorized scope
The user wants the notebook connected to its GitHub code and simplified for use by the user and two professors. Preserve all verified experiment, result, log, warning, figure, table, and code evidence. Do not change scientific outcomes.

## Interface
Use a compact research workspace with a clear page title, snapshot date, report action, goals/results table, latest CVK2 comparison and existing outcome colors. Remove promotional hero copy and redundant presentation sections. Keep readable type, mobile navigation and all seven evidence sections. Add direct links to the private website and research repositories.

## Source provenance
Each source viewer links to a verified immutable original GitHub revision when content matches the recorded original SHA-256 and that revision exists remotely. Otherwise link to the exact archived snapshot in the notebook repository and label it as an archived copy. Preserve source hashes, raw downloads and shareable line links. GitHub access is governed separately by each private repository; do not invent professor identities or send invitations.

## Hosting and privacy
Connect the existing Vercel project to the existing private notebook repository. Enable native Vercel Authentication for all deployments, including existing deployment URLs, to stop anonymous access. This setting is now available without an additional paid plan. Keep native protection while access preference is being resolved. The optional login preference received no answer while independent work continued. Use the simpler supported native group-link method: one revocable shareable link on the stable production alias, usable by both professors without account setup. Keep that bearer credential outside the repository, journal, source ZIP and public tool output. Confirm its alias scope and persistence through the next production deployment. No purchase or extra paid feature is authorized.

## Verification
Test source mapping behavior before implementation; compare every immutable GitHub target against recorded bytes. Build and test the application; inspect desktop/mobile layouts and evidence navigation. Test unauthenticated access to HTML, data, source, PDF, ZIP, and old deployment URLs. Verify authorized access and the repeatable publish workflow, then update maintenance documentation and packaged source.
