# Compact research review

The user wants the overview to be brief and professional, with the latest work first, older work available by scrolling or a Show all button, and the text of each row opening its detail page. This is an authorized refinement of the existing notebook.

Use compact linked rows with a documented period, phase title, short test label and current question outcome counts. Order phase starts newest first; unknown starts remain last. Each row is one native link to its research-log destination with an explicit detail marker, with no nested buttons or separate Phase log action. All phases remain in a bounded scrolling list; Show all removes the height limit and Show less restores it. Do not imply that a group of mixed research questions has one phase verdict.

Observations, next steps, scope cautions, question goals and artifact dates belong in the matching opened phase page. Preserve the current log route and filter behavior. Collapse the home area breakdown, show only two recent notebook updates, and combine download date disclosures. Keep overall inventory/outcome context and access to all 111 records, including the 80 without phase links. No scientific data, provenance, public repository permissions or private site access changes.

The compact list was selected over another wide table or a card grid: both would retain the density and separate controls the user asked to remove. Existing requirements settle the design without another approval gate.

Verify descending order and unknown-date placement, stable phase membership, keyboard/native link behavior, bounded scrolling and expansion, responsive layout, detail access and unchanged evidence. Then log the UI change, rebuild the portable source, publish through GitHub and verify Vercel and private access.

Detail links use an explicit view=phase marker with the exact phase ID. Matching future notes cannot break the link, ordinary text searches remain log searches, and excluding type/experiment filters are respected. Phase titles in the full log also open these details.
